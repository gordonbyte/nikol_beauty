# Changelog — 2026-07-07

Started the **Full Section Audit** (headings & accessibility — `section-audit-headings-a11y.pdf`) fix pass, working the **Custom**-origin group one section at a time. Changes local/uncommitted; nothing pushed to the live store.

## custom-faq.liquid — headings, WCAG 2.2, class rename, color settings
First section off the audit list. Full cleanup, appearance intended to stay ~identical.

- **Semantic headings:** section title `<div class="cw-faq-title">` → `<h2>`; each question `<div class="cw-faq-q-text">` → an `<h3>` wrapping a real `<button>`. Subtitle is now a `<p>` (supporting text, not a heading). Gives a clean h2 → h3 hierarchy under the page/product `<h1>` on both templates.
- **WCAG 2.2 / a11y:** the accordion was a click-only `<div>` (failed 2.1.1 Keyboard + 4.1.2 Name/Role/Value). Now a native `<button type="button">` with `aria-expanded` + `aria-controls`; answers are `role="region"` panels labelled by their trigger and toggled via the `hidden` attribute. Added `:focus-visible` outline, decorative plus/minus icons get `alt=""` + `aria-hidden` (state is conveyed by `aria-expanded`), `<img>` given width/height + lazy load. Full-width trigger satisfies 2.5.8 target size. Section root uses `aria-labelledby` → title.
- **Class rename:** `.cw-faq-*` → BEM `.custom-faq` / `__header/__title/__subtitle/__list/__item/__question/__trigger/__q-text/__icon/__answer`; state class `.active` → `.is-open`.
- **Colors:**
  - **Text** — all hardcoded `#2A2724` replaced with the universal body color `var(--COLOR-TEXT)` (set once on `.custom-faq`, inherited).
  - **Background** — now a section setting **"Section background"** (color picker, default `#F7F2ED`). ⚠️ Old value was `#F7F2ED66` (that beige at **40% opacity**); a color picker can't hold alpha, so the default is the solid brand beige — a touch warmer than before. Lighten in the editor if desired.
  - **Divider** — now a section setting **"Divider color"** (default `#CEC9C5` = the old brown-@-20%-over-beige blend, so it starts visually identical).
  - Per-instance colors are injected as CSS vars (`--custom-faq-bg` / `--custom-faq-border`) scoped to `#Faq-{{ section.id }}`, so multiple instances stay independent.
- **Verified:** Liquid tags balanced (for 1/1, if 7/7, unless 1/1, style 1/1, schema 1/1); 0 `cw-faq` and 0 `#2A2724` refs remain; audit heuristic no longer flags it (2 real headings).

**Active status / placements:** used on 2 templates, both instances enabled:
- `templates/index.homepage-style-2.json` — the **dormant** alt homepage (not the live default `index.json`); only reachable if Intelligems routes `?view=homepage-style-2`.
- `templates/product.coming-soon.json` — a **live-capable** PDP template; renders live for any product on the `coming-soon` template suffix.

**Grade:** F → A−

## custom-feature-hero.liquid — already compliant; two a11y refinements
Not on the audit's flagged list (it's a newer in-house build). Already had: real `<h2>` headings (desktop + mobile), clean BEM classes + `"class": "custom-feature-hero"`, and all colors as editable per-instance section settings (no hardcoded hex, no `#2A2724`). Only two genuine WCAG refinements were needed:

- **Redundant icon alt (1.1.1):** icon `<img>` alt defaulted to the label text, so a screen reader announced each feature twice (icon alt + visible label). Icons paired with a label are now decorative (`alt=""`); alt only falls back to the image's own alt when there is no label.
- **Dead CTA link (2.4.4 / 4.1.2):** the button rendered even with no link set, emitting `href="#!"` (a no-op link). Now the button renders only when both text and link are set; simplified `href` to the raw link.

**Verified:** liquid `for/endfor` 2/2; no `#!` dead links remain; edits were surgical (added one `if/endif` pair, tightened one condition, removed one filter).

**Active status / placements:** **not placed on any template** — built but currently unused/dormant (has a preset, so it's insertable in the editor). No live impact.

**Grade:** B+ → A

## custom-footer.liquid — heading hierarchy + dead-code removal (LIVE, output unchanged)
The live sitewide footer (`{% section 'custom-footer' %}` at `layout/theme.liquid:540`). Not on the audit's flagged list (already used real `<h3>` + a `<footer>` landmark + clean BEM classes). Rendered output kept **pixel-identical** — footer heading classes fully specify their own `font-size`, and heading levels share global font/margins, so tag swaps are appearance-neutral.

- **Heading hierarchy:** the footer had no top-level heading (everything `<h3>`). Promoted the subscribe title `<h3>` → `<h2>` (footer's primary heading); column + contact titles stay `<h3>`. Now a proper `h2 → h3` structure.
- **Dead-code removal:** deleted the commented-out Menu 2 & Menu 3 blocks, the never-rendering social `<h2 class="footer__block__title">` / `<div class="rte footer__social__text">` conditionals (their `title`/`text` vars were never assigned — kept the `<ul class="social">`), the stray `{% comment %}`-wrapped tag, the commented desktop-social block, and all `<!-- … -->` labels (also serves the no-unnecessary-comments preference).
- **Schema:** removed the 4 dead fields (Title 2, Link List 2, Title 3, Link List 3) and their now-unused liquid assigns — per your call. Stored values for these remain in `config/settings_data.json` as harmless orphans (Shopify ignores settings with no schema field); left that live config untouched. Schema down to 6 settings, JSON valid.
- **Left as-is:** clean BEM classes; the `<a><h3>` contact link (valid + accessible — restructuring risked a link-color shift); the `<br>` desktop spacer (removing changes spacing).
- **Verified:** 0 comments, 0 dead menu-2/3 refs, tags balanced (for 1/1, if 1/1, footer 1/1).

**Note — colors:** this section has **no inline styles or hardcoded hex**; the footer's colors live in `assets/main.css` (shared stylesheet). Not touched here — flagged for a separate CSS-level pass if you want footer colors wired to universal variables.

**Active status / placements:** **LIVE on every page** (static section in the theme layout). Storefront output unchanged by this cleanup.

**Grade:** B → A−

## custom-guarantee-list.liquid — fixed live homepage bug + full cleanup
Trust-badge strip (icons + text). Used on `index.json` (LIVE homepage), `product.coming-soon.json` (live-capable PDP), and `index.homepage-style-2.json` (dormant). Not audit-flagged (no title/heading classes), but had a real live bug + several a11y issues.

- **Live bug fixed:** the section relied on `.cw-desktop`/`.cw-mobile` to toggle its static-row vs marquee versions, but those classes are defined **only** in `cw-pdp-css.liquid`, which loads **only** on the coming-soon PDP (confirmed: not in any global stylesheet). So on the homepage **both** versions rendered (static row + scrolling marquee, stacked). Renamed to `custom-guarantee-list__desktop/__mobile` with the section's **own** responsive toggle — now self-contained, correct on every template.
- **Marquee re-engineered (your call — dropped GSAP):** removed the third-party `cdnjs` **GSAP** `<script>` (~70KB, the theme's only GSAP user) and all its cloning JS. Mobile scroll is now pure CSS (`@keyframes` translateX on a duplicated track). Satisfies **WCAG 2.2.2** via `@media (prefers-reduced-motion: reduce)` — the row stops animating and wraps/centers statically. Removed the misused `role="marquee"` (it made the strip a screen-reader live region); the duplicate marquee copy is `aria-hidden` so guarantees aren't announced twice.
- **Semantics (1.3.1):** guarantee items are now a real `<ul>`/`<li>` list.
- **Class rename:** `cw-pdp-feature-*` / `logo-scroller-*` → BEM `custom-guarantee-list__*`.
- **Colors:** text `#2A2724` → `var(--COLOR-TEXT)`; background `#F8F3EF` → section setting **"Section background"** (default `#F8F3EF`, solid — no alpha involved). Removed a dead `:last-child { border: none }` rule (no border was ever set).
- **Icons:** now decorative `alt=""` (were `alt="{{ feature_text }}"`, duplicating the visible text) and served via `image_tag` at a sized width + lazy load instead of raw `img_url: 'master'`.
- **Removed dead JS:** `window.reinitScrollerAfterPageChange` (defined here, called nowhere else — CSS animation needs no re-init).
- **Verified:** 0 gsap/cw-/role=marquee/`#2A2724`/script refs; tags balanced (for 3/3, if 3/3, style 1/1); schema valid.

**Note:** the CSS marquee uses a fixed 20s duration (GSAP previously scaled speed to content width). For the typical 4–8 badges it looks equivalent; very few badges won't fully fill the loop — acceptable, same degradation as before.

**Active status / placements:**
- `templates/index.json` — **LIVE homepage** (enabled). The both-versions-showing bug is fixed here.
- `templates/product.coming-soon.json` — **live-capable** PDP (2 instances).
- `templates/index.homepage-style-2.json` — dormant alt homepage (2 instances).

**Grade:** D → A−

**Follow-up fix (icon width):** `.custom-guarantee-list__icon` set `height: 24px` but no width, so the `image_tag`'s `width="120"` attribute + `object-fit: contain` rendered a ~120px-wide box (mostly empty), pushing `__text` to wrap unnecessarily. Added `width: auto` (icon now scales to the 24px height at natural aspect ratio) + `flex-shrink: 0`. Applies to desktop and the mobile marquee (22px).

## custom-hero-banner.liquid — h1, class rename, colors, image/a11y upgrade
Homepage hero. On the audit's flagged list (3 title-els). Placed on `index.json` (instance **disabled**) and `index.homepage-style-2.json` (dormant alt homepage) — so not on the live default homepage right now.

- **Headings:** hero title `<div class="cw-banner-title">` → `<h1>` (the page's primary heading). The feature "titles" are captions, not headings — renamed their class to `__feature-label` (accurate + removes them from the audit's title-el heuristic) rather than forcing heading tags.
- **Semantics (1.3.1):** feature icon+label groups (desktop + mobile) are now `<ul>`/`<li>` lists.
- **Class rename:** `cw-banner-*` → BEM `custom-hero-banner__*`. The `desktop/mobile` toggle is defined locally in this section (unlike custom-guarantee-list), so no cross-page dependency. Wrapped the section + the (outside-the-section) mobile feature bar in one `#HeroBanner-{{ section.id }}` container so per-instance CSS vars reach both.
- **Colors:** all `#2A2724` → `var(--COLOR-TEXT)`. The mobile feature-bar background `#F7F2ED66` → section setting **"Mobile feature bar background"** (default `#F7F2ED`). ⚠️ was 40% opacity; solid default is a touch more opaque — adjust in editor. Button colors already used `--BTN-*` vars (kept).
- **WCAG / perf:**
  - CTA now guarded against a dead link — renders only when both button text and link are set (was `href="{{ button_link }}"` even when blank).
  - Decorative arrow SVG gets `aria-hidden="true"` + `focusable="false"`, and `fill="white"` → `fill="currentColor"` so it tracks the button text color.
  - All images moved from `img_url: 'master'` (full-res) to `image_tag` with responsive `widths`/`sizes` + intrinsic width/height (CLS). Hero images (`right_image_1/2`) are `loading="eager"` (image 1 also `fetchpriority="high"` for LCP) with alt from the image's own alt; decorative star + feature icons are `alt=""`.
- **Verified:** 0 `cw-banner`/`#2A2724`/`img_url`/`F7F2ED66` refs; 1 `<h1>`; tags balanced (for 2/2, if 9/9, style 1/1, section 1/1); schema valid.

**Active status / placements:**
- `templates/index.json` — present but **`disabled`** (not rendering on the live default homepage).
- `templates/index.homepage-style-2.json` — **enabled** on the dormant alt homepage (only reachable if Intelligems routes `?view=homepage-style-2`).

**Grade:** C → A−

## custom-icon-features.liquid — invalid heading fix, class rename, colors, a11y
Ingredients/feature block (text + icon-feature list on the left, image on the right). Placed only on `product.coming-soon.json` (live-capable PDP); `enabled_on: templates: ["product"]`.

- **Invalid heading fixed:** `heading` is a **richtext** setting rendered as `<h2>{{ heading }}</h2>`, which produced invalid `<h2><p>…</p></h2>` (block `<p>` inside a heading). Now strips the `<p>` wrapper (`replace '<p>'/'</p>'`) so it renders `<h2>Made With Skin-Safe<br>Ingredients You Can Trust</h2>` — valid, keeps the line break, and needs no setting-type migration (stored value unchanged).
- **Heading hierarchy:** each feature `<h4>` → `<h3>` (the section is `<h2>`, so `<h4>` skipped a level — WCAG 1.3.1). Kept the h4's inherited heading margin so spacing is unchanged.
- **Class rename:** the CSS used dangerously generic global names (`.feature`, `.subtitle`, `.icon-box`, `.image-box`, `.ingredients-*`) that risk collisions — renamed all → BEM `custom-icon-features__*`. Removed the dead `.ingredients-visual` rule (no markup used it).
- **Colors:** `#2A2724` → `var(--COLOR-TEXT)`; section background `#FCFAF8` → section setting **"Section background"** (default `#FCFAF8`, solid — no alpha). Button already used `--BTN-*` vars.
- **WCAG / perf:** feature-icon alt was the title text (duplicate announcement) → decorative `alt=""`; dropped the hardcoded `alt="Ingredients Image"` on the main image (now uses the image's own alt via `image_tag`, merchant-controlled); CTA guarded against a dead link; images `img_url: 'master'` → responsive `image_tag` (icons lazy; main image responsive `widths`/`sizes`).
- **Verified:** 0 `#2A2724`/`img_url`/generic-class/hardcoded-alt refs; headings `h2 → h3`; tags balanced (for 1/1, if 7/7, style 1/1, section 1/1); schema valid.

**Active status / placements:** `templates/product.coming-soon.json` only — a **live-capable** PDP template (renders for any product on the `coming-soon` suffix).

**Grade:** C → A−

## custom-info-banner.liquid — real headings + colors (surgical, most-used section)
Block-based 2-column promo banner — the **most-used** custom section (5 instances) across `index.homepage-style-2.json` (dormant alt homepage) and `product.coming-soon.json` (live-capable PDP). Not on the live default homepage. Fixed surgically (not a full rewrite) given its size + reach.

- **Headings:** the "Heading" block rendered `<div class="info-banner-title">` (a fake-heading div — the audit's flagged title-el) → now a real `<h2>` (added `margin:0` to preserve spacing). Blog-slider article titles were bare `<a>`s → promoted to `<h3><a>` (added `margin:0` + `.info-banner-blog-title a { color:inherit; text-decoration:none }` to keep the look).
- **Colors:** six `#2A2724` text colors → `var(--COLOR-TEXT)`. Swiper pagination: active bullet `#D34270` (brand pink) → `var(--BTN-PRIMARY-BG-COLOR)`, inactive `#45413B` → `var(--COLOR-TEXT)`. Kept the blog overlay `#000` + white overlay text (functional — must stay readable over any image); left the editor-only blog placeholder `#F7F2ED` background.
- **Verified:** 0 residual `#2A2724`; no `<p>`/`<div>` nested in headings; tags balanced (for 4/4, if 28/28, case 1/1, unless 2/2, section 1/1); schema valid.

**Deferred (flagged, not done):**
- **Class rename** — classes are already namespaced (`info-banner-*` / `banner-bottom-*`, not `cw-`) and self-contained, so a full BEM rename of this 900-line most-used live-capable section is disproportionate risk for this pass. Can do it as a dedicated careful pass if you want.
- **Redundant Swiper CDN** — loads `swiper-bundle` from jsDelivr when a slider is used, but the theme already preloads `swiper.css`/`vendor.js`. If vendor.js exposes a compatible Swiper, the CDN load could be dropped (needs a version-compat check first).

**Active status / placements:** 5 instances across `index.homepage-style-2.json` (dormant) and `product.coming-soon.json` (**live-capable** PDP). Not on the live default homepage.

**Grade:** C+ → A−

## custom-instagram-feed.liquid — section heading + guarded links
Lightweight IG-feed section (feed populated by an external instafeed script into `#insta-feed`). No `<style>` block in the file — styling lives in `assets/main.css` (`.instagram-feed*`), classes are already clean BEM. **Not placed on any template** — orphaned/insertable (kept standalone after the pre-footer group was removed 2026-07-06).

- **Heading:** section title `<h3>` → `<h2>` (proper section-level heading). Appearance-neutral — `.instagram-feed__title` sets its own `font-size: 32px; margin: 0` in main.css, so the tag swap doesn't change size.
- **WCAG:** the two handle `<a>`s (desktop + mobile) now only render when **both** handle text and link are set — avoids an empty/nameless link (2.4.4) or a dead `href=""`. Wrapped the title in a blank-guard too.
- **Colors:** none in this file (the section bg/text are in shared `main.css` — e.g. `.instagram-feed{background:white}`, `#FCFAF8` on blog/related templates); not touched here.
- **Verified:** one `<h2>`; tags balanced (if 3/3, schema 1/1); schema valid.

**Active status / placements:** **not placed on any template** — dormant/insertable (has a preset). No live impact.

**Grade:** B → A−

## custom-newsletter.liquid — heading, class rename, form a11y
Email-signup band (posts to Shopify's native `/contact` as a `customer` form). Placed on `index.homepage-style-2.json` (dormant) and `product.coming-soon.json` (live-capable PDP). Self-contained styles (`cw-home-form-*` not in shared CSS — safe to rename).

- **Heading:** title was a fake-heading `<div class="cw-home-form-title">` (the flagged title-el) → `<h2>` (added `margin:0`; keeps the mobile `br { display:none }` rule).
- **Class rename:** `cw-home-form-*` (old `cw-` prefix) → BEM `custom-newsletter__*` throughout.
- **WCAG:** the email `<input>` had only a placeholder (no label — fails 1.3.1 / 3.3.2 / 4.1.2) → added `aria-label="Email address"`. The decorative submit-arrow and lock SVGs get `aria-hidden="true"` + `focusable="false"`; arrow `fill="white"` → `fill="currentColor"` so it tracks the button text color.
- **Colors:** the `#fff` values (title, input text/border, placeholder, note) are **functional overlay colors on the background image** — mapping them to `--COLOR-TEXT` would make text invisible, so they're kept as-is. No `#2A2724` present. Button uses `--BTN-*` vars.
- **Also:** background image `img_url: 'master'` → `image_url: width: 3000`; guarded the bg `style` so no empty `url()` when unset.
- **Verified:** 0 `cw-home-form` refs; one `<h2>`; input has `aria-label`; both SVGs `aria-hidden`; tags balanced (if 3/3, style 1/1, section 1/1, form 1/1); schema valid.

**Note (not changed):** the form is a raw `<form action="/contact">` rather than Shopify's `{% form 'customer' %}` tag, so there's no built-in success/error message. Left as-is (intentional native-contact signup per the 2026-06-26 rebuild) — flagged if you want a confirmation state later.

**Active status / placements:** `index.homepage-style-2.json` (dormant alt homepage) + `product.coming-soon.json` (**live-capable** PDP). Not on the live default homepage.

**Grade:** C → A−

## custom-product-coming-soon.liquid — SKIPPED (by request)
The 2999-line live-capable coming-soon PDP main section. Scoped it (0 real headings in-file — the product `<h1>` is in the shared `product-title` snippet; ~7 secondary fake-headings whose styles are split across `cw-pdp-css.liquid` + theme CSS; heavy app/Intelligems/JS wiring; only 3 inline `#2A2724`). Given its size + live-PDP criticality + cross-file CSS dependencies, deferred to its own careful, well-tested pass. No changes made.

## custom-product-slider.liquid — headings, BEM rename, colors, accessible arrows
Swiper product carousel with collection-tab blocks (`enabled_on: product`). Fully self-contained (CSS/JS/markup all in-file; `cw-collect-bestseller-*` used nowhere else). Placed only on `product.coming-soon.json` (**live-capable** PDP). Full rewrite.

- **Headings:** section title `<div class="cw-collect-bestseller-title">` → `<h2>`; subtitle `<div>` → `<p>` (supporting text, de-flagged); each product-card title `<div class="…product-title">` → `<h3>` (proper `h2 → h3` under the card). Added `margin:0` on the new headings.
- **Class rename:** `cw-collect-bestseller-*` → BEM `custom-product-slider__*` across CSS **and** the Swiper JS selectors (`.__swiper` / `.__next` / `.__prev`, now scoped to `#ProductSlider-{{ section.id }}`). Dropped the borrowed `cw-banner-button` class (its base styles were removed when custom-hero-banner was renamed; the wrap is `display:none` anyway) → `custom-product-slider__btn-wrap`.
- **WCAG:** the prev/next arrows were clickable `<div>`s (not keyboard-operable — 2.1.1) → real `<button type="button">` with `aria-label` ("Previous/Next products") + button reset CSS; Swiper still wires to them. All decorative SVGs (stars, arrows, CTA arrow) get `aria-hidden="true"` + `focusable="false"`; CTA arrow `fill="white"` → `currentColor`. Guarded the (hidden) CTA against a dead link.
- **Colors:** `#2A2724` (title, subtitle, tab label, product title/price, review-count) → `var(--COLOR-TEXT)`; section background `#F7F2ED66` → section setting **"Section background"** (default `#F7F2ED`; ⚠ was 40% opacity → solid default). Left the decorative SVG fills (`#45413B` arrow circle, `#FB6D00` stars — conventional icon colors).
- **Perf:** product image `img_url: '400x'` → responsive `image_tag` (widths/sizes + lazy); alt = `product.title` (names the product link).
- **Verified:** 0 `cw-collect-bestseller`/`cw-banner-button`/`img_url`/`#2A2724`/`F7F2ED66`; `<h2>`+`<h3>`; arrows are `<button>` with aria-labels; JS selectors match renamed classes; tags balanced (for 2/2, if 6/6, style/section/script/button all paired); schema valid.

**Notes (not changed):** the rating widget still reads the stale `product.metafields.judgeme.badge` (Judge.me uninstalled → reviews on Junip) so it renders nothing — left for the separate Judge.me cleanup. Redundant Swiper **v12** CDN kept (theme-wide, 6+ sections load Swiper v11/v12 from CDN — flagged for a separate consolidation). The tab-navigation UI has CSS but no markup (only the first panel shows) — preserved as-is (renamed).

**Active status / placements:** `product.coming-soon.json` only — a **live-capable** PDP template.

**Grade:** D → A−

## custom-product-waitlist.liquid — SKIPPED (by request)
Coming-soon's 2354-line twin (`product.waitlist` template, live-capable). Same architecture: external CSS, product `<h1>` in the shared `product-title` snippet, **no `cw-` classes and no inline `#2A2724`** (no class-rename/color work applies). Only 2 genuine heading candidates — `product__heading` and `complementary-products__title` — both **stock Broadcast patterns** shared with stock `product.liquid` + coming-soon; the other 2 flags were a false positive (`product__feature__heading` is an icon container) and body copy (`product__subheading`). Deferred to a coordinated PDP stock-heading pass (verify shared CSS once, fix product.liquid + coming-soon + waitlist + product-new consistently). No changes made.

## custom-steps-horizontal.liquid — class rename, colors, editable bg, a11y
"How it works in 3 steps" section. Self-contained styles. Placed on `product.coming-soon.json` (live-capable PDP). Headings were **already** proper `<h2>`/`<h3>` (not flagged) — so the work was classes/colors/bg/a11y.

- **Class rename:** classes were generic globals (`.step-card`, `.steps-section`, `.step-number`, `.step-image`, `.steps-header`, `.steps-btn-wrap`) — collision-prone names, though a check confirmed they're not currently reused elsewhere (custom-steps-vertical uses distinct `cw-steps-*` classes). Renamed all → BEM `custom-steps-horizontal__*` for namespacing/convention. Dropped unused classes (`step-images`, `image-{{ index }}`, `steps-btn`).
- **Colors:** the four `#2A2724` text colors (heading, subheading, step title, step text) → `var(--COLOR-TEXT)`. Left the step-number badge's specific decorative colors (`#45413B` bg, `#fff` text, `#FCFAF8` border, shadow) — not text/universal colors. Button uses `--BTN-*` vars.
- **Hardcoded background image → editable setting:** the section background was a **hardcoded Shopify CDN URL** in the CSS. Added a `bg_image` image_picker setting; the section now uses it if set, else falls back to that same URL (appearance unchanged, but now editable).
- **WCAG / perf:** step image alt was the (duplicate) title text → decorative `alt=""` (the `<h3>` conveys it); images `img_url: 'master'` → responsive `image_tag`; CTA guarded against a dead link (was `href` even when link blank).
- **Verified:** 0 old generic classes / `img_url` / `#2A2724`; `<h2>` + `<h3>` intact; tags balanced (for 1/1, if 7/7, style 1/1, section 1/1); schema valid.

**Active status / placements:** `product.coming-soon.json` only — a **live-capable** PDP template.

**Grade:** C+ → A−

## custom-steps-vertical.liquid — headings, BEM rename, colors, a11y
Alternating image/text "steps" section. Self-contained (`cw-steps-*` used nowhere else). Placed on `index.homepage-style-2.json` (dormant alt homepage). Full rewrite.

- **Headings:** section title `<div class="cw-steps-heading">` → `<h2>`; subtitle `<div>` → `<p>`; each step title `<div class="cw-steps-title">` → `<h3>` (proper `h2 → h3`). Step body text stays a `<div>` (`__text`). Added `margin:0` on new headings.
- **Class rename:** `cw-steps-*` → BEM `custom-steps-vertical__*` throughout.
- **Colors:** `#2A2724` (heading, subheading, step title, step text) → `var(--COLOR-TEXT)`; the step box background `#F7F2ED` → section setting **"Step background"** (default `#F7F2ED`). Button uses `--BTN-*` vars.
- **WCAG / perf:** step image alt was the (duplicate) title → decorative `alt=""`; images `img_url: 'master'` → responsive `image_tag`; CTA was rendered unconditionally (dead link when unset) → guarded on button label + link; decorative arrow SVG `aria-hidden` + `fill="white"` → `currentColor`.
- **Dead code removed:** the `step_label` markup block — it referenced `block.settings.step_label` which has **no schema setting** and `.cw-steps-step` had **no CSS** (never rendered anything). Removed.
- **Verified:** 0 `cw-steps`/`img_url`/`#2A2724`/`step_label`; `<h2>` + `<h3>`; tags balanced (for 1/1, if 7/7, style 1/1, section 1/1); schema valid.

**Active status / placements:** `index.homepage-style-2.json` only — the **dormant** alt homepage (Intelligems-routable). Not on the live default homepage.

**Grade:** C → A−

## custom-videos.liquid — headings, BEM rename, colors, dead-code, a11y
Stacked-video section (merged from cw-videos/cw-videosv2 on 2026-07-06). `enabled_on: index, product`. Placed on `index.homepage-style-2.json` (dormant) and `product.coming-soon.json` (live-capable PDP, with the Channelize live-shopping style toggle on). Full rewrite.

- **Headings:** section title `<div class="cw-collect-videos-title">` → `<h2>`; subtitle `<div>` → `<p>`. Added `margin:0`.
- **Class rename:** this section's own `cw-collect-videos-*` → BEM `custom-videos__*`. **Kept verbatim** the conditional Channelize `<style>` block's selectors (`.cw-bb-pvideo-*`, `.cw-desktop`, and the `div#shopify-block-…live_shopping_by_channelize_io…` hardcoded app-block IDs) — they target the external Channelize app DOM / the coming-soon PDP's video block, not this section's markup, so renaming them would break the app styling. Only the section's own `.cw-collect-videos-section` refs inside that block were renamed to `.custom-videos`.
- **Colors:** `#2A2724` (title, subtitle) → `var(--COLOR-TEXT)`; the hardcoded pink section background `#FCC9C6` → section setting **"Section background"** (default `#FCC9C6`). ⚠ The Channelize app widget's own `background: #FCC9C6` (in the verbatim app-targeting block) is a **separate** DOM node and can't inherit the section's scoped var — left hardcoded; keep the two in sync manually if the setting is changed.
- **Dead code removed:** `.cw-collect-videos-swiper` CSS (no matching markup — the videos just stack; there's no Swiper here) and the empty `.cw-collect-videos-overlay` / `.cw-collect-videos-play-btn` divs (no CSS, no JS — the `<video>` uses native `controls`).
- **WCAG / perf:** CTA was rendered unconditionally (dead link when unset) → guarded on button label + link; decorative arrow SVG `aria-hidden` + `fill="white"` → `currentColor`; video `<source>` only output when a URL is set; poster `img_url: 'master'` → `image_url: width: 1400`. Video keeps native `controls` (no autoplay → no 2.2.2 concern).
- **Verified:** 0 `cw-collect-videos` / dead swiper / overlay / `img_url` / `#2A2724` / comments; 6 app-targeting selectors preserved; `<h2>` present; tags balanced (for 1/1, if 6/6, style 2/2, section 1/1, video 1/1); schema valid.

**Active status / placements:** `index.homepage-style-2.json` (dormant) + `product.coming-soon.json` (**live-capable** PDP, live-shopping styling enabled there).

**Grade:** C → A−

## Isolation audit — all sections touched today (excl. coming-soon & waitlist)
Swept every section edited today to confirm each is self-contained (no CSS/JS that bleeds to or depends on other sections). Method: extracted every selector from each `<style>`/`{%- style -%}` block and flagged any not scoped to the section's own namespace; checked each `<script>` for global (unscoped) queries; and grepped shared classes for cross-section usage.

**Result: 12 of 13 already isolated; 1 gap found and fixed.**
- **Isolated ✓** — custom-faq, custom-feature-hero (every rule prefixed with its `#feature-hero-{id}` anchor), custom-guarantee-list, custom-hero-banner, custom-icon-features, custom-info-banner (`.banner-bottom-*`/`.info-banner-*` namespace confirmed used nowhere else; grid var scoped to `#InfoBanner-{id}`), custom-newsletter, custom-steps-horizontal, custom-steps-vertical, custom-videos. All CSS-var injection is scoped to per-section `#Id` blocks.
- **custom-footer & custom-instagram-feed** — no in-file `<style>`; their CSS lives in shared `assets/main.css` by the theme's design (not cross-section bleed).
- **Scripts** — all section JS is scoped: custom-faq (`#Faq-{id}`), custom-feature-hero (`getElementById(anchor)`), custom-info-banner + custom-product-slider (`querySelectorAll('#…-{id} …')`). No global DOM queries.
- **Fixed — custom-product-slider:** it had **global, unscoped** rating rules (`span.review-count { … }` and `.custom-rating-widget.cw-product-card-rating { … }`) — element/class selectors that could bleed to any matching element (and still carried a leftover `cw-` class). Confirmed these bare classes are used/styled nowhere else (not in coming-soon, not in shared CSS), then renamed to the section namespace: `.custom-product-slider__rating`, `.custom-product-slider__review-count`, `.custom-product-slider__stars`. Now fully scoped.

**Intentional cross-references (left in place — necessary, not bleed):** custom-videos' conditional Channelize block targets the external Live Shopping app's DOM (`.cw-bb-pvideo-*`, `div#shopify-block-…channelize…`) — that's the feature's purpose. Legitimate theme/library classes remain referenced where appropriate: `.page-width`, `.btn`/`.btn--*`/`.rte` (feature-hero), `.swiper*` (sliders), `.social`/`.social-link` (footer).

---

# Default (stock Broadcast) sections review
Going one-by-one through the **used content** Default sections to verify isolation (no cross-section CSS/JS deps), WCAG 2.2, and appropriate elements. Core infra (header/cart/product/search/etc.) is verify-only; the 33 unused stock sections are skipped. No renaming (stock code). Edits kept minimal — only genuine issues.

## blog.liquid — verified; 3 minor WCAG/validity fixes
Stock blog-listing section (lightly agency-modified: custom `blog-tags` menu nav + `youtube-blog` suffix handling). Placed on blog templates.

- **Isolation ✓** — only in-file CSS is a scoped `{%- style -%}` setting CSS vars on `#Blog--{{ section.id }}`; all visual styling is theme framework CSS (`.article`, `.grid`, `.blog-listing`) + theme snippets (`image`, `pagination`, `icon-rss`) + the global AOS library. No other-section dependency, no `<script>`.
- **Elements ✓** — semantic `<article>` per post, `<time>` for dates, `<ul>/<li>` tag nav, single `<h1>` (blog title) → `<h2>` article titles (correct hierarchy).
- **Fixed (WCAG 2.2):**
  - RSS icon link had **no accessible name** (4.1.2/2.4.4) → added `aria-label="RSS feed"`.
  - RSS **href bug** — `{{ blog.title }}.atom` (e.g. `News.atom`, dead) → `{{ blog.url }}.atom`.
  - `<time datetime>` held a formatted `full_date` string, not machine-readable → now ISO `%Y-%m-%d` (display unchanged). Both the onboarding and real-article spots.
  - (All three were behind default-off settings — `show_rss` / `show_date` — so no current live change, but correct now.)

## collection-image-with-title.liquid — verified clean, no changes
Stock collection banner (title + hero image); on collection templates.
- **Isolation ✓** — scoped `{%- style -%}` var block on its own `.collection-image-with-title` class; all styling is theme framework (`.hero__*`, `.collection__image`, `.align--*`, `.section-padding`) + the `image-hero` snippet. No other-section deps, no `<script>`.
- **Elements ✓** — collection title `<h1>` (visual `h4`), description `rte` in a `<div>`, image via snippet (alt handled). Two `<h1>`s in source but only one renders (mutually exclusive overlay vs no-image branches).
- **WCAG 2.2 ✓** — single rendered h1, no nameless links, decorative overlays only, hero image eager + fetchpriority high. Text-over-image contrast is merchant-configurable (not a code defect).

## contact-form.liquid — verified; 1.3.5 autocomplete fix
Stock Broadcast contact form (native `{% form 'contact' %}`); on the contact page.
- **Isolation ✓** — scoped `{%- style -%}` vars on `#ContactForm--{{ section.id }}`; styling is theme framework (`.custom-form__block`, `.field`, `.btn`, `.visually-hidden`). No cross-section deps, no `<script>`.
- **Elements ✓ (strong)** — every field has an associated `<label for>` (visually-hidden where placeholder-styled), radio groups use `<fieldset>`+`<legend>`, submit is a real `<button type="submit">`, `<hr>` spacers, native `required`.
- **Fixed (WCAG 2.2, 1.3.5 Identify Input Purpose — AA):** email + telephone inputs lacked `autocomplete` → added `autocomplete="email"` (both the block email and the auto-appended fallback email) and `autocomplete="tel"`. Lets browsers/AT autofill the user's own details.
- **Noted, left as stock (debatable/minor):** `formHeading` block renders `<p class="strong">` rather than `<h3>` (a form divider, not clearly a document heading); success/error messages aren't in an ARIA live region (page-reload submit flow); section heading is `<h2>` (assumes a page `<h1>` from the page template).

## list-collections.liquid — verified clean, no changes
Stock collection-list + an agency-added visually-hidden `<h1>`; on the collections-list page.
- **Isolation ✓** — scoped `{%- style -%}` vars on `.index-list-collections`; theme-framework styling + the `collection-block` snippet. No cross-section deps, no `<script>`.
- **Elements ✓** — hidden `<h1>` via standard clip technique (SR-accessible), visible heading `<h2>`, snippet-rendered collection grid.
- **WCAG 2.2 ✓** — page has an h1 (hidden) → h2; no nameless links/form issues in this file.
- **Left as-is (minor/intentional):** hidden h1 uses inline styles rather than the theme `.visually-hidden` class; its text differs from the visible h2 (deliberate agency SEO).

## related.liquid — verified; heading-level fix + flagged tabs a11y
Stock "Product recommendations" (related + recently-viewed, tabbed). Used in 12 templates.
- **Isolation ✓** — CSS vars via inline `style`; styling is theme framework (`.related__wrapper`, `.tabs`, `.tab-content`); tab-switching + related-products fetch handled by theme global JS (`data-tabs-holder`, `data-related-section`); renders `products-recently-viewed` snippet. No `<script>` in-file, no cross-section deps.
- **Fixed (1.3.1 heading level):** the section heading rendered as `<h6>` (way too deep — sits under the PDP `<h1>`) → changed both (related + recent) to `<h2>`. Appearance-neutral (size is class-driven via `heading-size-N`); only renders when `enable_tabs=false` (tabs are on by default).
- **Flagged, NOT changed (4.1.2 / 2.1.1):** the tab widget is `<ul class="tabs"><li tabindex="0" data-tab>` with **no ARIA tab semantics** (`role="tablist"`/`role="tab"`/`aria-selected`/`role="tabpanel"`) and keyboard activation depends on the theme's tab JS. A correct fix needs coordinated ARIA + JS keyboard handling on a stock widget used in 12 places — deferred to a dedicated tabs-a11y pass rather than a risky partial retrofit. (Arrow buttons already have visually-hidden labels.)

## section-accordion.liquid — verified; fixed broken form labels + autocomplete
Stock accordion, used in 14 templates. Uses a **native `<details>/<summary>`** disclosure — natively accessible (no ARIA retrofit needed).
- **Isolation ✓** — scoped `{%- style -%}` vars on `#Faq--{{ section.id }}`; theme-framework styling; collapse animation via the theme's `<collapsible-elements>` web component; theme snippets. No `<script>` in-file, no cross-section deps.
- **Elements ✓** — native details/summary; FAQ item titles are `<h3>`. (Minor, left as stock: the `<summary>` "FAQ" trigger is styled `h6` but isn't a heading element → absent from the heading outline.)
- **Fixed (WCAG 2.2, in the optional Contact-form block):**
  - **Broken `<label>` associations** (1.3.1/3.3.2/4.1.2) — all three `for` values mismatched their input `id`s (`section.id` vs `section_block_id`, and un-prefixed `contactFormEmail`/`ContactFormMessage` vs the `{{ section_block_id }}-`-prefixed ids). Aligned all three `for` → matching `id`, so the visually-hidden labels now actually label their fields.
  - **1.3.5 Identify Input Purpose** — added `autocomplete="email"` to the email input and `autocomplete="name"` to the name input.
  - (These affect only the accordion's `contact-form` block type, which isn't in the default preset.)

## section-collection.liquid — verified clean, no changes
Stock "Featured collection" (product grid/slider); used in 14 templates.
- **Isolation ✓** — scoped `{%- style -%}` vars on `#Collection--{{ section.id }}`; theme-framework styling; slider via the theme's `<grid-slider>` web component; products via the shared `product-grid-item` snippet. No `<script>` in-file, no cross-section deps.
- **Elements ✓** — section heading `<h2>`, description `rte` in a `<div>`, CTA `<a class="btn">` with real text (arrow icon supplementary).
- **WCAG 2.2 ✓** — single h2 under page h1, no nameless links, no forms/custom widgets in this file (product-card a11y lives in the shared snippet).

## section-columns.liquid → multicolumn.liquid — verified; heading-nesting fix
`section-columns.liquid` is a thin wrapper (`{% render 'multicolumn', type: 'images' %}`) — trivially isolated. Reviewed the shared `multicolumn` snippet (also used by section-multicolumn + section-text-row).
- **Isolation ✓** — scoped `{%- style -%}` vars on `#SectionColumns--{{ section.id }}`; theme-framework styling; `<grid-slider>` web component; theme snippets. No `<script>`.
- **Elements ✓** — image/button links have `aria-label`s, menu links have text, buttons have text.
- **Fixed (1.3.1 heading nesting):** the section heading is `<h2 class="grid__heading">` but **each column title was also `<h2 class="column__heading">`** (siblings of the section heading rather than nested). Introduced `column_heading_tag` — column titles now render `<h3>` when the section has a heading, and stay `<h2>` when it doesn't (avoids a skipped level). Applied to all three column types (image/icon/text). Appearance-neutral (size is the `heading-size-N` class). Benefits section-columns (+ the unused section-multicolumn/section-text-row).
- **Left as stock (minor):** empty-`href="#!"` fallback on column buttons when no URL is set (merchant-config; aria-label present).

## section-slideshow.liquid — verified; flagged live WCAG 2.2.2 (autoplay)
Stock hero carousel. Enabled on the live homepage (`index.json`).
- **Isolation ✓** — only an editor-preview `{%- style -%}`; theme-framework styling; slider is the theme's Flickity `data-slider`; renders `image-hero`. No `<script>` in-file, no cross-section deps.
- **Elements ✓** — slide titles `<h2>`, whole-slide links have `aria-label`, image alt fallback chain, buttons have text.
- **⚠ Flagged (WCAG 2.2.2 Pause/Stop/Hide) — NOT changed:** the homepage instance is `autoplay: true`, 5s, 9 slides, with **no explicit pause control**. Flickity mitigates partially (`pauseAutoPlayOnHover` + pause-on-focus) but that's borderline. No reduced-motion gate found in the CSS. Recommendation: disable autoplay on the homepage slideshow (zero-risk setting change) OR add a pause/play button (JS wired to the Flickity instance — deferred, risky on the live hero). Left for a decision.

---

## Every-page review — expanded scope
Broadened from homepage + coming-soon to **all templates**, reviewing every currently-enabled section (disabled instances skipped). App-owned sections (Zipify/OST/Rush/Zooomy) skipped; stock core infra (header/cart/product/search/etc.) is careful verify-only.

## custom-customer-quote.liquid — verified clean, no changes
Custom testimonial block; enabled on the **live homepage** (`index.json`) + dormant alt homepage.
- **Isolation ✓** — CSS scoped to `.custom-customer-quote*` (clean BEM), no `<script>`, no cross-section deps.
- **Elements ✓ (exemplary)** — `<section aria-label="Customer review">` → `<figure>` → `<blockquote>` + `<figcaption>` → `<cite>`; correct semantic quotation structure. Decorative `buyer_image` `alt=""` with adjacent "Verified Buyer" text.
- **Colors ✓** — already on `var(--COLOR-TEXT)` / `var(--FONT-STACK-BODY)`; no hardcoded values.
- **Noted, not changed:** star image has `alt=""` (rating not conveyed to AT) — can't auto-generate accurate alt from an image_picker; consider a rating/alt setting if the stars mean a specific score. Minor perf nit: star/buyer/bg via `img_url:'master'`.

## section-hero.liquid ("Image banner") — verified clean, no changes
Stock hero/banner; live on homepage + `collection.lips`.
- **Isolation ✓** — scoped `{%- style -%}` vars on `#Hero--{{ section.id }}`; theme-framework styling; `image-hero` snippet; `data-aos`. No `<script>`, no cross-section deps.
- **Elements ✓** — heading block `<h2>`, text `<p>`, buttons `<a class="btn">` with text; static banner (no autoplay).
- **WCAG 2.2 ✓ / merchant note:** the whole-banner `<a href>` (when a link is set) takes its accessible name from the image alt (via `image-hero`); no aria-label fallback and no clean section-level text to derive one → recommend ensuring linked banner images have alt text. Not a code fix. Minor: button `href="#!"` fallback (stock).

## section-highlights.liquid — verified; fixed malformed Liquid tag
Stock "Highlights" category-tile grid; live on homepage.
- **Isolation ✓** — scoped `{%- style -%}` vars on `#highlights--{{ unique }}`; theme-framework styling; `image` snippet; `data-aos`. No `<script>`, no cross-section deps.
- **Elements ✓** — section heading `<h2>`; category tiles are `<a href>`/`<div>` with an image + `<p>` label (named by the label text). Tile labels correctly aren't headings.
- **Fixed:** malformed `{% else> %}` → `{% else %}` (stray `>` — worked only because Shopify's tag-name parser stops at the `>`).
- **Noted, left as-is:** hardcoded `section.id == 'template--…'` per-instance branch (fragile agency hack, functional); `data-aos-order="{{ animation_order }}"` on the h2 uses an unassigned var → empty (harmless).

## section-double.liquid → section-double snippet — verified clean, no changes
"Image with text" brick section (thin wrapper → `section-double` snippet); live on homepage (blocks: heading + buttons), list-collections, lookbook.
- **Isolation ✓** — inline `style` vars, theme-framework classes, theme snippets, `<collapsible-elements>`/`<video-background>` web components. No `<script>`, no cross-section deps.
- **Elements ✓** — heading blocks `<h2>`, kicker/subheading `<p>`, native `<details>/<summary>` accordion block, buttons with text, collection links have `aria-label`.
- **Latent WCAG 2.2.2 (flagged, NOT active on homepage):** slider variant uses `autoPlay:4000, pauseAutoPlayOnHover:false`; video block autoplays/loops with `controls:false`. Only triggered by text-slide-slider or video blocks — the homepage instance uses heading+buttons only, so neither fires. Stock theme-component behavior; deferred.

## review-swiper-card.liquid (Agency) — verified; notes only
Review carousel; live on homepage.
- **Isolation ✓ (functional):** no dependency on other *sections* — relies on global theme assets (`theme.liquid` loads `swiper-bundle.min.js`; `main.js` inits `.review-cards .swiper-container`; CSS in `styles.css`). Works on any page. Agency shared-`main.js` slider pattern, not cross-section coupling.
- **Elements ✓** — section heading `<h2>`; product cards are named `<a>` (image alt + title); images `image_url` sized + lazy.
- **Notes (not changed):** h2 text is **hardcoded** ("Customers Love for Nikol Beauty") — recommend a schema setting; Swiper `autoplay.delay:66000` (66s, no pause) — minor 2.2.2.
- **Theme-wide confirmation:** `swiper-bundle.min.js` is loaded globally in `theme.liquid`, so the per-section `swiper@11/12` **CDN loads** (info-banner, product-slider, collection-tabs, customer-reviews, coming-soon, product-new) are **redundant** — candidate for a theme-wide cleanup.

## meet-nikol.liquid (Agency) — verified clean, no changes
Image + text + CTA; live on homepage.
- **Isolation ✓** — no in-file `<style>`/`<script>`; CSS in shared `styles.css`; no JS; no cross-section deps.
- **Elements ✓** — `<figure><img>`, `<h2>` title, richtext `<div>`, named CTA `<a>`.
- **WCAG ✓** — image alt (`image.alt` → `shop.name`), h2, named link.
- **Minor (not changed):** CTA renders even if text blanked (nameless-link edge; has default); image `loading="eager"` mid-page (perf nit).

## yt-videos-list.liquid (Agency) — verified; fixed arrow img alt, flagged 2
Video + product-slider hub; live on homepage + videos page. Slider init in global `main.js` (`.video-product-hub .swiper-*`).
- **Isolation ✓ (functional)** — global `main.js` init + global Swiper + `styles.css`; `product-grid-item`/`yt-video` snippets. No cross-section deps.
- **Fixed (2.1.1 / 4.1.2 / 1.1.1) — functional, not decorative:** the prev/next arrows were clickable `<div>`s wrapping alt-less `<img>`s → converted to real `<button type="button" class="swiper-prev/next" aria-label="Previous/Next products">` (keyboard-operable + named), imgs `alt=""` (decorative; the button carries the name). Kept the `swiper-*` classes so `main.js` navigation still wires them; added an in-file scoped button reset (`.videos-list .swiper-prev/next`) so appearance is unchanged.
- **Flagged, NOT fixed:** `slider_title` is `<h5>` (too deep; sibling video title is `<h3>`) — it has no size class, so bumping the tag would visibly enlarge it; needs a size-class to fix appearance-neutrally. Deferred (minor 1.3.1 nit, not a functional failure).

## section-custom-code.liquid — verified clean, no changes
Stock "Custom code" passthrough (`{{ section.settings.code }}`); on product/pages. Scoped `{%- style -%}` vars, no JS, no cross-section deps. Content a11y is merchant-entered (out of scope).

## image-grid.liquid (Agency) — fixed broken alt + dead link
Image grid w/ overlay links; on product pages (product.json, product.new, free-products, waitlist). No in-file CSS/JS (external `styles.css`); isolated.
- **Fixed (1.1.1) — functional alt:** `alt="{{ image | default: shop.name }}"` output the **image object** (its URL string), not alt text → now `alt="{{ image.alt | default: title | strip_html | escape }}"` (image's own alt, falling back to the tile title).
- **Fixed (2.4.4) — dead link:** overlay `<a href="{{ link }}">` emitted `href=""` when unset (reloaded page) → `href` now only emitted when a link exists (keeps the `<a>` for CSS; non-interactive when empty).
- **Noted (perf, not changed):** grid images `loading="eager"` + `image_url` without a width.

## product-new.liquid — DEFERRED to the PDP pass (like coming-soon/waitlist)
2304-line fork of stock `product.liquid`; live on `product.new` template. Same class/risk as the skipped coming-soon + waitlist PDPs → deferred to the coordinated PDP pass. Findings recorded:
- Shares the stock PDP heading patterns (`product__heading` `<p>`, `complementary-products__title`, `product__feature__heading`) — fix with the other PDPs against shared CSS.
- **Bespoke gallery (~lines 225-376):** agency override rendering a Swiper gallery of **hardcoded static images** (`bb_creme1-6.png` via `file_url`) — ignores `product.media`, so all products on this template show the same 6 BB-creme images. Its `<img>`s have **no alt** (1.1.1); prev/next are empty `.swiper-button-*` divs; loads **Swiper v11 from CDN** (redundant — global `swiper-bundle.min.js` exists). Wants a rethink (hardcoded to one product), not a band-aid.
- Isolation otherwise OK (scoped `#Product--{{ section.id }}` vars). No changes made.

## yt-videos-list.liquid — arrow alt follow-up (clarification)
Re-confirmed the arrow fix: controls are `<button aria-label="Previous/Next products">` with decorative `<img alt="">` inside. The `alt=""` is correct here — the button carries the accessible name; empty alt keeps the icon from being announced twice (≠ a missing alt, which would read the filename).

## section-custom-content.liquid — verified clean, no changes
Stock "Custom content" brick orchestrator; on lookbook/story/team. Scoped `#BrickSection--{{ section.id }}` vars; delegates blocks to `brick-*` snippets; collection slider has no autoplay; no `<script>`, no cross-section deps. Spot-checked `brick-text`/`brick-image`/`brick-newsletter` → real `<h2>` headings + `image-hero`/`image` snippet alts. Kickers are `<p>`.

## section-look.liquid ("Shop the look") — verified clean, no changes
Stock; on lookbook. Scoped `#IndexLook--{{ section.id }}` vars; `image`/`product-grid-item` snippets; `data-slider` (no autoplay); no cross-section deps. **Good a11y:** dots, mobile CTA, and close are all real `<button type="button" aria-label>` (the correct icon-control pattern). Nit (left): "Shop the look" title is a `<p class="…subheading">` kicker, not a heading.

## about-header.liquid (Agency) — fixed broken alt + mobile h1
About-page header; on `page.about`. Isolated (in-file bg `<style>` on own class; `ld+json` structured data; no cross-section deps).
- **Fixed (1.1.1):** `alt="{{ image | default: shop.name }}"` output the image object → `alt="{{ image.alt | default: title | default: shop.name | strip_html | escape }}"`.
- **Fixed (1.3.1):** title was `<h1>` on desktop but a plain `<div>` on mobile (mobile had no h1) → mobile title now `<h1>` too (display-toggled, so only the shown one is exposed to AT).

## about-quote-image.liquid (Agency) — fixed broken alt
About page. Isolated (no in-file CSS/JS); minor note — borrows `about-header__image` class (works since both load on About page).
- **Fixed (1.1.1):** `alt="{{ image | default: shop.name }}"` (image object) → `alt="{{ image.alt | default: shop.name | strip_html | escape }}"`.
- Note (left): quote rendered in a `<div>`, not `<blockquote>` (debatable; external CSS styles the div).

## about-text-grid.liquid + about-two-columns.liquid (Agency) — verified clean, no changes
About page. Trivial richtext-column layouts (2-3 `<div>` richtext blocks). No in-file CSS/JS, no images/links/headings of their own (content is merchant richtext), isolated.

## about-contact.liquid (Agency) — multiple fixes (form a11y, dead code, heading, alt)
Contact form; on About page (was on the original audit's flagged list).
- **Fixed (1.3.1/3.3.2/4.1.2) — no field labels:** first/last/email/message inputs had only placeholders and empty `id=""` → added visually-hidden `<label for>` + matching ids (`AboutContact-*-{{ section.id }}`).
- **Fixed (1.3.5):** added `autocomplete="given-name"/"family-name"/"email"`.
- **Fixed (1.3.1):** flagged `.about-contact__title` `<div>` → `<h2>`.
- **Fixed (1.1.1):** image alt output the image object → `image.alt` fallback chain.
- **Removed dead code:** a `<script>` running `$("#reason").selectBoxIt()` (jQuery + selectBoxIt plugin dependency) that targeted a **commented-out** `<select>` — removed the script and the commented select.
- Verified: 4 labels ↔ 4 ids, autocomplete present, 0 empty ids / selectBoxIt / commented select, form balanced.

## subscribe-form.liquid (Agency) — verified clean, no changes
Title + Klaviyo form (`klaviyo-form` snippet); on About page. No in-file CSS/JS, no cross-section deps, isolated. Nit (left): `{{ title }}` in a bare `<div>`, not a heading (debatable label vs heading; external CSS).

**— About page complete. —**

## new-article.liquid (Agency) — article-page fixes
Article template (`article`, `article.youtube-blog`). Isolated (comment toggle wired via global `main.js`; snippets; no cross-section deps).
- **Fixed (1.1.1):** hero image alt output the image object → `article.image.alt | default: article.title`.
- **Fixed (time):** `<time datetime>` used the formatted `full_date` → ISO `%Y-%m-%d` (display unchanged).
- **Fixed (form a11y 1.3.1/3.3.2):** comment form (author/email/body) had no `<label>`s → added visually-hidden labels (ids already existed) + `autocomplete="name"/"email"`; also corrected the email field `type="text"` → `type="email"`.
- **Fixed (2.1.1/4.1.2) — functional:** the comments toggle was a clickable `<div class="js-toggle-comments">` → real `<button type="button">` (keyboard-operable; `main.js` targets it by class) + a scoped chrome reset. (Did NOT add `aria-expanded` since main.js doesn't maintain it — a static value would misreport state; accurate state would need a main.js change.)
- **Fixed (elements):** `<label class="share__label">Share</label>` (no form control) → `<span>`.

## video-gallery.liquid (Agency) — fixed duplicate h1
Video-gallery page. Large self-contained section (own `.video-gallery-*` CSS + scoped JS + internal `window.*` vars); isolated. Video thumbs have alt, card titles `<h3>`, hero imgs have alt, Show-More are real buttons.
- **Fixed (1.3.1):** the bottom "Your Beauty Journey Starts Here" was an `<h1>` (a 2nd visible h1 beside the active tab's h1) → `<h2>` (class-driven size, appearance-neutral).
- **Noted, not changed:** the hidden makeup-tab panel still has an `<h1>` in source (display:none, so not exposed to AT); "Real Stories, Real Beauty" `<h3>` skips h2 (no size class → bumping would resize); hardcoded CDN hero image URLs (have alt). Deeper heading normalization deferred.

## video-gallery-youtube.liquid (Agency) — fixed duplicate h1
Near-duplicate of video-gallery (video-gallery-youtube page). Same fix: bottom "Your Beauty Journey Starts Here" `<h1>` → `<h2>` (appearance-neutral). Same deferred notes as video-gallery.

## videos-featured.liquid + videos-filter.liquid (Agency) — verified clean, no changes
Videos page. Both isolated (videos-filter's mobile slider inits via global `main.js`; no cross-section deps). videos-featured delegates to `yt-video*` snippets + text subscribe links. videos-filter's category filters are keyboard-operable named `<a href="#cat">` links.
- Notes (left): videos-filter active-filter state isn't announced to AT (could add `aria-current`); filters are `<a href="#">` rather than `<button>` (functional as anchors). videos-featured `youtube_link` can be blank (empty-href edge).

## col-sitemap.liquid (Agency) — verified clean, no changes
Sitemap page. In-file `{% style %}` scoped to `.col-sitemap*`; no JS; isolated. `<h1>` page title, semantic `<ul>/<li>/<a>` menu (nested dropdowns as nested `<ul>`), links have text. (Minor: could wrap in `<nav aria-label>`.)

## customer-account.liquid — verified; table a11y + markup fixes
Account order-history page. Isolated (native `<details>` mobile sidebar; `pagination` snippet; no cross-section deps). `<aside>` sidebar, semantic orders `<table>`.
- **Fixed (1.3.1):** orders-table `<th>`s had no `scope` → added `scope="col"` to all 5.
- **Fixed (markup):** an unclosed `<span class="note">` in the date cell → closed.
- Notes (left): page top heading is `<h2>` (no `<h1>` — uncertain if layout supplies one; logged-in/low-traffic); sidebar title is a `<p>`.

## customer-addresses.liquid — verified; fixed heading-tag mismatch + landmark misuse
Account addresses page. Isolated (no in-file CSS/JS; country/province via theme-global address JS). **Forms are well-built** — every field has a matching `<label for>`.
- **Fixed (markup):** edit-address heading opened `<h4>` but closed `</h2>` → `</h4>`.
- **Fixed (1.3.1/4.1.2):** section header had `role="banner"` (a second banner landmark beside the site masthead) → removed.
- Notes (left): no `<h1>` (starts at h2); address fields lack `autocomplete` (1.3.5 — many fields × 2 forms; labels present).

## customer-order.liquid — verified; table-header fix
Order-detail page. Isolated (native `<details>` sidebar; `image`/`icon` snippets; no cross-section deps). Uses `aria-hidden`/visually-hidden for the unit-price separator (good).
- **Fixed (1.3.1):** order-details `<th>`s had no `scope` → added `scope="col"` to all 5.
- Notes (left): no `<h1>` (h2 heading, like account); the mobile order table is a layout table (no headers).

## section-supporting-menu.liquid — verified clean, no changes
Stock footer bar (copyright/links/payment icons/locale selectors); in `group-footer`. Scoped `#SupportingMenu--{{ section.id }}` vars, `localization` snippet, semantic lists, payment icons via `payment_type_svg_tag`. Isolated. (Note: `group-footer` is commented out in favor of `custom-footer`, so likely not rendering live.)

## overlay-cookie.liquid — verified; fixed broken aria-labelledby
Stock cookie banner (native `<dialog aria-modal>`); in the overlay group. Named buttons, privacy link, `data-tracking-consent` (theme JS). Isolated.
- **Fixed (4.1.2/1.3.1):** `aria-labelledby="PopupCookie"` referenced a non-existent id → added `id="PopupCookie"` to the title `<p>` so the dialog is properly named.

---

## yt-video.liquid snippet — fixed nameless link + broken alt (Lighthouse)
Shared video-card snippet (used by videos-featured + yt-videos-list). Lighthouse flagged `a.yt-video__overlay` as a link with no discernible name.
- **Fixed (2.4.4/4.1.2):** the play-button overlay link wrapped only an alt-less play icon → added `aria-label="Play video[: {title}]"` and made the icon decorative `alt=""` (link carries the name).
- **Fixed (1.1.1 / functional alt):** thumbnail `alt="{{ image | default: shop.name }}"` output the image object → `alt="{{ image.alt | default: title | default: shop.name | strip_html | escape }}"`.

## Whole-site section-independence audit — no real conflicts found
Scanned all 124 sections for cross-section conflicts: CSS classes styled *globally* in 2+ sections, bare element selectors in section styles, JS `window.*` globals, and unscoped `document.querySelector` calls; cross-referenced against which templates each section renders on.
- **No same-page CSS or JS collisions.** Every flag resolved to a non-conflict:
  - `.swiper-pagination-bullet` (info-banner ↔ customer-reviews) — both **class-scoped** under their own section wrapper; no clash.
  - ~68 `.video-gallery-*` (video-gallery ↔ video-gallery-youtube) — identical CSS on **different page templates**; never same page.
  - `.index-page` (main-page ↔ zooomy-wishlist) — different page templates.
  - `.rc-container-wrapper` (all `*-zipifypages`) — app-owned (Zipify).
  - "bare element" flags (feature-hero, waitlist) — CSS/Liquid **comment text**, not selectors.
  - JS `activate` / `loadMoreVideos` / `createVideoCard` — **inside IIFE/DOMContentLoaded**, not global.
- **Genuine hygiene notes (not conflicts):** video-gallery ↔ video-gallery-youtube duplicate ~68 classes + JS wholesale (separate pages, so safe — but a consolidation candidate); video-gallery's `window.tutorialsVideos`/`window.fierceagingVideos` are real globals but self-consumed on a single page.
- No code changes made — the sections are independent as-is.

## Live-store data: cleared all remaining `custom.promo_percent` values (4th of July promo teardown)
Per request, removed the per-variant `custom.promo_percent` metafield **values** from everything that still had them — executed via the Shopify Admin API (user-confirmed mutation), following the undo plan in `changelog/promo-metafield-manifest-2026-07-02.md`.
- **Found remaining:** 100 values (all `25`), only on the 3 multi-variant bundles — The Best Seller Beauty Bundle (64), Beginner Beauty Bundle (24), Perfect Complexion Bundle (12). The 8 original sale products (31 variants) and the `promo-25` tags had already been cleared previously.
- **Deleted:** all 100 via one `metafieldsDelete` mutation; 0 errors. Verified `metafieldsCount: 0` on the definition afterward.
- **Kept:** the metafield definition itself (`gid://shopify/MetafieldDefinition/245003682096`, `custom.promo_percent`) — intact and reusable for the next sale; the theme's promo-badge system stays in place and simply renders nothing with no values/tags present.
- **Bundle prices reverted (same day, follow-up request):** restored all 101 variants to their pre-sale prices via `productVariantsBulkUpdate` (one call per product, 0 errors) — Best Seller $270 → **$229.50** (64 variants), Beginner $152 → **$129.20** (24), Perfect Complexion $135 → **$114.75** (12), Volume Up Mascara $48 → **$40.80** (1). Compare-at prices untouched, so the bundles' built-in ~15% markdown display is back. Verified live: every bundle's min=max price equals the pre-sale value with the original compare-at intact.

## Every-page review — status
Reviewed all currently-enabled content/agency sections across the live storefront (homepage, coming-soon PDP, collection, product, blog/article, About, video, sitemap, account, footer/overlay). **Remaining: stock core-infrastructure sections** (`header`, `cart`, `cart-drawer`, `product`, `search`, `main-page`, `password`, `section-404`, `collection`, `announcement`) — treated as verify-only (stock Broadcast, high-risk to edit, consistently well-built in spot checks). Plus the deferred PDP forms (`custom-product-coming-soon`, `custom-product-waitlist`, `product-new`).

## custom-alternating-rows.liquid — verified clean, no changes (exemplary)
Custom section; live on coming-soon PDP.
- **Isolation ✓** — scoped BEM, all colors on theme vars, no JS, self-contained, no cross-section deps.
- **Elements ✓** — `<section aria-labelledby>`, `h2 → h3` hierarchy, review as `<figure>`/`<blockquote>`/`<figcaption>`/`<cite>`.
- **WCAG ✓** — image alt setting (+ fallback); **stars rating-alt setting** ("5 out of 5 stars", blank=decorative); verified icon `alt=""` + text; CTA guarded. A model section.
- Nits: `img_url:'master'` (perf); a `{% comment %}` TODO about future slider conversion (left).

## apps.liquid — verified clean, no changes
Stock "Apps" passthrough section (`{% for block %}{% render block %}{% endfor %}`); on product pages. No CSS/JS/markup of its own → trivially isolated; content a11y is app-owned (out of scope).

**— Homepage + coming-soon PDP: all enabled sections reviewed. Continuing to other live pages. —**

## section-rich-text.liquid — verified clean, no changes
Stock "Rich text"; on collection page. Scoped `{%- style -%}` vars on `#Rte--{{ section.id }}`; theme-framework styling; `image`/`divider` snippets; no `<script>`, no cross-section deps. Heading `<h2>`, subheading `<p>`, text/columns `rte` `<div>`, buttons `<a>` with text. (Nit: button `href="#!"` fallback — stock.)
