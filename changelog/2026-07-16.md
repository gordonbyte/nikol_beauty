# Changelog — 2026-07-16

## Push-blocker fix: upsell-products block restored to cart-drawer schema
- User pushed the synced theme to the live store (new theme Nikolbeauty_7/16/26 #186589053232) — Shopify rejected sections/group-overlay.json: block type `upsell-products` not defined in cart-drawer schema. Root cause: live's group-overlay.json (kept per sync rules) has an ENABLED upsell-products block; the rendering code for it exists in dev's cart-drawer.liquid (lines 13/508-517, identical to live's old version), but the schema block definition was lost in an earlier rewrite. Restored the definition (upsell_auto_open + pin_to_bottom, verbatim from the pulled live version) in both folders.
- Closed the validation gap that let it through: block-level validator now checks every block type in all templates + group JSONs against each section's schema — 355 blocks, all pass. Re-push needed to complete the theme upload.

## Dev→live folder sync (user-approved; prep for pushing updated theme to live store)
Three-bucket strategy: code = dev wins (mirror with deletions); settings = live wins (untouched); templates = live content + dev structure.
- **Mirrored dev→live:** assets/, layout/, sections/, snippets/, locales/. Live folder gains: minified assets (theme.min.css/main.min.css/main.min.js), jQuery+fancybox-library removal, video-lightbox snippet, vanilla main.js, Zipify purge, a11y section rewrites, CSS guarantee marquee (replaces live's GSAP version — fixes the stacked desktop+mobile render bug), Intelligems loader removed (user decision: remove everywhere), GTM kept (user decision). Live folder loses dead code the pull had restored: custom.js, styles.css, selectboxit.*, jquery-ui, loading.gif, cw-videos, cw-videosv2, article.liquid, custom-collection-tabs, header-predictive-search, text-promo, video-gallery-youtube.liquid.
- **Preserved from live (settings):** config/settings_data.json + all group JSONs (group-header, group-header.context.us, group-overlay, group-footer, group-pre-footer) + all content-bearing template JSONs.
- **Pre-footer ported INTO dev** (live-store feature, confirmed rendering on live homepage): sections/prefooter.liquid + group-pre-footer.json copied to dev; `{% sections 'group-pre-footer' %}` added to dev theme.liquid before custom-footer (same position as live).
- **Templates:** deleted 19 dev-removed templates from live; took dev's re-wired copies of index.homepage-style-2.json, product.coming-soon.json, page.video-gallery-youtube.json (live copies referenced renamed/removed sections: cw-videos→custom-videos, video-gallery-youtube→video-gallery, custom-collection-tabs dropped) + gift_card.liquid; stripped zp-product-header/footer-content entries from live's product.json, product.new.json, product.waitlist.json (kept live settings otherwise).
- **Validated:** every section type referenced by live templates/groups exists; JSON valid + order arrays consistent everywhere; 0 refs to zipify/intelligems/selectboxit/code.jquery.com; fancybox remaining only as the `data-fancybox` attribute video-lightbox.liquid listens for (intentional); page.live-shopping.json intact; settings_data untouched by sync.
- **Post-push visual checklist (dev store):** homepage mobile guarantee marquee scrolls; pre-footer renders above footer; /pages/video-gallery-youtube page (template re-wired to video-gallery section); coming-soon PDP (custom-videos section); PDP renders clean w/o zipify sections; testimonials/filters/comments (jQuery rewrite).

## jQuery removed from shopify-dev (user-approved rewrite)
- Rewrote all jQuery usages in shopify-dev/assets/main.js to vanilla JS (1:1 behavior, null-guarded) and removed the jQuery 3.6.3 CDN `<script>` from shopify-dev/layout/theme.liquid (was loaded sitewide on every page, incl. homepage). Saves ~31KB + a third-party connection (code.jquery.com) per page view. Stock Broadcast never used jQuery — it was agency-added solely for main.js; verified zero other consumers in sections/snippets/templates/config.
- Converted features → test list: testimonials overlay show/hide (homepage `testimonials` section), video-hub category filter buttons (`yt-videos-list` on homepage + page.videos), article comments toggle (`new-article` on article.json + article.youtube-blog), plus $(document).ready → DOMContentLoaded wrapper (safe: main.min.js is deferred).
- Dead code found during rewrite, initially converted+kept, then **REMOVED same day (user-approved follow-up)**: whole `.hero-slider` block — Swiper init + title hover (no hero-slider anywhere in theme or live homepage — retired with hero consolidation ac7c2e3), `#about` header pin (no id="about" in theme or live about page body), `agree_cart`/`checkout_button` cart-terms toggle (checkbox doesn't exist in theme), `.bcsell-upsell-btn` upsell→quantity-counter listener (0 hits on live PDP; only references anywhere were the listener itself). main.min.js 4.5KB → 3.2KB.
- Two incidental fixes: removed a debug console.log (fired on every page with testimonials); guarded the video-hub URL-fragment `link.click()` (previously threw and killed the rest of the ready block when the hash matched no filter link).
- **scripts/minify-assets.mjs gotcha discovered:** it minifies from shopify-live/ sources and copies .min files into shopify-dev/ — written before the folders diverged. Running it clobbered dev's main.min.js with the old jQuery build; regenerated dev's copy directly via esbuild from dev's main.js. Script needs updating before next use (divergence #3: main.js/main.min.js + theme.liquid now differ between folders, after Intelligems 07-14 and Zipify 07-15).
- Verified: 0 jQuery refs in new main.min.js (4.5KB), parses clean, all feature selectors present. shopify-live/ untouched (still loads jQuery — its main.js unchanged).
- Section status: testimonials + yt-videos-list + review-swiper-card are ACTIVE on the live homepage template (index.json); videos-filter + yt-videos-list active on page.videos (live "Videos" page); new-article active on all blog articles. Change is dev-folder-only; needs dev-store push to test.

## Removed Feb Lip Gloss preload tombstone from theme.liquid (both folders; user-approved)
- Deleted the `{% if template == 'index' %}` block (lines 65–74) from **both** shopify-dev/ and shopify-live/ layout/theme.liquid. The block was the leftover shell of the retired Feb Lip Gloss homepage hero preloads (two `<link rel="preload" as="image">` tags removed during the 07-08→13 perf campaign because the homepage no longer rendered those images — they caused "preloaded but not used" warnings and wasted bandwidth). What remained was only an explanatory Liquid comment plus an orphan `</style>` that rendered as a stray invalid tag on the homepage.
- Safety verified before deletion: the `</style>` was truly orphaned — the only real `<style>` open/close pair in the file sits much later in the body (live 481/490, dev 449/458); nothing before the block opened a `<style>`. Browsers were already discarding it, so removal is behavior-identical.
- Verified post-edit: `<style>`/`</style>` 1=1, if/endif balanced (live 26=26, dev 23=23), 0 `Feb_Lip` references left in either theme.liquid. Pre-cleanup original (with the actual preload tags) still viewable in delete_later/layout/theme.liquid:72-83.
- Section status: theme.liquid is the sitewide layout — active on every page of both themes. Change needs a dev-store push to take effect there; live folder edit is repo-only (no live push).
- Related, NOT changed: shopify-live/templates/index.json:90 still references the `Feb_Lip_GLOSS_2000x719...webp` image in a section setting (theme-editor content; check whether that section is dormant before touching).
