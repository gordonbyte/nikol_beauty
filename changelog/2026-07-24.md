# Changelog — 2026-07-24

## JSON-LD cleanup, Change 3: product aggregateRating reunited with the main Product node (shopify-dev only)
- Context: the 07-23 JSON-LD audit found the Junip star rating living on a nameless orphan `Product` node in `snippets/seo-jsonld.liquid` (different `@id` than the real Product node) — risking review stars not attaching to the priced product in Google results + "missing field: name" Search Console warnings.
- Verified first that the user's own Changes 1–2 from yesterday are correct: `sections/header.liquid` now contains zero JSON-LD (both the ungated Organization block and the index-gated WebSite/SearchAction block removed); the Organization `description` was carried into `seo-jsonld.liquid` (line 32).
- **New:** `snippets/product-jsonld-rating.liquid` — single source of truth for the Junip `aggregateRating` object (same metafields + same only-when-reviews-exist guard as the deleted block; trailing comma inside the guard so JSON stays valid in both branches — both branches validated by parse test).
- **Edited (one-line insert each, after the `"description"` line of their Product JSON-LD):** `sections/product.liquid`, `sections/product-new.liquid`, `sections/custom-product-coming-soon.liquid`, `sections/custom-product-waitlist.liquid` → `{%- render 'product-jsonld-rating', product: product -%}`. The variant/offers logic in those blocks untouched.
- **Removed:** the orphan rating-only Product block from `seo-jsonld.liquid` (was lines 82–97); the surrounding product-page `if` and its FAQPage block kept. Snippet header comment updated (no longer claims AggregateRating; points to the new snippet).
- Result: each product page now emits exactly ONE Product entity carrying name + image + offers + aggregateRating together.
- **Verification still pending live render:** local `shopify theme dev` wasn't running (user starts servers). To verify: start theme dev → I fetch a reviewed product (e.g. /products/fbs-eye-brightener) and confirm single Product node with offers+rating; then Google Rich Results Test. Not pushed anywhere; `shopify-live/` untouched.
## JSON-LD cleanup, Change 4: single Article entity per blog post (shopify-dev only)
- `sections/new-article.liquid` Article block upgraded: author swapped from an inline mini-Person ("Nikol Johnson, Licensed Esthetician", hardcoded nikolbeauty.com URL) to `{"@id": "{{ shop.url }}#nikol-johnson"}` (Google merges by @id → full-credential Person from seo-jsonld; user confirmed no guest posts exist); publisher swapped from an inline mini-Organization (115px logo) to `{"@id": "{{ shop.url }}#organization"}` (also removes another nested Organization duplicate); image line wrapped in `{% if article.image %}` (fixes pre-existing empty `"image": ""` on imageless articles).
- `snippets/seo-jsonld.liquid`: BlogPosting block deleted (self-contained if-block); header comment updated. Both image branches of the resulting Article JSON parse-validated; no leftover `article` references in the snippet.
- Result: each article page emits ONE Article entity, linked to the canonical Person/Organization nodes.
## JSON-LD cleanup, Change 5: single Person entity (shopify-dev only) — cleanup COMPLETE
- `snippets/seo-jsonld.liquid` Person enriched: added `"url": "{{ brand_url }}/pages/about"` (canonical about-page link, carried from the deleted block) and merged `knowsAbout` topic lists (added Mascara, Professional Makeup Application, Anti-Aging Skincare, Beauty Tutorials; dropped "Licensed Esthetics" as redundant with Esthetics + jobTitle).
- `sections/about-header.liquid`: standalone Person JSON-LD block (lines 40–71) deleted — it carried the WRONG social handles (same set removed in the user's Change 1), the brand logo masquerading as Nikol's photo, and an inline duplicate mini-Organization. NOT carried over by design.
- Final emitter inventory verified: theme.liquid (breadcrumbs), seo-jsonld (graph), the 4 product sections (Product + rating snippet), new-article (Article), page.beauty-reinvented (Book/FAQ page-specific). Every duplicate from the 07-23 audit is now resolved.
- Person.image DONE same day: user uploaded a 1000×1000 headshot to live-store Files (`nikolbeauty_photo.jpg`, 96KB, URL verified 200) → added as ImageObject (url + width/height) on the Person node. Hardcoded CDN URL (live-store files CDN — resolves from dev previews too, unlike `file_url` which would 404 on stores lacking the file). knowsAbout "Esthetics" reworded to "Licensed Esthetics" per user.
- Live-render verification of Changes 3–5 still pending a running `shopify theme dev` (user starts it) or a theme push; then Google Rich Results Test.

## Dev → Live port (repo folders only; nothing committed, nothing pushed to the store)
- Full recursive diff of `shopify-dev/` vs `shopify-live/`: 17 differing files + 1 dev-only new file; ALL template JSONs identical (page settings never at risk). Per-file direction analysis confirmed zero unexpected live-only content — every live-only line was an old version of a deliberately changed block.
- **Copied dev → live (17):** `assets/main.css`, `main.min.css`, `main.min.js`, `theme.min.css` (min.js/theme.min.css differ only as minifier artifacts of identical sources), `config/settings_schema.json` (promo header info text — schema is code), `sections/about-header.liquid`, `custom-product-coming-soon.liquid`, `custom-product-waitlist.liquid`, `header.liquid`, `new-article.liquid`, `product-new.liquid`, `product.liquid`, `snippets/cart-bar.liquid`, `product-grid-item.liquid`, `product-price.liquid`, `seo-jsonld.liquid`, **NEW** `snippets/product-jsonld-rating.liquid`.
- **Kept live version (settings): `config/settings_data.json`** — only diff vs dev is an app-embed disabled toggle. Templates: no action needed (identical).
- This carries to production-readiness: JSON-LD cleanup (Changes 1–5 + author photo), per-discount badge colors/text/`{br}`, corner-ribbon badge style, promo styling metafield reads. Post-copy verification: folders now differ ONLY in settings_data.json.
- Committed by user request: `50300de` on develop — all shopify-live changes (22 files, +207/−167) incl. previously-unstaged mid-July sync state + both new snippets. Left uncommitted per scope: scripts/minify-assets.mjs, changelogs.

## FAQ Rewards page rebuild (preview file, not yet on the store)
- `faq-rewards-preview.html` created at repo root (user request): full restructure of the page.faq-rewards-program content. Semantic HTML (5 `<section>`s with h2 + 21 h3 questions — no h1, the page template provides it), jump-to-section `<nav>` at top (anchor links, no accordion per user — elderly audience), back-to-top link per section.
- WCAG 2.2 touches: left-aligned text at 76ch measure, ≥40px link targets in TOC, `:focus-visible` outlines, `scroll-margin-top` for anchored headings under the sticky header, smooth-scroll gated behind `prefers-reduced-motion`, no target=_blank, underline-for-emphasis replaced with `<strong>` (underline reads as link).
- Content fixes: broken KaTeX markup ("20 Roses = $1") cleaned, weird nested list flattened, mailto links normalized.
- SEO: embedded FAQPage JSON-LD with all 21 Q&As (validated; matches visible content; no other FAQPage emitter on page templates so no duplicate).
- Fonts/sizes: pasted block sets NO font-family/size — inherits theme (Cormorant headings/Inter body per live settings); the preview wrapper mimics those fonts for local viewing only.
- To apply: user opens the file in a browser to review; then copies the COPY START→COPY END block into Admin → Content → Pages → FAQ Rewards Program (HTML view).
- Iterated extensively with user same-day (welcome panel w/ rose + stat pills, cream jump menu: uppercase bold links, mobile 2-col grid w/ horizontal row separators + cream bg, desktop sticky row w/ vertical separators no bg; sticky uppercase section bands w/ full border + icons; tier-matched rose bullets + indented lists; VIP tier cards #FEF8FA bg, 20px gap; bold ::marker step numbers; mailto links weight 500; theme link-gradient suppressed on menu (`background: none !important`); all em dashes removed from content+schema; scrollspy built then removed per user).
- **FINALIZED for paste:** preview wrapper fully stripped — file now contains ONLY the page-content block (starts `<style>`, ends JSON-LD `</script>`; 0 preview/COPY/wrapper refs; schema re-validated). Renamed `faq-rewards-preview.html` → **`faq-rewards-page.html`**. First strip attempt sliced at the wrong "COPY START" occurrence (wrapper comment mentioned it) — caught by strict re-verification, fixed.
