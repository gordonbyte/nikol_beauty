# Changelog — 2026-07-22

## Discount Sync: per-discount styling verified end-to-end on dev store
- `npx prisma generate` completed after dev-server stop (yesterday's EPERM was the running server holding the query-engine DLL); client now exposes `badgeBg`/`priceColor`/`badgeText`.
- **App UI table polish:** Schedule column now stacks start/end dates on two lines (medium date + short time) instead of one wide side-by-side string; Style button converted to an icon-only paintbrush button (`PaintBrushFlatIcon`, a11y label "Edit badge & price style") inline right of Track/Stop tracking.
- **Root causes of "colors not showing" (user report):**
  1. The dev store's published theme ("nikolbeauty-dev" id 149231566950, Broadcast 5.5.0) predated the per-discount color code — the new snippet code existed only in the local `shopify-dev/` folder.
  2. That theme copy was also missing `templates/product.json` + `templates/collection.json` (+ 3 PDP variants) — earlier upload had silently rejected them because the referenced metafield definitions didn't exist on the dev store → ALL product & collection pages 404'd (product `.js` endpoints 200'd, proving publication was fine).
- **Fixes (dev store only; live store untouched):**
  - Pushed local `shopify-dev/` onto the published dev-store theme (user-approved). First pass uploaded everything except the 5 templates (same dynamic-source rejections).
  - Created the 15 missing metafield definitions via the app's refreshed offline token: product `custom.instruction_title_1-6` (single_line), `custom.instruction_content_1-6` + `tab_content` (multi_line), collection `custom.bottom_description` (rich_text), `custom.mobile_background` (file_reference) — types taken from the live Matrixify export headers.
  - Re-pushed the 5 templates → zero errors; PDPs and collection pages now 200.
- **E2E verification (storefront probes, password `shegin`):** collection page = 8 cards with orange `#c05107` badge "Big Sale 20% OFF"; rose-lip-balm PDP = struck $23.00, sale price in green `#2f8d07`, "On Sale" pill in orange on both main price and sticky cart bar. actually-i-can PDP correctly shows NO promo (sold out — theme suppresses promo on unavailable products by design). Homepage lagged behind briefly (section cache).
- Snippet status: `product-grid-item` / `product-price` / `cart-bar` changes are active sitewide on the dev store's published theme. NOT yet ported to `shopify-live/` — port + user-run live push required to take this feature to production, along with creating the 3 style metafield definitions… (live already has promo_percent).
- Reminder carried forward: automation gap — nothing calls `/api/sync` on a schedule yet, so scheduled discounts still need a manual Sync/Track click until the app is deployed with a cron (Phase 6).

## Second session: 404 diagnosis + template re-push (overlaps fix above)
- Independently diagnosed the same dev-store 404s (user report: `/products/eye-primer` → 404). Verified via Admin GraphQL: product ACTIVE + published, all 131 collections published, MAIN theme missing the same 5 templates (`product.json`, `collection.json`, `product.new/.coming-soon/.waitlist.json`).
- Re-pushed the 5 templates from local `shopify-dev/` (`shopify theme push --only ... --allow-live`, sandbox MAIN theme) — zero errors, presence verified via GraphQL. Clean upload consistent with the metafield definitions created earlier today being the original rejection cause (not app blocks).
- Remaining known 404s: `/pages/...` links (Videos, Rewards, policies, footer "Collections" → `/pages/collections`) — pages intentionally skipped in the 07-21 seed; recoverable via Matrixify pages-sheet import.

## Discount Sync UI/badge refinements (afternoon)
- **`{br}` line-break token for badge text** (user request — long custom text overflowed the round badge): theme `product-grid-item.liquid` splits the badge label on `{br}` into stacked `promo-box__line` spans (CSS already stacks them; no CSS/minify changes). Snippet pushed to the dev-store theme. App: Style-modal help text documents `{br}`; modal preview renders real stacked lines; table "Badge" chip deliberately renders `{br}` as a space (single line, per user) . THEME-CONTRACT.md updated. Active discount's badgeText set to `Big Sale{br}{n}% OFF` (via DB + `/api/sync`).
- **Sync-triggered cleanup observed live:** the user's "Test" discount (14:00–14:30Z) had expired, so the triggered sync correctly removed all 10 tags + 30 style metafields — badges disappeared from the storefront by design. `{br}` storefront verification pending the user reactivating/extending the Test discount in the dev-store admin (app intentionally lacks write_discounts).
- **Status badges:** replaced Live/Inactive with **Live** (green) / **Upcoming** (blue, startsAt in future) / **Expired** (gray) — "Inactive" read as if a scheduled sale had passed.
- **Sort order:** discount table now sorted by startsAt descending — upcoming (latest start) on top, then live, expired at the bottom.

## Discount Sync: 3-discount live test + conflict policy change
- **Status/sort UI**: Live/Upcoming/Expired badges + startsAt-desc ordering (see afternoon section) verified during test.
- **Noon test (user-requested), 12:00–12:30 EDT:** created 3 automatic discounts via MCP (re-authed to dev store after switch-shop revocation): Test A 15% (3 single-variant products, orange + "Big Sale{br}{n}% OFF"), Test B 25% (bb-cream + serum-foundation whole products, purple, default text), Test C 40% (variant-only: eyeliner Plum+Navy, lip-liner Silk+Coral, bb-cream Fair — overlapping B; crimson "Save {n}%"). Pre-tracked+styled via DB; timed background triggers fired `/api/sync` at 12:00:40 (webhook from user's discount edit had already applied everything — counts 0) and were superseded for cleanup (dev server crashed ~12:20 on route-edit hot-reload; restarted after killing orphaned node PIDs holding the Prisma DLL; vite port drifted 51636→53022 so the 12:31 timer missed — cleanup verified manually at 12:44: AppliedState empty, all promo tags/metafields removed, merchant tags preserved).
- **Conflicts observed at noon (old max-wins behavior):** rose-lip-balm 15% vs 20% (Test A vs user-extended old "Test" — accidental overlap) and bb-cream Fair 25% vs 40% (designed).
- **NEW conflict policy (user decision):** when any variant of a product is targeted by >1 tracked discount at DIFFERENT percentages, the app now writes NOTHING for that product (no tag/metafields/styling) instead of applying max-% — avoids displaying a possibly-wrong number; checkout still applies whatever Shopify resolves. Conflicts are now persisted in AppliedState and the admin UI shows a persistent warning banner naming each product + the clashing percents ("promo display paused") until a sync finds the overlap resolved. Implementation: `buildDesiredWrites` skips conflicted products; `syncShop` enriches conflicts with product title/handle (hydrateProducts now fetches title) and saves them in state; loader surfaces them; old transient banner replaced; api.sync empty-state literals extended. Coded + deployed to the restarted dev server but NOT yet exercised against a live overlap — needs a follow-up conflict test.

## Discount Sync: pre-live conflict warnings + Ignore, and corner-ribbon badge style
- **Predictive conflict warnings (user request):** reconciler now resolves targets across live AND upcoming (not-yet-ended) tracked discounts in one pass (`variantContribs` per variant), flags a conflict only when the clashing discounts' schedules actually overlap, and scopes it `live` (promo display paused — suppression unchanged) vs `upcoming` (amber "Heads up — scheduled discounts will overlap" banner BEFORE go-live). Verified pre-live: BB CRÈME 25% vs 40% surfaced as `upcoming` 47s before the 1:00 window went live, then escalated to `live` during the window.
- **Ignore/Restore (user request):** each conflict row has an Ignore button (hides that warning only — storefront suppression unaffected); dismissals persist in AppliedState (`ignored` keys), auto-prune when the conflict disappears (re-appearing conflicts warn fresh), and a subdued "N ignored — Restore" line brings them back.
- **Corner-ribbon badge style (user request, option 2; circle stays default):** adapted the user-supplied CodePen corner-ribbon CSS for product cards — demo scaffolding dropped, hardcoded green gradients replaced with per-discount color via `--promo-ribbon-color` + color-neutral white/black overlay gradients for gloss/shading + glare pseudo-element; tiny corner "curls" omitted (pixel-alignment fragile at card scale). New product metafield `custom.promo_badge_style` = `ribbon` (absent = circle). Theme: `product-grid-item.liquid` branches on it (`{br}` renders as space in ribbon mode — Liquid gotcha: `'{br}'` string literal is fine in `{% liquid %}` but breaks the `{{ }}` output-tag parser; precompute `promo_ribbon_label` instead), CSS in `main.css` + minified. **Found stale tooling:** `scripts/minify-assets.mjs` still sources from `shopify-live/` (predates the 07-20 dev-is-source-of-truth reset) — dev min built via direct esbuild this time; script needs updating.
- **App:** `badgeStyle` column added to TrackedDiscount (migration `badge-style`), Style modal gained a "Badge style" Select (Circle default / Corner ribbon), engine writes/clears `promo_badge_style` like the other style metafields.
- **Live verification:** test discounts rescheduled 1:35–2:10 PM EDT; sync wrote 26 metafields + 6 tags; BB CRÈME conflict live+suppressed; storefront search page renders serum-foundation with `promo-ribbon__band` "25% off" in Test B purple `#5C2D91`; circles render for A (orange 2-line) and C (crimson "Up to Save 40%"). 2:11 cleanup timer armed (port 53831 after server restart for the migration; earlier 1:31 cleanup verified: 25 metafields + 6 tags removed).

## Follow-ups: minify script fixed + ribbon flipped to top-left
- `scripts/minify-assets.mjs` rewritten: each theme folder now minifies from ITS OWN sources (dev no longer derives from live; live untouched by dev-only features). Verified: dev main.min.css 46.3KB (with ribbon), live main.min.css 45.2KB (without). Pre-existing `*zoom` IE-hack warnings in theme.css are harmless.
- Corner ribbon mirrored to the top-LEFT per user (container `left:0`, band `left:-38px` + `rotate(-45deg)`, glare mirrored). Minified + pushed `main.css`/`main.min.css` to the dev-store theme; verified served from CDN (`t/4/assets/main.min.css`).

## UpPromote investigation (live store — read-only, no changes made)
- Located all UpPromote references: single app-embed block in `settings_data.json` line 275 (live + dev copies); user disabled it via theme editor. Remaining console "UpPromote WebPixel" is a store-level web pixel (Settings → Customer events → "UpPromote Affiliate", Always on) — app-managed, not removable from that screen or from the theme; only removable by uninstalling the app.
- Flagged before uninstall: UpPromote is a configured, live affiliate program (portal af.upromote.com/nikolbeauty, 30-day cookie, affiliates/referrals/payments data) — uninstalling ends tracking and may delete commission data. Distinct from the Awin pixel and the hardcoded ShopMy script in `theme.liquid` line 482.
