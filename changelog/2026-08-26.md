# 2026-08-26

## Tolstoy migration — analysis, census, and the Carol pilot (Tolstoy-side complete)

- **Channelize architecture fully mapped (read-only):** 8 fierceaging metaobject pages drive everything; ~25 tutorials entries have no products and never rendered shoppable videos; 154 product-video pairings across 38 products = 125 unique videos (clips are reused across products); ~1,400 of the metaobject template's 1,830 lines are Channelize damage control (z-index wars, MutationObserver + 1s whole-page polling, modal hacks, live diagnostic console logs). Recommendation delivered: one Tolstoy widget per episode, publish ID on the metaobject's unused `shoppable_playlist` field, gated template (Tolstoy when ID present, Channelize otherwise) = per-episode switch, instantly revertible.
- **Recovery record:** KAN-204 (closed) holds every product's exact `fierceaging_playlists` JSON + `shoppable_playlist` values, the template/script/publicKey constants, and per-guest counts — the full revert kit.
- **Tolstoy account surveyed:** app installed on all 3 stores (live registered ✓, live catalog imported ✓ 67 products), 0 widgets before today, storefront untouched (no script loads — app embed off). Library census: ~150 shopable clips uploaded back in April for 10 guests — Jill 15 ✓, Gloria 13 ✓, Carol 15 ✓, Meg 18, Pam 14, Marci 20, Dawn 16 (1 short: Strong Brew), Mary 10 (6 short), plus Ruth 17 + Peggy 12 (no live episodes). Full tagging map published for Randell's review (artifact "Fierce Aging Tagging Map").
- **CAROL PILOT — Tolstoy side done (all writes against the LIVE store catalog):**
  - Tagged all 15 Carol clips with their products (20 tag operations incl. two 3-product clips, 0 failures)
  - Playlist "Carol — Fierce Aging" created (id 4cafcc32-5fdd-4b51-96e0-f9f2a957701c), all 15 added
  - Widget "Carol — Fierce Aging (episode page)" created: **publishId `5ocsnbya4m97d`**, type carousel, sourced from the Carol playlist (verified: exactly 15 videos), switched ON in Tolstoy
  - Storefront still shows NOTHING (by design): the Tolstoy app embed is off in the live theme; Tolstoy provided the enable deep-link for when we're ready
- **Gated template built (both folders, hash-verified; pushed to the dev store published theme #149231566950 — Liquid validated on upload, homepage render-probed clean):**
  - Embed markup confirmed from Tolstoy's own widget script (widget.gotolstoy.com/widget/widget.js): it scans for `.tolstoy-carousel[data-publish-id]` — the app embed supplies the script, the template only needs the div.
  - `templates/metaobject/beauty_tutorial_videos.liquid`: computes `use_tolstoy` from the metaobject's `shoppable_playlist` field (dash check keeps any legacy Channelize UUID from passing as a Tolstoy ID). Tolstoy mode keeps the Channelize layout exactly — each product's video cell in the FEATURED PRODUCTS grid renders `<div class="tolstoy-carousel" data-publish-id data-product-id>`: the episode's ONE widget, filtered per product (the widget script fetches each instance's config with its own element's product ID — confirmed in their source via `getEmbedProductId(element)`, and confirmed server-side by widget+productId queries returning exactly that product's clips). Skips the ~440-line widget-wrangling script (incl. the 1s polling) and the trailing diagnostic script. Field blank → the page is byte-for-byte today's Channelize path. Revert = clear the field. (First cut rendered one page-level "WATCH & SHOP" carousel; Randell wanted the per-product-cell videos like Channelize — reworked same day.)
  - `layout/theme.liquid`: the Channelize CDN script is now also skipped on episode pages whose metaobject carries a Tolstoy ID — a Tolstoy-mode page loads no Channelize code at all.
- **PILOT LIVE:** Randell pushed the gated template live, set Carol's `shoppable_playlist` to the publish ID, and enabled the Tolstoy app embed in the live theme editor — per-product carousels now rendering on Carol's page. (First symptom was empty divs; cause was the app embed still off — Tolstoy's storefront check confirmed `app_embed_off`, enabling it fixed it.)
- **Tolstoy app embed baked into theme settings (both folders, hash-verified; pushed to dev store theme):** added enabled app-embed block `shopify://apps/tolstoy/blocks/widget-block/06fa8282-42ff-403e-b67c-1936776aed11` to `config/settings_data.json` so future theme pushes can't switch the embed back off. Verified functionally: after pushing settings to the dev store theme, its storefront now loads widget.gotolstoy.com/play.gotolstoy.com scripts.
- **Per-product filtering turned on (Tolstoy-side):** the widget was showing all 15 Carol clips in every product cell (the storefront config API ignores productId for playlist-sourced widgets). Fix = `isPdpMode: true` on the widget — each cell's `data-product-id` now returns only that product's clips (verified: Manifest & Glow cell renders exactly its own clip; all 19 grid products have ≥1 clip). No theme change needed for this part.
- **Video cell styling fixed (template CSS, both folders, pushed to dev store theme):** Randell reported the video rendering as a narrow sliver with floating arrows. Root cause: Tolstoy renders a fixed 234×416 (9:16) tile but the grid cell was 182px wide → center crop; internal flex gaps ate another 39px; multi-clip cells stacked tiles vertically showing the seam. Final design (verified on the live preview theme at 1440px and 390px via Playwright + bot-detection workaround — Tolstoy's loader refuses to init for `HeadlessChrome`/webdriver): video column sized to exactly one full tile (`minmax(0,1fr) 234px`, ≥769px), Tolstoy's inner containers forced full-width with gaps zeroed, only the first tile shown per product, inline arrows hidden — a tap opens the fullscreen player which carries the product's full feed + in-player "Shop"/add-to-cart (verified open + product panel). Mobile: cell max-width 234px.
- **Grey tiles fixed — poster backfill script (template, both folders, pushed to dev store theme):** Randell reported most video cells rendering grey. Root cause: Tolstoy's carousel manager assigns a poster frame only to the FIRST carousel on the page (one-per-page assumption) — every other cell's video has an empty poster and never loads, even in-viewport. Fix: a small script in the Tolstoy branch fetches each cell's own one-product widget config from the same public CDN endpoint the widget uses (getWidgetConfig?publishId&productId — with isPdpMode on it returns exactly that product's clip incl. `stockAsset.shopifyPosterUrl`) and sets the missing poster (guarded: never overwrites one Tolstoy set). Verified on the preview theme: all 19 cells show their preview frame, and clicking a formerly-grey tile opens the mini-player with the right product + Shop button (Serum Foundation $46).
- **Video corners + autoplay (template, both folders, pushed to dev store theme; per Randell 08-27):** tiles get a 10px border-radius; the poster-backfill script extended into an autoplay manager — each cell plays its product's SD rendition (480p 1.5Mbps from `stockAsset.shopifySources.sd`) muted/looped/playsinline while ≥35% in the viewport and pauses off-screen (IntersectionObserver; skipped under prefers-reduced-motion; never overwrites a src Tolstoy set). Verified on the preview theme: in-viewport cells playing with advancing currentTime on the SD files, off-screen cells not started, all paused after scrolling away, radius 10px everywhere; screenshot shows each cell mid-play on its own product's footage.
- **Remaining pilot steps:** (1) Claude rewrites `templates/metaobject/beauty_tutorial_videos.liquid` with the ID-gate (inert until a metaobject has a publish ID); (2) Randell enables the Tolstoy app embed in the live theme editor; (3) template rides Randell's next live push; (4) Randell pastes `5ocsnbya4m97d` into Carol's metaobject `shoppable_playlist` field → her page flips; (5) verify in-player add-to-cart desktop+mobile. Revert at any point = clear the field.
