# 2026-08-10 (Monday — sale-prep day)

- Daily board review: Fierce Aging escalated to Highest (KAN-7 + launch subtasks; sale starts Wed 8/12); overdue post-push items re-dated to today (KAN-150 checklist, KAN-147 badge test, KAN-153 PageSpeed); KAN-139 → 8/14 to pair with the app-test day.
- KAN-147 badge test, automated half PASSED on live: badge (#reviews) scrolls 2453px, Junip section lands at viewport top, zero console errors on with-reviews AND 10x-mirror pages (Junip renders on every live PDP, so the "no-op" case doesn't arise). Awaiting Randell's manual smooth-scroll confirmation to close.
- Microsoft 365 connector hooked up; read the 3 Nikolbeauty_bot AI-fleet emails (Aug 9). Created tasks with subtasks + estimates: **KAN-161** scan triage (Tue 8/11, ~1.5h; scanner's "no schema/canonical" claims are provably false — verification-first) w/ KAN-162/163/164 · **KAN-165** verified SEO fixes (Thu 8/13, ~3h) w/ KAN-166-169 · **KAN-170** video sitemap + GSC indexing (Fri 8/14, ~2h) w/ KAN-171/172/173 (GSC check Mon 8/17). New `ai-fleet` label. Connector quirk found: subtask parent must go in additional_fields.parent, not parentIssueKey.
- Dev-store test discount for the Discount Sync app (per Randell, store identity verified = nikolbeauty-dev app-dev store): automatic 20% "TEST - Discount Sync 20% (excl bundles/gift cards, safe to delete)" targeting the "All products except bundles" collection (62 products, same handle as live sale discounts), combinesWith product discounts. gid://shopify/DiscountAutomaticNode/1567355437158. Delete after testing. (First ask was all-products; corrected to exclude bundles/gift cards matching the real sale shape. Also: refused to create it on LIVE — app not installed there + would've discounted real orders pre-sale.)
- `sections/custom-guarantee-list.liquid` upgraded (per Randell): background setting type color→color_background (solid OR gradient picker, same id so saved values persist); NEW top+bottom border (thickness 0-10px range default 0 + color picker default #F7F2ED); NEW text controls (color picker with empty=theme-text fallback, size range 10-28px default 18, weight select 300-700 default 400). All wired via scoped CSS vars; defaults chosen so existing instances render identically until touched. Follow-up same session: borders split into INDIVIDUAL top and bottom controls (border_top_thickness/color + border_bottom_thickness/color, replacing the shared pair). Pushed to dev theme #186425934130.
- Pulled Randell's theme-editor saves from the local-server Development theme (#186425573682 on nikolbeauty-dev; found after live store showed no dev theme) into shopify-dev: settings_data.json, product.json, page.app-ost-tracking-page template — now persisted in local files across dev-server restarts.
- Discount Sync app + theme: NEW per-discount "Ribbon shadow color" setting (per Randell; default = existing look):
  - App (`custom-nikol-apps/discount-sync`): prisma `shadowColor String?` column + migration `add_ribbon_shadow_color` applied; `sync.server.js` STYLE_METAFIELDS → writes `custom.promo_ribbon_shadow`; UI ColorField in the style modal (shown only when shape = Corner ribbon), loader/action/save wired with normColor.
  - Theme: `product-grid-item.liquid` assigns `promo_ribbon_shadow` (metafield, default #000000) → `--promo-ribbon-shadow-color` on the ribbon; `main.css` `.promo-ribbon__band` box-shadow now derives its dark inset ring (35%) and drop shadow (28%) from the var via color-mix — default #000 reproduces the original `#00000059`/`#00000047` shading (34.9%/27.8%); white gloss inset unchanged. THEME-CONTRACT.md updated. Minified + pushed to dev theme.
- Sale setup (Randell, a day ahead): **KAN-12 DONE** — automatic 20% product discount created + scheduled Aug 12 00:00 → Aug 16 23:59 ET (auto-deactivates; KAN-28 becomes verify-only), "All products except bundles" collection targeting, gift-card exclusion checked. **KAN-43 DONE** — Combinations → other product discounts enabled. Reminder standing: run Discount Sync locally before/at launch so theme badges show the sale.
