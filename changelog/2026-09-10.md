# 2026-09-10

## Coming-soon PDP — product FAQ content for all products (DEV store admin data, no theme changes)

- Authored and loaded per-product FAQ content into the DEV store (`nikolbeauty-dev-mab1srre`) for all 66 active products (gift card excluded): 532 `cs_faq_item` entries (4–9 per product), one `coming_soon_page` entry per product (fields set: `page_name`, `faq_subtitle`, `faqs`), and `custom.coming_soon` set on every product. BB Crème's existing page entry (270836007014) kept its GID; its 9 FAQ items were rewritten in place and `faq_subtitle` added.
- Question selection came from the FAQ Bank (Junip review mining, 579 candidate questions); answers were grounded in Gorgias resolutions and macros (read-only): airless-pump troubleshooting (shake, pump facing down, gentle pumps → Customer Care with batch-number photo if it persists), damaged/incorrect arrivals (48 hours, photos + invoice), all-sales-final for personal-care items with a pre-purchase shade-help offer (hello@nikolbeauty.com + natural-light photo), restock alerts ("Notify me when back in stock"), one discount code per order, 15% bundle savings, free US shipping at $125, and product facts from the product descriptions (shades, undertones, liner matches, sizes, formulas, origin). Negative anecdotes, internal notes and customer details were deliberately left out; every answer is a resolution or how-to.
- Facts I could not verify were not claimed (mirror power source, hair-clip count, full-size claims for bundles, Skinny Brow Pencil sharpening) — those questions were replaced with answerable ones.
- Verified on the dev storefront (theme 149231566950): `/products/serum-foundation?view=coming-soon` and `/products/pencil-sharpener?view=coming-soon` render their FAQ list and subtitle; the steps-and-below sections that have no metaobject content stay hidden (strict no-fallback, as designed 2026-09-09). BB Crème still renders its full steps/skin-safe/switched/banner content.
- Export for live replication: `changelog/coming-soon-faq-export-2026-09-10.json` (all 66 pages with question/answer text and subtitles). Scratch ID map: `faq-ids.json` in the session scratchpad. KAN-211 (live replication) updated with the added scope — the export must be recreated on LIVE before the coming-soon section files are pushed.

## Coming-soon PDP — `.product__content` made dynamic (ratings + description)

- `sections/custom-product-coming-soon.liquid` (live folder edited, copied to dev, pushed to dev theme 149231566950):
  - **Rating summary block** now reads `product.metafields.junip.rating_average` / `junip.rating_count` (the metafields Junip actually writes — the same ones the product slider uses). The old lookup (`reviews.rating.value.rating`) never resolved, so live showed the typed-in "4.76/5 stars | 12,000+ happy women" on every product. Output is now "`<avg>/5 stars | <count> <suffix>`" with a thousands separator; the block is hidden entirely when the product has no Junip rating. Manual `star_rating`, `rating_text` and `review_count_text` settings removed from the schema (only `summary_suffix`, link and anchor remain).
  - **Payment & Features block**: the typed `description` richtext is gone; the intro now renders `product.description` inside the theme's `toggle-ellipsis` (collapsed to 175px with Read more). The three feature bullets stay as template settings (Randell's choice; still BB copy on other products — candidate for per-product metaobject fields later).
  - **Accordion**: `tab_richtext` now renders `product-tabs` directly with `hide_description: true`, so the auto "Description" accordion no longer duplicates the intro; INGREDIENTS / BENEFITS / SHIPPING & RETURNS remain (first one opens by default now that the description tab is gone).
- `snippets/product-tabs.liquid`: new optional `hide_description` parameter (blanks the description content when true). Default behaviour for the main PDP is unchanged.
- `shopify-dev/templates/product.coming-soon.json`: removed the orphaned `star_rating`, `rating_text`, `review_count_text` and `description` values (dev only; the same keys on the live template are ignored once the schema no longer lists them).
- Verified on dev with a temporary `junip.rating_average` = 4.76 / `junip.rating_count` = 1203 on BB Crème (deleted again afterwards): strip rendered "4.76/5 stars | 1,203 happy women"; Serum Foundation (no rating) hid the strip and showed its own description; accordion shows only the three rich-text tabs; no Liquid errors. Live will show real Junip numbers once pushed (all live products carry `junip.*` metafields).
- KAN-211 updated: `snippets/product-tabs.liquid` added to the live push list.

## Coming-soon PDP — check-mark benefits inside the dynamic description; static bullet list removed

- `sections/custom-product-coming-soon.liquid` (Payment & Features block): the product description is now pre-processed in Liquid so the "Key Benefits / Key Features" lines that the product descriptions write as dash paragraphs (`<p>- …`, `<p>-…`, and the Word-pasted `<p class="MsoPlainText">- …` / `>-…` / `> - …` variants) become `<p class="cw-bb-check-item">` — the dash is dropped and the black check icon takes its place. Real `<ul><li>` lists inside descriptions (lipsticks, glosses, brushes, Rose Lip Balm, Eye Primer…) get the same check styling via CSS. The static `feature_1/2/3` list and its schema settings are removed; `.cw-bb-color-features` markup no longer renders.
- `snippets/custom-pdp-css.liquid`: new rules for `.cw-bb-color-description p.cw-bb-check-item` and `.cw-bb-color-description ul li` (desktop 20px icon / 30px indent, mobile 15px / 25px), reusing the existing check-mark PNG.
- `shopify-dev/templates/product.coming-soon.json`: `feature_1/2/3` values removed (dev only; leftover keys on live are ignored).
- Verified on dev: BB Crème shows 6 check items, Strong Brew 4, Lux Brush Soap 2 (no-space dash variant), NikRay's `<ul>` benefits get checks; no leftover dash paragraphs, no Liquid errors. Product descriptions themselves are untouched.
- Live push list unchanged apart from `snippets/custom-pdp-css.liquid` now also being required (KAN-211 comment).

## Coming-soon PDP — `.product__page` responsive pass (tablet 750–1023px + thumbnail arrows)

- Findings (Playwright at 375/600/768/900/1024/1280/1600): Broadcast's rigid 50/50 grid from 750px squeezed the content column to 354px at 768 (title wrapped to two lines), and the thumbnail prev/next arrows (`left/right: -11%` of the strip) sat at the viewport edge at 768, half off-screen at 900, and on top of the colour swatches / behind the content column at 900–1024.
- `snippets/custom-pdp-css.liquid` only:
  - Title: `font-size: clamp(34px, 4.4vw, 56px)` instead of a fixed 56px (mobile ≤600 keeps its 34px rule) — one line at every width.
  - Thumbnails: `width: 100%; max-width: 120px; aspect-ratio: 1/1` instead of a fixed 120×120, so four always fit the strip (Swiper is `slidesPerView: 4`); ≈48px at 768, 67px at 900, 85px at 1024, 112px at 1280; mobile 70px rule unchanged.
  - Arrows (≥601px): the strip gets `padding: 0 46px` (40px on tablet) and the arrows sit at `left/right: 0`, vertically centred — always inside the gallery column.
  - Tablet (750–1023px): `.index-product .product__page` → `minmax(0,45fr) minmax(0,55fr)`, content `padding-left: 24px`.
- Verified on dev: 768 → columns 319/389, title 37px tall (one line); 900 → 378/462; 1024/1280 unchanged 50/50 with arrows inside; 375/600 stacked layout unchanged. No theme files touched; live push list unchanged (snippet already on KAN-211).

## Coming-soon PDP — thumbnail counts per breakpoint + "Watch Our Products In Action" on phones

- Gallery thumbnails (`custom-product-coming-soon.liquid`, thumb Swiper): `slidesPerView: 3` up to 1023px (phones and tablets), `4` from 1024px (was a flat 4 everywhere; briefly 2 on phones before Randell asked for 3). Phone thumbnail arrows no longer shrink to 24px — the ≤600px `svg { width: 24px }` rules are removed so they stay the full 36px like every other width; phone strip padding 32→44px to make room. Phone thumbnail images switched from fixed 70×70 to fluid (`width: 100%; height: auto`, inheriting the 120px cap and 1:1 ratio); slides centre their image so the thumb sits centred when the cap applies.
- `.cw-bb-pvideo-section` (Tolstoy "Watch Our Products In Action" under the gallery) had a `cw-desktop` class that hides it under 600px, and no mobile counterpart exists — so phones never saw it. Class removed; the section now renders at every width (on phones it sits between the gallery and the buy column in the stacked layout). The `cw-desktop`/`cw-mobile` utility rules stay in `custom-pdp-css.liquid` for anything else that uses them.

## Section h3s — no single-word last line (2026-09-11)

- New `snippets/custom-no-orphan.liquid`: echoes `text` with the final space replaced by `&nbsp;` when the title has 3+ words (two-word titles pass through untouched so they can still break on very narrow screens instead of overflowing). Used for the h3 in `custom-steps-horizontal` (metaobject + block paths), `custom-icon-features` (both paths) and `custom-alternating-rows` (both paths). Guarantees at least two words on the last line in every browser.
- Extended to the section titles: `.cw-bb-pvideo-title` ("Watch Our Products In Action", static text in `custom-product-coming-soon`), `.custom-videos__title` ("What Women Are Saying"), `.custom-faq__title`, `.custom-product-slider__title` ("Products You May Also Like") and `.custom-info-banner__title` ("Stop Wearing Makeup…", via `ib_heading` so both template and metaobject headings are covered) — each rendered through the snippet, each rule given `text-wrap: pretty`. Also `.custom-newsletter__title` ("Let's Get This Beauty Started.") in `custom-newsletter.liquid`; the title's `<br>` (hidden ≤600px) is preserved since the snippet only touches the final space. And `.custom-icon-features__heading` (rich-text metaobject heading and block heading paths both captured into `heading_html` first, then rendered through the snippet). And `.custom-alternating-rows__title` ("Why Thousands of Mature Women Switched to Nikol Beauty BB Crème", via `ar_title`). And the FAQ subtitle `.custom-faq__subtitle` (per-product `faq_subtitle` from the Coming Soon Page metaobject, e.g. "…from choosing a shade to daily&nbsp;wear.").
- `text-wrap: pretty` added to the same three h3 rules as progressive enhancement (Chromium 117+/Safari 26 avoid orphans and pick nicer break points; unsupported browsers ignore it and rely on the nbsp).

## "Why Women Switched" rows — images fill their column (2026-09-11)

- `sections/custom-alternating-rows.liquid`: `.custom-alternating-rows__media img { width: 100% }` added (was intrinsic width capped at `max-width: 610px`, so smaller uploads sat narrower than the column). The 610px cap stays on desktop; the ≤600px rule already had `max-width: 100%`.
- 601–1024px (initially 768–1024, widened on request): `.custom-alternating-rows__body` (and its `--reverse` variant) drop the 60px side gutter (`padding-left/right: 0`), so the text uses the full half column on tablets; desktop keeps 60px, ≤600 was already 0.

## "Why Women Switched" rows — titles match the other section h3s (2026-09-11)

- `sections/custom-alternating-rows.liquid`: `.custom-alternating-rows__row-title` no longer carries the theme `heading-size-9` class (both render paths); it now uses the same treatment as `.custom-icon-features__feature-text h3`: Cormorant, `clamp(23px, 1.32vw + 15.1px, 32px)`, weight 600, line-height 120%, letter-spacing 0 (was theme heading size, weight 500, 110%, −1px). The steps-card h3 shares everything except a 28px max. Later: desktop (≥1025px) row titles bumped to a fixed 36px; tablet/mobile keep the 23–32px clamp.

## Footer — N crest above the contact-card title on small phones (2026-09-11)

- `sections/custom-footer.liquid` (design 2): the engraved-N crest only lived in the masthead, which is hidden below 785px (and on the coming-soon template entirely), so phones never saw the logo. Added a second crest (`.nbf__crest.nbf__crest--card`, same emboss markup/CSS) as the first child of `.nbf__card`, `display: none` by default and `display: block; margin: 0 auto 12px` at ≤768px (initially ≤420, widened on request; the masthead crest is hidden ≤785 so there is no overlap window where both show) — centred above the "Get in touch" heading; emboss height 52px on the card copy (masthead copy stays 72px). Site-wide footer, so it appears on every template's footer at that size.

## FAQ — all questions closed on load (2026-09-11)

- `sections/custom-faq.liquid`: both render paths (metaobject `cs.faqs` list and section blocks) gave the first item `is-open` + `aria-expanded="true"`; removed so every question starts closed. Toggle JS unchanged.

## Footer — masthead hidden on the coming-soon template (2026-09-11)

- `sections/custom-footer.liquid` (design 2): the `.nbf__masthead` block (Klaviyo subscribe form + engraved N crest) is wrapped in `{% unless template.suffix == 'coming-soon' %}` so it does not render on `/products/*?view=coming-soon`, where the page already has its own newsletter section directly above the footer. Every other template is unchanged; the links grid, contact card and bottom bar still render on the coming-soon pages. Done in Liquid rather than CSS so the Klaviyo form script is not loaded for nothing.

## Skin-safe features — stacked, centred features on small phones (2026-09-11)

- `sections/custom-icon-features.liquid`, ≤420px: `.custom-icon-features__feature-inner` switches from the icon-beside-text row to a centred column (`flex-direction: column; align-items: center; text-align: center; gap: 12px; margin: 0 auto 35px`), with icon and text each at 100% width so they sit on their own rows. 421px+ unchanged (row with 15px gap).
- Feature `h3` weight 500 → 600 at all widths (matches the steps card h3 change).
- 425–1024px band: `.custom-icon-features__container { gap: 30px }` (100px desktop / 20px ≤600 before), `.custom-icon-features__content { padding-left: 40px }` (200px desktop, 80px ≤1600, 15px ≤600 before — right padding at ≤600 stays 15px), `.custom-icon-features__feature-icon img { max-width: 80px }` (130px / 100px before), `.custom-icon-features__feature-text h3 { padding: 0 }`, `.custom-icon-features__subtitle { padding-bottom: 20px }` (40px elsewhere), and `.custom-icon-features__content { padding-top/bottom: 40px }` for breathing room above the heading and below the button. Block sits after the ≤600 rules so it wins in the 425–600 overlap.

## Info banner — two columns from 768px (2026-09-11)

- `sections/custom-info-banner.liquid`: the stacked single-column layout applied up to 1024px and the media/content split only from 1025px. Breakpoints moved so the image and copy sit side by side from 768px: stacked rules now `max-width: 767px`, the per-section split (`media_width` / `content_width` grid, absolute media) now `min-width: 768px`. The desktop 100px column gap and `calc(50% - 50px)` media width apply on tablets too. The 40px phone padding rule moved to `max-width: 767px` to match (768+ uses the section's own padding). The `<img sizes>` hints (`(min-width: 1025px) 50vw…`) updated to 768px too so tablets download the half-width candidate.
- 768–1024px: the media block's "media padding" setting (80px, applied inline as `padding` on the section) is overridden to `40px 0 !important`, `.custom-info-banner__main` column gap 100→30px, and the absolute media width formula uses the 30px gap (`calc((100% - 30px) * media_width)`). Desktop keeps the 80px padding and 100px gap.

## Info banner ("Stop Wearing Makeup That Makes You Look Older") — phone padding (2026-09-11)

- `sections/custom-info-banner.liquid`: the ≤1024px rule forces `padding: 0 !important` (image flush to the section top, button 5px from the bottom). Added a later ≤768px rule `padding: 40px 0 !important` (initially ≤420, widened on request) so the section has breathing room above the image and below the button on phones and small tablets; 769–1024 unchanged (0).

## Steps section — number badge anchored to the image (2026-09-11)

- `sections/custom-steps-horizontal.liquid`: `.custom-steps-horizontal__number` was absolutely positioned against the card with a fixed `top` (285px desktop / 250px ≤600px) while the image height scales with viewport width, so the badge only met the image's bottom edge at one width (at 375px it sat 17px too high; at 320/420 further off). The badge now lives inside `.custom-steps-horizontal__image` (both the metaobject and block-driven renders), the wrapper is `position: relative; max-width: 390px; margin: 0 auto`, the img is `display: block` (removes the inline-descender gap), and the badge is `bottom: -17.5px` — half its 35px height below the image edge at every width. Fixed `top` values removed; hover scale and the `nth-child(3)` baseline tweak unchanged.
- Section padding at ≤420px raised to `70px 0` (50px at 421–600, 80px desktop).
- Card `h3` weight set explicitly to 600 (was inheriting 500) at all widths.
- 768–1024px: cards wrap 2 + 1 — `.custom-steps-horizontal__container { flex-wrap: wrap; justify-content: center; gap: 40px 24px }`, `.custom-steps-horizontal__card { flex: 0 0 calc(50% - 12px); width: calc(50% - 12px); margin: 0 }` so all three cards share one width and the lone third card is centred instead of stretching. Desktop (3 across at 33%) and ≤600 (stacked) unchanged; 601–767 still uses the desktop 3-across.
- Number badge enlarged 35→44px (border 2.23→2.5px, numeral `clamp(24px … 28px)` from 20–24px, baseline padding 7→8px and the step-3 tweak 13→15px), `bottom: -22px` so it still straddles the image edge by half its height.

## FAQ — closed items no longer reserve 20px (2026-09-11)

- `sections/custom-faq.liquid`: `.custom-faq__answer { padding-bottom: 20px }` was still rendered when the `grid-template-rows: 0fr` collapse closed the row (padding is not shrinkable content), so every closed item carried a 20px empty strip under the question and the item spacing looked uneven (measured: closed collapse 20px, item 90px vs trigger 69px). Padding moved to an `::after` spacer inside the answer, which collapses with the row and animates with it; open state is visually identical.

## Product slider — gap between image and rating (2026-09-11)

- `.custom-product-slider__img { margin-bottom: 10px }` — the same image→info spacing the grid card gets from `.product-information { margin-top: 10px }`. Placed on the image rather than the rating so the gap also applies for products with no Junip rating (title follows the image directly).

## Small-phone pass, 320–420px (2026-09-11)

- Gallery thumbnails (`custom-product-coming-soon.liquid`): Swiper base `slidesPerView: 2`, `3` from 421px, `4` from 1024px.
- Rating strip (`custom-pdp-css.liquid`, ≤420px): `.cw-bb-rating-box a` wraps, `.cw-bb-rating-image` takes its own line (`flex: 0 0 100%`), stars 120px wide (was 84), text 16px (was 14); both rows centred via `flex-direction: column; align-items: center` on the link (the image stays shrink-wrapped to the stars, so the absolutely-positioned `.cw-bb-rating-fill` overlays the grey stars exactly — an earlier `flex: 0 0 100%` attempt stretched the wrapper and left the orange fill at the left edge). Block order unchanged: after the gallery, before the title.
- Colour swatches: `.cw-bb-color-swatch img { max-width: 60px }` at ≤420 (73px elsewhere, per the earlier no-shrink request which now applies from 421px up).
- Complete-the-look slider (`.complete-look-slider-main .swiper-button-prev`): `left: 75% !important` at ≤420 (73% remains for 421–600); both arrows `top: -23% !important` at ≤420 (−18% for 421–600, −25% desktop).
- Upsell (`.product__block.product__upsell`): the `width: auto; flex: 1 1 auto` override on `.product-upsell__content` now applies only from 421px, so ≤420 falls back to the theme's own width; the add-to-cart wrapper (`.btn__outer`, holds `.product-upsell__btn`) is hidden at ≤420. Could not render-verify on dev — BB Crème has no upsell configured there.
- "Products You May Also Like" (`custom-product-slider.liquid`): Swiper base `slidesPerView: 1`, `2` from 421px (16px gap), 3/4/5 breakpoints unchanged.
- Footer (`sections/custom-footer.liquid`, ≤420px): `.nbf__social .social { gap: 7px }` (18px elsewhere); applies to all three footer designs since the rule is unscoped. Footer is site-wide, so this shows on every page.
- Newsletter (`custom-newsletter.liquid`): `.custom-newsletter__note` 15px now up to 768px (initially ≤420, widened on request); at ≤420px form set to `display: flex; flex-direction: column` with input-wrap and button-wrap at 100% so the field and button are explicitly on their own rows (they already stacked as blocks — measured at 375: input y=9116/h64, button y=9180 — the rule guards against any future inline layout).

## Product slider — card titles match the collection-grid cards (2026-09-11)

- `.custom-product-slider__product-title` now also carries the theme's `product-item__title` class, so it inherits the same rules the collection/grid cards use (`main.css`: Cormorant 20px, weight 600, uppercase) and will follow any future change to that class. The slider's own font-family/size/weight rules (incl. the 22–26px clamp and ≤600px override) are removed; what remains is spacing (`margin: 0 0 4px`, same title→rating gap as the cards), `color: var(--text)` to match the card colour, and `line-height: 1.4` / `1.2` under 750px — the values the grid card's `<p>` inherits from its `.product-information` wrapper (28px desktop, 24px phone); the slider slide has no such wrapper so `inherit` resolved to `normal`. Rating padding-top dropped to 0 so title→stars→price stack like the grid card. Price left as-is (Inter, base size) — the grid price is 14px/300/light grey; say if that should match too.

## Product slider ("You may also like") — two cards on phones

- `sections/custom-product-slider.liquid`: Swiper base config (below the 750px breakpoint) changed from `slidesPerView: 1.2, spaceBetween: 20` to `slidesPerView: 2, spaceBetween: 16` so phones show two full products side by side instead of one and a sliver. Tablet/desktop breakpoints (3 / 4 / 5) unchanged. Section is active on the coming-soon template and wherever else it is placed (homepage-style-2).
- Side gutters + no edge fades: `.custom-product-slider__inner` now gets `padding: 0 var(--outer, 50px)` — the theme's own page gutter (50px desktop, 16px phone), matching the sections above and below — so cards and header no longer run to the screen edge. The ≥1200px `::before/::after` gradient fades on `.custom-product-slider__slider.is-scrollable` are removed (the `is-scrollable` class is still set by the JS but is now inert). Redundant phone-only 15px paddings on the header, tabs and content wrapper removed since the gutter covers them.
- Arrows beside the cards, not over them: the prev/next buttons moved out of the `.swiper` element to the panel level (`.custom-product-slider__panel`, position relative) and sit at `left/right: 0`; the Swiper gets `margin: 0 64px` (48px circle + 16px gap; 44px on ≤749px where the circles are 36px) so the card row is clipped to the space between the arrows. Swiper `navigation` now resolves the buttons via `el.closest('.custom-product-slider__panel')`. Same 48px circle SVGs and vertical-centre transform as `custom-videos__nav`; kept the dark #45413B fill because this section's background is light (the videos section inverts to white circles against its dark background).
- Hover clip fix: with the arrows at the panel edges, `.custom-product-slider__content { overflow: hidden }` cut off the circle when it scaled to 1.08 on hover. Changed to `overflow: visible` — the `.swiper` element already clips the card row, so nothing else relied on it.

## Coming-soon PDP — max-widths removed from the buy column

- Between 601px and 749px the page is single-column but `.form__width` was capped at 550px (custom rule, applied at all widths; Broadcast's own cap is ≥990px only), so the buy column rendered narrower than the gallery and the sections around it. Removed every fixed cap in `custom-pdp-css.liquid`: `.form__width` → `max-width: none !important` (also overrides the theme's 550px on desktop), `.product__block.tabs-wrapper` 570px, `.product-upsell` 570px, and `.cw-new-popup-thumb-slider-vt` 570px (thumbnails already cap at 120px each). The legacy `.pmslider-*` 630px rules are for the old media-slider app markup, not rendered here, and were left alone.
- Follow-up: the phone-only `max-width: 85%` on `.cw-new-popup-thumb-slider-vt` is removed too, so the thumbnail strip matches the main image width on phones; the phone arrows now sit inside the strip (32px side padding, `left/right: 0`, vertically centred) like the larger breakpoints instead of hanging 11% outside it.
- Colour swatches (`.cw-bb-color-swatch img`) no longer shrink to 56px on phones — the ≤600px override is removed so they stay 73px at every width; `.cw-bb-color-image-wrapper` gets `flex-wrap: wrap` so a product with more swatches than fit on one line wraps instead of overflowing; phone gap tightened 20→16px so four 73px swatches still fit one row at 375px.
- Upsell: `main.css` sets `.product__block.product__upsell .product-upsell__content { width: calc(100% - 180px) }` globally; overridden on this template only (`width: auto; flex: 1 1 auto; min-width: 0` in `custom-pdp-css.liquid`) so the upsell text block fills the space next to the image. `main.css` untouched.
- Phone description size: `.cw-bb-color-description` and `.toggle-ellipsis__content p` ≤600px raised 16→17px.

## Coming-soon PDP — accordions all closed on first load

- `snippets/product-tabs.liquid` auto-opens the first rich-text accordion whenever there is no Description tab (Broadcast default); with the Description tab hidden on this template that meant INGREDIENTS was always open on load. Added an optional `accordions_closed` parameter that suppresses the auto-open; the coming-soon section passes `accordions_closed: true`. Main PDP unchanged (parameter absent).

## Coming-soon PDP — gallery no longer sticky

- Removed the `position: sticky; top: 100px` rules that pinned the product gallery (`.product__page > .cw-new-popup-kl`) while the buy column scrolled — both the template-agnostic rule in the section's `{% style %}` block and the older `#Product--template--26106250068272__main` rule (plus its mobile `position: inherit` reset) in `custom-pdp-css.liquid`. The photo now scrolls with the page at every width. Broadcast's own sticky *form* option (`product_sticky_enable`) is untouched.

## Coming-soon PDP — fluid typography across the whole page

- Every heading/body size on the coming-soon page was a fixed desktop px with a single ≤600px phone override and nothing in between, so tablets (601–1100px) rendered full 56px headings and 18px body. 32 desktop declarations across `snippets/custom-pdp-css.liquid`, `custom-steps-horizontal`, `custom-icon-features`, `custom-faq`, `custom-info-banner`, `custom-videos` and `custom-product-slider` now use `clamp(<phone value>, <vw slope>, <desktop value>)`, calibrated so the phone value applies at 600px and the desktop value from 1280px — no new sizes invented, phone rules untouched. Scales: section H2 34→56 (`clamp(34px, 3.24vw + 14.6px, 56px)`), banner title 34→62, card/feature H3 23→28 / 23→32, FAQ question + accordion title + slider product title 22→26, subtitles base→20 (the clamp the FAQ/banner already used), body 16→18, price 20→26, "Watch Our Products" + upsell title 28→36, rating strip 14→18, benefits check-title 21→24. The icon-features ≤1600px 52px override was folded into the same clamp, and the product title moved from `clamp(34px, 4.4vw, 56px)` to the shared H2 curve so it eases like the section headings instead of jumping between 768 and 1024.
- Generated by `scratchpad/fluid-type.cjs` (line-anchored replacements with old-value assertions). Verified with computed-style readings at 375/768/1024/1280/1600 (`probe-type.cjs`).

## Coming-soon PDP — "Show less" for the dynamic description

- Broadcast's `toggle-ellipsis` only ever expands (it hides its own actions once `is-open` is set). Added a `Show less` button (`.cs-desc-less`, `caps--link` style) after the `toggle-ellipsis` inside `.cw-bb-color-description`, plus a small script in `custom-product-coming-soon.liquid`: Read more reveals the button; Show less resets `--height` to the collapsed 175px, removes `is-open` (which brings the Read more gradient/button back) and scrolls the description top back into view if it has scrolled off. No theme JS touched.
- CSS for the button in `snippets/custom-pdp-css.liquid` (hidden by default, `inline-block` when `.is-visible`).
- Collapsed preview height raised from 175px to 260px (`data-height` on the description `toggle-ellipsis`; the Show-less script reads the same attribute) so roughly eight lines show before Read more.
- BENEFITS accordion: the `&gt; ` markers typed into the template rich text (`<p><strong>&gt; Title</strong></p>`) now render as the black check icon with the title forced bold (700). Done in code, not in the template setting: `snippets/product-tabs.liquid` gained an optional `check_prefix` parameter that rewrites `<p><strong>{prefix}` / `<p>{prefix}` to `<p class="cw-bb-check-item">…`; the coming-soon section passes `check_prefix: '&gt; '`. Main PDP unaffected (parameter absent). CSS for `.product-accordion .accordion__content p.cw-bb-check-item` (+ `strong` 700, desktop/mobile icon sizes) in `custom-pdp-css.liquid`. Verified: 6 check items, zero leftover `&gt;`, no Liquid errors. Titles then switched to the heading face: `'Cormorant', serif`, 24px desktop / 21px mobile, line-height 120%, #2A2724; bold removed on request, then nudged up one step to weight 500 (Cormorant Medium — a weight the theme already uses in main.css); forced with `!important` so the `<strong>` tag in the rich text doesn't re-bold it.
- Verified with a headless-browser click sequence on dev BB Crème: collapsed 175px (now 260px) → Read more → open (1664px, Show less visible, Read more hidden) → Show less → collapsed again with Read more back → reopens correctly.

## Coming-soon sections back on template settings — metaobject mode now opt-in (2026-09-11)

- Why: Randell is taking the coming-soon template live himself and wants the sections to render from the theme/template settings, not from the `coming_soon_page` metaobjects (which exist only on dev). The metaobject work stays in the code for later.
- `custom-steps-horizontal`, `custom-icon-features`, `custom-alternating-rows`, `custom-faq`, `custom-info-banner`, `custom-videos`: new section checkbox `use_metaobject` ("Use Coming Soon Page metaobject", default **off**). The `cs_mode` condition is now `template.suffix == 'coming-soon' and product and section.settings.use_metaobject`; with it off the sections render their template blocks/settings exactly as before the metaobject work, on dev and on live. Turning it on restores the strict no-fallback metaobject rendering. Nothing else in the sections changed (all the responsive/design work from Sep 10–11 is untouched).
- `custom-info-banner` rating block: stars/text now come from Junip (`junip.rating_average` / `junip.rating_count`) whenever a product is in context — previously Junip only in metaobject mode, manual `star_rating`/`star_text` otherwise. Manual settings still apply on non-product pages (homepage-style-2 uses this section four times); schema `info` texts say so. The block hides until Junip has synced, so on dev (no Junip metafields) it is invisible and on live it shows the real BB numbers. Manual `star_text` ("12,000+ happy women") removed from the template block.
- `templates/product.coming-soon.json` (both folders, now identical): the FAQ section's 9 `faq` blocks replaced with the rewritten BB Crème Q&As from the metaobject (`changelog/coming-soon-faq-export-2026-09-10.json`, entry `bb-creme`: shade choice, coverage, dry/combination skin, moisturizer/primer, fine lines, airless-pump troubleshooting, redness/age spots, bottle life, BB vs Serum Foundation); subtitle → "Everything you need to know about BB Crème, from choosing a shade to daily wear." Block IDs unchanged. Live copy also caught up with dev: orphan `rating_summary` (`star_rating`/`rating_text`/`review_count_text`) and `payment_details` (`description`/`feature_1-3`) values removed, Junip product-review app block (`junip_reviews_cs`) added to the `1771850328ecb59717` apps section. Steps / icon features / alternating rows / videos template content already matched the metaobjects, so nothing to copy there.
- Pushed to dev theme #149231566950 (6 sections + template). `shopify theme check`: no offenses in the changed files. Browser-verified on dev (new storefront password) at 375/420/600/700/768/900/1024/1025/1280 (`scratchpad/probe-template-mode.cjs`, `probe-sections-shots.cjs`): all six sections render from template settings, no Liquid errors, no horizontal overflow; FAQ shows the 9 new questions + new subtitle, all closed; both rating spots hidden (no Junip metafields on dev). Also confirmed the changes that had been pushed blind while the password was unknown: info banner stacked + 40px padding ≤767, side-by-side with 30px column gap + 40px padding 768–1024, 80px/100px from 1025; steps 2+1 centred grid 768–1024; icon features gap 30 / padding 40 / 80px icons 600–1024; alternating row titles 36px ≥1025 and 23–28.6px clamp below, weight 600, body side padding 0 at 601–1024, image max 610px; footer masthead hidden on this template at every width, N crest above "Get in touch" ≤768 (52px) and hidden from 900; newsletter note 15px ≤768. Nothing pushed to live (Randell pushes).
- Go-live: push the theme files, then set the BB Crème product to the `coming-soon` template on live. Sections render from the template as-is; the rating spots (buy column + info banner) fill from Junip automatically.
- `.custom-alternating-rows__row-title` desktop (≥1025px) size raised 36px → 41px on request; the tablet/phone clamp (23–32px) is unchanged. Verified on dev: 28.6px at 1024, 41px at 1025/1280/1600.

## Newsletter — field and button on one row on desktop (2026-09-11)

- `sections/custom-newsletter.liquid`: new `@media (min-width: 1025px)` block turns `.custom-newsletter__form` into a wrapping flex row: the email field (`.custom-newsletter__input-wrap`) grows to fill (`flex: 1 1 0; min-width: 0`), the button wrapper is capped at 220px (`flex: 0 1 220px; max-width: 220px`, its 15px vertical padding dropped so both sit on one line), 12px column gap; the success/error message and the padlock note take the full row (`flex: 0 0 100%`), note gets `padding-top: 15px` to keep its distance. Below 1025px nothing changed: field, button and note stack as independent rows (the existing block flow + the ≤420px column rules). Active on the coming-soon template; the section is also placed on homepage-style-2, which gets the same layout.
- Verified on dev: 768/1024 stacked (field, button, note each full width, one under the other); 1025/1280/1600 field and button share a row (button 220px, field 241/368/403px), note on its own row below. First push attempt errored (CLI network hiccup, no partial upload — the retry succeeded).

## "Why Women Switched" — review card desktop-only, quote flush left (2026-09-11)

- `sections/custom-alternating-rows.liquid`: `.custom-alternating-rows__review` (stars + quote + author) hidden ≤1024px (`display: none`), visible from 1025px. Only the second row (C.M. quote) has `show_review` on; the section is only placed on the coming-soon template.
- The quote's left indent/border came from the theme's global `blockquote` rule in `theme.css` (`padding-left: 1.5em; border-left: 5px solid var(--text)`), not from the section. Countered on `.custom-alternating-rows__quote` with `padding-left: 0; border-left: 0` (margin was already 0). The stars' `margin-left: 30px`, which lined them up with the indented quote text, is set to 0 so stars, quote and author now share the same left edge.
- Verified on dev: review `display: none` at 375/768/1024, `flex` at 1025/1280; quote padding-left/border-left 0, stars margin-left 0.
