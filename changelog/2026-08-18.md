# 2026-08-18 (Tuesday)

- **homepage-style-2 missing images FIXED on app-dev store (Randell-requested inspect+fix):** template references 56 unique shop_images; audit found only 8 existed in the app-dev Files library (banner-desktop, banner-usp1-4, reviews-banner, Stars_Container, Vector — the ones copied in last week's index.json fix). **48 missing** (feature*, benefit*, tabimg1-6, category-desk/mob1-6, new-step1-3, review icons/stars/quote/Frame/Button, FAQ +/- icons, cw-video2 poster, mkd1/makup-bg/step-pair/Group/looks/happy-senior/cl-bg/banner-bottom/advice-bg/form-bg, banner-girl/cosmetic). All 48 HEAD-verified on live CDN (all native jpeg/png — no webp conversion risk), copied via fileCreate-by-URL in 3 batches of 16, re-queried: **48/48 READY with exact filenames** (incl. exact Group.png among fuzzy matches). **KEY FINDING: the published theme's index.homepage-style-2.json kept ALL its shopify:// refs intact** (queried via OnlineStoreTheme files API) — unlike last week's index.json ref-dropping; missing files alone broke rendering, so NO theme re-push needed. Store identity verified (-mab1srre) before writes. Template renders via /?view=homepage-style-2 or the theme editor's template picker.
- **Randell's homepage-style-2 editor padding edits RESCUED into the local folder:** his theme-editor changes existed only on the dev-session theme #149852258406 (updated 15:21 UTC) — `shopify theme dev` WITHOUT `--theme-editor-sync` never writes editor edits to local, and the next session would have served the stale local file over them. Local file backed up to scratchpad, then CLI-pulled the template from the dev-session theme. Captured edits: media_padding 0→80 on the makeup info banner, quiz banner, and blog/advice banner; 0→70 on the "Stop Wearing Makeup" banner (+ media_width 50 where new); editor also materialized explicit values for all schema settings (block paddings 5/5, section bg colors, review-slider settings — render-identical to prior defaults). Verified post-pull: 80/80/70/80 in the local file. LESSON: to have editor edits persist locally, start the session as `shopify theme dev --theme-editor-sync`; otherwise ask Claude to rescue-pull from the Development theme before restarting the session.
- **LIVE theme flip-flop discovered + resolved during KAN-137 verification:** live had been switched to the 8/7 BACKUP (reverting the 8/12 perf fixes — likely Monday's teardown method); after flagging, Randell republished **8/12 as live** and did teardown edits on it directly: announcement SECTION disabled (sale text dormant inside the disabled block — nothing renders), hero slides back to June banners. VERIFIED read-only. ⚠ LEFTOVER flagged: the June hero slide has empty button text + `first_button_link` still = the Fierce Aging sale collection, and section-slideshow.liquid:112 makes the WHOLE SLIDE clickable when text is blank — live homepage hero currently links to the ended-sale collection (his live-editor fix). **KAN-137:** theme-dev store VERIFIED removed ✓; live half NOT verifiable — blank.png still present on live 8/12 (published), 8/7, and his live Development theme; his live edit apparently never saved. Task held open. NOTE: local shopify-live folder = 8/12 sale-state; live = 8/12 + teardown edits → settings drift again (re-pull before next settings-dependent work).
- **Guarantee-list icons missing — root-caused + fixed on dev folder:** live homepage's [Custom] Guarantee List blocks have NO feature_icon settings at all (never configured on this instance — the local shopify-live mirror confirms it shipped that way 8/12; the section correctly hides unset icons). The app-dev homepage version HAS icons (feature1-4.png), which set the expectation. Fix: added `feature_icon: shopify://shop_images/feature1-4.png` to the 4 blocks in shopify-dev/templates/index.json (order-mapped; #3 "Shipping Internationally" got feature3.png — Randell to eyeball whether that icon suits the text, easy to swap in the editor). feature1-4.png verified present in BOTH stores' Files (live: HEAD 200 today; app-dev: copied today). LIVE fix = editor action (image picker on each block) — template settings don't ride pushes per live-sync-code-only.
- **Dev homepage synced to live's post-teardown state (Randell-requested):** guarantee-list SETTINGS matched to live (pink sale gradient/white/500/border-5 → cream gradient 98%/#212121/400/border-0; icons KEPT — dev is live + the icon addition); hero slideshow images Fierce_Aging_Day desktop+mobile → New_Banner_June_desktop.png + Banner_for_Mobile_June.png (all other slide settings already matched live, including the stale sale-collection full-slide link — left matching live per request; flagged separately for his live fix). Both June banner files copied to app-dev Files (were missing; fileCreate-by-URL, both READY exact names) so the local session renders them.
- **Friday 8/21 go-live scheduled (Randell):** all live-associated tasks re-dated to 8/21 — KAN-193, KAN-194 (5 pages), stale-sync KAN-138/140/141/142/144 (payloads ride the push, verify each). KAN-26 + KAN-31 found already closed by Randell. NEW **KAN-195** created (KAN-119, High, 8/21): consolidated push manifest (footer redesign + social snippet, truck feature 7 files, H1 batch) + the 6 post-push editor steps (footer design2, truck message string, guarantee icons, hero link repoint, announcement text, KAN-141's 2 stored settings) + the ⚠ re-baseline-live-settings-before-push caution.
- **KAN-137 CLOSED — blank.png removed from LIVE (explicitly authorized single live write):** Randell connected the MCP to the live store and scoped "only fix the blank.png issue". Executed: get-shop-info verified LIVE (nikolbeauty.com) → fresh CLI pull of live settings_data.json → guarded surgical removal (1 occurrence / 18-byte delta or abort) → `theme push --live --only config/settings_data.json` → re-pull VERIFIED gone. Also re-removed from both repo folders (8/14 baseline had re-imported it). Dead-config re-verified first: blank.png 404s on live CDN + no "Blank" product option. Theme-dev store half was Randell's editor edit (verified earlier). OUTCOME on the task; transitioned Done.
- **Dev→live folder consolidation (Randell: work on account pages from the most-updated theme):** full folder diff (17 differing + 2 dev-only), 15 code/template files copied dev→live (main.css/js, theme.liquid, about-header, cart-drawer, custom-footer, group-header, group-overlay, section-404, video-gallery, social-icon snippet, both homepage templates, truck+trophy snippets); settings_data MERGED not copied — live's fresh copy kept as base (it carries newer app-managed SE-Wishlist block state dev lacked) + `footer_design: design2` patched in, then synced back to dev; minified; **verified both folders byte-identical** (excl. .claude). Both folders now = current live theme + all Aug 17-18 work = the Friday push payload, ready early. KAN-195's re-baseline caution: SATISFIED.
- **KAN-161–164 scan-triage chain CLOSED (Randell dropped the reply-to-Raymond step from 164):** fresh Playwright probe of 7 LIVE pages (home, 2 PDPs, lips collection, blog index+article, about) — ZERO Cloudflare challenges, all data clean. Verdicts: 5/7 scanner claims FALSE (schema 2-3 blocks/page, canonicals 7/7, OG 9-11+3 twitter 7/7, 0 imgs missing alt of 180, meta descriptions 7/7); 2/7 PARTIAL at tiny scale (h1: only home=0 + about=2 — both already tracked KAN-165/193; titles: the 12 from the 8/14 census, parked KAN-166). Scanner verdict: systematically wrong, evidenced Cloudflare-challenge failure mode. Full table in KAN-161 OUTCOME; AEO digest (8.9/10, trends list) preserved in KAN-163. All 4 transitioned Done. Probe script + JSON in scratchpad (kan162-probe.cjs / kan162-results.json).
- **KAN-154 product-card review stars SHIPPED to both folders (Randell: stars below product name):** new `snippets/custom-product-rating.liquid` — reads `junip.rating_average` + `junip.rating_count` (same metafields as product-jsonld-rating.liquid); ACCURATE partial-fill stars (gray #DDD6CF base + orange #FB6D00 overlay clipped to avg/5 width — deliberately NOT the homepage slider's static always-5-stars graphic) + "(count)"; renders nothing at 0 reviews; visually-hidden "Rated X out of 5 stars, N reviews" + aria-hidden visuals. Rendered from `product-grid-item.liquid` between title and price → applies to collection grids AND related sliders everywhere the card snippet is used. CSS in main.css both folders; minified. shopify-live got the IDENTICAL 3-file change (fresh-pull base) for his local live preview → rides Friday's push (add to KAN-195 manifest). CAVEAT: app-dev seeded products may lack junip.* metafields (empty grid = missing data, not broken code); real verification on live preview/post-push. `"disabled": true` on the announcement section in shopify-dev/sections/group-header.json — now matches live's disabled state. Dev's dormant block keeps the NEW "Free US Shipping Over $125" text (+ leftover sale-collection link on the block, inert while hidden); live's dormant block still holds the old sale text — whenever the bar is re-enabled on live, paste the new text then. pulled ALL editor-editable JSON (templates/sections-groups/settings_data) from dev-session theme #149852258406 to scratch and diffed against local. 3 files differed: (1) `sections/group-header.json` = HIS EDIT — announcement bar changed from the pink "20% OFF Fierce Aging" sale bar to "Free US Shipping Over $125" (bg #F8F3EF, text #44403F) — ADOPTED into local (post-sale cleanup; NOTE: makes the dev folder's announcement bar diverge from the live theme, which Randell tears down separately per KAN-47 — already done on live 8/17); (2) settings_data.json + (3) page.app-ost-tracking-page = dev theme merely MISSING app-embed/app-block entries (ReStock/Klaviyo/Clarity/OST etc. not installed on app-dev — editor strips them) with ZERO devtheme-side additions — local richer, NOT adopted; footer_design design2 confirmed present on both. LESSON: when rescue-pulling editor edits, never adopt settings_data/app-block templates wholesale from a Development theme — apps missing on the dev store get silently stripped there.
- **Shipping Terms & Conditions page restyled in the FAQ-Rewards house style (app-dev store page content, page "Shipping Policy" / handle shopping-terms):** old body was raw Outlook paste (x_elementToProof divs, empty <br> paragraphs, inline text-aligns) — rebuilt with the `.faq-rewards` architecture under a scoped `.shipping-terms` class: centered 2.2em title, gradient welcome hero (package icon + 3 stat pills: FREE US shipping $125 / 3–5 business days / worldwide), cream sticky jump-menu (4 links), sticky uppercase h2 section bands with icons (globe/truck/envelope/shield), pink-left-border h3 questions, back-to-top links, pink notice card for the lost/stolen disclaimer, help card with the same self-contained pink button, mobile-first + 768px breakpoint identical to the reference. Content reorganized into 4 sections (Where we ship / Delivery & expedited / Confirmation & tracking / Damaged packages) with 2 added h3s to give loose paragraphs headings (free shipping, duties). NEW: FAQPage JSON-LD (8 Q&As mirroring visible text) + SEO metafields (title_tag 43ch + description_tag ~160ch). h1 typo fixed "SHOPPING Terms" → "Shipping Terms". Verbatim text kept EXCEPT unambiguous typos: "2- Business Day shipping options"→"2-Business-Day shipping option", "for the US only orders"→"for US-only orders", "If you order is placed"→"If your order is placed". FLAGGED for Randell, not changed: "Fresh Beauty Studio" old-brand mention in the confirmation answer; the country exclusion list (Israel/Ukraine/Turkey/Spain/Austria worth confirming as current); page handle is "shopping-terms" (renaming would break links — leave unless he wants a redirect).
- **Store Return Policy page restyled in the house style (app-dev, handle `policy`):** scoped `.store-policy` class, same architecture scaled to the short content — hero (shield icon + 2 pills: All sales FINAL / Damaged report within 48 HOURS), 2 icon section bands (Returns & exchanges / Damaged items), the all-sales-final statement in the pink notice card, "xoxo, Nikol Beauty Team" kept as a centered italic sign-off, help card, FAQPage JSON-LD (2 Q&As). No jump menu (only 2 sections — overkill). Text verbatim; Loop portal link + target=_blank noopener; stray `<meta charset>` paste artifact removed. Existing SEO metafields were already good — untouched.
- **Privacy Policy page restyled in the house style (app-dev, handle `privacy-policy`):** old body was one giant `<br><br>`-separated blob with ALL-CAPS run-in headings and NO h1 — rebuilt under scoped `.privacy-policy`: h1 added, hero (privacy-shield icon + intro, no stat pills — nothing quantitative to pill), 8-link sticky jump menu, 8 icon h2 section bands (Your information / Disclosure / Consent / Shopify & payment / Third parties / Security / Age of consent / Changes), h3 subs (consent Q&As, Shopify, Payment, Links), back-to-top links, Questions & contact = the help card with the email button. Deliberately NO FAQPage JSON-LD (privacy policy ≠ FAQ content — mismarking would hurt). Text verbatim throughout; existing SEO metafields good, untouched. FLAGGED: opening paragraph says "Fresh Beauty Studio/NiKOL Cosmetics/ Nikol Beauty" (old brands, same family as the shipping-page flag).
- **Your Privacy Choices page (/pages/data-sharing-opt-out) — restyle BLOCKED at the API, prepared for manual paste:** this is the Shopify-managed auto-generated privacy page (Settings → Customer privacy); pageUpdate BODY writes silently no-op on it (3 attempts incl. a minimal test edit — userErrors empty, updatedAt frozen, body unchanged; live page unaffected). SEO metafields DID save (title_tag "Your Privacy Choices | Nikol Beauty" + description). The restyled body (house style: h1 + hero + 2 icon sections + Shopify opt-out widget preserved byte-for-byte in a card wrapper + help card; Privacy Policy links added) is saved at `delete_later/privacy-choices-restyled-body.html` for pasting via the admin Pages editor if the UI allows it. LESSON: Shopify-managed pages (this one so far) accept metafield updates but silently ignore API body updates.
- **WCAG audit of today's 3 restyled pages + fixes (Randell: fix all except contrast):** audit found 3 issues — (1) "HERE" ambiguous link text (WCAG 2.4.4) on shipping + return policy Loop-portal links → FIXED: descriptive phrase "our customer service/support portal" is now the link; (2) the target=_blank I'd added to those links lacked new-tab announcements → FIXED: sr-only "(opens in new tab)" spans (+ sr-only utility class added to both pages' scoped CSS); (3) help-card button white-on-#D34270 = 4.39:1 (hair under AA 4.5; inherited from FAQ Rewards house style; hover #B93A62 = 5.46 passes) → DECLINED by Randell, left as designed. Everything else passed: contrast (body ~10:1, pills #B93A62 5.46 bold, bands/notices), heading structure (single h1 each incl. the added privacy h1), nav/section landmarks + aria-labelledby, aria-hidden SVGs, focus outlines, scroll-margin under sticky bands, 58px TOC touch targets, reduced-motion gating. Privacy Policy needed no fixes (no HERE links / no new-tab links).
- **Footer: 3 selectable redesigns added to `sections/custom-footer.liquid` (dev folder; Randell to pick):** new "Footer design" select setting — Original (DEFAULT, exact old markup preserved byte-for-byte, zero visual change until switched) / Design 1 Boutique columns / Design 2 We're-here-to-help / Design 3 Large-&-simple. All new designs: same `--COLOR-FOOTER-BG` pink, elderly-friendly (16–18px sentence-case links vs old 14px all-caps, hover = BLACK + underline replacing the near-invisible white-on-pink ~1.3:1 hover, 22–26px bold tap-to-call phone, larger social icons, visible copyright on ALL devices — old one hid it on mobile, focus outlines, nav landmarks). New Shopify-supported additions: optional Shop menu column, customer-service-hours line, payment-type icons row (shop.enabled_payment_types). New settings: shop/help menus + titles, hours_text, show_payment_icons; new-design CSS scoped `.nbf`/`.nbf--d1/2/3` inline in the section ({% style %}) — main.css untouched, no minify needed. Section is static in theme.liquid so settings live in settings_data.json (⚠ SETTINGS at go-live). Switching = theme editor → Footer → [Custom] Footer → Footer design dropdown.
- **Footer round 2 (Randell: keep 3, redo 1+2):** Designs 1 & 2 rebuilt in Design 3's large-and-simple DNA — D1 "Centered & airy" (everything centered single-column: newsletter, one horizontal dot-separated 18px link row, large contact + 26px phone, big social, centered bottom bar); D2 "Links with contact card" (newsletter top, grid: merged 18px 2-col link list left + soft white rounded contact CARD right with 20px heading/26px phone/hours/social). Both now use the merged shop+help link list like D3. Old boutique-columns + helpband markup/CSS removed; Design 3 untouched.
- **Footer WCAG/SEO/elements pass (all 3 designs):** contact block → `<address>` (font-style:normal); copyright → `<small>` (font-size re-pinned 14px — flex-child small would UA-shrink); D1 dot separators → `content: "·" / ""` (empty alt string so screen readers skip them); payments div aria-label (ignored on plain divs) → visually-hidden "Accepted payment methods:" text; D2/D3 navs → `aria-labelledby` tied to their visible headings; sr-only "Contact us" headings added where contact had no visible heading (D1 h2, D3 h3); `.nbf__sr-only` utility. **Shared `snippets/social-icon-fontawesome.liquid` hardened (benefits Original footer too):** all 11 svgs + `aria-hidden focusable=false`, links + `rel="noopener"` (were target=_blank without it), sr-only fallback text + " (opens in new tab)". Verified pre-existing passes: footer/nav landmarks, list semantics, focus-visible outlines, black hover 13:1, sr-only .icon-fallback-text gives social links names, heading order h2→h3 in all designs, crawlable menu links. Klaviyo form internals = Klaviyo-injected (not auditable from theme).
- **Footer DECISION (Randell): Design 2 "Links with contact card" selected** — `footer_design: "design2"` set in settings_data.json (dev folder). Existing settings carry over: help menu falls back to the "footer" linklist, contact title "Get in touch" heads the card, hours/payment-icons defaults active. ⚠ SETTINGS at go-live: footer_design + any new-setting values need the live theme editor (or ride a full-settings push if one ever happens).
