# Changelog — 2026-07-20

## custom-nikol-apps kickoff: Discount Sync app planned
- First custom app: mirrors automatic discounts onto the storefront via the theme's EXISTING promo
  system (per-variant `custom.promo_percent` metafields for multi-variant products, `promo-<n>` /
  `promo-up-to-<n>` product tags for single-variant — contract verified in product-price.liquid).
  Variant-level percentages supported (automates what the July-4th sale did by hand). No theme app
  extension / app embed / proxy — the provided skeleton's display layer will be stripped.
- Full phased plan written to `custom-nikol-apps/discount-sync/PLAN.md` (Phases 0–6: audit →
  scaffold on dev store → variant-aware sync engine → cron/webhooks → dev-store E2E tests →
  enhancements → production rollout). All dev/test on nikolbeauty-dev; user performs all live installs.
- MCP connection switched to the dev store (nikolbeauty-dev.myshopify.com) and verified; dev store's
  published theme is still the 7/08 build — needs a fresh push (by user) before storefront rendering
  tests in Phase 4.

## shopify-dev rebuilt as exact mirror of shopify-live (user-requested)
- User declared shopify-live/ the most up-to-date copy (fresh live pull incl. their editor/admin changes) and wants to build dev work on top of it. Mirrored shopify-live → shopify-dev with robocopy /MIR (`.claude/` local tool settings excluded); 354 files copied, 34 had differed, no files existed on only one side. Post-mirror `diff -rq` confirms 0 differences.
- This supersedes the old three-bucket sync split (code=dev / settings=live): dev now intentionally carries live's settings_data.json, group JSONs, and template JSONs as its baseline.
- Safety snapshot of pre-mirror dev saved to scratchpad: `shopify-dev-backup-2026-07-20.zip` (session temp dir — copy it somewhere durable if long-term rollback matters).
- Note: shopify-dev/ is intentionally untracked in git (commit 162cf88 "Update live theme; remove dev theme and exclude local tooling") — dev work has no git safety net; only shopify-live/ is versioned.
