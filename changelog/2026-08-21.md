# 2026-08-21 (go-live day, KAN-195)

- **Shoppable-video Add to Cart investigation (read-only probes of live site + Channelize APIs):** tutorial pages' shoppable videos showed link-only product cards while /pages/streams showed Add to Cart. Root cause: the streams page content includes Channelize's Shopify cart bridge (`cdn.channelize.io/live-broadcast/shopify/client.js`, pasted as raw HTML in the page body) — the tutorial pages' embed loader in theme.liquid never loaded it. Products ARE correctly attached to the shoppable videos (verified in Channelize dashboard screenshot + API payloads). Tried adding the client.js bridge to theme.liquid's tutorial-pages gate — preview test showed no ATC (the player bundle also needs injected cart request functions + full variant data fetched on click, which the standalone playlist embed doesn't wire up; per the minified player source, cards without those props — or without a SKU — always redirect to the product URL). **Randell's call: not worth pursuing since the Tolstoy migration replaces Channelize** — script addition REVERTED (both local folders back to pre-investigation state). KEY FINDING FOR TOLSTOY SCOPE: in-player add-to-cart on the tutorial pages is a currently-missing capability that Tolstoy provides natively — count it as a migration benefit, and tag products with variants when setting up Tolstoy.
- **Best Sellers slider edge fade scoped to desktop only:** the mask now applies only ≥1200px (the 5-across range, and still only when scrollable); the 25px mobile fade and the tablet coverage removed per Randell. Mirrored to shopify-live.
