# 2026-08-07

- Removed the 10x Mirror out-of-stock notify customization (per Randell — product launched, dead code now):
  - `snippets/product-buttons.liquid`: removed all four pieces — the `custom_oos_handle = '10x-mirror'` assign switch, the hide-elements style block (`.nm-portal/.product__submit__item/.wishlist-engine display:none`), the prelaunch-notify widget markup (Klaviyo list R9z9N3 signup), and the trailing style+script block. 197 lines gone; standard Add to Cart / Sold Out button always renders now.
  - `snippets/product-grid-item.liquid`: removed the hardcoded card branch showing raspberry "Notify me when it goes live" for the 10x Mirror — sold-out cards all show the standard "Sold Out" label.
  - NOTE: first removal attempt with a lazy regex ate lines 105-140 (an unread second `show_custom_oos_button` block made the match span the form opening) — caught by the file-size sanity check, restored via `git checkout` (file had no other uncommitted changes), redone with anchored replacements + structural balance checks.
  - Verified on dev after push: 10x Mirror PDP shows disabled "Sold Out" button, no widget; its card shows standard "Sold Out".
  - Untouched on purpose: `product.waitlist.json` + `sections/custom-product-waitlist.liquid` (the full waitlist system with its own settings-driven notify + same Klaviyo list) — Randell is deleting that template himself.
- Waitlist template REMOVED (per Randell, same session — 10x Mirror launched):
  - Verified first: no external references (only a doc-comment mention in `product-jsonld-rating.liquid`, updated) and no dev product assigned any template suffix (all 69 products null).
  - `git rm` (staged, not committed): `templates/product.waitlist.json`, `sections/custom-product-waitlist.liquid`.
  - Deleted from dev theme #186425934130 via scoped `shopify theme push --only <both files>` WITHOUT `--nodelete` (the MCP connector blocks themeFilesDelete); confirmed gone by theme-files query.
  - LIVE cleanup is deliberate/manual (normal push is --nodelete): steps commented on KAN-146 — check no live product is assigned "waitlist" first, then delete both files in the live code editor or via an approved scoped push.
- Jira consistency pass after the removals (per Randell): KAN-138 scope reduced 4→2 files (its two waitlist files no longer exist); KAN-146 checklist description rewritten — waitlist half of the upsell-block step struck, new step 6b (delete the 2 waitlist files on live, check product assignments first), collection-template editor edits promoted from comment to step 7, ride-the-push list updated. KAN-157 already current.
- Uniform no-image collection title (per Randell, e.g. /collections/best-sellers): `main.css` — `.collection-image-with-title .collection__title--no-image` gets the same treatment as the image variant's hero__content band: padding 24px top/bottom (was `var(--gutter) var(--outer)`), hairline border top+bottom, heading trailing margin zeroed when last child. Verified on dev: 24/24 + 1px rgb(247,247,247) borders, white bg (already via --bg), title margin 0.
- Bottom description section evened out (per Randell, /collections/eyes): `templates/collection.json` bottom Rich text section (bottom_description metafield) padding_top/padding_bottom 0/100 → 24/24, text block padding_bottom 16 → 0. Verified on dev: section box = 24 + content + 24 exactly. Live = editor steps (KAN-146 step 7 extended via comment).
- Follow-up optical balance (per Randell — the heading inside the metafield content adds ~19px of built-in space above the text, making the top look bigger): bottom Rich text section padding_bottom 24 → 44. Measured visual gaps on dev now 43px top / 44px bottom.
- Facebook sameAs schema fix (per Randell's error report): `snippets/seo-jsonld.liquid` — `facebook.com/nikolcosmetics` → `facebook.com/nikolfreshbeautystudio` in BOTH the Organization and founder-Person sameAs arrays; verified served JSON-LD on dev. Randell then CONFIRMED nikolfreshbeautystudio is the page for the whole site — footer `facebook_link` setting corrected in the repo's settings_data.json (not pushed; settings never ride pushes) + live editor step on KAN-146. Other footer socials incl. TikTok `@nikolcosmetics` confirmed correct, unchanged.
- Product card price color (per Randell): `settings_schema.json` `card_price_color` default `#000000` → `#564646`; verified computed rgb(86,70,70) on dev cards (no explicit settings_data value, so the schema default flows through).
- Settings-overwrite protection for the transfer (per Randell — he pulls LIVE settings over the dev work, which wipes template/settings changes): created **`dev-settings-to-reapply.md`** (repo root) with every settings-side change + exact values + verification spot-checks, and **KAN-160** (KAN-119 epic, live-sync, due ~8/10 placeholder) as the reminder to re-apply after the settings pull. Cross-referenced with KAN-146.
