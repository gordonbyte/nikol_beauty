# Changelog — 2026-07-06

Resumed the `shopify-live` `cw-*` cleanup (paused during the 4th-of-July promo work). Changes local/uncommitted; git commits handled separately. Nothing pushed to the live store.

## cw-videos + cw-videosv2 → merged into custom-videos
The last two standalone `cw-*` sections were the same base video section duplicated — `cw-videosv2` differed only by ~31 lines of CSS for the Channelize.io "Live Shopping" shoppable-videos app widget (plus one mobile padding value). Merged into one section.

- Created `sections/custom-videos.liquid` ("[Custom] Videos"). Base = the old `cw-videos`; the Channelize widget CSS is now behind an optional **"Enable Live Shopping widget styling"** checkbox (default off), emitted after the base styles so its overrides win. Kept the Channelize rules verbatim (they target the app's hardcoded block IDs).
- Genericized the schema defaults (title "Featured Videos", subtitle "See the products in action.", button "Shop Now") — replaced the old brand-specific copy. Added `enabled_on: { templates: ["index", "product"] }` (covers both current placements). Internal `.cw-collect-videos-*` classes left as-is (self-contained, already decoupled from other sections).
- Re-wired both instances (all three name layers) to `custom-videos`:
  - `index.homepage-style-2.json` (dormant alt homepage) → toggle **off** = identical to old `cw-videos`.
  - `product.coming-soon.json` → toggle **on** + existing content preserved = pixel-identical to old `cw-videosv2` (Channelize styling intact). This is the one instance with live-capable impact.
- `git rm` of `cw-videos.liquid` and `cw-videosv2.liquid`. Verified: schema JSON valid, Liquid tags balanced (if 3/3, for 1/1, comment 1/1, schema 1/1), both templates still valid JSON, and no `cw-videos`/`cw-videosv2` references remain (only a historical note in a code comment).
- This clears the **last standalone `cw-*` section**. Only `snippets/cw-pdp-css.liquid` remains from the `cw-*` list (dead `.cw-bb-*` rules — a separate cleanup pass, ~55 references).

## Removed the Pre-footer section group
The `custom.pre_footer` section group was rendered sitewide in `theme.liquid` just above the footer, but all three of its sub-sections (Broadcast newsletter, `custom-instagram-feed`, `prefooter` trust-badges) were `disabled` — so it displayed nothing anywhere. Removed it.

- Removed the `{% sections 'group-pre-footer' %}` line from `layout/theme.liquid` (layout now goes content → `custom-footer` directly).
- `git rm` of `sections/group-pre-footer.json`.
- `git rm` of `sections/prefooter.liquid` (the trust-badges band; the group was its only consumer — now orphaned, so deleted per request).
- **Kept** `sections/custom-instagram-feed.liquid` per request — it's the only Instagram *feed* section, now a standalone reusable section (has a preset). The footer/header Instagram *social-link* icon is a separate, settings-driven mechanism and is unaffected.
- No storefront impact (everything in the group was disabled). Verified no functional references to the group or the prefooter section remain.
- Left untouched (cosmetic, inert): 9 Broadcast **core** sections still list `"custom.pre_footer"` in their schema `"groups"` arrays — a harmless editor hint now that the group is gone. Flagged for optional later cleanup.

## Removed the unused Text promo section
- `git rm` of `sections/text-promo.liquid`. It was **not placed on any template or section group** (unused everywhere), so zero storefront impact.
- Note: it's actually **stock/near-stock Broadcast** (uses core partials `render 'image'` / `render 'no-blocks'` and `hero__*` classes), not agency-built — it only surfaced as "non-stock" because the `Broadcast-Original` reference folder is a slightly different Broadcast version. Removed per request since it's unused.
- The separate `sections/overlay-text-promo.liquid` (different section) is untouched.
- Left the orphaned `.text-promo*` CSS rules in the stylesheets in place (harmless; for a later CSS sweep).

## Revised custom-sections list
- Generated `custom-sections-list-revised.pdf` (via `scripts/generate-custom-sections-pdf.js`) — the agency-built custom sections still needing cleanup, excluding stock, app-injected (Zipify/OST/Rush/Zooomy), Shopify system sections, and everything already handled. After the Text promo removal, 19 sections remain on the list. (Caveat: the list is built from the imperfect `Broadcast-Original` baseline, so a few entries may actually be stock — re-audit pending.)
