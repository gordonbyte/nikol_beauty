# 2026-08-20

**END-OF-DAY MIRROR (Randell approved):** all of today's work copied dev → shopify-live, folders verified byte-identical. 10 files: layout/theme.liquid (Swiper gating), sections/custom-{hero-banner, guarantee-list, customer-quote, product-slider}.liquid, templates/index.json + index.homepage-style-2.json, 3 new assets/custom-usp-*.svg icons. Product-slider slider stars swapped to the shared custom-product-rating snippet (real Junip averages, homepage-consistent) before the mirror. Everything below now rides the Friday 8/21 push.

**Evening round (mirrored as made, final sweep verified byte-identical):** custom-videos.liquid height removed from `.custom-videos__video` base rule (mobile 220px override kept; flagged iframe-collapse risk + aspect-ratio alternative); product-slider rating polish — padding 8px top/5px bottom, stars scaled 74×14→90×17 with 2px-step inter-star spacing (scoped path translateX, overflow visible, container 100px), count full-opacity theme color (was 75%); hero stars converted to honest partial fill — gray #DDD6CF base + orange overlay at rating/5 width, new "Star rating (0-5)" text setting (default 4.76). Customer-quote stars stay deliberately full (quotes one 5-star review). **Page-too-wide fix (Best Sellers slider):** the native grid--slider's outer-offset negative margins + arrow strip extended past the viewport in our custom section (stock Broadcast sections are wrapped in clipping containers; ours wasn't) → horizontal page scroll. Fixes: scroller margins neutralized, arrow strip pinned 0/0, arrows repositioned 40px from viewport edge, and (Randell's call, live-tested before applying) `overflow: hidden` on `.custom-product-slider__content` as the containment layer. Playwright-verified 0px overflow at 1440/1024/768/390. Debug detour: the dev server's file watcher had died silently (serving stale section, ignoring saves — worse than the usual 500) — restart revived it.

Late additions (mirrored + sweep-verified): Best Sellers button hover (site convention: BRIGHTER bg transition + arrow slide, focus-visible, hover-gated) and product-card second-image cross-fade on hover (0.35s, lazy, object-fit cover, single-image products unaffected, reduced-motion instant swap).

Work log (written during the day as dev-only; superseded by the mirror above):

- **Guarantee list tablet overflow-scroll (custom-guarantee-list.liquid):** marquee CSS un-scoped from the ≤600 media query; JS now measures the items' natural single-line width (off-screen clone, 40px min gaps) vs the bar's usable width on load+resize and toggles `is-scrolling` — the mobile marquee activates on any viewport where the static items genuinely don't fit (tablets), deactivates when they fit again; marquee speed re-scales at activation (was measured-at-load only, would have been wrong when hidden). NOTE: section is ALSO active on the real homepage (index.json).
- **Hero banner (custom-hero-banner.liquid) full day of iterations:**
  - Fluid tablet sizing via clamp(): title 62→34px (~45px @1024), description 20→16px, left padding 140→24px (15px on phones via existing rule), icons 80→56px, features gap; later batch media-scoped to ≤1024 per Randell ("desktop already looked good"): button padding 15px 0, button 45px/16px, features bottom padding 20px.
  - Feature labels + rating text: fluid 18px (desktop, unchanged) → 16px ≤1240px (covers iPad landscape 1112-1366 that a 1024 query missed); labels extended further down to 13px @900px; phones pinned 16px (labels) / theme base (rating).
  - Image 1 slide-right: min-width 680px + overflow:hidden on the right column — image stops shrinking below ~1310px viewport and crops off the right edge instead of leaving dead space above; mobile unchanged (min-width reset).
  - Image 2: fluid 630→262px (43.8vw), seamless into the 262px mobile size; silver-bag pinning — element bottom:0 + translateY(17.9%) (pixel-measured: bag bottom = row 283/346 of banner-cosmetic.png) so the bag's bottom rides the section line at every size while lipsticks/compact overlap below; mobile keeps -25px + transform:none. If the artwork is replaced, re-measure the 17.9%.
  - Rating stars PNG→inline SVG: 5 stars via one path + <use>×5, #FB6D00 (pixel-sampled; same orange as Junip review stars), 95px/78px same as before, aria-hidden; star_image setting removed from schema + both index templates (Stars_Container.png still used by customer-quote section — do not delete from Files).
  - Feature icons 1-3 PNG→SVG (assets/custom-usp-{cruelty-free,esthetician-approved,paraben-free}.svg): color-separated potrace trace @4× (exact palette #D34270 ring / #2A2724 lines), coords rounded (24/22/18KB), same-color stroke-width 4 (Randell tuned: 8 too thick, 0 too thin); per-block "SVG icon" select overrides the image picker, blocks 1-3 set in both index templates; usp4 (Gluten-Free, disabled block) still PNG pending decision. Note: SVGs are ~2-3× the PNG bytes — trade-off accepted for crispness.
  - `<br>` stripped from feature labels at render + cleaned from stored data (both templates).
  - SEO/WCAG audit (contrast excluded per standing rule): fixed stale srcset sizes (image1 claimed 52vw on tablet but renders fixed 680px since the slide change — browsers would pick blurry sources; now `(min-width:1310px) 52vw, (min-width:601px) 680px, 100vw`; image2 → 43.8vw), added alt-text settings for both hero images with sensible defaults (memory: functional alt over decorative). Passing already: h1 title, eager+high-priority LCP image with width/height attrs (no CLS), decorative icons/stars/arrow correctly alt=""/aria-hidden with adjacent text labels, ul/li semantics, real <a> for navigation, no animations needing reduced-motion handling, display:none duplicate mobile list hidden from AT.
  - Life/animation pass: button hover now matches site convention — background transitions (0.3s) to var(--BTN-PRIMARY-BG-COLOR-BRIGHTER) (same var Broadcast solid buttons use), arrow svg slides 5px right over 0.5s, both gated behind @media (hover: hover), plus :focus-visible gets the hover color for keyboard users; entrance = Broadcast-NATIVE data-aos="hero" staggered on rating(1)/title(2)/description(3)/button(4)/features(5) + data-aos="fade" on both images, all anchored to #HeroBanner-{id} — uses the theme's own AOS so it obeys the global animation setting and reduced-motion automatically, and no-JS-safe (elements stay visible without theme.js).
- **Customer quote section (custom-customer-quote.liquid — ACTIVE ON BOTH index.json and homepage-style-2):** top+bottom padding now equal and fluid `clamp(45px, 4.52vw + 20px, 85px)` (tracks the hero products-image overhang formula + 20px gap; 45px even on mobile) — flagged to Randell: on the regular homepage the hero is disabled so this adds visible space with no overlap to clear; stars PNG→inline SVG (same 5-star geometry/#FB6D00 as the hero), keeps the meaningful star_alt as role="img" aria-label (falls back to aria-hidden when blank); star_image picker removed from schema + both quote instances' template data (custom-info-banner's rating block + product.coming-soon still use their own star images — untouched). SEO/WCAG/elements audit (contrast excluded): `<cite>` → `<span>` for the author name (cite is for titles of works, not people); added "Show stars" checkbox (default on — SVG swap had made the stars unremovable); passing already: section aria-label, figure/blockquote/figcaption structure, decorative buyer icon alt="" with adjacent text, stars aria-label, no CLS, Review/AggregateRating structured data correctly absent (self-serving testimonial markup risk, same reasoning as the hero rating).
- **Product slider (Shop Best Sellers) interaction + efficiency overhaul (custom-product-slider.liquid, DEV ONLY):**
  - Tab hover life: fade to full opacity, circle image scale 1.08, center-out underline (1px, grows from middle via ::after scaleX, 0.35s) — underline persists under the selected tab (button pink var(--BTN-PRIMARY-BG-COLOR)) vs lighter pink (BRIGHTER var) on hover; hover-gated + reduced-motion-safe; tabs row now fits-or-scrolls at any width (width:max-content + margin auto + overflow-x, scroll-snap; replaced the hardcoded 570px mobile hack); 8px vertical padding inside the scroll container so the hover zoom doesn't clip (margins compensated).
  - "Why 5 not 3": swiper container's 280px side padding exposed loop-clone neighbors inside the clip area + 300px slide cap left extra space on wide monitors. Interim fix (1060px capped container, arrows moved out of the swiper into panel gutters) then SUPERSEDED by:
  - **De-Swiper migration**: section rebuilt on Broadcast's native `<grid-slider align-arrows>` element ([data-grid-slider]/[data-grid-item], native auto-injected themed arrows, drag+scroll-snap, lazy self-init only when visible/overflowing); exactly 3-across ≥750px via scoped `--item-width: calc((100% - var(--gap)*2)/3)` override, native mobile = 1 card + 50px peek; tab-switch dispatches theme:grid-slider:init on reveal; ALL Swiper markup/init/arrow CSS removed from the section.
  - Efficiency fixes: per-card 5-path star SVG deduped to one section-level <defs> path + 5 <use> per card (~50KB less HTML across 60 cards); srcset sizes '300px' → '(min-width:750px) 30vw, 90vw'.
  - **Swiper bundle gated in theme.liquid**: swiper.css + swiper-bundle.min.js (160KB combined) now load ONLY on index/product/page templates (all swiper-consuming sections live there: custom-info-banner, custom-customer-reviews, custom-product-coming-soon, custom-pdp-css, testimonials, yt-videos-list, videos-filter, review-swiper-card; main.js Swiper calls are all element-guarded so absent bundle can't throw). Blog articles (KAN-179's worst page), collections, cart, account, search drop ~160KB. CAVEAT: adding a swiper-based section to a non-index/product/page template in the editor would render without its slider — full de-Swiper of the remaining 8 sections is the long-term fix (candidate task for KAN-174).
- Dev server wedged repeatedly on editor .tmp files during saves (known bug); Randell restarted each time.
