# 2026-09-03

- **Testimonial card background on the coming-soon PDP (custom-product-coming-soon.liquid, both folders):** `.mp-review-box` gets `background: #f7f2ed` per Randell. Verified computed on localhost. Note: the box has no inner padding, so the cream background sits flush against the stars/quote — padding offer left with Randell. Rides the code push. Follow-ups same session: `padding: 20px 30px` on the box (mobile override rebalanced from lopsided `padding-left: 15px` to `padding: 15px 20px` so the card reads evenly with the new background), `.mp-author-name` 14px (redundant mobile re-size to base removed so it holds at all widths), `.mp-author-box` top margin removed per Randell. Verified computed on localhost: 20px 30px / #f7f2ed / 14px / margin-top 0. `.mp-verified-text` also set to 14px to match the author name (its redundant mobile re-size removed too) — both verified 14px.
- **Yesterday's wedged sync confirmed resolved:** after the `shopify theme dev` restart, the stale `custom-product-coming-soon.liquid` is syncing again — the "Watch Our Products In Action" Tolstoy wrapper now renders and both embeds carry publish id `089bmq19bnsc0` (2 on the page). Tolstoy visuals still need checking on the store-domain preview (dead on localhost).
- **Grey-tile fix for the second Tolstoy carousel + all visible thumbnails loop (custom-tolstoy-player.liquid, both folders):** Randell reported the bottom "What Women Are Saying" carousel rendering grey boxes while the identical-widget carousel above it loads fine — the documented Tolstoy one-carousel-per-page bug from the episode pages (their manager only assigns posters to the FIRST carousel on a page). Ported the episode fix into the shared snippet, adapted for multi-tile carousels: one `getWidgetConfig` fetch per publishId (no productId — full playlist), posters filled per tile index from `carouselSteps[i].stockAsset.shopifyPosterUrl`, and each tile's SD preview (`shopifySources.sd`) loops muted while in the viewport via IntersectionObserver (paused off-screen, skipped under reduced motion) — this also delivers the "all thumbnails playing at once" request (visible-tiles flavor) for BOTH carousels. Caveat: poster/tile matching is by index; if the widget's PDP-mode filter makes Tolstoy render a subset, indexes can misalign — turning PDP-mode OFF on the BB Cream widget (already recommended, and required anyway for the all-videos goal) removes that risk. JS syntax validated; renders on localhost; visuals verified by Randell on the store-domain preview only (Tolstoy dead on localhost, headless bot-detected). Live store confirmed untouched (probed: no coming-soon Tolstoy markup on nikolbeauty.com); no admin writes anywhere.
- **Arrow support for the unmanaged bottom Tolstoy carousel (custom-videos.liquid + custom-tolstoy-player.liquid, both folders):** same root bug as the grey tiles — Tolstoy's manager binds arrows only on the FIRST carousel per page, so the "What Women Are Saying" carousel's arrows rendered dead. Fix scoped to `.custom-videos__tolstoy` only (the managed top carousel keeps Tolstoy's handlers): its `tolstoy-carousel-videos-container` becomes a native horizontal scroller (overflow-x auto, nowrap, scrollbars hidden), and a delegated click handler in the shared snippet scrolls it two tile-widths per arrow click, direction inferred from which side of the carousel the arrow sits on, smooth behavior. Written blind against Tolstoy's known DOM classes — could NOT be observed anywhere I can reach (Tolstoy dead on localhost; both development themes refuse tokenless preview_theme_id, \"missing required files\"; the app-dev store's published theme is now \"Labor Day Sale 26\" without the Tolstoy episode pages) — Randell verifies on his CLI preview. JS syntax validated, renders on localhost.
- **Refresh-grey fix, dev widget configured, and coming-soon work pushed to the dev store theme:** (1) Randell reported grey tiles when refreshing mid-page at the section — Tolstoy tears down and re-creates tile videos on its lazy init when the carousel starts in view, after the poster script's one-shot pass ended; the snippet now keeps a MutationObserver on each carousel and re-applies posters/previews to any new video elements (no timeout). (2) Dev widget `ju3618ffu43hb` fully configured via API (retry approved): sourcesIn = BB Creme playlist `0791c35b-…` (UUID learned from the live widget — playlists are account-shared), isPdpMode OFF, live ON — all 11 videos verified resolving dev-side. Template (both folders) switched from the live widget id to `ju3618ffu43hb` per Randell (dev store shows the dev widget; live editor pastes `089bmq19bnsc0` per KAN-209, unchanged). (3) Pushed the 8 changed files (product.coming-soon template + custom-videos/product-coming-soon/icon-features/alternating-rows/steps-horizontal sections + custom-pdp-css/custom-tolstoy-player snippets) to the dev store's published theme `nikolbeauty-dev` #149231566950 via CLI `--only --nodelete --allow-live` (store domain triple-checked: nikolbeauty-dev-mab1srre; NOT the real live store). Verified serving via authenticated fetch (storefront password re-enabled on the store — password from memory works; both carousels in HTML, dev publish id ×2). Headless browsers can't render Tolstoy tiles even on the store domain (bot detection beyond UA) — visual verification of posters/arrows/autoplay is Randell's. ALSO: the claude.ai Shopify MCP is currently connected to the LIVE store (Randell's warning + the themes list proved it) — only reads done through it today; CLI is the dev-store path.
- **Split the two coming-soon carousels onto separate widgets; single-widget workaround code removed (custom-tolstoy-player.liquid, custom-videos.liquid, product.coming-soon.json — both folders + pushed to dev store theme):** per Randell — one widget per placement, for clean per-placement analytics and because Tolstoy manages each distinct widget natively (its one-per-page limitation is per widget instance), which obsoletes the whole unmanaged-carousel workaround. Randell created LIVE widget "BB Cream - Bottom" (`ea0tfbf1c0mkc`, BB Creme playlist); its PDP-mode turned OFF via API (per-product filter would hide untagged clips on the PDP). Snippet rewritten to just the player treatment (centered modal + backdrop close) — config-fetch poster filler, viewport autoplay, arrow handler, and the scrollable-track CSS all removed. Template mapping now: TOP "Watch Our Products In Action" = `ju3618ffu43hb` (dev widget, dev store), BOTTOM "What Women Are Saying" = `ea0tfbf1c0mkc`; LIVE mapping (top `089bmq19bnsc0` / bottom `ea0tfbf1c0mkc`) documented in KAN-209. Pushed the 3 files to the dev store theme (#149231566950, domain re-verified) and confirmed serving: both ids on the page, workaround code gone. Trade-off accepted with the simplification: the custom all-visible-thumbnails autoplay is gone — tile preview behavior is now whatever Tolstoy does natively per managed carousel; the sale-running LIVE store untouched throughout (no pushes, MCP reads only). Follow-up per Randell: top swapped `ju3618ffu43hb` → `089bmq19bnsc0` so BOTH placements use the LIVE-workspace widgets on the dev site too — template now matches the KAN-209 live mapping exactly (top 089bmq19bnsc0 / bottom ea0tfbf1c0mkc); pushed and verified serving on the dev store. Dev-workspace widget `ju3618ffu43hb` is now fully unused — deletable in the Tolstoy dashboard.
