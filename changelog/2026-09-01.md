# 2026-09-01

- **REAL fix for the dead clicks: `data-priority="high"` on every Tolstoy cell (template, both folders, pushed to dev store theme):** decoding Tolstoy's v2 loader revealed the actual villain — their built-in lazy-init only starts a below-fold cell's initialization ~200px before it scrolls into view, so shoppers were ALWAYS clicking freshly-initializing cells (the race window travelled with the scroll). Their loader has an official escape hatch: `data-priority="high"` initializes the cell at page load. Result on the dev store: all 19 cells report loaded by 2.5s with NO scrolling; last-cell clicks at ≥4s open instantly 6/6 (previously dead ~50% even at 10–18s); realistic scroll-then-click 4/4. Residual: clicks ≤~3.5s after load on the bottom cell can still hit the init race (physically near-impossible for a real shopper; Tolstoy ticket covers it). Rides the pending KAN-205 live push.
- **Click shield REVERTED same day (per Randell — spinner-wait felt worse than the race):** all custom click intervention removed from the template — no shield, no spinner CSS, no retries; clicks pass straight through to Tolstoy. KEPT: all player styles (centered modal, close X, backdrop close), cart bridge, posters/autoplay, and drawer-closes-on-video-click. The early-click init race remains Tolstoy's bug (support ticket) — until fixed, a click in the first seconds after load can still occasionally dead-end a cell (refresh recovers). Verified on the dev store: shield/spinner/retry markers gone, all kept features present. Randell declined a custom player.
- **Bottom-cell dead clicks root-caused: a Tolstoy init-race bug, mitigated with a click shield; the synthetic retry system REMOVED (template, both folders, pushed to dev store theme):** extensive instrumented testing on the dev store proved (1) each cell's player becomes openable at a wildly variable time (2s–18s+ per load, last cells last — nothing observable signals readiness: DOM, listeners, modals, and playerReady all complete <1s while openability lags); (2) ANY click (trusted or programmatic) landing before a cell is openable has ~50% chance of PERMANENTLY wedging that cell (no click ever opens it again — refresh required); (3) programmatic clicks never open the player reliably — meaning the old retry system never worked (its "successes" were the original trusted click landing late) and its synthetic re-clicks were actively poisoning cells; (4) likely why Tolstoy doesn't see this: our per-product layout runs 19 instances of ONE widget per page, far outside their one-carousel design. SHIPPED MITIGATION: clicks during the first 8s (tunable constant TOLSTOY_SAFE_AT) are swallowed before Tolstoy sees them and the cell shows a dim+spinner "loading" state until the window ends; the shopper's next tap then works; NO programmatic replay/retry anywhere (net harmful). Earlier true fixes kept: retry-target discovery (video element holds their handler) documented; cart bridge unchanged. Residual risk: on very slow loads a click after 8s can still hit an unwired cell (Tolstoy's bug — added to the support-ticket list with full repro: 19-instance pages, premature-click wedge, no readiness signal, no open API). Rides the pending KAN-205 live push.

- **Dev store: all 7 remaining guests flipped to Tolstoy (per Randell) — dev episode pages now mirror the live rollout.** The dev Tolstoy workspace needed everything re-done with its OWN identifiers: (1) product tags — the dev catalog's product IDs are a different series (8112…) than live despite the "ID-preserving clone" (Tolstoy keeps separate product records per store), so all ~105 clip→product tags were re-written against a handle-matched live→dev ID map (~121 tag writes incl. multi-product clips); (2) asset IDs — most clips share IDs across workspaces, but ALL of Dawn's 16 and Gloria's Just Peachy are separate uploads with different UUIDs on dev (the first tagging pass on those went to ghost IDs — tag_video_product doesn't validate; re-tagged against the real dev IDs). (3) Widgets — 7 new dev widgets created (Meg ung67vxgctym1, Mary adqvqfw6xuexs, Pam afvufkn17cid0, Marci jlwkpi0eghhy6, Jill xwcsjneir1r5u, Gloria 95jvdifnrdgy2, Dawn 8w9uqpm6mtsyj), carousel/isPdpMode/live. (4) Playlists — per Randell mid-task: NO new playlists; the widgets source the ORIGINAL "«Name» Shoppable" playlists (account-shared across workspaces — verified they resolve dev-side asset records incl. Dawn's; Gloria's dev Just Peachy covered by a manual widget rule). The 8 "(dev)" playlists created before the course-correction are unused — Randell deletes in the dashboard. (5) Dev metaobjects — publish IDs written to the 7 dev-store metaobjects (246…-series GIDs, distinct from live). VERIFIED: per-product filtering exact on all 7 widgets (8 config-API spot checks) and all 7 dev episode pages render Tolstoy mode with 15–22 product cells. Known dev-only gap: Jill's New Beauty Lip Gloss cell is empty (product absent from the dev catalog). Note: the 7 new dev widgets have default player settings — mirror the KAN-207 dashboard pass (Cart button, bag off) for realistic add-to-cart testing.
