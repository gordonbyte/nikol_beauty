# 2026-08-27

## 2026-08-28

- **All 7 remaining guests FLIPPED to Tolstoy on the live store (per Randell's explicit "assign the publish IDs" request):** mass tagging pass completed — 105 clips tagged against live product IDs with ZERO failures (Meg 18, Mary 10, Pam 14, Marci 19, Jill 15, Gloria 13, Dawn 16), mappings sourced from each clip's per-product playlist memberships (April curation) with filename fallback for only 3 clips (Meg 31b51ef1→Fiercely+Brush Set, Meg 89b3c7bc→Creamy Concealer, Pam d4635499→Eye Primer); full map preserved in the session tagging plan. Per-product PDP filtering then verified on all 7 widgets via the storefront config API — each returned exactly the one planned clip for its spot-check product (Meg/RoseLipBalm→d9b15815, Mary/EyeRoller→98b37dcc, Pam/WPLipLiners→1a097cfb, Marci/BBCreme→1e2cd43c, Jill/EyePrimer→91b66bf4, Gloria/RoseLipBalm→e78d6945, Dawn/WPLipLiners→04c31409). Publish IDs written to the 7 LIVE metaobjects' `shoppable_playlist` (store identity verified first; note the live GIDs differ from the dev clone's — 1836/1843-1845-series, fetched fresh by type): Mary→s7h1dmprdapa3, Meg→q1bz9b9fj5j0a, Marci→my9ej1c4ov7fi, Pam→o0cwvoi5tsuy3, Jill→v4ypayluzlpax, Dawn→yndjauy09g9m3, Gloria→5k6g2zkb33nbn; Carol already carried 5ocsnbya4m97d. SAFE NOW / LIVE LATER: the published live theme's Channelize template ignores the field, so nothing changes for shoppers until Randell pushes the Tolstoy template — that single push flips all 8 episodes at once (his NikolBeauty_test theme shows them immediately). Still on Channelize by design: Ruth and Peggy (no Shoppable playlists); Mary is missing 6 clips and Dawn 1 in the Tolstoy library (recoverable from Channelize S3 later — pages work with what's there).
- **All-guest Tolstoy widgets created + Carol repointed to the pre-existing playlist (per Randell):** Randell surfaced that the April setup already includes per-guest "«Name» Shoppable" playlists — my hand-made "Carol — Fierce Aging" playlists were redundant. Carol's LIVE (5ocsnbya4m97d) and DEV (x1sk32uv9l3cs) widgets repointed to "Carol Shoppable" (47f2cf72-8661-4fff-bb73-ac5845e68ac5; 16 items — one more than our 15 tags, harmless in PDP mode); per-product filter re-verified on both. Randell deletes the two redundant playlists in the dashboard (no MCP delete). SEVEN new live-store widgets created, each wired to its existing playlist, isPdpMode, live-in-Tolstoy: Mary `s7h1dmprdapa3` (playlist 86d90186…, episode rebuilding-a-top-lip…), Meg `q1bz9b9fj5j0a` (82d0033b…, droopy-eyelids), Marci `my9ej1c4ov7fi` (96da289e…, dark-circles-over-60), Pam `o0cwvoi5tsuy3` (f9c230b0…, am-i-too-old), Jill `v4ypayluzlpax` (2b33073a…, bringing-life-back), Dawn `yndjauy09g9m3` (8bca73f9…, losing-your-eyebrows), Gloria `5k6g2zkb33nbn` (582b5aa3…, even-out-skin-tone-at-70). Guest↔episode mapping confirmed from live metaobject video_identifier fields. NOT YET FLIPPED: no publish IDs pasted into the 7 metaobjects — their clips have ZERO product tags, so PDP-mode pages would render empty cells; tagging pass (filename-based, ~100 clips) must run first, pending Randell's map decisions (St Barths→Cream Blush; smudge-brush→Brush Set; everything-shown convention) + Mary's 6 and Dawn's 1 missing clips.

- **Retry detection hardened + cart-drawer-closes-on-video-click (template, both folders hash-verified, pushed to dev store theme):** Randell's "bottom videos don't click" was the ghost player striking again — the poller-based open-detection still missed sub-poll open→close cycles; the ghost's invisible modal then consumed the next click (event-logged). Detection now event-based: `window.__tolstoyLastOpenAt` stamped by a 120ms watcher AND by every close action (X/backdrop/Escape — a close proves an open happened); retry fires only if no open ever occurred after the click. NEW per Randell: clicking any video while the theme cart drawer is open closes the drawer (cart-drawer.closeCartDrawer(), event fallback) — symmetric with the add-to-cart flow. Verified on dev store (cache-busted): cells 18/17/16/15 all open first-click with 0.4s-dwell rapid closes, zero ghosts/synthetic clicks; drawer-open → video click → drawer closed + player open.

- **Player ghost-reopen + stuck-next-open — BOTH caused by yesterday's click-retry, fixed (template, both folders, pushed to dev store theme):** Randell reported (a) the player randomly popping back open after closing and (b) sometimes being unable to open another video until refresh. Event-logged root cause: the naive retry checked "player open?" 1.4s after a play click and re-clicked if not — an open-then-quick-close read as a failed click → synthetic re-click = ghost reopen (caught in the log: trusted=false click at exactly +1.4s); the ghost's re-click also collided with the NEXT open attempt → alternating dead clicks. Fix: a 200ms poller tracks whether the player opened AT ANY POINT after the click (open-then-close never retries); retry #2 now calls closePlayer() first to reset stuck state before re-clicking; overlay-close switched to Tolstoy's own close button with API fallback. Verified with event-logged runs: human timing 3/3 clean, rapid-fire 5/5 clean, zero synthetic clicks. (Lesson re-learned: first post-push verification hit a CDN-cached old page and showed phantom failures — always cache-check the served template before diagnosing.)

- **In-player product-card slider misalignment — TOLSTOY BUG, not ours (investigated per Randell):** on multi-product videos, the player's product-card strip arrows scroll a fixed 200px per click while a card step is 289px (281 card + 8 gap) → each click lands ~89px into the next card ("slides to half of the product card"). Verified NOT caused by our 80vh zoom: identical 200px movement with zoom force-disabled. Inside Tolstoy's cross-origin iframe (element `MultipleProductsContainer__ArrowContainer`) — not theme-patchable; to be reported to Tolstoy support alongside the watermark request. Affects only multi-product clips (the two 3-product clips + brush-set clips); strip remains drag/swipeable as a workaround. Also confirmed: Randell's dashboard styling landed (card Add-to-Cart now brand pink; fonts Cormorant/Inter; shopping bag off).

- **SMS carrier-compliance clause added to the LIVE privacy policy (per Randell, explicit request; carrier requires it before campaign verification):** exact required language — "No mobile information will be shared with third parties/affiliates for marketing/promotional purposes. All the above categories exclude text messaging originator opt-in data and consent; this information will not be shared with any third parties." Two privacy surfaces exist on live: (1) `/pages/privacy-policy` (the designed page) — UPDATED via pageUpdate: new "SMS & mobile" section after Consent (phone icon, matching style) + jump-menu entry; verified live, all sections intact. (2) `/policies/privacy-policy` (Shopify legal policy) — connector lacks the `write_legal_policies` scope; Randell to paste the clause manually (Settings → Policies → Privacy policy, after the SMS/TEXTS-Electronic Communications paragraph). Original page body preserved in session context for rollback.

- **Tolstoy: center play button removed from inline tiles, then RESTORED same day per Randell (template, both folders, pushed to dev store theme):** the hide-CSS was added and later deleted — the black play circle shows over the previews again (tapping the video also still opens the player). Two behaviors Randell flagged trace to Tolstoy DASHBOARD settings (not theme code, not settable via the MCP): (1) the player opens as a side mini-player because the widget has `isFullscreenFeed: false` / `slideFrom: "side"` — switch to fullscreen in the widget's player settings; (2) the player's product card shows a "Buy now" primary button (straight to checkout via accelerated checkout, confirmed POST /shopify_pay/accelerated_checkout) — switch the primary button behavior to "Cart" in the video player settings (options: Cart / Product page / Buy now).

- **Tolstoy pilot page iterations** (grey-tile poster fix, per-product filtering via isPdpMode, wider 234px video cells, arrows removed, 10px tile radius, in-viewport muted autoplay) are logged with the rest of the pilot in `2026-08-26.md` to keep the story in one place. Randell stages these on the unpublished NikolBeauty_test theme; the published live theme still runs Channelize everywhere.
- **Footer contact card → Call / Text / Email service block (`sections/custom-footer.liquid`, shopify-live only per new workflow; pushed to dev store theme):** the shared contact block in the redesigned footers (live site runs design2's "Get in touch" card) now renders three labeled methods — CALL 954-537-1772 (`tel:+1…`), TEXT 954-833-0936 (`sms:+1…`, new setting `contact_text_number`), EMAIL hello@nikolbeauty.com (existing `contact_info`) — with small uppercase letter-spaced labels, both numbers at 22px, and the SMS compliance notice below ("By texting us, you agree to receive text messages from Nikol Beauty related to your inquiry. Message frequency varies and msg & data rates may apply. Reply STOP to opt out or HELP for help." — new `sms_notice` textarea). Both new settings ship with defaults so NO theme-editor action is needed on live — the block appears as soon as the section file is pushed. Phone/text hrefs are normalized to +1 E.164. Notice renders only when a text number is set; clearing the setting reverts the footer to call+email. Verified on the dev store at 1440px and 390px: labels, all three links with correct hrefs, notice text, card styling intact. For Sanya-coverage text support — Randell forwards the homepage link to the SMS provider once live.
- **N crest in the footer (same file; 2 rounds per Randell):** first placed atop the "Get in touch" card (38px); then moved per Randell into a new masthead row with the newsletter — subscribe block left, the sticky header's N (`logo-2.png`) right at 96px, centered over the contact-card column (masthead grid mirrors the 1.3fr/1fr grid below); mobile: N first, centered, 48px. Links to the homepage with aria-label.
- **N crest round 3 (same file):** shrunk 96→72px; hidden entirely on mobile (≤785px) per Randell; ENGRAVED effect without needing an SVG — the PNG letterform is used as a CSS `mask`, so the glyph rebuilds in layers: charcoal fill + true inner shadow hugging each stroke's upper edge (dark paint masked to full-box-minus-glyph-offset-3px via `mask-composite: subtract` / `-webkit-mask-composite: destination-out`, blurred 1.1px) + light rim offset 2px below (the lit carve edge). Verified at 3× zoom on the dev store; mobile computed display:none.
- **Newsletter polish (same file):** a "Subscribe supporting text" line was added then REMOVED same day per Randell (markup, CSS, and setting all deleted — title sits directly above the form again); the Klaviyo form restyled to theme via CSS overrides — white input (48px, charcoal border), SUBSCRIBE button in brand pink `var(--BTN-PRIMARY-BG-COLOR)` with brighter hover. Klaviyo doesn't render on the dev store (form keyed to the live domain), so the button/input restyle was verified by injecting the CSS on the LIVE site's real form (white field + pink button confirmed); layout/crest/subtext verified on the dev store.
- **Tolstoy add-to-cart "not working" — diagnosed + cart bridge shipped (template, both folders, pushed to dev store theme):** the add WAS working (player button now "Add to cart" after Randell's dashboard change; POST /cart/add.js → 200, item lands in the Shopify cart with `_isAddedFromTolstoy`, Tolstoy's in-player "My Cart" shows it) — but Broadcast's header count and cart drawer never react, since Tolstoy adds via its own ajax and the theme only updates on its own events → looks like nothing happened. Fix: Tolstoy-mode script wraps `window.fetch` (guarded, pass-through); on a successful `/cart/add` it dispatches `theme:cart:change` with the fresh count (header badge) and calls `cart-items.getCart()` (drawer rebuild via `?section_id=api-cart-items`). Verified deterministically on the preview theme: add → badge 0→1, drawer contains the item; Tolstoy's cart call confirmed to be a bare call-time `fetch` in their source, so the wrapper intercepts it. Drawer does NOT auto-open (the player has its own cart UI; one-line change if wanted).
- **Cart bridge v2 — official Tolstoy API + player-close + drawer-open (template, both folders, pushed to dev store theme):** researched whether theme-cart integration should ship by default — Tolstoy DOES add to the real Shopify cart natively, but theme-UI integration (badge/drawer refresh, drawer open) is officially merchant-wired via their documented `window.tolstoyWidget.subscribe('tolstoy_add_to_cart_success', cb)` API (help article "How to Fix Add to Cart Not Working"; they only ship native drawer integration for the Rebuy Smart Cart app). Bridge rebuilt around that: on a successful add → refresh header count + rebuild drawer + `tolstoyWidget.closePlayer()` + open the THEME's cart drawer (`theme:product:added`) per Randell's ask; the proven fetch-watcher kept as fallback, debounced 1.5s so both paths handle an add once. Note: Randell flipped the widget to fullscreen in the Tolstoy dashboard (player now opens 1440×950 fullscreen). Subscribe-path end-to-end needs a human click to verify (Tolstoy's message bus rejects synthetic events; headless clicks in the fullscreen player flaky) — fallback path fully verified earlier.
- **Tolstoy player recentered as a video-width modal (template, both folders, pushed to dev store theme):** Randell disliked the fullscreen player's full-width layout (product panel on the right, "Go to Checkout" strip) and wanted a centered, video-width player with the dim overlay full-screen. The fullscreen layout is fixed inside Tolstoy's cross-origin iframe (narrowing it just squeezes the panel — tried, rejected), so the recipe is: Randell flipped fullscreen OFF and disabled the in-player shopping bag in the dashboard, and the theme now restyles the COMPACT player's page-level wrapper — `.tolstoy-modal[class*="SmallModal"]` stretched over the viewport carrying rgba(0,0,0,.72) dim, the iframe centered at `min(88vh*0.5625, 380px)` wide (≤380px keeps their no-side-panel layout; product info stays overlaid on the video), 9:16 aspect, 14px radius, drop shadow; desktop-only (mobile keeps Tolstoy's native fullscreen mobile player). SmallModal guard means the CSS never touches the fullscreen feed modal if that mode ever returns. Verified with screenshot: centered player over dimmed page, bottom product card (name/$/Shop), no panel, no checkout strip. ROUND 2 per Randell — player enlarged to 80% of viewport height via CSS `zoom`: the iframe stays 380×676 internally (wider triggers Tolstoy's side panel; and forcing position:static blanked their render — reverted) and is zoomed `min(80vh/676px, 92vw/380px)`, centered with negative margins. Verified 427×760 (= exactly 80vh at 950) centered, video playing, layout intact; non-supporting browsers gracefully fall back to the unscaled 380px player.
- **Player modal polish — close X on the video + backdrop-click close (template, both folders, pushed to dev store theme):** Tolstoy's page-level close button (`.minimize-tolstoy-modal`, zero-sized on desktop) surfaced as a 38px white circle pinned 10px inside the video's top-left corner (left = 50% − min(22.5vh, 46vw) + 10px, i.e. half the zoomed video width); clicking the dim overlay now also closes the player (capture-phase listener → `tolstoyWidget.closePlayer()`, ignoring clicks on the iframe/X). Verified: X at exactly video-corner +10px, X-click closes, overlay-click closes, screenshot clean. In-player icons/product-card styling is NOT theme-reachable (cross-origin iframe) — that's Tolstoy dashboard Design/branding (buttonsDesign currently black #090A0B primary, radius 10; fonts already Jost).
- **Full dev-store Tolstoy replica built (per Randell):** Carol's episode now runs end-to-end on nikolbeauty-dev-mab1srre for safe testing. KEY DISCOVERY: the dev store is an ID-preserving clone of live — identical variant IDs, identical metaobject GIDs, all 34 beauty_tutorial_videos entries already present (the "metaobjects skipped" note from the July seed was wrong or superseded). Work done: (1) Tolstoy dev-store side — all 15 Carol clips tagged against the 20 dev product IDs (resolved via Tolstoy's exact-SKU catalog search; 20 tags, 0 failures, same mapping as live incl. Brush Set's 2 clips), playlist "Carol — Fierce Aging (dev)" (id 1dbfaa27-98f8-4306-8605-7f2b0397039b), widget "Carol — Fierce Aging (DEV episode page)" publishId `x1sk32uv9l3cs` (carousel, isPdpMode, live, sourced from the dev playlist; per-product filter verified). (2) Shopify dev side — only ONE write needed: Carol's dev metaobject `shoppable_playlist` → `x1sk32uv9l3cs` (products list already valid verbatim). Verified on the dev storefront: page 200, 19 Tolstoy cells on the dev publishId, 0 Channelize, posters + autoplay working, player opens as the centered 427×760 modal. Note: Shopify MCP re-authorized mid-task and now points at the DEV store.
- **Dead-click fix — play-click retry (template, both folders, pushed to dev store theme):** Randell's intermittent "clicking a video does nothing" (Waterproof Eyeliner locally, Brush Set in sweeps) root-caused as a LOADING RACE: each cell's player wires lazily and a click during the dead window is a silent no-op — heavier cells and slow environments (localhost proxy) have the longest window; full 19-cell sweeps on the dev replica confirmed every cell opens once wired (incl. repeat open/close via X, Escape, and the closePlayer API). Fix: capture-phase click listener on the play button/video — if no player modal is open 1.4s after a click, re-click (max 2 retries, counter resets on success). Verified under 4× CPU throttle with an instant click on the last cell: player opens.
- **Tolstoy tile border-radius REMOVED per Randell (template, both folders, pushed to dev store theme):** the 10px rounding on the inline video tiles (added earlier today) deleted — tiles render square again. The centered player modal keeps its 14px radius (separate rule, not part of the request). Also noted: the "Powered by Tolstoy" watermark in the player is NOT theme-removable (inside their iframe; account flag `white-label.hidePoweredBy=false`; Business/Enterprise plan feature — Randell to request via Tolstoy support).
- **Workflow change (Randell):** `shopify-dev/` verified byte-identical to `shopify-live/` (386 files each; only `.claude/settings.local.json` differs — local tooling config, never ships). From today ALL theme edits happen in `shopify-live/` only; `shopify-dev/` is a frozen snapshot, no more per-change mirroring. Dev-store test pushes now run from `shopify-live/`.
