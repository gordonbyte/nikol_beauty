# 2026-08-12 (Wednesday)

- **Spaced 3-run PSI averages (3-min gaps beat PSI's result cache):** cart m68 (vs 52 baseline) / d~80 · contact m68 / **d92** (already past 90; no page-specific work exists — rides KAN-175) · **rewards m29 / d43 with LCP 17.4s — new worst page**. Rewards root cause: ZERO server-rendered content — entire page injected client-side (Yotpo Loyalty/Roses widget); created **KAN-192** (High, 8/24) — fix = server-render above-fold content + preconnect, fold into the planned rewards redesign.
- **Fresh site sweep (pre-KAN-175 baseline; no cart/search/rewards per Randell):** product 75/93 · collection 69/91 · homepage 67/60⚠ · videos 53/95 · contact 64/93 · about 60/93 · list-collections 59/56⚠ · blog 52/81. **Averages: mobile 62 / desktop 83 (≈91 excluding the two ⚠ desktop TBT-spike outliers — both had LCP 1.3-1.5s)**. Blog genuinely improved 35→52 mobile / 65→81 desktop (inherits this week's shared fixes); about's desktop anomaly from 8/11 gone (93).
- **Jira closeouts:** KAN-178 (videos) + KAN-189 subtask, KAN-180 (cart) + KAN-188 subtask — all Done with OUTCOME records (root causes differed from hypotheses; remaining gaps ride KAN-175/176).

- **Videos page perf items 3+5 SHIPPED (KAN-178; item 4 skipped per Randell):** `snippets/yt-video.liquid` — `fetchpriority="high"` added on the eager path (featured cover only); `assets/main.js` — the 6 below-fold `.video-product-hub` product swipers now init via IntersectionObserver (200px rootMargin) instead of all at DOMContentLoaded (falls back to immediate init without IO support); `main.min.js` regenerated via minify script. Pushed scoped `--nodelete` to dev theme #186425934130; verified served markup + minified JS (storefront CDN cache took ~2 min to propagate — old HTML persisted across cache-busted fetches until purge completed; server-side file pull confirmed content while waiting).
- **Final videos-page numbers vs 08-11 baseline:** desktop 74 → **95** (TBT 350→60ms, LCP 1.7→1.2s — task goal exceeded); mobile 58-61 → **67** (LCP 8.9→6.0s, TBT 300→120ms, weight 3,160→~2,390 KiB). One mobile run read 52/LCP 13s — PSI outlier (that run's FCP regressed across the board); adjacent runs confirm 67. Remaining mobile gap = render-blocking CSS chain (KAN-175). Manual check pending: product-slider arrows after scroll (init behavior changed).
- **Cart perf diagnosis + change A SHIPPED (KAN-180):** empty-cart LCP element = the HEADER LOGO (page has zero content images; TBT 20ms, CLS 0 — purest shared-chain page). Causes: CSS chain (KAN-175); desktop header opacity:0 until is-loaded on DOMContentLoaded (theme.css:7081) — **Randell: NEVER change this (memory saved)**; logo load started ~533ms late. Fix: `sections/header.liquid` now emits `<link rel="preload" as="image" fetchpriority="high">` for the logo with imagesrcset/imagesizes byte-identical to the img (verified via section-render API — no double download). Measured: LCP resource-load-delay 533→294ms mobile / 205ms desktop; scores within lab noise until KAN-175. Change C (dedupe theme.liquid CSS preloads) explained, awaiting decision; B permanently declined. Ops gotchas hit: CLI token expired mid-push (Randell re-authed via `!` command) AND a repo-root push with `--only` "succeeds" while uploading nothing — always `--path shopify-dev`.
- **Change C SHIPPED (KAN-180 approved):** `layout/theme.liquid` — deleted the 3 duplicate CSS `preload_tag` lines (theme.min.css, main.min.css, swiper.css; old lines 55-57). theme/main CSS keep their preload via the stylesheet_tag Link header; **swiper.css returns to its intended lazy `media="print"` load** (the old preload tag was defeating that optimization). Kept vendor.js/theme.js script preloads. Server-side verified via pull (1 remaining style preload_tag = conditional swatches.css duplicate at ~line 191 — left for KAN-175 scope). Leftover swatches dup noted for Monday.
- **Live Sync task created: KAN-191** (updated same day: now 6 files incl. header.liquid + layout/theme.liquid) (under KAN-119, label live-sync) — 4 files to push to live: yt-video.liquid, videos-featured.liquid, main.js, main.min.js; verify on nikolbeauty.com/pages/videos.
