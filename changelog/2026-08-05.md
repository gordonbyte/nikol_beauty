# Changelog — 2026-08-05

## KAN-69 (backlog #69): stock core sections review — REVIEW DONE, 5 of 8 defects + best-sellers blob fixed (approved: "fix 2,3,4,6,7")

- **Review (3 passes: scripted dead-settings scan, structured file review, behavioral browser pass):** zero dead settings (all 45 candidates snippet-consumed), zero broken renders, zero app leftovers, healthy landmarks. 8 defects + ~12 smells recorded on the ticket.
- **Fixed + pushed to dev theme, all browser-verified (0 pageerrors):**
  - cart.liquid: empty-state double-h1 → `<p class="cart__title">` (1 h1 on /cart now)
  - search.liquid: results caption p→h1 (both branches) + `h1.search__caption` pin (font-style/line-height) in theme.css — search page finally has its h1, typography identical (11px Inter)
  - cart-drawer.liquid: `role="dialog" aria-modal aria-label="Cart"`; header.liquid cart toggles + `aria-haspopup="dialog" aria-controls="cart-drawer"` (skipped on cart template); hamburger close button "Show menu"→"Close", bogus aria-haspopup/expanded removed
  - product.liquid: stray duplicate `</zoom-images>` removed (served PDP now 1 open/1 close); line-item dropdown ARIA rebuilt on block.id + the missing `-label` id added
  - **cart-drawer best-sellers JSON blob REMOVED** (Randell-approved after provenance check: agency-added pre-repo, absent from stock Broadcast, zero consumers ever incl. deleted custom.js; leaked 8 products' tags into every page's source)
- Pending Randell's rulings: toolbar_block dead feature, filter-sidebar anchor typo, cart-message color read, sticky logo, smells list. KAN-69 stays open. All changes = code files → Friday push.

## KAN-69 defect #1 follow-up: header Toolbar feature REMOVED entirely (Randell's ruling)

- First restored the lost `assign toolbar_block` line (feature worked for the first time ever); Randell tested and ruled "remove this toolbar entirely."
- Removed: init var + height block, the whole toolbar capture (~140 lines incl. toolbar_menu/gift_card_link/utilities), toolbar-fed locale fallback assigns, the mobile-drawer toolbar branches (menu strip/gift-card/toolbar-socials — all dead-in-practice since before July), the render site, and the 12-setting Toolbar schema block. header.liquid ~45.4KB→35.6KB, zero toolbar refs left, schema JSON validated, CRLF preserved (node script with LF normalization — Edit-tool strings don't match CRLF files).
- Verified on dev theme: desktop header 42 nav links / no Liquid errors / 0 pageerrors; mobile hamburger drawer renders, socials row unchanged (off). Group JSONs carry no toolbar instances. Rides Friday push.

## KAN-69 defects #5 + #8 + sticky logo — FIXED (approved); all 8 review defects now resolved

- **#5:** collection.liquid + search.liquid filter-panel close anchors `#filters-group` → `#filter-groups` (real panel id; no-JS fallback repaired). Verified on dev.
- **#8:** cart.liquid cart-message color read `section.settings` → `block.settings` — the block's Text-color picker functions now (latent; matches drawer implementation).
- **Sticky logo wired to settings:** new `sticky_logo` image_picker in header schema; markup uses the responsive `image` snippet when set, falls back to the original logo-2.png asset when empty (live look unchanged until chosen); href → routes.root_url; alt → shop name; hand-edit residue cleaned. Fallback verified identical on dev, 0 pageerrors.
- KAN-69 now has all 8 defects resolved; only the ~12 minor smells remain recorded on the ticket.

## KAN-69 CLOSED: a11y mini-batch (approved "fix them") + 9 smells consciously accepted

- `role="alert"` on both cart error regions (cart.liquid + cart-drawer.liquid) — injected errors now announce to screen readers; both free-shipping `<progress>` bars gained `aria-label` via new locale key `cart.general.free_shipping_progress` (en.default.json validated). Verified on dev /cart: alert region + label present, 0 pageerrors.
- Remaining 9 smells accepted with line-number record on the ticket (duplicated free-shipping block, dead local vars, footer schema ordering, divider shopify_attributes, #buynow anchor, underlay dismissal, modal aria-hidden pattern, thumbnail anchors, comment mislabels).
- KAN-69 Done: est ~2h → actual ~4.5h; the biggest triage item is finished.

## KAN-120 (backlog #58): lazy-load + resolution caps on 4 sections — RESOLVED (approved)

- meet-nikol (homepage pos 10): eager→lazy. image-grid (homepage pos 18 + PDPs): was uncapped master + eager → lazy + srcset 400-1200 + sizes + width/height (CSS pre-checked: absolute-fill/cover = both axes safe). customer-quote: star master→260, buyer icon master→48, bg→2000, img_url→image_url. alternating-rows (coming-soon, active): photo master→900+srcset, stars→200, verified icon→48, bg→2000, + height:auto guard on __media img (July letterbox lesson).
- Verified on dev home/PDP/coming-soon: exact caps served, rendered sizes unchanged (540×540 grid squares, 610×488 photos proportional), 0 pageerrors. Noted out-of-scope: ~17 other below-fold eager imgs belong to stock sections. Rides Friday push.

## KAN-129 (backlog #64): section-highlights hardcoded branch — DELETED as dead code (approved)

- The `if section.id == 'template--22389875638576__c6d81760…'` branch points at a retired theme: live renders the instance under theme 27247694741808, dev under 26799384854834 — neither matches, so the special `.product-information` styling has fired nowhere since the 07-17 relaunch; both stores show standard styling (the approved look). Hardening would have resurrected unfamiliar styling — deleted the branch instead.
- Pushed + verified: 6 highlight tiles standard classes, 0 pageerrors. Rides Friday push.

## KAN-127 (backlog #53): Channelize widget background sync — DOCUMENTED

- The constraint (app widget's hardcoded #FCC9C6 can't follow the "Section background" setting) now documented at both decision points: schema `info` under the color picker in the editor + CSS comment at the hardcoded rule in custom-videos.liquid. Schema validated, pushed, videos page 200.

## KAN-105 (backlog #23): 33 dead "alternate" collection suffixes CLEARED ON LIVE (approved) + KAN-106 parked

- Live connection re-established by Randell; store identity verified TWICE (get-shop-info + shop.url) immediately before writing. Full 33-item before-list (handles + GIDs) presented; Randell reviewed collections live first and asked the right question — "what's the difference vs default?" Answer: NONE today (collection.alternate.json exists nowhere in the theme or git history back to June 15 — pre-relaunch fossil; Shopify falls back silently). 
- 33 × collectionUpdate templateSuffix→"" in 3 batches, 0 userErrors; re-sweep of all 133 collections = zero alternates; lip→lips (valid) untouched; /collections/eyes, /lip-gloss, /lip all 200. Pages/products stay per Randell's keep-live-assignments ruling. Reversible via recorded GIDs.
- **KAN-106 PARKED UNTOUCHED (Randell: change nothing):** discovery — rbrfb.fastbundleconf is ALIVE (Fast Bundle config, is_active + has_required_plan, 0 active bundles, updated Jan 2026) → the app is likely STILL INSTALLED, not a leftover; deleting its config would be wrong. Pending his decision on Fast Bundle itself (possible subscription for zero bundles — worth checking billing).

## KAN-125 (backlog #37): dev store meta/shop description — DISMISSED (Randell)

- Fourth "dev is a code sandbox" ruling of the day: SEO fields on a password-protected, never-indexed store affect nothing. Manual 2-min paste in dev admin if ever wanted.

## KAN-67 (backlog #41): dev-store app install check — DISMISSED (Randell)

- Same reasoning as the #7/#42 dismissals: the dev store is a code sandbox, not an app mirror — app/data-dependent verification runs against live via the read-only local dev server (exactly how today's wishlist/reviews/blog checks worked; Junip's absence on dev breaks nothing). §4 of dev-store-parity-checklist.md stays as reference if the strategy ever reverses.

## Verification batch KAN-58/62/64/65 — ALL CLOSED (Randell: "they all work, count as complete")

- **KAN-64 (#34) PASS:** 7-page live browser pass — PDP 5 native selects + contact 1 all visible, zero selectboxit remnants. Dropdowns healthy post-removal.
- **KAN-65 (#36) PASS:** desktop heart click on live opens the SE Wishlist popup without navigating (href stays as no-JS fallback). First attempt hit the hidden mobile duplicate — visible-element pick pattern matters.
- **KAN-62 (#32) PASS:** all 7 live page types fully styled (Inter, header 93px, 29-43 stylesheets, 0 unstyled/CSS errors; 2 'Failed to fetch' = this IP's 429 noise) + the swept CSS has been live since 07-17 with 3 weeks of clean daily use.
- **KAN-58 (#5) VERIFIED:** the automated batch couldn't render a badge (zero live promo data at the time), but Randell render-checked it himself same day — "it shows correctly." Clean pass; the Fierce Aging glance is now optional.

## KAN-124 (backlog #9): corner-ribbon curls — CONSCIOUSLY SKIPPED (Randell)

- "Its showing ok now so lets not mess with it. leave it as is" — the 07-22 ribbon badge stays curl-less; the fold triangles are pixel-fragile ornamentation at responsive card scale. Zero changes.

## KAN-84 dismissed + KAN-83 deferred (Randell's rulings)

- **KAN-84 (#16) DISMISSED:** the SpiceGems /products/null report — preview-only bug, "i dont care what happens on unpublished-theme previews"; diagnosis preserved in memory + ticket for reopening if it ever hits the published theme.
- **KAN-83 (#15) DEFERRED to the very end of the triage:** Zipify × CreditsYard vendor tickets — priority dropped to Low, comment logged; drafting happens when he picks it up.

## KAN-130 (backlog #70): guarantee marquee — width-measured duration (approved, "done correctly")

- Chose real measurement over the count-based approximation: inline script sets animation-duration = (trackWidth/2)/50 on window load → constant 50px/s whatever the items/text; CSS 20s stays as no-JS fallback; reduced-motion untouched. Verified on 390px viewport: 1129px → 22.57s = exactly 50px/s, animation live, 0 errors. Rides Friday push.

## Triage re-planned: hardest-first (Randell's directive)

- The 23 remaining items ordered most→least complicated; working the top down. Also 3 board corrections on Randell's rulings:
  - **KAN-123 (#7) DISMISSED** — dev-store content gaps accepted (404s harmless; content work tests against live data via the local read-only dev server).
  - **KAN-126 (#42) DISMISSED** — draft-article import pointless while dev has no blogs; live-CDN images cosmetic.
  - **KAN-59 (#10) MOVED** — discount-sync test matrix is app testing, not triage: recreated as **KAN-148** under the KAN-119 Live Sync epic (due 8/14 unchanged), KAN-59 closed as relocated. (API can't convert subtask→Task; the parent link again needed a post-create edit — known connector quirk.)

## KAN-85 (backlog #17): rare live JS errors — ROOT-CAUSED + theme guard shipped to dev (approved; read-only on live per Randell)

- **`undefined.is_enable_app`** = SE Wishlist Engine app-side: its `/apps-config` fetch feeds `app_widget_data`, read by two unguarded setIntervals — crashes only when that call fails (429/outage). Human-paced probe across 6 live page types: config 200 every time, zero occurrences (the 07-29 crawler self-inflicted its 429s). Randell ruled: no action, no vendor report.
- **`undefined.options`** = stock Broadcast color-swatch component in theme.js (exact expression pinpointed): per-tile `products/<handle>.js` fetch → `.catch(console.error)` swallows failures and passes undefined → `.options` crash; same path logs the recurring `Unexpected token '<'` JSON error when a 429 returns HTML. Reproduced on live /collections/eyes — but only because this machine's IP is heavily rate-limited (every subresource 429'd, incl. Shopify pixels); real shoppers essentially never see 429s, and the only symptom is missing color dots.
- **Fix (shopify-dev only; live untouched):** `if(!e||!e.options)return;` guard in the theme.js swatch handler (theme.js is not minify-pipeline — ships as-is). Pushed to dev theme; served file verified; dev /collections/eyes: 14 tiles, 24 swatch elements, 0 pageerrors. Rides Friday's code push. js-errors mission item ✔.

## KAN-63 (backlog #33): July PageSpeed re-run — SUPERSEDED (Randell)

- "I will be doing an entirely new performance review so no need to compare on this" — the fresh review runs under the standing KAN-30 project with new baselines; noted there to measure after the 08-08 push so this week's perf wins are in the baseline.

## KAN-86 → KAN-149: GTM session moved out of the triage (Randell)

- Randell suspected the Bing UET double-fire was already gone; a fresh probe proved it STILL LIVE (bat.js in main frame via GTM-PWZTLKXQ with tm=gtm002 beacon + in the SS-Bing-Conversion pixel sandbox — double beacon for tag 187177478 per pageview).
- Per his direction: recreated as KAN-149 under the Marketing/Analytics epic (KAN-5) as an independent, self-run task with a deliberate experiment design — baseline Bing data → pause the GTM UET tag (keep the custom pixel) → probe-verify single fire → unused-tag review → measure the data difference. KAN-86 closed as relocated. Triage down to 3 open (KAN-60 Friday, KAN-106 parked on Fast Bundle, KAN-83 decision).

## 🏁 TRIAGE COMPLETE — KAN-50 closed (a week ahead of the Aug-12 plan)

- Randell's final directive: remaining triage items become independent tasks. Created: **KAN-150** (post-push visual checklist, KAN-119, due 08-08, expanded with all of this week's changes), **KAN-151** (Fast Bundle decision, KAN-2, full findings + 3 paths), **KAN-152** (Zipify×CreditsYard long-term, KAN-2 — with the new permanent ruling "never uninstall CreditsYard, we use that"; memory corrected, uninstall path retired). KAN-60/106/83 closed as relocated.
- KAN-50 closed with the full campaign OUTCOME: all 75 items dispositioned 07-30→08-05; backlog file header stamped complete and remains the permanent index. Continuations: KAN-146/147/148/149/150/151/152.
