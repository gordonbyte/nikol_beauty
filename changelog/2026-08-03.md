# Changelog — 2026-08-03

## KAN-102 (backlog #72): meet-nikol CTA blank-text guard — RESOLVED

- Confirmed latent (Randell's homepage renders the CTA fine): the anchor rendered even with cta_text blanked → nameless invisible link for AT users. Fix: `{% if cta_text != blank %}` guard in `sections/meet-nikol.liquid`; pushed to dev theme; homepage verified unchanged, 0 errors. Rides KAN-141 to live (file already in its set; comment added).
- Side observation: the 07-10 SEO wording "Read More About Nikol" was reverted to "Read More" at some point (screenshot + repo index.json agree) — design call, not re-flagged.

## Board reprioritization (Randell's directive: triage first, only Fierce Aging prep interrupts)

- Standing fact recorded: Discount Sync app runs LOCALLY (Randell triggers syncs) — production go-live is NOT sale-critical. Memory saved.
- KAN-133/134/135/136 (app go-live): High Aug 5-7 → Medium Aug 19-21 (post-sale)
- KAN-59 (test matrix): High Aug 4 → Medium Aug 14 (gates the Aug 19 deploy, not the sale — Fierce Aging is a percentage discount on the proven pathway)
- KAN-11 (popup redesign): High today → Medium Aug 21; KAN-26 (Beauty Reinvented): Aug 7 → Aug 20
- KAN-143 (sort dropdown bug) confirmed for Thursday Aug 6
- KAN-50 (triage) due date set to the projected completion: **Wed Aug 12** (~36h remaining at ~5h/day with sale-prep interrupts only)

## Working agreement re-affirmed (Randell, midday)

- "Do not make changes without me approving — explain each task and each error, then recommend fixes." KAN-101's first attempt was reverted untouched (never pushed) and redone properly: explanation → he viewed the page → approved → applied. Memory updated (explain-before-changing).

## KAN-101 (backlog #68): col-sitemap nav landmark — RESOLVED (approved by Randell)

- /pages/collections menu was a bare `<ul>` — no navigation landmark for assistive tech (WCAG 1.3.1). Wrapped in `<nav aria-label>` (label resolves to page title "Collections"). Verified on dev: identical visuals, 42 links, 0 errors. Live = NEW collective task KAN-145 (a11y batch files accrue there as items complete).
- Observation (no action): section references a `confirm_child` setting missing from its schema — dead branch, noted for cleanup.

## KAN-100 (backlog #67): About-page quote semantics — RESOLVED (approved by Randell)

- The "title" half of the flag was already fixed 07-07 (mobile h1). The quote on /pages/about rendered in a plain div — now wrapped in `<blockquote>` (screen readers announce it as a quotation, WCAG 1.3.1) with a reset rule in main.css. Live: section file on KAN-145; CSS rides KAN-141's main.css.
- **Defect caught by Randell, fixed same hour:** the first reset missed that theme.css globally styles `blockquote` (font-size var(--font-3) + border-left + padding) — the quote rendered SMALLER than the intended 31px. Scoped rule extended with `font-size: inherit; line-height: inherit` → quote inherits the parent's responsive sizes (31px/45px desktop, 30px/40px ≤785px). Verified in the SERVED dev-store main.min.css. New standing practice memorized: grep theme.css for global element rules before introducing a new element type; verify served CSS, not just markup.
- Incident + fix: a PowerShell round-trip while annotating the backlog file double-encoded its em-dashes/arrows/checkmarks; repaired with char-code replacements, verified zero mojibake remaining. Lesson: always edit that file with the Edit tool, never PS Get/Set-Content.
- **Regression caught by Randell + fixed same day:** theme.css's global `blockquote` rule (≈4497) sets its own smaller font-size — the scoped reset missed it, so the quote briefly rendered undersized. Added `font-size: inherit; line-height: inherit` (parent's responsive 31/45 desktop, 30/40 mobile applies at all breakpoints); re-minified, pushed, re-verified. Full Monday review sweep after: about blockquote ✓, homepage CTA ✓, collections nav + 42 links ✓, view=new fallback ✓, 0 errors everywhere.

## KAN-99 (backlog #66): contact-form a11y items — CLOSED, NO CHANGES

- The headline sub-item ("h2 assumes a page h1") was a FALSE ALARM: /pages/contact's hero rich-text section provides a real h1 and the form's own h2 never renders (title setting blank). My initial "zero h1" reading came from a mangled fetch counted without sanity-checking — Randell caught it by looking at the page. Other two sub-items dismissed (form dividers ≠ document headings; live region moot in a reload flow). contact-form.liquid remains untouched stock — zero drift.

## KAN-98 (backlog #65): accordion summary heading — DISMISSED (Randell approved)

- Verified active first: faq ("Beauty Instructions") + shipping accordions are ENABLED on the main product template — 4 `<summary>` elements render on every standard PDP. Dismissal rationale: native details/summary is an accessible disclosure pattern (announced as expandable button, keyboard built-in — same pattern we chose for KAN-42); heading-in-summary has inconsistent AT support; stock Broadcast file stays untouched (zero drift). Heading variant documented in the ticket if ever wanted.

## KAN-97 (backlog #63): Videos-page filter a11y — RESOLVED (Randell approved 1+3, dismissed 2)

- `sections/videos-filter.liquid`: initial "All" filter links carry `aria-current="true"`; `assets/main.js` (+min): click handler now swaps aria-current alongside `.active` — screen readers hear the selected category. `sections/videos-featured.liquid`: both Subscribe links guarded against a blank youtube_link setting. Anchors→buttons conversion dismissed (keyboard-operable named links). All agency/custom files — zero stock drift.
- Verified on dev /pages/videos: 0 errors, attributes present, links + 12 filter buttons intact. Live = KAN-145 (main.js/min explicitly noted — not in KAN-141's set).

## KAN-96 (backlog #62): video-gallery heading normalization — RESOLVED (Randell chose fix)

- `sections/video-gallery.liquid`: hidden `<h2 class="visually-hidden">Videos</h2>` above the makeup-tab grid; "Real Stories, Real Beauty" h3 → `<h2 class="h3">` (identical size — .h3 utility pins --font-7; verified no .videos-desc child rules to lose). Outline per tab now h1→h2→h3→h2. Covers both gallery pages (shared section). Verified on dev: markup counts correct, 0 errors, visually identical. Live = KAN-145.

## KAN-95 (backlog #61): address-form autocomplete — RESOLVED (Randell approved after break-risk review)

- 20 standard autocomplete tokens added across both address forms in `sections/customer-addresses.liquid` (given-name/family-name/organization/address-line1-2/address-level1-2/country-name/postal-code/tel). Passive hint attributes — names/IDs untouched, zero behavior or visual change; contact-form precedent live since 07-07. Counts verified (each token ×2 = 20); pushed to dev theme. Page is login-gated → code-level verification; optional manual test-address on dev. Live = KAN-145.
- **Side discovery while answering "what does original Broadcast have?":** `Broadcast-Original/` is Broadcast **8.1.1** (Presidio Creative), NOT our 5.5.0 (Invisible Themes) — an upgrade reference, not current-version stock. KAN-143's "deviates from stock" finding corrected (may be a version difference); memory updated so it's never miscited again.

## KAN-94 (backlog #60): customer account pages h1 — RESOLVED (Randell approved)

- Premise code-verified first (login-gated pages): no h1 on /account, /account/addresses, or order pages — titles were h2. Three one-line swaps: `customer-account.liquid` + `customer-order.liquid` (`h2.page__heading` → `h1.page__heading.h2`), `customer-addresses.liquid` (bare h2 → `h1.h2`). `.page__heading` doesn't pin size, so the `.h2` utility carries the exact current look. Pushed to dev theme; code-level verification (login-gated); optional manual glance at the dev account area. Live = KAN-145.

## KAN-93 (backlog #59): href="#!" dead-link fallbacks — DISMISSED (Randell approved)

- Rendered-page sweep (home/About/Videos/collection/PDP/sitemap on dev): ZERO `href="#!"` render — every configured button has a URL; fully latent merchant-config edge. Fixing = guards across 12 stock files (mostly dormant library sections) — not worth the drift. The `| default: '#!'` pattern + full file list documented in KAN-93 for future audits.

## KAN-92 (backlog #57): homepage testimonial star rating alt — RESOLVED (Randell approved)

- Star image downloaded + visually inspected first: 5 filled stars = a 5/5 rating, so the default alt is factually accurate. `custom-customer-quote.liquid` gains a "Star rating alt text" setting (default "5 out of 5 stars", blank = decorative) wired to the previously-empty img alt. Verified RENDERED on the dev homepage (`alt="5 out of 5 stars"`), 0 errors, zero visual change. Live = KAN-145; schema default applies, no editor step.

## KAN-91 (backlog #52): review carousel heading + autoplay — RESOLVED (final design after 3 iterations)

- Heading "Customers Love for Nikol Beauty" was hardcoded → now a section setting (default = same text, zero change).
- Autoplay design iterated live with Randell: v1 remove-autoplay → v2 hover-to-start/stop-on-leave → **v3 FINAL: auto-rotate + visible pause/play toggle + speed setting** (his pick, the textbook 2.2.2 pattern). New "Auto-rotate speed" range setting (3–20s, default 10s) → data attribute → Swiper delay; new 44×44 round pause/play button bottom-right of the section (inline SVGs interchange on click, aria-label swaps, focus-visible outline, palette-matched); reduced-motion users start paused.
- Files: `review-swiper-card.liquid`, `main.js`(+min), `main.css`(+min). Node syntax check; minified markers verified; pushed to dev theme; homepage renders button + 10s attr, 0 errors. **Interactive check pending: Randell to try rotation, the toggle, and button placement on the dev homepage.** Live = KAN-145.

## KAN-56 (backlog #40): product-new hardcoded BB Crème gallery — DELETED (last booby-trap)

- Randell chose delete over rebuild (coming-soon is the launch-page pattern). Findings: gallery hardcoded to bb_creme1-6.png, no alt text, redundant Swiper CDN load (also #47); template DORMANT — zero products assigned.
- `git rm sections/product-new.liquid templates/product.new.json`; product-jsonld-rating.liquid doc-comment updated.
- **FULL push to dev theme** (deletions need full sync) — side benefit: dev theme now fully matches the git-tracked repo; July-8-base mixed state ended.
- Verified: homepage/PDP/coming-soon/collection 0 Liquid errors; `?view=new` falls back to default PDP.
- Live = KAN-144 (delete both files with next sync; caution: first confirm no live product is assigned template "new").
