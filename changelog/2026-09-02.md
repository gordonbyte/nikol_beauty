# 2026-09-02

- **Firefox picture-in-picture toggle hidden on Tolstoy preview tiles (beauty_tutorial_videos.liquid, both folders, pushed to dev store theme):** Firefox overlays its native PiP button on any hovered playing video; on the muted preview loops it was misleading (clicking it pops out the silent loop in a floating mini-window — it never opens the centered player, and Randell was seeing it on dead tiles and reading it as part of the bug). Probe confirmed Tolstoy never sets `disablePictureInPicture` itself, so a small MutationObserver now marks every injected preview video `disablePictureInPicture` as it appears. Cosmetic only — zero effect on clicks or the init-race issue. Verified on the dev store: 20/20 preview videos flagged at 4s and 8s. Rides the pending live push.
- **Coming-soon PDP images fixed on the local preview (app-dev store content, no theme changes):** sections on `?view=coming-soon` (Steps Horizontal, Icon Features, Alternating Rows, Info Banner + star/verify images) showed no images on `127.0.0.1:9292` because the running `shopify theme dev` is connected to the app-dev store (nikolbeauty-dev-mab1srre), whose Files library was missing 15 of the 22 `shopify://shop_images/...` files the template references (image_picker resolves blank → section skips the img; blocks with hardcoded live-CDN URLs were unaffected, hence "some sections"). Uploaded the 15 missing files to the app-dev store via Admin API `fileCreate` (originalSource = live CDN, exact filenames, RAISE_ERROR on duplicates). Verified: all 22 files 200 on the app-dev CDN, all sections render with images on the local preview (screenshots), and the same page was already correct on the theme-dev store and live — nothing to sync anywhere.
- **Icon Features rows tightened — phantom double-height images fixed (custom-icon-features.liquid, both folders):** the "spread out" look Randell flagged after the file backfill was never the 35px row margin: Shopify's `image_tag` writes `width`/`height` attributes (icons 260×260, main image 1000×1353) and the CSS only capped `max-width`, so the height attribute stuck — every icon rendered in a 130×260 box (~65px invisible band above/below each row) and the right-side image in a 647×1353 box (~480px of dead height). Before the backfill the images resolved blank on the app-dev store, so the section rendered compact text-only rows and the built-in oversizing was never visible; live has always rendered oversized. Fix: `height: auto` on `.custom-icon-features__feature-icon img` and `.custom-icon-features__image img` (mobile's explicit 450px-cover main image intentionally still overrides). Measured on localhost: feature rows 264px → 134px tall, main image 1353px → 875px, section ~470px shorter; 35px inter-row margin left as-is. Section is active on the `product.coming-soon` template only (both folders), i.e. the `?view=coming-soon` PDP. Rides the pending live push.
- **"What Women Are Saying" video restored on the coming-soon PDP (product.coming-soon.json, both folders + one file backfill):** the `custom-videos` section (`cw_videos_WcxwdJ`) rendered as a bare heading because it never had `video_item` blocks of its own — its videos historically came from a Channelize shoppable-videos app block in the following `apps` section, which was removed from the template long ago; `enable_live_shopping_style: true` remained behind, injecting dead Channelize CSS and stripping the section's bottom padding. Per Randell: added a `video_item` block using the template's own leftover clip (the retired `cw-bb-video` section's `c39362bf…mp4`, 21s "THE BB CREAM MADE FOR MATURE SKIN", poster `vid.jpg`) rather than the homepage-style-2 testimonial clip, kept the current heading, no button, and set `enable_live_shopping_style: false`. Backfilled `vid.jpg` to the app-dev store Files (was 404 there; exists on live/theme-dev). Section is active on the `product.coming-soon` template only. Verified on localhost: video renders with poster at 1000×562, readyState 4, normal padding. Template JSON = settings → live needs editor steps, tracked as KAN-209 (live-sync, under KAN-119).
- **Info banner media padding 0 → 80px on the coming-soon PDP (product.coming-soon.json, both folders):** per Randell — 80px breathing room around the left image of the "Stop Wearing Makeup" banner (`custom_info_banner_Adm6cV`, block `image_1`, `media_padding` setting; previewed 40/60, Randell picked 80 mid-task). The setting renders as inline padding on the whole section; desktop-only in practice since the section CSS forces `padding: 0 !important` below 1024px (mobile verified unaffected). Verified 80px computed on localhost. Section is active on the `product.coming-soon` template only. Live replication folded into the KAN-209 editor steps.
- **Info banner button double arrow fixed on the coming-soon PDP (product.coming-soon.json, both folders):** the section always appends an animated SVG arrow to `.custom-info-banner__button`, and the block's `button_text` also carried a literal "→" — two arrows. Removed the literal one from the text ("GET YOURS NOW"), kept the SVG (it has the hover slide animation). The other three "GET YOURS NOW →" buttons on the template (steps, icon features, alternating rows) render text-only, so their literal arrow is their only one — left untouched. Verified on localhost: 1 SVG, 0 literal arrows. Live replication added to the KAN-209 editor steps.
- **Alternating-rows review stars converted to the homepage SVG system (custom-alternating-rows.liquid + product.coming-soon.json, both folders):** the review box in `custom-alternating-rows` rendered stars from a Stars_Container PNG (image_picker); replaced with the fractional star SVG used by custom-hero-banner/custom-customer-reviews — grey `#DDD6CF` base + orange `#FB6D00` fill span clipped to `star_percent`, same star path/geometry, ids namespaced per block (`AltRowsStar-{{ block.id }}`) since the section allows multiple review boxes. Schema: `stars` image_picker + `stars_alt` text replaced by a `star_rating` range (0–5, step 0.1, default 5, 0 hides); a11y via sr-only "Rated X out of 5 stars" (added `__sr-only` clip CSS). Sizes preserved (95px desktop / 85px mobile). Template's review block updated to `star_rating: 5` (old `stars` value removed locally; on live the orphaned value is simply ignored once the code ships — the default renders 5 stars, so NO editor action needed, this one rides the code push). Section active on `product.coming-soon` only. Verified on localhost: 2 SVGs, 0 imgs, fill 100%, sr text "Rated 5.0 out of 5 stars".
- **Alternating-rows review box spacing pass (custom-alternating-rows.liquid, both folders):** per Randell, iterated live — final state: `__review` is a flex column (no gap); stars flush against the quote (removed the quote's old 5px top padding), author line keeps 10px top margin; `__stars` gets 30px left margin. (Intermediate uniform-gap version — mine at 14px, Randell's hand-tweak to 10px — superseded by the no-gap layout the same hour.) Desktop-only visual change, mobile rules untouched. Verified on localhost with screenshot; rides the code push, no live editor action.
- **Coming-soon PDP star images converted to the SVG system — rating summary + testimonial card (custom-product-coming-soon.liquid, custom-pdp-css.liquid, product.coming-soon.json, all both folders):** two more Stars_Container PNG spots replaced with the homepage fractional-star SVG. (1) `rating_summary` block (`.cw-bb-rating-image`): schema's `rating_image`/`rating_image_url`/`rating_image_alt` replaced by a TEXT `star_rating` (default "4.76" — text, not range, because 4.76 isn't a 0.1-step value; same approach as custom-hero-banner); SVGs aria-hidden since the adjacent text carries the rating. Fill verified at 95.2%. (2) `testimonial_card` block (`.mp-review-stars`): `star_image`/`star_image_url`/`star_image_alt` replaced by a `star_rating` range (0–5, 0.1, default 5) + sr-only "Rated X out of 5 stars". CSS in both places: img rules → wrapper (relative, line-height 0) + clipped fill span + svg width rules, sizes preserved (94/84px rating, 95/85px testimonial). Template blocks updated (`code_QTP4Qc` star_rating "4.76", `code_dVeTYP` star_rating 5); defaults match, so live needs NO editor action — rides the code push, orphaned old settings ignored. Verified on localhost: both spots 2 SVGs / 0 imgs. Pre-existing cosmetic bug spotted (not fixed): the rating text renders "4.76/5stars" — the `{%- -%}` trim in the rating_text markup eats the space before "stars".
- **PDP testimonial spacing matched to alternating rows + star-fill audit (custom-product-coming-soon.liquid, both folders):** per Randell — `.mp-review-box` is now a flex column with the quote's 5px top padding removed, so stars sit flush against the quote and the author line keeps its 10px separation, mirroring the final `custom-alternating-rows__review` layout. Also per Randell: audited every SVG star spot's fill against its adjacent rating — PDP rating summary 95.2% ↔ "4.76/5" text ✓; PDP testimonial and alternating-rows reviews 100% (quote-only, no rating shown → full stars) ✓ — and changed the rating summary's blank-setting fallback from '4.76' to 5 so an empty rating always renders full stars (schema default stays "4.76" for new blocks). Rides the code push, no live editor action.
- **Coming-soon CTA buttons upgraded to the homepage interactive pattern (custom-steps-horizontal / custom-icon-features / custom-alternating-rows .liquid + product.coming-soon.json, both folders):** the three plain "GET YOURS NOW →" buttons now match the hero/videos/info-banner treatment — `transition: background 0.3s`, hover + `:focus-visible` → `--BTN-PRIMARY-BG-COLOR-BRIGHTER`, `gap: 5px`, and the shared 22×13 arrow SVG that slides 5px on hover (inside `@media (hover:hover)`); literal "→" stripped from all three template button texts so no double arrows. Verified on localhost: all four page CTAs report 1 SVG / 0 literal arrows / background transition, hover swatch changes rgb(211,66,112)→rgb(204,47,97). Rides the code push; template text change needs no live editor action IF the code ships first (the SVG replaces the arrow) — strictly the live text still says "→" until the KAN-209 editor pass, where it's covered by the existing button-text step pattern.
- **Rating summary wired to the real review rating (custom-product-coming-soon.liquid, both folders):** per Randell — the `cw-bb-rating-box` on the coming-soon PDP now represents the same data as the native `product__rating` badge: `product.metafields.reviews.rating.value.rating` (Junip) drives both the SVG star fill and the bold "X/5" text (round: 1, matching the native badge). The `star_rating` and `rating_text` settings become fallbacks used only when the product has no review rating (>0); no rating at all → full stars per the earlier rule; schema info texts updated. Also fixed the pre-existing `{%- -%}` whitespace-trim bug flagged earlier — text read "4.76/5stars | 12,000+happy women", now "4.7/5 stars | 12,000+ happy women". Verified on localhost (bb-cream, variant 44969140814128): summary "4.7/5 stars | 12,000+ happy women", fill 94.6% (= 4.73/5 unrounded metafield), native badge also 4.7. Rides the code push.
- **Add-to-cart button no longer collapsed to 190px on the coming-soon PDP (custom-pdp-css.liquid, both folders):** pre-existing bug, confirmed identical on live and theme-dev — `custom-pdp-css` makes `.product__block--buttons` a flex row (for the wishlist heart beside it), which turned `<product-form>` into a shrink-to-content flex item with nothing granting it width, so the whole form collapsed to the label's natural 190px inside the 550px column. Fix: `product-form { flex: 1 1 auto; min-width: 0 }` scoped under the buttons block. Verified on localhost: 550px desktop, 358px (full column) mobile. Rides the code push.
- **custom-videos section can now host a Tolstoy playlist carousel (custom-videos.liquid + product.coming-soon.json, both folders) — BLOCKED on widget creation:** per Randell the coming-soon "What Women Are Saying" video should be the full Tolstoy "BB Creme" playlist (all videos, one carousel, existing playlist — none created). Section got a `tolstoy_publish_id` text setting: when set, it renders `<div class="tolstoy-carousel" data-priority="high" data-publish-id>` (no `data-product-id` → full playlist, same embed pattern as the episode pages incl. KAN-205's priority init) in place of the Video Slide blocks, which remain the fallback when blank; width overrides for the carousel containers added. Neither store has a BB-Creme widget yet (checked both via Tolstoy MCP: only the 8 per-guest episode widgets each) and my `create_widget` call was permission-blocked, so the template ships with the setting EMPTY (BB-cream MP4 still shows). NEEDED: a carousel widget per store sourcing the existing "BB Creme" playlist (dashboard: New widget → Carousel → source playlist "BB Creme" → live), then its publishId goes into the section setting (dev id in the repo/dev templates, live id via the KAN-209 editor pass). FOLLOW-UP same day per Randell (Tolstoy is for the PDP instance ONLY, homepage keeps its video): added an explicit "Content source" select to the section — "Video Slide blocks" (default) vs "Tolstoy playlist" — so the mode is a deliberate per-instance choice rather than implied by a non-empty ID; Tolstoy mode still falls back to the Video Slides while the publish ID is empty. Coming-soon instance set to `content_source: "tolstoy"`; homepage-style-2 instance untouched (defaults to Video Slides, verified no content_source key in its template). SAME-DAY RESOLUTION (course-corrected per Randell — the widget belongs to the LIVE Tolstoy workspace, not dev): Randell created live widget **"BB Cream"** (`089bmq19bnsc0`, carousel, live/on, sourcing the existing BB Creme playlist — 11 videos verified via API: Dawn, Helaine, Jill, Jo-Anne, Laurel, Marci, Meg, Pam, Peggy, Ruth, Vera). Its publish ID is wired into the template (both folders, verified in localhost HTML) — the dev store page renders the LIVE widget (fine for visuals; in-player product CTAs resolve against live-store records). My earlier API-created dev widget `ju3618ffu43hb` is unused — Randell can delete it in the dashboard (no API delete). CAVEAT: the live widget has `isPdpMode: true` (per-product filter) — if the coming-soon page shows fewer than all 11 playlist videos, untick "show only videos featuring the current product" on the widget (couldn't change it via API — update_widget permission-blocked). Live editor steps in KAN-209. Follow-up per Randell: the Tolstoy carousel is now FULL PAGE WIDTH — `.custom-videos__tolstoy` breaks out of the 1000px parent/page-width via `width: 100vw; margin-left: calc(50% - 50vw)`, with `overflow-x: clip` on the section guarding against scrollbar-width overflow (no global body overflow guard exists in theme.css). Verified on localhost: wrapper 1400/1400px at left 0, no horizontal scroll. Heading/subtitle/button stay at page width; Video Slides fallback unaffected.
- **Episode-page player treatment ported to the coming-soon Tolstoy embeds + BB Cream carousel added under "Watch Our Products In Action" (new snippets/custom-tolstoy-player.liquid; custom-videos.liquid, custom-product-coming-soon.liquid, product.coming-soon.json — both folders):** per Randell the player was opening bottom-right (Tolstoy compact default). Extracted the beauty_tutorial_videos.liquid treatment into a shared `custom-tolstoy-player` snippet — desktop-only centered-modal CSS (`.tolstoy-modal[class*="SmallModal"]` full-viewport dim, iframe centered + zoomed to 80vh, surfaced white close X) + backdrop-click-to-close JS (self-guarded via `window.__customTolstoyPlayerInit`, safe to render from multiple sections) — rendered from custom-videos (Tolstoy mode) and from the main section. Main section also gained a `pvideo_tolstoy_publish_id` setting: when set, the "Watch Our Products In Action" box renders the same full-playlist `tolstoy-carousel` (data-priority high) instead of the dead legacy Channelize @app loop (kept as fallback when blank); template sets it to the live BB Cream widget `089bmq19bnsc0`. VERIFICATION BLOCKED: the dev theme is serving a stale custom-product-coming-soon.liquid — the new markup never rendered despite multiple re-copies/retouches while OTHER files synced fine in the same window (custom-videos, template) → theme dev watcher wedged on this file (and one direct Edit-tool write on the dev copy happened mid-debug, the known .tmp trigger). Folders cleaned + verified identical, no .tmp files present; needs a `shopify theme dev` restart, then re-verify pvideo wrapper + player modal on the store-domain preview. Also learned: Tolstoy carousels currently do NOT initialize AT ALL on 127.0.0.1:9292 (0/15 tiles even on Jill's episode page — stronger than the known player-only localhost limitation; verify Tolstoy rendering on the dev store, never localhost).
- **"Watch Our Products In Action" centered + badge offset (custom-pdp-css.liquid, both folders):** per Randell — `.cw-bb-pvideo-title` gets `text-align: center`, plus `.cw-bb-pvideo-container { width: 100% }` restored (its old full-width came from the Channelize override CSS retired earlier today; without it the title centered inside a 409px shrink-wrapped box). `.product__badge` gets `margin-top: 15px` desktop-only (`min-width: 601px`; mobile verified 0px). Snippet is rendered only by custom-product-coming-soon, so scope is the coming-soon PDP. Verified on localhost: title spans 650/650px centered, badge 15px desktop / 0 mobile. Rides the code push.
- **Console readiness log added to the Tolstoy episode pages (beauty_tutorial_videos.liquid, both folders, pushed to dev store theme):** per Randell — a `[tolstoy-status]` line + `console.table` fires once when every video tile is ready to play (Tolstoy tile status `loaded` + preview video element present), showing per-tile product ID, Tolstoy status, preview buffering state, and readiness time in ms; 60s timeout path prints the same table with stragglers visible. Deliberately NOT gated on preview buffering — Tolstoy only buffers preview data when a tile scrolls into view, so off-screen tiles legitimately sit at readyState 0 (first draft gated on it and always timed out). "Click will open the player" remains unloggable — Tolstoy exposes no such signal (on their ticket). Verified on dev store: all 19 tiles ready at 767ms, table renders. Rides the pending live push.
- **Readiness log fix — `notFound` tiles counted as resolved (same file, both folders, pushed to dev store theme):** the log initially fired only on Carol's page; every other guest page has tiles whose product has no clip from that guest (Tolstoy marks them `notFound`, never attaches a video — Dawn 5, Jill 3, Pam 2 on dev), so "all ready" never came true there and only the 60s timeout table printed. `notFound` is now treated as terminal and the summary line reports both counts (e.g. Dawn: "all 20 tiles resolved at 512ms — 15 ready to play, 5 with no clip for the product (notFound)"). Verified on Dawn, Jill, and Carol dev pages, no console errors.
