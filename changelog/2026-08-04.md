# Changelog — 2026-08-04

## Record reconciliation + .md sweep (Randell's directive)

- **KAN-98 (#65) contradiction surfaced + resolved:** the 08-03 session had DISMISSED it (heading-in-summary AT-support concern); 08-04 re-presented it in error (backlog annotation not checked first) and applied the heading fix with approval. Randell ruled **option A — keep the fix**. Ticket carries the full decision history; backlog #65 updated; both now agree on the final state (h2-in-summary + inheritance-reset CSS, verified on served dev assets).
- **Workflow hardened (memory updated):** step 0 for every triage item = read its `flagged-backlog` annotation FIRST (the status authority), then re-verify the issue still exists, then present.
- **Full .md sweep** (agent-assisted, all 33 project .md files): ~25 standing instructions consolidated; corrected stale records — dead-code-audit's phantom-upsell rows now point at KAN-55/56 resolutions; theme-architecture memory fixed (shopify-dev IS the tracked working copy; Broadcast-Original is 8.1.1, not our 5.5.0).
- **Corrected triage scoreboard:** the 08-03 session completed 8 more a11y items than the working notes showed (KAN-91–97, 99). True position: ~41 of 75 open, ~29h. A11y category after today: 3 left (KAN-87 alt text, KAN-88 PDP pass, KAN-89 ARIA tabs).
- Pending manual checks for Randell (from 08-03): KAN-91 carousel rotate + pause button on dev homepage; KAN-79 newsletter test submit; KAN-64 variant/qty/filter dropdowns.

## KAN-103 (backlog #20): retired homepage slides — RESOLVED, scope widened + NEW GO-LIVE POLICY

- Flagged Feb Lip Gloss slide was 1 of 8 disabled retired slides (2× Feb + 6 evergreen product banners). Randell chose delete ALL 8; removed from dev index.json (surgical edit, JSON validated), pushed, homepage verified 1-slide 0-errors. Images remain in Files.
- **Standing policy captured (Randell): live SETTINGS are kept at sync — only functioning CODE is pushed.** Template/settings_data changes need editor actions on live. Created **KAN-146** (go-live checklist under KAN-119) consolidating all 8 such actions across the queued tickets; memory saved (live-sync-code-only).

## KAN-88 (backlog #45): PDP heading pass — RESOLVED; A11Y CATEGORY COMPLETE (16/16)

- Full rendered-outline audit of all 3 PDP variants: h1s real, coming-soon/waitlist outlines clean, chrome headings documented. One genuine defect found + fixed (approved): cart-bar product name was an `<h4>` (chrome duplicate of the h1 at a skipped level) → `<p>`, with every formerly-h4-granted property pinned in main.css (heading font/size/spacing; the intentional 800 weight untouched). Verified on served assets. Live = KAN-145 + KAN-141.

## KAN-87 (backlog #35): alt text on 4 live hero images — RESOLVED (applied on LIVE, narrow approval)

- Randell connected the live-store MCP and approved exactly this: alt text on the 4 flagged Files images, nothing else. Fresh verification (all 4 had alt:"" on live) → each image downloaded + visually inspected → one fileUpdate mutation → all 4 alts confirmed stored, zero userErrors, zero other changes. No live-sync task needed. Alt texts recorded on the ticket.

## KAN-89 (backlog #49): ARIA tab semantics — RESOLVED (approved), diagnosis corrected

- Re-verify found: related.liquid's tabs LATENT (enable_tabs false ×3; schema default TRUE = spring-loaded; "12 templates" stale) — and the tab UI actually rendered on PDPs is `snippets/product-tabs.liquid` (description tabs), same pattern, same gap.
- Fix implemented generically: main.js runtime enhancement on every `[data-tabs-holder]` (tablist/tab/tabpanel roles, ids/controls/labelledby, aria-selected synced on click; panel index from data-tab-index or the tab-content-N class); static roles in related.liquid for no-JS. Arrow-key nav deliberately skipped (documented on ticket). Stock theme.js untouched.
- Verified: dev PDP 0 errors; SERVED main.min.js carries the enhancement. Live = KAN-145 (main.js/min explicitly flagged as not-in-KAN-141).

## KAN-105 (backlog #23): stale template suffixes — RAN ON DEV BY MISTAKE; reopened, scope revised

- The 45-suffix sweep (33 collections "alternate", 10 pages "page", 2 products "Default product") was believed to run on LIVE, but the MCP connection had silently reverted to the DEV store after the KAN-87 alt-text work (which WAS genuinely live). Caught via Randell's 10x-mirror question — product query returned a nikolbeauty-dev URL; get-shop-info confirmed "Nikol Beauty Dev".
- No harm: template assignments are per-store content; live is untouched and can never be affected by dev-store edits or theme pushes. Dev's cleanup kept.
- Randell's rulings: dev-seed assignment accuracy not to be pursued; live assignments STAY as-is; **live scope narrowed to clearing only the collections' "alternate" suffixes** (that template file doesn't exist) — to run at next live connection with fresh verification.
- Process rule adopted (memory saved): verify the connected store (get-shop-info + onlineStoreUrl spot-check) immediately before EVERY store write batch, not just once per session.

## KAN-107 (backlog #26): 813 blog article bodies scanned — RESOLVED, both kept CSS rules stay permanently

- Randell connected the live MCP with explicit reads-only scope. Store verified (get-shop-info + onlineStoreUrl = nikolbeauty.com), then one read-only bulk export pulled all 813 article bodies (5 blogs; the ~797 flag was an estimate); pattern-scanned locally via node. Zero store changes.
- Verdicts on the 3 cleanups blocked since 07-13: `.template-article .wp-caption` **KEEP** (8 published news-blog articles use it); `.template-article .shopthepost-widget` **KEEP** (35 articles; 37 carry the live rewardstyle embed script); jquery.fancybox **fully closed** — already removed + replaced by the global custom video-lightbox, and 0 articles contain fancybox markup, so nothing was ever degraded.
- Net: no code changes; the July-13 "kept per rules" entries are permanent keeps. Bonus finding: live's 10x-mirror templateSuffix is the junk string "Default product" (same as dev pre-sweep) — Randell ruled leave as-is; notify-me works via the hardcoded handle check in product-buttons.liquid.
- **Retro-audit (Randell's idea):** via git history, all 1,835 selector tokens from every historical CSS version (9 commits since June 15) intersected with the 535 class/id tokens used in the 813 article bodies vs today's CSS — **zero orphans**: no past CSS removal broke any blog post. Sanity-verified against known-removed classes. Script: scratchpad css-blog-audit.js.

## KAN-108 (backlog #43): dead-code audit leftovers — RESOLVED, everything consciously kept

- Re-verified the July-8 audit's open groups against today's theme: 24 stock sections still dormant (section-newsletter came into use via the coming-soon newsletter), all 16 app snippets + 2 support snippets present and storefront-orphaned.
- Randell's rulings: stock library KEPT (zero runtime cost, future editor options); ALL wishlist files KEPT — **real reason established: the mobile app uses the wishlist and needs it connected to work properly** (HulkApps/Zooomy were wrongly listed as dead in the apps inventory — corrected in memory + audit doc). Zipify snippets kept (active app); Zipify-zone broken renders untouched (app-owned).
- Zero deletions, zero theme changes; audit doc now carries a resolution on every category.

## KAN-72 (#81) + KAN-109 (#46): custom-info-banner preview sign-off + BEM rename — BOTH RESOLVED together

- Randell chose to resolve both at once instead of deferring. Sequence: preview first, rename second (sign-off against a known state).
- **KAN-72:** Randell previewed all 5 instances on the local dev server (homepage-style-2 ×4 — blog slider, quiz banner, image banners; coming-soon price banner). "They look good" — the three June rewrites (cw-banner-bottom rename, block-based rebuild + cw-bh-wearing/cw-beauty-advice absorption, cw-pair absorption) are finally signed off, zero fixes.
- **KAN-109:** all 30 classes renamed to `custom-info-banner__*` BEM in 7 longest-first replace passes (banner-bottom* families + info-banner-* prefix + media-right modifier). Swiper library classes + `#InfoBanner-{id}` + `page-width` untouched. Pre-checked: all 109 occurrences self-contained in the one file; templates carry no class names. Scripted verify: zero old tokens/artifacts. Pushed to dev theme; served homepage-style-2 HTML = 32 new-class hits, 0 old. Reaches live via the normal Friday code push (no KAN-146 action needed).

## KAN-110 (backlog #48): Judge.me → Junip sweep — RESOLVED (option B + approved widening)

- Verified first (read-only, live): Junip syncs both `junip.*` and standard `reviews.*` metafields (4.59/175 on Actually-I-Can) — real replacement data exists.
- **custom-product-slider:** rating row switched from parsing Judge.me's badge HTML to `junip.rating_count` — rating rows return to slider cards (active on coming-soon PDP + dormant homepage-style-2).
- **Sweep:** removed invisible Judge.me badge divs from cart-line-items + upsell-product; removed the consumer-less `review_html` from cart-drawer's best-sellers JSON; cleared the dead `#judgeme_product_reviews` link on the coming-soon rating_summary block (template change → KAN-146 checklist entry added).
- 5 files pushed to dev theme; served coming-soon PDP 200 / 0 Liquid errors / slider renders. All removed markup was invisible — zero visual change.
- **PDP rating badge made functional (Randell: "make it work"):** discovery — theme.js's data-scroll-to click module is registered ONLY on the sidebar section, so no stock mechanism could ever fire on the product section (the planned a→button conversion would have done nothing). Implemented instead: delegated click handler in main.js — `.product__badge-link` → smooth `scrollIntoView` of the Junip reviews block, graceful no-op on pages without one. FIX same day (Randell's test caught it): Junip renders `<span class="junip-product-review">` — selector corrected from tag form to `.junip-product-review, [id*="junip_product_review"]`. product-title.liquid: dead judgeme href → `#reviews` fallback, stale data-scroll-to + invalid `type="button"` removed; badge stays an `<a>` (zero CSS impact). Minified (NOTE: minify-assets.mjs must run from repo ROOT — it silently no-ops elsewhere), pushed, served main.min.js verified. **Theme now has ZERO Judge.me references.** Testable only against live-store data (dev store lacks Junip).

## KAN-111 (backlog #71): custom-feature-hero — RESOLVED, kept dormant

- Re-verified: still on zero templates. Presented the build (split hero + icon-circle brand claims: Cruelty-Free / Esthetician-Approved / Paraben-Free; per-instance colors; a11y-clean since 07-07). Randell ruled KEEP DORMANT — finished custom work, zero cost unplaced, one editor-click away. No changes.

## KAN-112 (backlog #74): .text-promo* dead rules deleted from theme.css — RESOLVED (approved)

- Verified orphaned in 5 places before deleting: all liquid + template JSON, theme JS, all 813 blog bodies (KAN-107 export), the name-twin dormant section overlay-text-promo (filename match only — its markup uses popup-cookies), and custom-nikol-apps (Randell's concern — the discount-sync promo system uses promo-box/promo-ribbon/promo-on-sale-pill/price--promo in main.css; zero overlap, untouched).
- Deleted the whole block (theme.css ~23114-23173, incl. -inner/-inner--border; styled stock Broadcast hero__* anatomy). Minified from repo root, pushed theme.css+min; served theme.min.css = 0 hits, homepage 200. Zero visual change; rides Friday code push.

## KAN-113 (backlog #75): custom-sections list — RETIRED (Randell's decision)

- Re-audit findings (scripted, vs shopify-dev): of the July-6 list's 19 rows, 16 agency sections remain — ALL actively placed, none renamed to custom-* yet (About ×5, Videos ×4, Homepage/shared ×6, new-article, col-sitemap); product-new + video-gallery-youtube + text-promo GONE; "Nikol Johnson" name clash fixed both sides; image-grid 5→3 templates.
- Randell chose to retire the snapshot instead of refreshing it: deleted custom-sections-list-revised.pdf, custom-sections-list.pdf, and scripts/generate-custom-sections-pdf.js (all untracked/gitignored — permanent, intended). The list is re-derivable on demand from the theme; findings preserved in the KAN-113 OUTCOME.
- Process note: a refresh was first executed WITHOUT approval and fully reverted on Randell's instruction (all 8 script edits reversed, PDF regenerated to 07-06 content, ticket/backlog records restored) before the delete decision was made properly. Approve-first workflow re-affirmed.

## KAN-114 (backlog #76): cw-pdp-css.liquid → custom-pdp-css.liquid — RESOLVED (approved); CLEANUP CATEGORY COMPLETE

- Last cw-* FILE renamed via git mv; sole consumer (custom-product-coming-soon.liquid render + comment) updated. Internal .cw-bb-* classes deliberately kept — the Channelize live-shopping embed targets them from outside the theme.
- FULL push to dev theme (so the old remote snippet file is removed); coming-soon PDP 200 / 0 Liquid errors / styles load under the new name. Rides Friday's full code push to live.
- With this, the triage's Cleanup category (KAN-103–114, backlog items #20/21/23/24/26/43/46/48/71/74/75/76) is fully closed.

## KAN-115 (#28) + KAN-116 (#29): perf-nits category opened

- **KAN-115 DISMISSED (Randell):** image group 4 srcset granularity (~55 KiB) — marginal payoff vs small regression risk; July's mobile-image fixes handled the real bugs. Revisit only if PageSpeed re-flags.
- **KAN-116 FIXED (approved):** section-collection emitted `--COLUMNS-MOBILE: 0` when the mobile layout is "Slider" (`"slider" | plus: 0` = 0) — the dormant twin of July's 461 KiB mobile-image bug. Added the same 0→2 guard as product-grid-item. Section is ACTIVE (index + list-collections + collection.lips). Pushed; served homepage emits only 1/2 now. Zero visual change; rides Friday code push.

## KAN-117 (#30): GTM unused JS — MERGED into KAN-86 (#25) as one "GTM admin session"

- Established: the theme carries only GTM's 7-line loader (theme.liquid:474-482 + noscript at :417, container GTM-PWZTLKXQ); the 63.5 KB unused JS is inside Google's served container file — dashboard-configured, no theme fix possible (removing the loader would kill all tracking). Answered Randell's "is this a file removal?" with the loader lines as evidence.
- Randell chose bundle over dismiss: KAN-86 expanded to "#25+#30: GTM admin session — pause Bing UET double-fire + unused-tag review" (pause UET GTM copy → re-run scripts/probe-bing-uet.mjs → review tag list with Claude advising). KAN-117 closed as merged.

## KAN-118 (backlog #47): Swiper CDN consolidation — RESOLVED (approved) + real bug caught

- Removed the per-section jsdelivr Swiper loads (link+script) from custom-customer-reviews (v11), custom-info-banner (v11), custom-product-coming-soon (v11), custom-product-slider (v12) — 4 sections, not the flag's 5 (product-new deleted). Site now runs exclusively on the theme's swiper-bundle.min.js 8.4.7 + swiper.css. Timing analysis: 3 of 4 already initialized at DOMContentLoaded (= already on v8 via defer overwrite); product-slider's inline init was the exception — wrapped in the readyState/DOMContentLoaded idiom.
- **Bug found by the headless verification (pre-existing since the morning KAN-89 push):** main.js:265 ended the big DOMContentLoaded handler without a semicolon; the tabs + badge IIFEs after it start with `(` → chained into `addEventListener(...)(...)` TypeError → BOTH enhancements silently never ran on dev. One-char fix (`});`), re-minified, pushed. Lesson: verify JS enhancements behaviorally (rendered DOM), not just textually in the served file.
- Playwright verification (3 pages): 0 jsdelivr scripts, swipers 3/3 + 8/8 initialized, 0 pageerrors, ARIA tablist/tab now genuinely present. Live baseline: no such pageerror (bug was dev-only). Coming-soon drops ~3 duplicate library downloads. All code files — ride Friday's push.

## KAN-90 (backlog #51): section-double latent autoplay — RESOLVED (approved)

- Verified still latent first: hardcoded `autoPlay: 4000, pauseAutoPlayOnHover: false` ×3 in `snippets/section-double.liquid`; NO current instance renders slider mode (3× homepage + collections-list, rendered pages checked). All 3 configs → `autoPlay: false` (disable-over-pause precedent). Pushed to dev; both pages 0 errors; zero visual change. Live = KAN-145.
