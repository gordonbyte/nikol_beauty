# 2026-08-31

- **(logged 09-01) Tolstoy player NEVER opens on localhost — environmental, not a theme bug (no code change):** Randell's "video randomly doesn't open, especially bottom cells after load" reproduced as a hard rule, not randomness: on http://127.0.0.1:9292 every cell at every delay (0.8s→12s, cells 0/5/9/14/16/17/18) is dead, while the identical template on the dev store opens first-click top AND bottom. Mechanism: the player iframe's src carries the host origin (`url=http://127.0.0.1:9292/...`) and after a click ZERO postMessages return from play.gotolstoy.com — their player refuses the handshake with an unregistered localhost origin. The perceived randomness = which tab (localhost vs dev store) he happened to be testing in. Standing guidance: localhost is valid for layout/posters/autoplay (our script drives those); ALL player interactions (open, add-to-cart) must be tested on the dev store or a real-domain theme. Optionally ask Tolstoy support to whitelist a localhost origin (bundle with the watermark + arrow-scroll ticket).

- **Tolstoy mobile add-to-cart fixed — the "official" cart hook never existed (template, both folders, pushed to dev store theme #149231566950):** Randell reported mobile adds leaving the player open, the drawer closed, and the badge stale until refresh. Root cause from decoding Tolstoy's minified widget: `tolstoyWidget.subscribe` validates against a 3-event whitelist (`tolstoy_add_to_cart`, quick-shop click, product-card click) — our subscribed `tolstoy_add_to_cart_success` is an INTERNAL message name and was rejected with a console error on every page load, so the bridge only ever worked via the `window.fetch` fallback, which the mobile fullscreen feed player bypasses. Fix: primary hook re-pointed to the valid `tolstoy_add_to_cart` (fires on tap, all devices) + a cart-count poll (500ms × 12) that waits for the add to land before firing the UI updates (bows out if the fetch fallback got there first — debounce preserved); player-close hardened for the mobile feed modal (closePlayer + click the visible modal's own `.minimize-tolstoy-modal` at +600ms). Bonus: kills the "Event ... is not a valid event" console error sitewide on episode pages. Verified on the dev store under iPhone emulation: SDK logs "Subscribed to event tolstoy_add_to_cart", synthetic add → badge 0→1 + drawer rebuild + drawer-open event. Real-tap E2E = Randell on his phone (synthetic taps rejected by their player). Rides the pending KAN-205 live push.

- **Discount Sync app: per-discount "On Sale" pill corner radius (new styling control):** the style modal gains an "On Sale pill corner radius (px)" field (0–999, blank = theme default fully-rounded) with a live pill preview next to the badge preview. Plumbing mirrors the existing styling fields end-to-end: `TrackedDiscount.pillRadius` column (migration `add_pill_radius`, applied to dev.sqlite), server-side `normRadius` validation (whole px only), sync writer emits product metafield `custom.promo_pill_radius` for the winning discount and clears it on the same delete path as the other style metafields. THEME-CONTRACT.md updated.
- **Theme: pill radius consumer (3 snippets, BOTH folders — edited in shopify-live, Copy-Item'd into shopify-dev to dodge the theme-dev .tmp wedge):** `snippets/product-price.liquid`, `snippets/cart-bar.liquid`, `snippets/product-grid-item.liquid` each assign `promo_pill_radius` from the metafield and append `border-radius: {{ n }}px` to the `.promo-on-sale-pill` inline style only when set — no metafield = untouched CSS default (999px pill). NOT on the live theme yet; rides Randell's next push (these 3 snippets join the pending payload alongside the Tolstoy metaobject template). Active status: product-price + cart-bar render on every live PDP; product-grid-item on all collection/grid cards — but the pill only shows while a synced discount is live AND `promo_badge_enable` is on.
- **Discount Sync dashboard: saved pill radius visible in the discount list (2 rounds per Randell):** first built as a separate mini "On Sale" pill + "2px" caption next to the badge chip ("Badge & pill" column); Randell preferred no extra element — final version applies the saved radius directly to the EXISTING badge chip's own corners (radius set = chip drawn with it; unset = original 6px), column stays "Badge". The set radius is visible at a glance without opening the style modal.
- **Prisma client regen pending (RESOLVED same day):** the running `shopify app dev` (started 11:13) holds the query-engine DLL, so `prisma generate` couldn't complete — Randell restarts the app dev (predev hook regenerates automatically). Until then, saving styles in the app would error on the new field.
- **shopify-live/ re-baselined to LIVE settings (per Randell, pre-push):** pulled the live theme's settings files into the folder so a full push cannot change live settings. Only 2 of 35 settings JSONs actually differed — `templates/product.json` (live editor state; upsell-free, code block intact) and `templates/product.coming-soon.json` — both refreshed from live; `settings_data.json`/`markets.json` proved already byte-identical (Shopify's API checksumMd5 normalizes JSON, which had made them look stale). Double-pull verified byte-stable. shopify-dev/ deliberately NOT equalized (holds the dev-store Labor Day hero staging + dev settings). ⚠️ Mirror is point-in-time: re-pull before pushing if any live editor edits happen first (e.g., Wednesday's hero publish).
- **Dev store: Labor Day test discount created** (Randell-authorized session work, logged for context): automatic "Labor Day Sale 20% (dev test)", 20% off all products, now → Sep 7 11:59 PM PT, on nikolbeauty-dev-mab1srre — the intended test target for the pill-radius + sync round-trip.
