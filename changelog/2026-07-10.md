# Changelog — 2026-07-10

## Collection-rules parity check: live vs dev (verified identical — no fix needed)
- User reported conditions visible on live 'Bestselling Makeup' but seemingly absent on dev. Ran bulk exports of all 131 collections (id/handle/title/sortOrder/ruleSet) on BOTH stores (read-only on live; user-approved connector switches) and diffed them with a normalized comparator.
- Result: **perfect parity** — 105 smart + 26 custom on both; all 105 smart rule sets identical (columns, relations, conditions, AND/OR mode); all sort orders identical; zero type mismatches; zero missing handles. 'Bestselling Makeup' (best-selling-products) is byte-identical incl. its tautology rules (TITLE contains OR not-contains 'Best Selling' = matches all) + BEST_SELLING sort.
- Likely cause of the confusion: near-duplicate collection titles — 'Best Sellers' (best-sellers, 1 condition: price > 0) vs 'Bestselling Makeup' (best-selling-products, 2 conditions); dev (and live) also have two 'Beauty Tools', two 'Lashes', two 'Lipstick For Gray Hair'.
- Exports kept in session scratchpad (live-collections.jsonl / dev-collections.jsonl). Connector left on the LIVE store at end of this check.

## Best-seller seeding on dev (user-approved approach: synthetic orders, zero customer PII)
- Goal: make dev's BEST_SELLING collection sorts mirror live without importing orders/customers. Captured live's true ranking read-only via collection products(sortKey: BEST_SELLING) on best-sellers — 69 rankable products (gift card excluded: absent on dev; ™-brush handle normalized).
- On dev: orderCreate is blocked for the connector (offline-token-only mutation), so used draft orders: created 5 drafts tagged bestseller-seed (17+17+17+17+1 line items, NO customer attached, notes marked 'safe to delete'), completed all 5 as PAID → orders #1001–#1005. Quantities rank-weighted 138 down to 2 in steps of 2.
- Inventory fully restored: completion decremented stock, so ran inventoryAdjustQuantities (+4,830 units across all 69 tracked items at the single dev location) — net inventory change zero.
- Note: orders NOT flagged test:true on purpose — test orders are excluded from sales-derived sorting.
- Status at end of session: sort not yet recomputed (Shopify rebuilds best-selling rankings asynchronously; expect minutes–hours). Verify later: dev best-sellers top 5 should become waterproof-eyeliner, eye-primer, volume-up-mascara, just-peachy, bb-cream.
- Cleanup path if ever needed: delete/cancel the 5 orders tagged bestseller-seed (ranking then reverts).

## PageSpeed fixes (user-approved, applied to both theme folders; dev store only until pushed)
- **Intelligems CORS error killed on dev:** theme.liquid had a HARDCODED Intelligems loader (line ~484, cdn.intelligems.io bundle) + the window.igProductData data block (lines ~458–482) — separate from the (disabled) app embed, so it loads on every page regardless. On dev the CDN rejects the domain → CORS + ERR_FAILED in PSI. Fix: wrapped both in {% if shop.permanent_domain == 'nikolbeauty.myshopify.com' %} — live behavior unchanged whenever pushed; dev no longer requests it. (User chose guard over removal to preserve live A/B wiring.)
- **6 aspect-ratio warnings fixed (homepage category tiles):** .highlights__item__background builds its square via padding-bottom:100% + 1px border → 305×307 box; images had no object-fit so they stretched 2px (0.99 ratio) → Lighthouse flagged all 6 highlight tiles. Fix: added .highlights__item .image-wrapper img { object-fit: cover; } next to the existing highlights rules in theme.css. Same 2px now crops instead of stretching; visually identical.
- Verified: both files byte-identical across shopify-live/ and shopify-dev/; theme.liquid if/endif 30/30 balanced.
- **Not actionable (explained to user, no changes):** otlp metrics/logs ERR_BLOCKED_BY_CLIENT = auditing browser's ad blocker; monorail-edge 400 + shop.app/pay/hop 429 = Shopify-hosted internals, not theme-controllable.
- Section note: section-highlights is ACTIVE on the live homepage template (index.json, 'shop by category' tiles); the same distortion exists on the live storefront until these files are pushed live (user decision, not taken).

## PageSpeed SEO fixes round 2 (user-approved per item)
- **Meet Nikol 'Read More' link (index.json):** cta_link was hardcoded to https://nikolbeauty.myshopify.com/pages/about — on dev it navigated visitors to the LIVE store. Changed to relative /pages/about (both folders). User chose link-only; button text stays 'Read More' (descriptive-link-text audit intentionally left flagged).
- **Hero image alt:** set alt on the dev store's bbposter.webp file via fileUpdate — 'Nikol Johnson holding a tube of Nikol Beauty BB Crème' (theme correctly uses image.alt; the file had none). NOTE: the live store's copy of bbposter.webp presumably also lacks alt — candidate for the same fix on live when approved.
- **Lazy-load placeholder (snippets/image-hero.liquid:153):** added alt="" to the blank.svg placeholder img — genuinely decorative, per the functional-alt rule's empty-alt exception. Both folders; verified byte-identical.
- **Wishlist icon crawlability: SKIPPED per user** — active desktop icon keeps href=javascript:void(0) (audit stays flagged); wishlist keep-rule untouched.
- **Meta description: manual step for user** — dev store has empty homepage meta description + shop description (store setting, no API). Live's text captured for pasting into dev admin → Online Store → Preferences: 'Shop professional beauty products by Nikol Johnson, Licensed Esthetician with 16+ years experience. High-performance mascara, skincare & makeup loved by 1.1M+ subscribers.'
# Theme changelog — 2026-07-10

## image-hero alt attribute fix (Lighthouse: "Image elements do not have [alt] attributes")
- `snippets/image-hero.liquid` line 38: alt fallback chain extended — passed `alt` → `image.alt` → `image_desktop.alt` → `image_mobile.alt` → `''` — so the rendered `<img>` always carries an alt attribute even when the file has no alt text in admin (previously `image_tag` dropped the attribute entirely).
- One snippet fix covers all callers: section-hero (homepage heroes — the reported bbposter.webp case), section-slideshow, section-double, video, section-countdown-timer, section-map, featured-posts, brick-image, collection-image-with-title, password.
- **Functional-alt follow-up for Randell (content side):** add real alt text in Admin → Content → Files on both stores for the homepage hero images: bb_nikol_web_slide_1.webp, bbposter.webp, Nikol_holiday_group_on_white_GLITTER.jpg, on_left_lipsticks.jpg, Untitled_design_1.jpg — values flow through automatically once set.
- Synced to shopify-dev. Verify by re-running Lighthouse on the dev homepage after pushing.

## Wishlist header icon: crawlable link + accessible name (Lighthouse SEO "Links are not crawlable")
- `sections/header.liquid` (both header variants, lines ~483/534): `href="javascript:void(0)"` → `href="/pages/wishlist"` + `aria-label="Wishlist"` on the SE Wishlist trigger anchors. Popup behavior unchanged (app JS handles the click); crawlers now see the real wishlist page, and the icon gracefully falls back to it if the app script fails. aria-label fixes the accessible name (was just the count "0").
- Verify on dev after push: click the heart icon — popup should open with no navigation. If it navigates instead, fall back to converting the anchors to buttons.
- Synced to shopify-dev.

## Meet Nikol link text (Lighthouse SEO "Links do not have descriptive text")
- Homepage "Meet Nikol" section CTA: "Read More" → **"Read More About Nikol"** (user-chosen wording) — `templates/index.json` saved setting + `sections/meet-nikol.liquid` schema default. Only instance of the section is the live homepage.
- Synced both files to shopify-dev.

## Heading order fixes (Lighthouse a11y "Heading elements are not in a sequentially-descending order")
- `sections/yt-videos-list.liquid`: block slider titles ("FEATURED PRODUCTS") `<h5>` → `<h2 class="h5">` — semantically a section-level title following Meet Nikol's h2 (no level skip), visually identical via Broadcast's `.h5` utility class.
- `sections/review-swiper-card.liquid`: reviewer names inside the `custom.product_review` rich-text metafields are stored as Heading-6 nodes (9 of 10 reviews, e.g. "Antoinette Schue-Hoy"); output now post-processed — `metafield_tag | replace '<h6…' → '<p class="review-author">…'` — names render identically (italic kept) but as paragraphs, not headings. Fixes all current + future reviews on both stores without editing metafield values; becomes a no-op if the rich text is ever reformatted properly in admin.
- Both sections are live on the homepage. Synced to shopify-dev.
