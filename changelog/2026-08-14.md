# 2026-08-14 (Friday)

- **Baseline reset: dev folder = current live (Randell-requested).** Pulled current settings from the live theme #187275378992 into shopify-live (store reads only — captured his launch-night editor edit: Fierce Aging hero slides desktop+mobile in index.json, plus live-normalized copies of the imported sale settings; settings_data unchanged since 8/12). Then mirrored shopify-live → shopify-dev (8 files updated: settings_data, theme.liquid, group-header/overlay, index/product/coming-soon/app-ost templates; 0 deletions). Verified **374/374 files byte-identical** across both folders and matching the live theme. NOTE: the dev STORE theme (#186425934130) now lags the dev folder on these settings — push before storefront-testing settings-dependent work. KAN-32 re-dated to today (Randell).
- **H1 fixes round 1 SHIPPED to dev (KAN-167):** `sections/section-404.liquid` — 404 heading h2→`<h1 class="page__heading h2">` (.h2 utility keeps exact size; .page__heading only centers); `sections/video-gallery.liquid` — "Discover Nikol's Favorite Products…" h1→h2, **#FIERCEAGING Series is now the page's single h1 (Randell's pick)**; class-based styling = zero visual change. Pushed to dev theme #186425934130, storefront-verified: 404 h1 ✓, gallery exactly 1 h1 ✓. Both sections active (404 template + /pages/video-gallery-youtube).
- **About page single-h1 SHIPPED to dev (KAN-167, Randell: "Meet Nikol" only):** the 2 h1s were the SAME title rendered twice (mobile-only above copy + desktop-only below). `sections/about-header.liquid` reduced to ONE `<h1 class="about-header__title">` — safe because desktop positions it absolutely (DOM order irrelevant) and mobile flows it exactly where the mobile copy sat; mobile-only/desktop-only verified as pure display toggles. Pushed to dev theme, storefront-verified: exactly 1 h1. Section active on live /pages/about; rides next live push. Randell to eyeball dev about page desktop+mobile.
- **Bot emails read in full (Microsoft 365 re-authed):** "SEO/AEO Migration Doc: 6 Critical Fixes" — all 6 prescriptions rejected as written (would have ADDED duplicate schema/OG tags, truncation logic; references Debut-era file names; its code blocks literally empty); "Google Indexing + Video Sitemap" — the credible one (real GSC data via InsightfulPipe). Own crawler got Cloudflare-challenged mid-sweep today = live proof of the scanner's failure mode (evidence for the KAN-164 reply to Raymond).
- **Video sitemap DECIDED NOT TO DO (Randell):** KAN-170-173 closed with the decision record. Verified first: no video sitemap exists (6 sub-sitemaps scanned, 0 video tags; robots.txt clean; common paths 404) BUT the 34 video metaobject pages are already crawled via sitemap_metaobject_pages_1.xml. Rationale: videos are YouTube-hosted (own channel), already in Google's video index; upside was a modest CTR play. The email's real GSC data preserved in KAN-170 outcome (~200 clicks/day, position 8.9, CTR 1.3%, products <20 organic clicks/mo).
- **KAN-32 CLOSED (About 404 images):** originals lost everywhere (both theme folders, Broadcast-Original, live Files CDN 404) → removed the two dead background-image rules from about-header.liquid (bg color kept — zero visual change; visitors only ever saw the color). Pushed to dev theme + storefront-verified. Last genuine theme-fixable console error from the July sweep is gone (on dev; live via KAN-193).
- **Live Sync task KAN-193 created** (KAN-119, live-sync): the 3-file batch — section-404.liquid, video-gallery.liquid, about-header.liquid (h1 fixes + 404-image removal). Grows if homepage/videos h1s ship today. (createJiraIssue rendered \n literally again — fixed via editJiraIssue re-edit; quirk seems to hit createJiraIssue descriptions containing escaped quotes.)
- **KAN-168 + KAN-169 CLOSED (claims disproven):** OG/Twitter/canonicals present on all 12 verified pages (168 = nothing to do); alt-text PASS — 0 missing alts, all empty-alts legitimately decorative (arrows/play icons/verified-buyer check); ONE content note: Untitled_design_13.webp (homepage brick) needs alt set in admin.
- **Title census (KAN-166) → PARKED by Randell (Low, no due date):** browser sweep beat Cloudflare for 122/221 pages → 12 confirmed >60ch (11 agency SEO collections + 1 product; 99 long-tail pages unverifiable). Live ShopifyQL (read-only, store verified): the 12 = ~2.9% of 30d traffic; only /collections/eyes matters (995 sessions, ~#12 sitewide). Decision deferred pending traffic review; GSC connector available for CTR-grade data. Site traffic shape recorded: homepage 31%, top-11 pages >50%.
- **Matrixify import unblocked on app-dev store** (store verified -mab1srre before writes): his Export_2026-08-14_132412.xlsx (Products/Collections/Pages/Metaobjects/Menus/Translations) failed on missing catalogs [International, United States, canada]. Created: International MARKET (15 representative countries) + catalog "International" (+USD price list), catalog "United States" (+USD price list) on the US market, renamed "Catalog for Canada"→"canada" (CAD price list existed). ⚠ The export contains customer emails (old restock-notify metafield JSON) — keep out of git like the subscription CSVs.
- **Matrixify import gap-fill COMPLETED on app-dev store** (audit vs Export Summary: Products 70/70 ✓, Pages 30≥29 ✓; gaps filled via Admin API): **(1) Metaobjects** — created 2 custom definitions (`beauty_tutorial_videos` w/ publishable + onlineStore page capability at /beauty-tutorial-videos, field types inferred from theme template + Matrixify formats: products=list.variant_reference, featured_bundle=product_reference, thumbnail/video_url=url, upload_date=date; `app_home_section` w/ json config + integer position) + **all 39 entries** (34 videos w/ resolved variant/product gids + 5 app sections) — verified by listing; 24 `shopify--*` taxonomy defs NOT creatable (Shopify-managed, auto-generate from product categories). **(2) Collection** `excluding-gift-cards-bundles` created + 61 members (exported rules were empty — recreated as manual collection; note: created on live only this morning 07:11). **(3) Menus** footer-menu1 (5 items; SHOP_POLICY items → /policies/* links) + apr-2026-including-youtube (42 items, 3 levels; YouTube item → HTTP /blogs/youtube-videos since dev lacks that blog). **(4) Translations sheet = originals only** (live has just EN) — nothing to import. Store identity verified (-mab1srre) before all writes.
- **App-dev homepage broken images fixed:** theme settings referenced `shopify://shop_images/*` files living only in LIVE's Files library (Files never travel with themes/products). Audit of the 8 references: 6 existed (incl. Randell's fresh sale-hero uploads); missing = **logo.png** (why the logo showed as text) + **N_Fav__2x_1.jpg**. Both copied to the app-dev store via fileCreate from the live CDN URLs (same filenames → references resolve); both READY. Theme-dev store homepage audited separately: 67/67 images loading fine.
- **App-dev logo root-caused + fixed:** the header's `logo` setting (and `main_menu_linklist`) had been SILENTLY DROPPED by Shopify at theme upload because the referenced file/menu didn't exist in the store then — uploading logo.png alone couldn't help (the setting was gone, and the header logo is actually Nikol_logo.png). Fix: fileCreate Nikol_logo.png from live CDN → re-added `logo: shopify://shop_images/Nikol_logo.png` + `main_menu_linklist: apr-2026-including-youtube` (the menu created earlier today) to group-header.json → pushed → re-pulled to verify both stored server-side. Lesson: Shopify drops invalid file/menu refs on theme upload; upload the files FIRST, then the settings.
- **App-dev homepage FULL image restore:** systematic diff of live index.json vs app-dev's found **29 dropped image references across 13 sections** (hero image_1/mobile, custom-hero-banner bg/star/USP icons, customer-quote banner, highlights, meet-nikol, video covers, section-doubles, image-grid, disabled heroes) — all victims of Shopify's silent drop-invalid-refs-at-upload behavior. 26 missing files copied from live CDN via one fileCreate (lipstick.jpg already existed); all 29 settings restored into index.json; pushed; **re-pull verified 29/29 survived** (hero image_1=bb_nikol_web_slide_1.webp ✓). App-dev homepage should now match live.
- **App-dev image restore round 2 (the .webp gotcha):** the first template push landed while files were still PROCESSING and — worse — fileCreate-by-URL stored the .webp files under CONVERTED names (Shopify saved the CDN's actual served bytes: bb_nikol_web_slide_1.webp→.jpg, bbposter→.jpg, imgtag→.jpg, VideoFile→.jpg, Untitled_design_13→.jpg, Frame_2085670276_2→.png), so 6 refs pointed at filenames that don't exist → "Liquid error: invalid url input" on meet-nikol/yt-video + settings degraded to raw strings (also temporarily broke the working slideshow). Fix: rewrote the 6 refs to the actual stored filenames, re-pushed index.json + group-header.json with ALL files READY; re-pull verified 10/10 spot-checks + logo ref stored. Lessons: (1) fileCreate-by-URL can change extensions — always re-query actual stored filenames before writing refs; (2) never push templates before fileStatus=READY.
- **The real story of the "broken homepage": Randell views via `shopify theme dev` (local folder + Development theme), NOT the published theme** — my published-theme fixes were invisible to him; local settings = yesterday's live mirror referencing live-only filenames. Published-theme template rolled back to his pre-intervention state (29 added refs removed) at his request. For the LOCAL view: uploaded the live hero PNGs (Fierce_Aging_Day_png + mobile — names live's index.json uses) + **staged-upload fix for the 6 .webp files**: downloaded true WebP bytes from live CDN (Accept: image/webp, magic-bytes verified), stagedUploadsCreate → POST to GCS (6×201) → fileCreate with forced filenames — all 6 stored as exact .webp names, READY (bypasses the fetch-by-URL extension-conversion gotcha). Store now has every filename the local settings reference. Randell to restart dev session. Orphan .jpg/.png conversions from round 1 remain in Files (harmless; deletable on request).
- **Full shopify-dev folder PUSHED to the app-dev published theme (#149231566950, Randell-requested):** the store's published theme now equals the local blueprint (live-mirrored code + settings), with every referenced image existing in the store's Files. Pull-back verification: 10/10 real index.json refs present (incl. all 6 .webp names + live hero PNGs), header logo + apr-2026 main menu intact; N_Fav__2x_1.jpg confirmed a settings_data ref (not index) and its file exists. App-dev store is now fully consistent: local dev session AND published theme render the same, complete homepage.
- **Read-more chain CLOSED end-to-end (KAN-131, KAN-132, parent KAN-121):** Randell manually confirmed the toggle on live; headless sweep verified 14/14 collections render the native `<details>` disclosure with zero legacy dead-click markup. KAN-131's files had ridden the 8/12 push (no dedicated push needed). This fully resolves the original KAN-42 Clarity dead-click bug on live — the July 30 dev work is now live and verified.
- **Date correction:** this session spanned Thu 8/13 afternoon → Fri 8/14; the changelog was initially filed all under 8/13 and split into the two dated files after Randell flagged it. KAN-165/167 due dates = today 8/14 (homepage h1 + videos-page h1 remain).
