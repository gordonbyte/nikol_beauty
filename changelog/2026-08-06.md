# 2026-08-06

## Jira board updates (morning planning, per Randell)
- KAN-21 (email flows) and KAN-22 (analytics audit) rescheduled to 2026-08-18 — pushed until after the Fierce Aging sale window (Aug 12–16), dated comments added.
- KAN-34 (Fierce Aging: decide sale colors) → **Done**, 4 days early. OUTCOME recorded: sale color is `#ff59b9` (hot pink) and the sale design is ready — unblocks banner (KAN-13), popup (KAN-35), mobile banner (KAN-45).
- KAN-7 (Promo: National Fierce Aging Day, Aug 12–16) → **In Progress**; comment records the color + design status and the escalation to Highest from ~8/9.
- **KAN-153 created** (epic KAN-4, High, `perf`, due today, **In Progress**): run every main-template page through Google PageSpeed and improve each score — Randell's fresh performance review (supersedes the July comparison, KAN-63). Cross-link comment added on KAN-30 (standing PageSpeed project). Timing note baked into the task: this week's dev perf work isn't live until the 08-07 push; re-measure after.
- KAN-143 (collection sort dropdown mispositioned slide-in) → **In Progress** — first work item today, followed by KAN-153.

## KAN-143 — collection sort dropdown (dev theme, pushed to #186425934130)
- Root cause (confirmed by Playwright measurement on live /collections/eyes): `.collection__nav` had `overflow: hidden` + hand-added `padding-bottom: 50px` — the downward-opening sort list was clipped to one option's height; a separate custom width 0→200px animation made it slide in from the right.
- `theme.css` (~18288): removed `overflow: hidden` from `.collection__nav`. Dropdown now shows all 9 options (verified on dev).
- `main.css` (end of file): **TRIAL block** restoring the stock Broadcast boxed-toolbar look, scoped to the collection toolbar only — toolbar background, top/bottom hairlines, standard hairline color on the button borders (replaces beige-on-transparent, which caused the orphaned vertical border bleed). Delimited `/* KAN-143 TRIAL ... END KAN-143 TRIAL */`; delete the block to revert to the previous minimal look. `padding-bottom: 0` inside the trial (the 50px band would look wrong inside a boxed bar).
- Ran `node scripts/minify-assets.mjs` from root; pushed theme.css/main.css + .min copies to the dev theme.
- Still open: Randell's verdict on stock vs border-removal (B1); slide-in width animation unchanged (not yet approved); re-sort-on-select check pending.
- Round 2 (per Randell — no sticky toolbar, fix spacing at the root, add header border):
  - `theme.css`: removed the `.has-header-sticky .collection__nav` top-offset rule and the orphaned `--border-width` rules — with `position: relative` the offset had been shoving the bar 93px down over the grid at all times (the real bug behind the old 50px padding hack). Also removed the ` padding-bottom: 50px;` hack itself. Toolbar now sits in its natural slot, 32px clear gap to the first product (verified: nav bottom 736 = grid top 736, first item 768, overlap 0). Toolbar deliberately NOT sticky (Randell's choice; stock sticky behavior declined).
  - `main.css`: added `.theme__header { border-bottom: 1px solid #eee; }` (site-wide header separator, per Randell). Removed the now-redundant `padding-bottom: 0` from the KAN-143 trial block (trial block is now purely the boxed-toolbar look).
  - Minified from root, pushed theme.css/main.css + .min copies to dev theme #186425934130; verified via Playwright (computed header border = 1px solid rgb(238,238,238); toolbar scrolls away with page as intended).
- Round 3 (per Randell — sidebar border, title/description spacing):
  - `main.css`: `.collection__sidebar__slider { border-right-width: 0 }` — removed the vertical hairline between the desktop filter sidebar and the grid (stock had right+bottom borders; bottom stays).
  - `main.css`: `.collection-image-with-title .hero__content { padding: 24px top/bottom }` + heading `margin-bottom: 0` when last child — uniform title strip (was 20px above / 16px below via the h4's margin).
  - Discovered blocker: `templates/collection.json` hero section carried theme-editor **Custom CSS** `.hero__content.hero__content--transparent {padding-top: 20px !important; padding-bottom: 0px !important;}` — section-ID-scoped + !important, silently defeating any stylesheet rule. Removed from the dev template (approved path: kill the hack, keep the clean main.css rule).
  - `templates/collection.json` description section (section-rich-text rendering collection.description): section padding_top/padding_bottom 0→24, text block padding_bottom 10→0 — same 24px rhythm as the title strip.
  - Verified on dev /collections/eyes: title band 24/24, description band 24/24, sidebar right border gone.
  - LIVE replication = theme-editor steps (template settings are live-owned, file must NOT be pushed) — exact steps commented on KAN-146 go-live checklist.
- Mega menu text bump (per Randell, option 3 of presented sizes): `main.css` desktop-scoped — column headings (`.navlink--child > .navtext`) 13px→15px, item links (`.navlink--grandchild > .navtext`) 15px→18px Cormorant. Mobile menu + top-level nav row (11px) untouched. Verified on dev homepage hover.
- Filter group titles (per Randell): `main.css` `.filter-group__heading { font-size: 14px }` — "Availability / Price / Product type…" headings in the collection filter sidebar were inheriting 13.2px; option labels (12px) unchanged. Verified on dev.
- Toolbar button labels (per Randell): existing `main.css` rule `.collection .popout__toggle span:not(.filter-count)` 11px→14px — "Show Filters" / "Hide Filters" / "Sort by" all verified at 14px on dev.
- Collection title strip background (per Randell): `main.css` `.collection-image-with-title .hero__content--transparent { background-color: #fff }` — band was transparent, showing the off-white body (rgb 252,251,249) between the white image band and white description band ("choppy"). Now matches the description's #fff; verified computed rgb(255,255,255) on dev.
- Uniform hairline borders (per Randell): title strip got `border-bottom: 1px solid var(--hairline)`; the header separator switched from the hardcoded `#eee` to `var(--hairline)`. Verified on dev — header, title strip, toolbar top/bottom, and toggle borders all compute to the same rgb(247,247,247) (the theme's hairline setting).
- Product grid price weight (per Randell): existing `main.css` rule `.product-item .price` font-weight 300→500. Verified computed 500 on dev.
- Product grid price colors + sale layout (per Randell, settings-driven not hardcoded):
  - `main.css`: `.product-item .price` and `.product-item .new-price` color `#46413C` → `var(--text)` (theme text-color setting, currently #212121). The old hardcode was also blocking the theme's sale-color setting on sale items.
  - `snippets/product-grid-item.liquid`: compare-at sale path reordered — crossed-out `old-price` now renders BEFORE `new-price` (promo path already old-first). Stock `.new-price:last-child` rule auto-drops its right margin.
  - `main.css`: `.product-item .old-price { font-size: 0.85em; margin-right: 6px }` — crossed-out price slightly smaller than the new price (em-based, scales with the base size); its grey stays `var(--text-light)` (settings-driven, #646464).
  - Verified on dev /collections/all sale card: order old→new, old 15.3px grey strike, new 18px #212121 w500.
- NEW theme settings for card price colors (per Randell — settings, not hardcodes; separate from Discount Sync):
  - `config/settings_schema.json` Colors group, under "Sale": **"Product card price"** (`card_price_color`, default #212121) and **"Product card sale price"** (`card_sale_price_color`, default #d34270 deep raspberry — chosen: already in brand vocabulary via the notify accent, says "sale" without the clinical #d20000 red, distinct from Discount Sync hot pinks, ~4.8:1 contrast on white).
  - `snippets/css-variables.liquid`: new `--COLOR-CARD-PRICE` / `--COLOR-CARD-SALE-PRICE` (fall back to text/sale settings if blank).
  - `main.css`: card price rules now read the new vars; added `.product-item .price.sale .new-price { color: var(--COLOR-CARD-SALE-PRICE) }`.
  - Verified on dev sale card: new price rgb(211,66,112) = #d34270, regular cards #212121, strike grey untouched. All code-side — rides the Friday push; live picks up schema defaults automatically (no editor step needed unless values should differ).
- Card price default → pure black (per Randell): `settings_schema.json` `card_price_color` default #212121 → #000000. Verified computed rgb(0,0,0) on dev regular cards.
- Collection title strip: added matching `border-top: 1px solid var(--hairline)` (same as the bottom border) — hairline now separates hero image from the title band.
- New backlog tasks created (epic KAN-4, Low, no deadline — Randell wants them for a later date): **KAN-154** review stars on product cards · **KAN-155** redesign default theme badges (square + bold) incl. documenting where badge text content comes from · **KAN-156** theme sale badge background = card sale-price color (cross-linked with KAN-155; Discount Sync pill explicitly out of scope for both) · **KAN-157** back-in-stock notify investigation (card button, missing ReStock PDP button, guest email signup) · **KAN-158** replace website logo with the color packaging version (blocked on Randell sourcing the asset) · **KAN-159** redesign the customer account pages (dashboard/order/addresses/login+register; design direction to be agreed first; ~6h).
