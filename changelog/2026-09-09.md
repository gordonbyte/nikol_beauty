# 2026-09-09

- **Live homepage stray Tolstoy bubble diagnosed (read-only):** the floating video bottom-right on nikolbeauty.com was the "Bubble TEST (DEV)" Bubble Feed widget (publishId `g1of50z42n3lc`) created 2026-09-08 during dev-store testing. Bubbles need no theme block and are configured per account appKey — shared by live and dev — so the live theme's Tolstoy app embed rendered it site-wide. Randell disabled it in the Tolstoy dashboard; verified gone (embed no longer requests `/settings/bubble`, no bubble shadow root). Memory saved: tolstoy-bubbles-account-shared.
- **Debug console logs removed from beauty_tutorial_videos.liquid (both folders, pushed to dev store theme):** (1) the `[tolstoy-status]` readiness log + table added 2026-09-02 (served its purpose confirming eager-init timing; was live on the dev store only — the live theme never carried it); (2) the legacy "CHANNELIZE PLAYLIST DIAGNOSTICS" block (~100 lines, marked "Remove after fixing the issue") that still fired ~15 console logs on every Channelize-mode episode page INCLUDING on the live site — its whole `{% unless use_tolstoy %}` wrapper removed; safe because it was pure logging (the actual Channelize loader lives in theme.liquid). KEPT: Firefox PiP hide, cart bridge, `data-priority` eager init, all player styles. Verified on dev store: no log output, 19/19 tiles loaded, PiP still applied, no page errors. Live site sheds the Channelize diagnostics on Randell's next live push.
- **Coming-soon PDP: variant color now switches the gallery image (custom-product-coming-soon.liquid, both folders, pushed to dev store theme):** the custom gallery was a static Swiper over `product.images` with no media IDs and no variant listener — the functionality never existed (Broadcast's picker fired `theme:variant:change` into the void). Fix: both slide loops now iterate `product.media` (images only, same set/order) stamping `data-media-id` on each slide, and a listener on `theme:variant:change` slides the main swiper to the variant's `featured_media` (thumbs follow via sync). Verified on dev store /products/bb-cream?view=coming-soon: all 12 slides carry media IDs, clicking each of the 4 shade swatches (Fair/Light/Light-Medium/Tan) lands on that shade's closed-smudge image, no JS errors. Section is ACTIVE on the coming-soon product view (`templates/product.coming-soon.json` main section). Live gets it on Randell's next push.
- **Coming-soon PDP: thumbnail clicks no longer misfire as swipes (same section, both folders, pushed to dev store theme):** clicking a thumbnail (edge ones especially) sometimes scrolled the strip like an arrow click instead of selecting the image. Root cause: Swiper's default drag `threshold` is 0, so a click with a few px of natural hand movement registered as a swipe — the click is suppressed and the strip flicks to the next thumbs (reproduced 4/4 with 6–10px jitter clicks; NOT arrow overlap — the -11% positioned arrows sit clear of the strip at desktop widths). Fix: `threshold: 12` on the thumb Swiper. Verified on dev store: 6–10px jitter clicks now select the image 4/4, and a real 90px swipe still scrolls the strip.
- **Coming-soon PDP: `.mp-review-box` is now a mini Junip review carousel (same section, both folders, pushed to dev store theme):** the static testimonial block now loads the current product's real Junip reviews via their public storefront API (`api.juniphq.com/v1/product_reviews`, CORS `*`, authed by the public store key auto-detected from the Junip app embed's `data-store-key` — present on BOTH stores; Junip is installed on dev too, its workspace just has no reviews). Shows one review at a time in the existing design (accurate star fill, title, body, "First L." name via include=customer, verified badge only when verified_buyer) with prev/next arrows + "n / total+" counter; pages of 10 fetched lazily via cursor as the shopper advances. All review content rendered via textContent (no HTML injection). FALLBACK: no reviews / fetch failure / no key → static editor content unchanged, nav hidden. New block settings: "Load Junip reviews" (default ON) and an optional store-key override (auto-detect covers both stores; a dev-template override attempt was reverted — unneeded). Verified on dev store with the API rerouted to live BB Crème data: review 1 "Excellent"/Pauline M./80% stars/verified, next/prev navigation exact, rode 40 clicks to "41 / 50+" across 5 lazy pages, zero JS errors; and the no-reviews fallback path holds (static quote intact, nav hidden). Live needs only the next section push — key auto-detects there. FOLLOW-UP FIX same day: the nav arrows were visible even in the static-fallback state (Randell hit dead arrows on the dev store, where products have no reviews) — the `.mp-review-nav { display:flex }` author rule overrode the UA's `[hidden]{display:none}`; added `.mp-review-nav[hidden]{display:none}`. Re-verified: no-reviews state hides the arrows entirely; with reviews they show and a human-coordinate click advances 1/10+ → 2/10+.
- **Review carousel polish (per Randell; same section, both folders, pushed to dev store theme):** (1) smooth direction-aware slide transition between reviews à la the complete-the-look slider — content wrapped in `.mp-review-slide`, slides out 26px opposite the travel direction and in from the incoming side (0.28s ease, 200ms swap point, `animating` guard against double-clicks, honors prefers-reduced-motion; star fill width also animates 0.3s); (2) 10px gap between stars and review title; (3) review bodies clamped to 3 lines (`-webkit-line-clamp`); (4) uniform box height — `.mp-review-box--junip` (class added only when Junip reviews actually load) reserves 1 title line + 3 body lines + author row via min-heights, so title-less/short reviews don't change the box size; static fallback rendering untouched (no class → no clamp/min-heights). Verified on dev store (live data rerouted): clamp 3 + overflow hidden, 10px star gap, mid-transition snapshot shows the slide animating (opacity 0.22, translateX −20px), box height an identical 239px across 7 consecutive reviews. The lone "Failed to fetch" in the probe came from Junip's own widget hitting the test's rewritten route — test artifact, not the section code (its fetch is fully caught).
- **Review carousel: click-to-scroll to the full reviews section (section in both folders + DEV template JSON, pushed to dev store theme):** clicking the review (or Enter/Space — role=button, tabindex, focus ring) smooth-scrolls to the full Junip reviews list, landing it ~110px from the viewport top; honors prefers-reduced-motion. Junip's widget has no API to open one specific review, so jumping to the exact clicked review isn't possible — the smooth scroll is the agreed fallback. The coming-soon template had NO reviews section (both its "apps" sections were empty), so the Junip Product Reviews app block (`junip-product-review`, settings copied from the regular PDP: center, mostRecent) was added to the first apps section on the DEV template. The click binding only attaches when the `.junip-product-review` target exists AND reviews loaded — on live the box is simply non-clickable until the block is added there: **KAN-210** (live-sync, under KAN-119) has Randell's editor steps. Verified on dev store: arrows still navigate independently (nav sits outside the clickable slide), review click scrolls to the reviews section; one earlier probe "arrow failure" was a layout-shift test artifact (Junip widget images loading mid-test), disproven in isolation 3/3.
- **Review carousel: capped at 8 reviews + italic body (same section, both folders, pushed to dev store theme):** fetch size and hard cap both 8 (single request, no lazy paging anymore), counter reads a definite "n / 8" (no "+"), next arrow disables at 8/8; review body paragraphs italicized (`.mp-review-text p` — applies to the static fallback quote too, keeping the box consistent; titles stay upright). Verified on dev store: font-style italic, counter starts "1 / 8", rides to "8 / 8" with next disabled.
- **Review carousel: 5-star reviews only (same section, both folders, pushed to dev store theme):** the mini review box (the ratings block under the Shipping & Returns tabs) now requests only 5-star reviews — Junip's storefront API supports `filter[rating]=5` server-side (tested against live BB Crème data: 8/8 returned rating 5), so it's one URL param, no client-side filtering or extra requests. The 4★ "Excellent" review that used to lead is gone from the box. Verified on dev store: request carries the filter, star fill is 100% on every review stepped through. Note: the standalone star-rating summary at the top of the PDP and the full Junip reviews widget still reflect ALL reviews — only the mini carousel is curated.
- **Review carousel: titled reviews only (same section, both folders, pushed to dev store theme):** Junip has no server-side title filter, so pages of 25 are fetched and reviews without a title are dropped client-side, auto-chaining up to 4 pages (100 reviews scanned max) until 8 titled 5-star reviews are collected; titles are common (21 of BB Crème's first 25 five-star reviews), so it's one request in practice. Verified on dev store: all 8 slots show a title + 100% stars ("BB CREME - Light" → "LIght, easy to apply."), counter 1/8→8/8.
- **Review carousel: author snug under the description, box still fixed-size (same section, both folders, pushed to dev store theme):** short reviews left dead space between body and author because the height reservation lived on the text element; moved it to the slide container (`min-height: 162px` desktop / `150px` mobile, text min-height removed) so the author row sits directly under the body and the spare space falls below the author instead. Verified on dev store: text→author gap 0px on every review, box a constant 240px across bodies of 1, 2, and 3 lines. Then per Randell an 8px gap was added between description and reviewer name (`.mp-author-box` margin-top) with the slide min-height bumped to 170px/158px to keep the box uniform — re-verified: gap 8px on all reviews, box a constant 248px.
- **Review carousel: "View all" link top-right (same section, both folders, pushed to dev store theme):** small underlined 13px button absolutely positioned in the box's top-right corner (20/30px desktop, 15/20px mobile), same smooth-scroll to the full Junip reviews section as the review-click; only appears when reviews loaded AND the reviews section exists on the page (hidden in the static fallback). Verified on dev store: hidden in fallback, visible at exactly 20px/30px with reviews, click lands the reviews section 110px from viewport top.
- **"What Women Are Saying" Tolstoy slider: edge fades removed (custom-videos.liquid, both folders, pushed to dev store theme):** deleted the 260px `::before/::after` gradient overlays (desktop ≥1200px) on `.custom-videos__tolstoy`; floating circle arrows and everything else untouched. NOTE the section is shared — the dormant homepage-style-2 variant's video slider loses the fade too. ACTIVE: coming-soon PDP (cw_videos_WcxwdJ). Verified on dev store: no pseudo-element backgrounds, both nav arrows present, tiles render edge-to-edge (screenshot).
- **Coming-soon PDP made multi-product — per-product content via metaobjects, strict no-fallback (8 sections + product-tabs snippet in both folders, pushed to dev theme; admin objects created on DEV store):** per Randell: one product-specific FAQ list per product, swatch picker auto-hides without color variants, and NO fallback — an empty field hides that element, an empty section renders nothing. ADMIN (dev store): metaobject definitions `cs_faq_item` (Q/A), `cs_content_item` (image/title/text — reused for steps, skin-safe features, switched rows; Shopify's 40-field cap forced this split, main definition is 28 fields), `coming_soon_page` (all page content incl. tolstoy_top/bottom, swatches JSON keyed by EXACT option values — "Light/Medium" with slash, related collection, FAQ list), product metafield definition `custom.coming_soon` (metaobject ref). BB Crème reference entry created via API (18 child entries + page entry, current template copy incl. rich-text conversions) and linked to dev product 8112848339046. THEME: sections gate on `template.suffix == 'coming-soon'` (`cs_mode`) so the dormant homepage-style-2 instances are untouched; in cs_mode all swappable content reads the metaobject; rating strip + info-banner rating now auto-read Junip metafields (reviews.rating/rating_count) and hide without them; "Watch Our Products In Action" hides without tolstoy_top; What Women Are Saying hides without tolstoy_bottom; review box uses metaobject fallback quote and self-hides when no reviews AND no quote; INGREDIENTS/BENEFITS accordions per-product (Shipping tab stays generic); product slider takes its collection from the metaobject. GOTCHA (cost an hour): a `list.metaobject_reference` field can't be for-looped inline (`for x in cs.steps.value` yields nothing) — assign it to a variable first, then loop; single-item metaobject fields work inline. VERIFIED on dev store: BB Crème renders fully from the metaobject (intro+3 bullets, 4 swatches, ING/BEN accordions, both Tolstoy sections, 3 steps/features/rows incl. row-2 review, 9 FAQs, banner, slider 10 products, zero JS errors); rose-lip-balm (no entry) hides every swappable section cleanly. LIVE replication: **KAN-211** (live-sync) — definitions + entries must exist on live BEFORE the section push (no-fallback means pushing first blanks the live page). Per-product setup going forward: 1 Coming Soon Page entry (+ its FAQ/content items) + 2 Tolstoy widgets + the 6 instruction metafields.
- **SCOPE REDUCED same day (per Randell): metaobjects only from the Steps section DOWN.** Everything above "Natural, Polished Skin In 3 Simple Steps" reverted to plain template settings (shared across coming-soon products): rating strip (back to editor text + reviews.rating preference), intro blurb + bullets, color swatches (template blocks, auto-hide without a Color option unchanged), INGREDIENTS/BENEFITS accordions (product-tabs snippet fully reverted), "Watch Our Products In Action" (section setting publish id; the empty-id wrapper gate kept), review-box static fallback (block settings, JS hide-empty removed). Metaobject definition trimmed 28→20 fields (deleted intro_text, intro_features, swatches, ingredients, benefits, review_fallback_quote/author, tolstoy_top on the dev store). STILL metaobject-driven: steps, skin-safe features, switched rows (+row-2 review), What Women Are Saying (tolstoy_bottom), FAQ list+subtitle, bottom banner (heading/text/image; rating line = Junip metafields), related-products collection. Re-verified on dev store: BB Crème full page correct (static upper incl. "4.76/5 | 12,000+" strip and 4 swatches + metaobject lower); rose-lip-balm shows the shared upper page and cleanly hides everything from steps down; zero JS errors. KAN-211 updated to the reduced scope.
- **Further trims (per Randell): "You may also like" slider back to template settings (shared Lips collection, shows on every coming-soon product — `related_collection` field deleted) and the row-2 embedded review removed from the Why Switched section (`row_review_quote`/`row_review_author` fields deleted; that was the small 5-star C.M. quote inside the second row — NOT the main review box, which is untouched). Definition now 17 fields. Re-verified: BB rows render without the embedded quote, slider shows on both test products, no errors. KAN-211 field list note applies (dev admin is the source of truth for the live replication).
- **Metaobject entry form grouped with divider headers (dev store):** Shopify has no native section headers in metaobject forms, so 6 divider pseudo-fields ("━━━ 3 SIMPLE STEPS ━━━" etc., single-line, description "Divider only — leave empty") were inserted between the groups using `metaobjectDefinitionUpdate` with `resetFieldOrder: true` (submitted-field order becomes the form order). Dividers are never read by the theme — leaving them empty is correct. Groups: What Women Are Saying / 3 Simple Steps / Skin-safe / Why Switched / FAQ / Bottom banner. Replicate the same order+dividers on live (KAN-211).
