# 2026-09-04

- **ATC + cart-bar solid buttons matched to the custom-button hover (custom-pdp-css.liquid, both folders, pushed to dev store theme):** Randell flagged the cart bar's "Select color shade" button — no background fade and GREY hover text. Root cause is Broadcast's `.btn--solid` system: hover slides a skewed `::after` wipe (`--btn-bg-hover`) instead of transitioning background, and global `.btn:hover` sets `color: var(--btn-text-hover)` which one theme scope maps to `--BTN-PRIMARY-BORDER-COLOR` (the grey). Scoped fix on the coming-soon PDP only (`.product__submit__buttons .btn--solid` + `.cart-bar__submit .btn--solid`): `::after` wipe removed (`content: none`), `transition: background-color 0.3s`, hover = `--BTN-PRIMARY-BG-COLOR-BRIGHTER` bg/border with text held at `--BTN-PRIMARY-TEXT-COLOR`. Verified on localhost: both buttons fade 211,66,112 → 204,47,97 with text staying white, wipe pseudo gone. Rest of the site keeps native Broadcast behavior. FOLLOW-UP — Randell's actual report was about `.custom-alternating-rows__cta-link` (misread as the cart bar; that fix stands as a bonus): the CUSTOM buttons had the same grey-hover-text bug from the other direction — theme.css's global `a:hover { color: var(--link-hover) }` (specificity 0,1,1) beats their class-only color (0,1,0), turning button text rgb(70,65,60) on hover; their background DOES fade but the brighter token is subtle so it read as \"no transition\". Fixed by pinning `color: var(--BTN-PRIMARY-TEXT-COLOR)` in the :hover AND :focus-visible rules of ALL SIX custom button sections (alternating-rows, videos, info-banner, icon-features, steps-horizontal, hero-banner — the homepage shares the bug via the shared sections). Verified on localhost: CTA hover now 204,47,97 bg with WHITE text. All six pushed to the dev store theme. Classic check-global-element-styles lesson: `a:hover` was never countered when these `<a>` buttons were built.
- **"What Women Are Saying" Tolstoy carousel matched to custom-product-slider's edge treatment (custom-videos.liquid, both folders, pushed to dev store theme):** per Randell, for a uniform look — (1) EDGE FADES: `::before/::after` on `.custom-videos__tolstoy`, 260px solid `var(--custom-videos-bg)` at 30% dissolving inward, desktop ≥1200px, pointer-events none, z-index 2 (same recipe as the product slider); (2) ARROWS ON TOP: the product slider's exact 48px dark-circle SVG buttons (36px ≤749px, left/right 40px→10px, hover scale 1.08, focus-visible outline) float at z-index 3 on the solid zones. Tolstoy's native arrows stay in the DOM but are opacity-0/pointer-events-none; our buttons forward a full pointer-event sequence (pointerdown→click) to the geometrically-matched native arrow (leftmost = prev), so Tolstoy still drives the scrolling — no custom scroll logic. Initially the nav was gated behind a `has-nav` class added only once Tolstoy's native arrows were detected — on the real page that detection didn't fire while the CSS hide of the native arrows did, leaving NO arrows (Randell reported it); the gate was removed same day, the circle buttons now always render in Tolstoy mode (48x48 verified visible on localhost) and simply no-op until Tolstoy boots. Markup renders on localhost; schema + scripts validated; ARROW FORWARDING NEEDS RANDELL'S CLICK-TEST on the dev store (Tolstoy dead on localhost/headless). Iteration 3 per Randell's dev-store test (right arrow worked, left didn't; wanted colors inverted): (a) buttons restyled to a WHITE circle with the arrow chevron in the section background color (`svg rect` fill #fff, `svg path` fill `var(--custom-videos-bg)` — computed 252,201,198 verified); (b) prev-arrow bug root-caused — Tolstoy renders only its RIGHT arrow at the start position, so the old \"leftmost match = prev\" geometry pointed BOTH buttons at the right arrow; direction now resolves by class/aria hints (left|prev|back vs right|next|forward) with a carousel-midpoint geometric fallback, dispatching on the deepest arrow element so inner-button handlers fire; prev correctly no-ops while no native left arrow exists. Iteration 4 (Randell: both arrows dead after iteration 3): the rewritten deepest-element/class-hint picker didn't match Tolstoy's real DOM at all — reverted to the ORIGINAL selector+sort that provably drove the right arrow (`[class*="arrow"] button, button[class*="arrow"], [class*="arrow-button-container"]`, sorted by x, extremes) and fixed prev with just a midpoint sanity check (picked element on the wrong side of the carousel center → null, so prev can't grab the lone right arrow at start position). Awaiting Randell's re-test; if anything still misbehaves, the real fix needs the carousel's actual DOM from his browser inspector (first transient push failure \"Error connecting\" — retry succeeded). Iteration 5 — DEFINITIVE: left arrow still dead, so stopped guessing and read Tolstoy's own source (https://widget.gotolstoy.com/we/widget.js carries the carousel CLASS MAP): the direction buttons are literally `.tolstoy-previous-button` / `.tolstoy-next-button` (arrowsContainer = `tolstoy-carousel-arrows-container`, per-arrow container = `tolstoy-carousel-arrow-button-container`). All heuristics (geometry sort, midpoint checks, class-hint regexes) replaced by those two exact selectors; missing button = no-op. Class map noted for future Tolstoy styling work (also saved to memory). Iteration 6 per Randell (the carousel "pops in"): the bottom Tolstoy now FADES UP gracefully when its content arrives — the carousel starts opacity 0 / translateY(18px) (gated on the section's `anim-ready`, so reduced-motion/theme-off/no-JS all see it instantly) and a MutationObserver adds `is-loaded` (0.8s ease fade) the moment real tiles exist (`video, [class*="tile-container"], img` in the subtree), with a 15s safety timeout so nothing can stay hidden if Tolstoy never loads. Verified on localhost by simulating a tile injection: opacity 0 → fade firing (0.65 mid) → settled 1.0.
- **Thumbnail-style arrows on the "Watch Our Products In Action" Tolstoy carousel (custom-product-coming-soon.liquid + custom-pdp-css.liquid, both folders, pushed to dev store theme):** per Randell — the TOP carousel now gets the gallery thumbnail slider's exact 30px dark rounded chevron buttons (#45413B, same SVGs as the thumb slider's swiper-button-prev/next), positioned 8px inside each edge, hover scale 1.08, focus-visible outline, aria-labels. Same proven mechanism as the bottom carousel: Tolstoy's native arrows hidden (opacity 0/pointer-events none) and our buttons forward the pointer sequence to `.tolstoy-previous-button`/`.tolstoy-next-button` (the exact class-map selectors). Verified rendering 30x30 with #45413B fill on localhost; carousel behavior needs Randell's click on the dev store. CONFIRMED WORKING by Randell on both carousels. Size follow-up: same SVGs but Swiper's CSS renders the thumbnail arrows at 27x30 while ours rendered 30x30 — pinned ours to 27x30 first; Randell sees the thumbnails as 36x36 on his end (Swiper sizes via --swiper-navigation-size so render varies with viewport/zoom), so final size per his call: **36x36** (verified computed). Arrows also moved flush to their edges per Randell (left/right 8px → 0), then to 2px insets (measured 2px both sides). Also per Randell: Tolstoy's CSS-module inner container (hashed class `_carouselContainer_xxx`) renders at 90% width — overridden to 100% via stable-substring selector `[class*="carouselContainer"]` in BOTH carousel scopes (`.cw-bb-pvideo-tolstoy` in custom-pdp-css + `.custom-videos__tolstoy` in custom-videos.liquid; hash-proof against their redeploys). Verified via injected hashed-class element resolving to full container width (650px), then adjusted to **95%** per Randell (the module-container selector split out of the shared 100% rule in custom-videos so the full-bleed containers stay 100%); verified 95% resolving in both scopes (618/650px top, 1330/1400px bottom). Pushed.
- **Space between the product-slider header and its content (custom-product-slider.liquid, both folders, pushed to dev store theme):** `padding-bottom: 30px` on `.custom-product-slider__header` per Randell ("Products you may also like"). NOTE: this section is ACTIVE on BOTH product.coming-soon and index.homepage-style-2, so the homepage slider gets the same 30px. Verified computed on localhost.

- **Video-gallery hover overlay removed (video-gallery.liquid, both folders, pushed to dev store theme):** per Randell — the `.video-gallery-video-overlay` (dark tint + white play icon over every thumbnail) deleted from all three render paths (two Liquid card templates + the JS `link.innerHTML` template literal) along with its CSS (`.video-gallery-video-overlay`, its `:hover` rule, and `.video-gallery-play-icon`, which only existed inside the overlay — zero refs remain). Section is active on page.video-gallery and page.video-gallery-youtube (the homepage-style-2 grep hit was only link URLs). Schema validated. Rides the code push for live. FOLLOW-UP per Randell: play icon restored on /pages/video-gallery — as a new `show_play_icon` checkbox (default false) rendering just the white play SVG centered over the thumbnail (drop-shadow for legibility on light thumbnails, no dark overlay), wired into all three card render paths; `page.video-gallery.json` sets it true (youtube gallery left off — tick in the editor if wanted). GOTCHA CONFIRMED TWICE TODAY: pushing a template + its section's new schema setting in ONE `shopify theme push` silently strips the unknown setting from the template (validation runs against the old schema) — push the section first, then the template; verified via theme pull, re-pushed template alone, icons now render (36 refs on the dev store page). Live editor step: none needed if code+template push order is respected; the setting is in the repo template.
- **Episode-page cart handling ported to the coming-soon Tolstoy embeds (custom-tolstoy-player.liquid, both folders, pushed to dev store theme):** per Randell — the `?view=coming-soon` template now handles Tolstoy the same way `/pages/beauty-tutorial-videos/` does. Player styling (centered modal, surfaced close X, backdrop close) was already in the shared snippet; now the CART BRIDGE is too, verbatim from the episode template (guarded by `window.__tolstoyCartBridge`, so no double-wiring): `tolstoy_add_to_cart` subscribe + cart-count poll confirms the add landed, then refreshes the header count, rebuilds `cart-items`, closes the Tolstoy player (with the mobile fullscreen-modal fallback), and fires `theme:product:added` to open the drawer; `window.fetch` `/cart/add` watch as fallback, debounced. Also ported drawer-closes-on-video-click, with selectors adapted to the coming-soon containers (`.custom-videos__tolstoy` / `.cw-bb-pvideo-tolstoy`). JS parses, pushed, and verified serving on the dev store page (8 bridge markers in HTML). Add-to-cart flow needs Randell's manual test on the dev store (headless can't drive the Tolstoy player).
- **Product-wrapper reveal animations on the coming-soon PDP (custom-product-coming-soon.liquid, both folders, pushed to dev store theme):** per Randell — same animation system as the homepage-style-2 sections (hidden start states ONLY under a JS-added `cs-anim-ready` class → no-JS safe; skipped under prefers-reduced-motion and when theme animations are off). Gallery sliders (`cw-new-popup-main-slider` + vertical thumbs — NOT `.product__images`, which doesn't exist on this custom gallery) fade in opacity-only (transforms would fight sticky); every `.product__content .product__block` fades up 18px via IntersectionObserver with burst staggering (70ms apart within a viewport batch, capped 560ms, batch resets between bursts so below-fold scroll reveals start instantly); per-block transition-delay cleaned up on transitionend so it can't slow later theme transitions; blocks with no layout (display:none bundle items / empty upsell) are pre-revealed so they can't be stuck transparent if the theme shows them later. Verified on localhost: 14/14 blocks revealed after full scroll, all displayed blocks + gallery opaque, zero residual delays. FOLLOW-UP (Randell saw no change on refresh): the code was on all four surfaces, but the reveals started at script-parse time and the whole cascade finished BEFORE first paint — invisible by design flaw. Fixed: hidden states still apply during parse (no flash), but observers now start at DOMContentLoaded + double requestAnimationFrame (after the first painted frame). Verified: staggered opacity ramp 0.3→1.0 over ~750ms measured AFTER DCL. Re-pushed to the dev store theme.
- **Duplicate installments banner removed — the static one (custom-product-coming-soon.liquid + custom-pdp-css.liquid + product.coming-soon.json, both folders, pushed to dev store theme):** the coming-soon PDP showed "Pay in 2 interest-free installments of $21.50 with [Shop Pay] Learn more" twice — Shopify's native `<shopify-payment-terms>` element (variant-connected, live per-variant pricing, real Learn-more modal) AND a hardcoded copy in the `payment_details` block's top bar (static $21.50, Learn more → "#"). Removed the static one per Randell: the whole `cw-bb-color-top-bar` markup, its six schema settings (installment_text, payment_logo, payment_logo_url, payment_logo_alt, learn_more_text, learn_more_url), the template values, and all its CSS (desktop + mobile `cw-bb-color-top-text` rules) — zero refs remain. The block keeps its description + feature list. Verified on localhost: only the native shopify-payment-terms renders. Pushed section+snippet FIRST, template second (per the schema-validation push gotcha). Live: rides the code push; no editor step (template setting removal is a deletion — orphaned values on live are ignored once the code ships). Follow-up: `.cw-bb-color-description` top padding (15px) removed — with the static installments bar gone the description leads the block, so the offset had nothing to sit under; verified 0px computed, pushed.
- **Image hover micro-zooms on the coming-soon PDP (custom-steps-horizontal / custom-alternating-rows / custom-icon-features .liquid, both folders, pushed to dev store theme):** iterated with Randell in-session — a settle-on-reveal variant and then an idle-motion pass (7s float on step photos, 14s Ken-Burns drift on the big photos) were both built, verified, and REMOVED same day per his feedback ("only on hover"). Final state, all CSS under `@media (hover: hover)` with 0.45s ease transitions: step-card hover zooms its photo to 1.03 and pops the step number to 1.12 (preserving its translateX(-50%) centering); alternating-row hover zooms its photo to 1.02; icon-features portrait hover zooms to 1.02. Gallery deliberately untouched. Verified on localhost via forced hovers (idle animation-names all 'none'; hover transforms measured 1.02/1.02, steps img+number mid-transition — a Klaviyo popup intercepting pointer events had to be removed for the test, localhost artifact only).
- **Scroll-in reveal for custom-icon-features (both folders, pushed to dev store theme):** same anim-ready/is-inview idiom (no-JS safe, reduced-motion + theme-animations guards, IO threshold 0.15): heading → subtitle → three feature rows → button → portrait fade up 26px with a JS-assigned 90ms batch stagger in DOM order (nth-child CSS delays would be fragile in this mixed-element column); per-element transition-delay cleaned up on transitionend; the image reveal targets the WRAPPER div so it can't collide with the img hover zoom added earlier today. Verified on localhost: hidden before scroll, mid-reveal stagger measured (0.94/0.85/0.70/0.47/0.16/0/0), settled 7/7 all opaque, zero residual delays. Active on product.coming-soon only; rides the code push. Follow-up per Randell (portrait overflowing the section under the hover zoom): `overflow: hidden` on `.custom-icon-features__image` — the zoom is now the classic contained crop-zoom — plus `overflow: hidden` on the section as a guard for mid-reveal offsets; verified under hover that the wrapper clips and nothing extends past the section edge. Second follow-up (image a few px short of the section bottom): the wrapper now `align-self: stretch`es to the section's full height and the img is `display: block; height: 100%; object-fit: cover` (block kills the inline baseline gap; cover fills the stretched column edge-to-edge). Measured: image column 875/875px section height, 0px top/bottom/right gaps. Mobile's 450px-cover rule unaffected.
- **Scroll-in reveal for custom-alternating-rows (both folders, pushed to dev store theme):** same anim-ready/is-inview idiom + 90ms batch stagger (title → subtitle → each row as reached → CTA, fade up 26px); the section tag gained `id="AltRows-{{ section.id }}"` for the script; reveals ride the ROW element so they can't collide with the row-hover photo zoom; `overflow: hidden` added on the section proactively (the icon-features lesson — mid-reveal offsets stay inside the background). Verified on localhost: hidden before scroll, 6/6 revealed all opaque after full scroll, zero residual delays. Active on product.coming-soon only; rides the code push.
- **Scroll-in reveal for custom-steps-horizontal (both folders, pushed to dev store theme):** same anim-ready/is-inview idiom as custom-videos (no-JS safe, reduced-motion + theme-animations guards, IO threshold 0.18): header, the three step cards, and the button fade up 26px; cards stagger via CSS transition-delays (0/0.12/0.24s — numbers ride inside the cards so they travel with the transform). Verified on localhost: hidden before scroll (opacity 0), mid-reveal stagger measured (0.86/0.65/0.28), settled 5/5 revealed all opaque. Active on product.coming-soon only; rides the code push.
