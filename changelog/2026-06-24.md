# Changelog — 2026-06-24

Theme cleanup session (`shopify-live`). All changes are local/uncommitted; git commits pending (handled separately).

## Renames (→ `custom-` convention)
- `sections/cw-pdp-collection.liquid` → `sections/custom-product-slider.liquid` (editor name **"[Custom] Product Slider"**). Restricted to product templates (`enabled_on: templates: ["product"]`); cleaned misleading "best seller" default copy. Re-wired `templates/product.coming-soon.json` (type + label + instance key → `custom_product_slider_CByXCN`).
- `sections/cw-banner_vb.liquid` → `sections/custom-hero-banner.liquid` (**"[Custom] Hero Banner"**). Re-wired `templates/index.json` and `templates/index.homepage-style-2.json` (types + instance keys → `custom_hero_banner_*`).
- `sections/cw-banner-bottom.liquid` → `sections/custom-info-banner.liquid` (**"[Custom] Info Banner"**). Re-wired `templates/index.homepage-style-2.json` (type + key → `custom_info_banner_bPCMH4`).
- `templates/index.new.json` → `templates/index.homepage-style-2.json` (homepage variant suffix `new` → `homepage-style-2`).

## Deletions
- `templates/product.bb-cream.json` — no product used the `bb-cream` template suffix; redundant with `product.coming-soon.json`.
- `sections/productv2.liquid` — orphaned (was bb-cream's main section; unreferenced after the template deletion).
- `sections/cw-banner.liquid` — duplicate hero banner; consolidated into `custom-hero-banner` (the more updated / more mobile-friendly version).
- `templates/index.vb.json`, `templates/index.vc.json` — homepage A/B variants that differed only by one banner image.

## Banner consolidation + dead-code cleanup
- Kept `custom-hero-banner` over `cw-banner`; repointed `index.homepage-style-2.json` to it (identical schemas, so settings carried over).
- Removed dead/bleed code from `custom-hero-banner`: vestigial `?view=vc` `<script>`, the `.vc-variant` CSS rule, and three cross-section bleed selectors (`.cw-pdp-feature-box`, `.cw-pdp-feature`, `.cw-single-review-description`).

## Decoupling (`custom-info-banner` ⇄ `custom-hero-banner`)
- Made `custom-info-banner` fully self-contained: namespaced shared `.cw-banner-*` classes to `.info-banner-*` and bundled their desktop + mobile styles locally (no longer depends on the hero banner's CSS).
- Image-toggle fix (`custom-info-banner`): renamed `.cw-desktop`/`.cw-mobile` image classes to `.info-banner-img-desktop`/`.info-banner-img-mobile` with local responsive rules — removed reliance on the PDP-only `snippets/cw-pdp-css.liquid`; the desktop/mobile image switch now works on the homepage.
- Toggle fix (`custom-hero-banner`): renamed its `.cw-desktop`/`.cw-mobile` wrapper classes to `.cw-banner-desktop`/`.cw-banner-mobile` with local responsive rules — same PDP-snippet dependency removed; its desktop/mobile feature-list toggle now works on the homepage.

## Notes / follow-ups
- ⚠️ **Intelligems (or any A/B app):** update routing that referenced `index.new`, `?view=vb`, or `?view=vc` — those suffixes no longer exist.
- Remaining consumers of the PDP-only `.cw-desktop`/`.cw-mobile` utility (still have the latent homepage reliance): `sections/cw-beauty-advice.liquid` and `sections/cw-main-collections.liquid` (agency `cw-*` sections on `index.homepage-style-2`). Not fixed this session.
- Orphaned-section audit run: the only genuinely orphaned/superseded section file is `sections/article.liquid` (article templates use `new-article`); `sections/custom-feature-hero.liquid` is built but not currently placed. Neither removed.
- Pre-existing uncommitted changes already in the working tree (NOT from this session): `config/markets.json`, `config/settings_data.json`, `layout/theme.liquid`, deletion of `custom-best-sellers` section + `collection.custom-best-sellers` template, and `index.context.us` / `group-overlay.context.us` / `group-pre-footer.context.us` removals.
