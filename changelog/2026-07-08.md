# Changelog — 2026-07-08

## Committed + merged yesterday's work
- Created branch `theme-a11y-audit-2026-07-07`, committed the 2026-07-06 cleanup + 2026-07-07 audit work (54 files, +3,580/−3,467) incl. the three changelog docs, merged into `develop` with a `--no-ff` merge commit (`e8028df`). Not pushed to GitHub; nothing deployed to the live store.

## Store data (Admin API, user-approved): 4th-of-July promo teardown completed
- Deleted the remaining 100 `custom.promo_percent` variant values (Best Seller 64, Beginner 24, Perfect Complexion 12) via `metafieldsDelete`; verified `metafieldsCount: 0`; definition kept for future sales.
- Reverted all 101 bundle variant prices to pre-sale values via `productVariantsBulkUpdate` (Best Seller $270→$229.50, Beginner $152→$129.20, Perfect Complexion $135→$114.75, Mascara $48→$40.80); compare-at untouched; verified live. Promo manifest marked FULLY UNDONE.

## Dead-code audit (report only — nothing deleted)
Full scripted scan of every theme file for orphaned sections/snippets/assets, broken renders, commented-out markup, unused schema settings, and dead in-file CSS; all headline findings hand-verified (Section-Rendering-API sections confirmed alive; dynamic `settings[]`/append-built classes/app-DOM selectors filtered out). Full report: `dead-code-audit-2026-07-08.md` (repo root, untracked).
- Headlines: 2 true orphan sections (`article.liquid`, `header-predictive-search.liquid`); 25 dormant stock library sections; 16 orphaned snippets (all app/keep-rule — zero theme-built); 5 orphaned assets (~135KB incl. `main.min.css` 55KB, `password-page-background.jpg` 77KB, never-loaded `custom.js`); 13 broken render refs (12 Zipify-zone); 11 commented-out markup blocks in 6 files; ~20 genuinely unused schema settings in 10 files; 52 dead in-file CSS classes in 5 files (incl. 14 in `cw-pdp-css.liquid` from the removed BB template).
- Flag to verify: `templates/gift_card.liquid` calls `{% section 'gift-card.liquid' %}` with the `.liquid` suffix in the name — check the gift-card page for a Liquid error.
- Wishlist keep-rule respected: HulkApps/Zooomy snippets + wishlist.css/js reported but marked DO NOT DELETE.

## Dead-code cleanup #1: removed the 2 true orphan sections
Working the audit findings in order, per request. Final pre-delete sweep confirmed zero references anywhere (the lone `"type": "article"` hit is featured-posts' schema using Shopify's *article picker* setting type — unrelated).
- `git rm sections/article.liquid` (691 lines) — old article-page section; both article templates use `new-article`.
- `git rm sections/header-predictive-search.liquid` (216 lines) — old predictive-search section; no preset, placed nowhere, theme.js fetches `predictive-search` instead. Its broken render ref (`search-empty-product-item`) goes with it — the theme now has **zero** broken theme-side render references.
- Sections count: 124 → 118 tracked (incl. earlier deletions). No storefront impact (both were unreachable). Local/staged only — not committed, nothing pushed to the live store.

## Dead-code cleanup #2: removed 4 of 5 orphaned assets (~134KB)
- `git rm assets/main.min.css` (55.2KB — never loaded; theme loads main.css), `assets/password-page-background.jpg` (77.1KB), `assets/loading.gif` (1.7KB), `assets/custom.js` (0.9KB — never loaded; only mention was a comment in theme.js telling devs to use it).
- **KEPT `assets/blank.png`** — the pre-delete sweep found its name in **live theme settings**: `swatch_color_list` maps swatch "Blank: blank.png" (settings_data.json + schema default). The swatch snippet resolves image names via `file_img_url` (Files CDN, not theme assets), so the assets copy is technically unreachable — but with the filename live in settings it's kept to avoid confusion. Note: for the "Blank" swatch to actually display, `blank.png` must exist in **Content → Files**; the theme-assets copy was never served.
- wishlist.css / wishlist.js kept per the standing wishlist rule. Local/staged only.

## Rebuilt shopify-dev from shopify-live
- `shopify-dev/` held a stock Dawn 7.0.1 theme (untracked); moved it to `delete_later/shopify-dev-dawn-2026-07-08/` instead of deleting.
- Copied all 7 theme folders (419 files: 51 assets, 3 config, 3 layout, 6 locales, 122 sections, 175 snippets, 59 templates) from `shopify-live/` (Broadcast 5.5.0, including today's staged dead-code deletions) into `shopify-dev/`; verified file counts match. Local copy only — nothing uploaded to any store yet.

## Dead-code cleanup #3: removed commented-out code blocks (15 blocks, ~634 lines)
- `video-gallery.liquid` + `video-gallery-youtube.liquid`: 5 blocks each (~296 lines each) — the disabled tab-navigation HTML, both disabled "Spotlight" sections, and the tab CSS + tab JS comment blocks of the same feature. Note: the spotlight blocks contained *nested* Liquid comments; a first regex-based removal mis-paired them and left stray `endcomment` tags — both files were restored from HEAD and re-processed with a nesting-aware parser. Final state verified: comments 27/27, if/endif balanced, schemas valid, zero fragment leftovers.
- `blog.liquid`: 3 blocks (~17 lines) — old tag-nav block + two commented excerpt lines.
- `snippets/product-buttons.liquid`: 1 block (~24 lines) — commented-out custom "Buy now" button + fetch script.
- `snippets/related-article.liquid`: 1 line — commented excerpt.
- **SKIPPED `sections/header.liquid`** (1 block, ~7 lines): it's a commented-out **wishlist header icon** (`h-wishlist-icon wishlist-page-widget`) — kept per the standing do-not-delete-wishlist directive.
- Follow-on: with the spotlight/tab markup gone, their CSS classes (`video-gallery-spotlight-*`, tab classes) become definitively dead — to be picked up in cleanup #5 (dead CSS). Minor no-op leftovers noted, not removed: two top-of-file `{% assign …_product %}` vars + the `{% if false %}` JS blocks in the video galleries.
- Heads-up: these 5 files were edited **after** the shopify-dev rebuild above, so `shopify-dev/` copies of them are now one step behind `shopify-live/`.

## shopify-dev re-sync after upload errors
- User's theme push surfaced `Unexpected outer 'else' tag (line 63)` in both video-gallery sections: the dev copies were the **broken intermediate** versions from cleanup #3's mis-paired comment removal (copied mid-process, before the restore-and-redo). Re-copied both from `shopify-live/`; tag balance verified (if/endif, comment/endcomment, for/endfor all paired). The two `page.video-gallery*` template errors were cascades (section files rejected → templates couldn't reference them) and should clear on re-push.
- Full hash sweep: every file in all 7 folders now identical between `shopify-live/` and `shopify-dev/` (blog.liquid, product-buttons.liquid, related-article.liquid were already current).
- Remaining push errors are **store config, not files**: `Dynamic source … does not exist` for `custom.bottom_description` (collection) and `custom.instruction_content_4/6` (product) means the dev store lacks those metafield definitions.

## Dead-code cleanup #4 (part 1): video-gallery consolidated + all settings wired
Per request: keep the video-gallery file with "the extras," delete the twin, migrate the placement, and fix the section's hardcoding.
- **Kept `video-gallery.liquid`** — it has the extra custom-thumbnail feature (`video.thumbnail` metaobject field preferred over the YouTube auto-thumbnail). **`git rm video-gallery-youtube.liquid`** — identical except minor CSS tweaks (tighter 15px grid row-gap, 61% cropped thumbs) and it *lacked* the custom-thumbnail logic.
- **Repointed `templates/page.video-gallery-youtube.json`** `"type": "video-gallery-youtube"` → `"video-gallery"`; both templates' settings values were identical so everything carries over unchanged. Minor visual note: that page now uses the kept file's grid (48px row-gap, natural thumb height) instead of the twin's tighter/cropped look.
- **Wired the 4 dead settings** in the kept file: `hero_image` + `fierceaging_hero_image` (used when set, falling back to the previously hardcoded CDN image — the two tab panels can finally have different photos), `store_url` (GO TO STORE href, default `/collections/all`), `youtube_channel_url` (new "VISIT YOUTUBE CHANNEL" button next to GO TO STORE — renders **only** when the setting holds a real URL; both templates currently store `"#"`, so nothing shows until a real URL is entered).
- **Also de-hardcoded the bottom hero** with 4 new settings (defaults = current content, zero visual change): `bottom_hero_image` (fallback to the hardcoded image), `bottom_title`, `bottom_description`, `store_button_text`. Schema paragraph documents the YouTube-button behavior.
- Verified: schema valid (18 settings, **all 13 value-settings wired — zero dead**), tags balanced (comment 27/27, if 40/40), template JSON valid, zero `video-gallery-youtube` references remain. This also eliminates the ~68-class CSS/JS duplication flagged in the isolation audit.
- shopify-dev note: `video-gallery.liquid` changed again and `video-gallery-youtube.liquid` + its template changed — dev copies are behind until re-synced.

## Dead-code cleanup #4 (part 2): merged custom-collection-tabs into custom-product-slider
User call: the two were near-twins (collection-tabs = the tabbed original with a working accessible tab UI; product-slider = its PDP fork with the tab UI stripped, leaving dead tab CSS + 2 dead settings). Merged into **one** section.
- **Kept `custom-product-slider.liquid`** (live-capable placement stays untouched) and ported in collection-tabs' full tab UI: WAI-ARIA tabs (`role=tablist/tab/tabpanel`, real `<button>`s, roving tabindex + arrow/Home/End keys, `hidden` panels), focus-visible styles, and the visually-hidden "reviews" count text.
- **Display rules (per user):** tab bar renders only with **2+ blocks**; each tab shows its image and/or label only when set; a block with neither falls back to its collection title (so no unreachable panels); a **single block renders the plain slider** exactly as the PDP does today. Documented in a schema paragraph.
- The previously-dead `tab_image`/`tab_label` settings and the 5 dead tab CSS classes are now **live** (wired to real markup). Removed the truly dead section-level `button_label`/`button_link` (body only ever used the per-block CTA) and the obsolete `display:none` CTA hack (the per-block CTA is gated on label+link; the PDP block's link is empty → unchanged).
- **`git rm custom-collection-tabs.liquid`**; repointed its `index.homepage-style-2.json` instance → `custom-product-slider` (6 tab blocks carry over 1:1 — identical block schema), editor label updated, and `background_color: "#F7F2ED66"` set explicitly to preserve the old default translucent background; dropped the orphaned section-level button settings from the instance. `enabled_on` widened to `["index", "product"]`.
- PDP nuance: its single block has a leftover "Lips" label, but the 2+-blocks rule keeps the PDP tab-free → pixel-identical.
- Verified: schema valid, **all 9 settings wired (zero dead)**, tags balanced, template JSON valid (type/blocks/bg), zero `custom-collection-tabs` references remain. Sections: 117 tracked.

## Dead-code cleanup #4 (part 3): new-article — show_image wired + Related Posts became the block (option B)
- **`show_image` wired:** the hero photo column now renders only when the checkbox is on AND the article has an image (also fixes a subtle bug — image-less articles used to output an empty figure with a blank `src`). Both article templates store `true` → no visual change.
- **The phantom "Recent articles" block is now real (user's option B):** the section previously declared blocks (`recent` + `@app`) but never rendered any — the block saved on both article templates was invisible, while a hardcoded "Related Posts" strip did the actual work (which, since Shopify orders blog articles newest-first, already WAS a recent-articles feature). Now the strip **is** the block: rendered via a proper block loop with `block.shopify_attributes`, heading from `block.settings.title` (last word wrapped in `<span>` to preserve the styling pattern), removable per-template by deleting the block. `@app` blocks now render too (apps can inject article widgets).
- Schema: block renamed "Recent articles" → **"Related posts"**, default heading "Related Posts", added an explainer paragraph. Both templates' saved block titles updated "Recent articles" → "Related Posts" so the live heading is unchanged.
- Verified: schema valid, `show_image` + `recent.title` both WIRED, block loop present, tags balanced (endfor/endform grep artifact accounted), both template JSONs valid.

## Dead-code cleanup #4 (part 4): custom-product-coming-soon — removed the `enable_zoom` checkbox
- The setting existed **only in the schema** — no zoom/lightbox code, no standard media-gallery machinery anywhere in the section body (stock-fork leftover; the agency's bespoke gallery has no zoom). Wiring would mean building a zoom feature on the deferred live PDP → removed the checkbox instead (zero behavior change; it already did nothing). The adjacent `enable_video_looping` is genuinely used and stays.
- Verified: 0 `enable_zoom` refs, schema valid.

## Dead-code cleanup #4 (part 6): header settings wired, pinned to current look
- Restored the missing assigns in `header.liquid` (the blank gap at lines 7-8 where stock Broadcast's variable block was deleted): `padding_top`, `padding_bottom`, `show_separator_line` now read from `section.settings.*`.
- **Pinned to current behavior per user:** stored `group-header.json` header values changed `padding_top/padding_bottom: 15 → 0` (the nil-variable rendering was effectively 0, so the live header is pixel-identical); `show_separator_line` already `false`. The editor's padding sliders + separator toggle are now genuinely functional going forward.
- Untouched (still nil, same as today): the `_medium`/`_mobile` padding variants and `menu_height_*_default` vars that were also lost in the same deletion — those feed only the same degenerate header-height math as before; candidates for the stock-infra pass.
- Verified: assigns present, group-header.json valid (0/0/false).

## Dead-code cleanup #4 (part 7): cart-drawer upsell — kept removed per user decision
User tested the upsell feature (works on the cart page; drawer rendering was the missing piece) and decided **against** upsells in the cart flyout; they deleted the "Upsell products" block in the theme editor. Aligned the repo with that decision:
- Removed the `upsell-products` block + its `block_order` entry from `sections/group-overlay.json` (otherwise a future theme push would have **resurrected** the block they deleted).
- Removed the phantom `upsell-products` block **type** from `cart-drawer.liquid`'s schema — it could never render in the drawer (no `when` case) and now can't be re-added as a ghost. Deleting the block (not unchecking auto-open) is the correct off-switch: the checkbox only controlled open/closed, never visibility.
- **Cart PAGE untouched:** `cart.liquid`'s "Pair it with" accordion still works on `/cart` (its case + block type intact) — flagged to user as a separate decision.
- Verified: 0 upsell refs in cart-drawer, drawer schema valid, group-overlay.json valid (blocks: cart-message, title, free-shipping, products, checkout-buttons), cart.liquid untouched.

## Dead-code cleanup #4 (part 5): investigated the four "verify" settings — no changes yet, findings documented
- **header.liquid (padding_top/padding_bottom/show_separator_line):** the body computes header heights and CSS vars from same-named **bare Liquid variables that are never assigned from settings** (nil) — `--PT: {{ padding_top }}px` renders invalid `--PT: px`, and the separator toggle is permanently falsy. Stored values: padding 15/15, separator false. Wiring padding would suddenly apply 15px top/bottom to the **live sitewide header** (visible) → deferred to a dev-store QA session; `show_separator_line` alone is safely wireable (stored false → no change).
- **cart-drawer.liquid (upsell_auto_open):** the machinery is complete — `snippets/cart-block-upsell.liquid` reads the setting and emits `data-upsell-auto-open`, and theme.js has a component consuming it — but only **cart.liquid** renders that snippet; the drawer never does. Stored value is **true**: the merchant expected auto-opening upsells in the drawer and the feature is silently dead. Restoring = live drawer behavior change → deferred to the stock-infra pass with dev testing.
- **product.liquid + custom-product-waitlist (upsell.upsell_product_list):** **phantom block types** — neither file has a `when 'upsell'` case at all (product-new and coming-soon kept theirs), yet upsell blocks are saved on product.json, fastbundle, free-products, lipstick, and waitlist templates — all invisible. Wiring means porting the upsell case from product-new, which would make upsell UI *appear* on live PDPs → a feature-restoration decision for the deferred PDP pass.

## Dead-code cleanup #5: dead in-file CSS classes removed (3 files)
Re-verified every audit candidate against the current code first — the original "5 files / 52 classes" became **3 files** after earlier work: `video-gallery-youtube.liquid` was deleted (its 16 gone with it) and `custom-product-slider`'s 5 tab classes went **live** in the collection-tabs merge.

- **sections/video-gallery.liquid (1,458 → 939 lines):** full defs-vs-markup scan found **38 dead classes** (audit had 16) — removing the commented spotlight/tab markup in cleanup #3 orphaned the entire spotlight ecosystem (product card/price/options, add-to-cart + view-product buttons, quote, tab-navigation) on top of the known old-button family and the parallel `bottom-hero` style set. Removed 76 rules block-aware (comma-lists kept live parts; one emptied `@media (1024px…769px)` dropped). Also removed the `video-gallery-video-thumbnail-large` CSS + its no-op spotlight click-handler JS (`querySelectorAll` on markup that no longer exists) and two stale narration comments. Verified: braces/tags/schema balanced, re-scan = 0 dead / 24 alive classes, live bottom-hero untouched (it reuses the `hero-*` classes).
- **snippets/cw-pdp-css.liquid (807 → 568 lines):** correction — the suspected "duplicated chunk" is NOT a paste error; it's base styles + a normal mobile `@media` override block. No de-dupe needed. Removed dead (base + mobile copies): the whole `cw-bb-wearing-*` family (9 classes, removed BB-Crème "wearing" section), `cw-bb-switched-review-box`, `img.cw-color-child`, `cta--secondary` (no element carries it; only `cta--solid` exists), `cw-bb-pvideo-wrapper`/`-item` + the snippet's bottom `<script>` play/pause handler (the pvideo grid is now a Channelize app embed — `-section`/`-title` kept, they're live), and the **whole custom-qty family** (`custom-qty-wrapper`, `qty-btn`, `qty-minus`, `qty-plus`, `custom-qty-input` + webkit spin rules) — its companion JS in coming-soon provably bails out (the null-guard added earlier; zero markup anywhere). KEPT everything app/runtime/dynamic: `pmslider-*` (GG Slider), `wishlist-*` (SE Wishlist), `swiper-rtl`/`swiper-slide-thumb-active` (Swiper runtime), `template-product` (dynamic body class), `product__price`, `product__block--lines`/`--divider`, `product-upsell__price` (Liquid-conditional class attrs the scanner can't see).
- **sections/footer.liquid:** removed the 2 `.footer-group` rules (lines 28-29). Section is dormant stock anyway (group-footer render is commented out of theme.liquid; custom-footer is live).
- **Active status:** video-gallery is live on the Videos pages (`page.video-gallery` + `page.video-gallery-youtube` templates); cw-pdp-css loads only on the Coming Soon PDP (`product.coming-soon.json`); footer.liquid renders nowhere.
- Synced all three files to shopify-dev.
- Flagged for PDP pass: the no-op custom-qty/ATC `<script>` block in `custom-product-coming-soon.liquid` (~line 1413) — its CSS is now gone too; the whole feature was never wired on this template.
