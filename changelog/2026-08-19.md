# 2026-08-19

- **Hero banner (homepage-style-2) dial-in (DEV ONLY, custom-hero-banner.liquid):** left column vertically centered (parent stretch + flex column center); **root-cause fix for images not reaching the section bottom** — image_tag's width/height attributes kept the attribute height while CSS shrank width, distorting the box and letterboxing the artwork (object-fit contain centered it with empty space below); fixed with `height: auto` + `display: block` on both right images (mobile already had it, which is why mobile looked fine); products image hang: desktop bottom -56px, mobile -25px (was -6%/-12%), values tuned via pixel-measuring the artwork gaps (lipsticks ~12-21px above the file edge, compact ~6px); star rating stacked onto its own line at ≤1024px (tablet+mobile), inline on desktop; feature icons height 80px; `<br>` stripped from feature labels at render + cleaned from stored data in index.json AND index.homepage-style-2.json. Randell made his own concurrent edits in the file (label width:auto, mobile margins) — preserved. Also: `shopify theme dev` wedged on my editor's .tmp files mid-save (known bug, restart-dev-on-tmp-files) — Randell restarted. Dev-only: custom-hero-banner.liquid + both index templates differ from shopify-live pending his ship/hold decision.

- **Footer: removed invented service-hours line (both folders):** `custom-footer.liquid`'s `hours_text` setting had a made-up default "Monday–Friday, 9am–5pm ET" (I had borrowed Randell's working hours when building the footer designs — never real customer-service hours). Default removed from the schema; no stored value existed anywhere, so the contact card now renders no hours line. Setting kept (blank) so real hours can be typed in the editor later. Mirrored live↔dev; no CSS/minify impact.
- **Footer Design 2 spacing (both folders):** phone→social gap was doubled (card's `.nbf__social` margin-top 14px + the inner `ul.social`'s own 14px) — inner ul margin zeroed in the d2 card, wrapper set to 8px (~34px → ~14px visual gap, tighter now that the hours line is gone); Help & Policies link rows tightened 8px → 5px top/bottom padding (16px → 10px between rows). Styles are scoped in `custom-footer.liquid`'s `{% style %}` block, no asset minify needed.

- **Folder sync check (morning):** verified shopify-live and shopify-dev are byte-identical — 378 files each, all theme files matching by hash. Only difference was `.claude/settings.local.json` (Claude Code permission cache, not a theme file, never pushed).
- **Sale/badge box redesign round 1 (DEV ONLY):** per Randell — removed the fixed 58px (desktop) / 40px (mobile ≤607px) circle width+height from `.product-item .badge-box/.sale-box` in main.css; border-radius 50% → 2px; boxes now auto-size to their text (padding 6px 10px, max-width caps at card edge); mobile font 8px → 10px since text no longer crams into a 40px circle. NEW-item gold badge: `badge-box--gold` class added in product-grid-item.liquid when the badge text contains "new" or "launch" (covers New/NEW/New!/Just Launched/Just Launched!, and also "New Clean Formula" — flagged to Randell); gold linear-gradient (#C99A3A→#E9C568→#F9ECC0), dark-gold text #4A3A10, 1px gold border + soft gold glow shadow, slow 7s background-position sheen loop (subtle, disabled under prefers-reduced-motion). `!important` on gold bg/color needed to beat theme.css:12520's `!important` pink badge colors. Re-minified. Color recommendations for the other badge messages presented to Randell, NOT yet applied.
- **Sale/badge box round 2 — 65px circle survivor (DEV ONLY):** height was still applying — theme.css (not main.css) had a legacy duplicate block near its end (~line 24136) with `height/width: 65px !important` + `border-radius: 50%` + `font-size: 10px !important`, outranking the main.css redesign. Geometry block deleted (main.css now sole owner); the `.preorder-box, .badge-box` COLOR block below it (#f981a0 bg / white text — the actual current badge colors) deliberately kept pending Randell's palette decision. theme.min.css regenerated; KAN-197 payload now includes theme.css.
- **Sale/badge box round 3 (DEV ONLY):** font-weight 300 → 600 on the shared `.product-item .badge-box/.sale-box` block; gold badge's own font-weight: 500 removed (now inherits 600). Full cascade audit performed — every property from today's redesign confirmed winning (details in session): geometry/radius/padding owned by main.css:1731 unopposed; badge colors #f981a0/white from theme.css end-block (last !important wins); gold overrides all via higher-specificity !important; NOTE pre-existing `.product-item__image .sale-box` rule (main.css:1776) makes the SALE box render beige #F7F2ED/#46413C (later same-specificity rule beats the #FCC9C6 line) — pre-existing behavior, left as-is, flagged to Randell with the color decisions.
  - New section `shopify-dev/sections/custom-collections-picker.liquid` ("[Custom] Scroll picker" — first attempt "[Custom] Collections picker" was rejected on upload: schema names max 25 chars). Renders a fixed nav on the left of the screen, vertically centered, that follows scroll. JS auto-detects every `section.index-products` with a `.grid__heading` on the page (the stacked section-collection blocks: EYES, LASHES, LIPS, face, BRUSHES & BUNDLES, EYEBROWS) — no manual list to maintain; new/removed collection sections are picked up automatically. Gift-card section-double is ignored (not a collection section).
  - Active detection: the section spanning the viewport's vertical center is "current" — its picker title goes bold #000; the others are gray (#757575) with opacity fading further with distance from the current item (0.55 → 0.15 floor).
  - Titles are clickable: smooth-scrolls to that collection, offset by `--header-height` (respects prefers-reduced-motion → instant jump).
  - Enable/disable checkbox in the section settings, default ON; the section can also be hidden/removed in the editor. Added to `templates/list-collections.json` (after "main"), so it exists only on the /collections page.
  - Accessibility: `<nav aria-label="Collections on this page">`, `aria-current="true"` on the active link, hover/focus-visible forces full opacity + black so faded items stay usable, focus outline inherited from theme.
  - CSS appended to `shopify-dev/assets/main.css` (`.collections-picker*` block); hidden below 1200px viewport width (would overlap product grid / mobile); re-minified via `node scripts/minify-assets.mjs`.
  - Round 2 (Randell approved the feature, asked for smoother/more animated transitions): added a 3px black marker bar that glides (0.5s cubic-bezier) alongside the list to the active title; color/fade transitions lengthened 0.25s → 0.5s; active title now slides 4px right; marker + slide disabled under prefers-reduced-motion; resize now forces a re-measure so the marker stays aligned. Marker sits outside the `<ul>` (valid HTML, survives list re-render), positioned via offsetTop against the fixed nav.
  - Initially dev-only per Randell's instruction; superseded by the end-of-day mirror (see below).
- **Badge color system built + FULL DEV→LIVE MIRROR (Randell: "yes build it that way on the dev. and then make sure you bring over all of the changes we made so far to -live"):**
  - Keyword→class mapping in product-grid-item.liquid replaces the gold-only flag: badge text contains "new"/"launch" → `badge-box--gold`; "best seller" → `badge-box--dark` (#000 bg/#fff text); "matte"/"cream formula" → `badge-box--neutral` (#F7F2ED/#46413C); unmatched (incl. "Featured in OK! Magazine") keeps the default pink #f981a0/white from theme.css. `!important` on dark/neutral to beat theme.css's badge recolor block.
  - Sale box unmuted — now driven by the theme setting (Theme settings → Sale badges Background/Text → `--COLOR-SALE-BG`/`--COLOR-SALE-TEXT`): removed the beige `.product-item__image .sale-box` hardcode, added `color: var(--COLOR-SALE-TEXT, #AF7B88)` rule. ⚠️ Visible change at go-live: card Sale boxes switch beige #F7F2ED/#46413C → settings values #fcc9c6/#af7b88; retint anytime in the editor, no code.
  - Whole day's dev work copied → shopify-live (7 files: assets/main.css + main.min.css + theme.css + theme.min.css, sections/custom-collections-picker.liquid, snippets/product-grid-item.liquid, templates/list-collections.json); folders re-verified byte-identical (only `.claude/settings.local.json` differs, never pushed). Collections picker + badge/sale redesign now ride the Friday 8/21 push (KAN-195). KAN-196/197 stay open for post-push live verification only.
- **Badge/sale refinements (both folders, mirrored + verified identical):** gold badge box-shadow removed (kept 1px gold border + sheen); gold keyword narrowed to "new" only per Randell — "Just Launched" badges (Fleur Lipstick #34, Dream Melt Lip Mask, No-Crease Hair Clips) now fall to default pink; sale badge color changed in settings_data.json current block: `sale_bg_color` #fcc9c6 → **#e6247f** (deep brand pink from the free-shipping gradient family), `sale_text_color` #af7b88 → **#ffffff**. NOTE: settings_data.json is now part of the push payload delta — the theme-editor Sale badges setting on live will reflect this after Friday's push; retint in editor anytime.
- **Badge cursor (both folders):** `cursor: default` added to the shared `.product-item .badge-box/.sale-box` block so hovering a badge shows the normal arrow instead of the pointer inherited from the card's clickable area. Minified, mirrored, full folder hash-check: byte-identical.
- **Badge dead-click fix (both folders):** `cursor: default` replaced with `pointer-events: none` on the shared badge/sale-box block — clicks on a badge now fall through to the product-image link underneath (badges render after the `</a>` inside `.product-item__image`, so they were swallowing clicks). Cursor over a badge is now the pointer from the link beneath, which is the honest signal since clicking navigates. Minified, mirrored, folders re-verified byte-identical.
- **Badge WCAG/SEO audit:** structurally clean — real DOM text (crawlable/SR-readable), spans are the right element (non-interactive, duplicate of the link), outside the anchor so link text stays the product title, sheen + picker respect prefers-reduced-motion, uppercase via CSS not content. Contrast ratios measured but NOT acted on per Randell ("do not worry about contrasts"): gold 4.29–9.34:1 across gradient stops, dark 21:1, neutral 9.07:1, default pink 2.42:1, sale 4.27:1.
- **Jira closeouts:** KAN-196, KAN-197, KAN-155 (Aug 6 badge-redesign backlog task — completed by today's work; badge content sources documented in its OUTCOME), KAN-156 (sale badge color — closed with deviation note: #e6247f chosen over matching #d34270 card price) all Done with OUTCOME records; KAN-195 runbook updated with payload additions + Thursday settings deltas; KAN-141's sale-badge #fcc9c6 editor step marked superseded.
