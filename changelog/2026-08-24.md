# 2026-08-24

- **Pre-redesign page backups (read-only on live; Randell updates the pages himself after):** captured the full live body HTML of 4 pages via Admin GraphQL and preserved each in TWO places — a closed Jira task with the code in comment(s), and a local file under `backups/live-pages-2026-08-24/`:
  - KAN-198 — Store Return Policy (`/pages/policy`, last edited 2025-01-23)
  - KAN-199 — Shipping Policy (`/pages/shopping-terms`, last edited 2026-07-06; contains "Fresh Beauty Studio" old-brand mention)
  - KAN-200 — Privacy Policy (`/pages/privacy-policy`, last edited 2025-01-23; contains "Fresh Beauty Studio/NiKOL Cosmetics" mentions)
  - KAN-201 — FAQ Rewards Program (`/pages/faq-rewards-program`, last edited 2026-07-27 — note this live version is ALREADY the restyled house-style page; backed up in 2 comment parts, local file is the authoritative byte-exact copy)
  - Note: the Jira connector cannot upload file attachments, so "attachments" = code in task comments (agreed approach). All 4 tasks created under KAN-4 with label `backup` and closed immediately as recovery records.
- **DEV-store /pages/policy hero copy professionalized (write on DEV store, Randell-directed; store identity verified before write):** "The short version: … all sales are final — but if something arrives damaged, we will make it right. The details are below." → "**At a glance:** because our products are personal-care items, all sales are final. However, if your order arrives damaged, we will gladly make it right. Full details are provided below." Only the hero paragraph changed; rest of page byte-identical. This is part of the page code Randell will copy to live (KAN-194-adjacent). Round 2 (Randell: first version could incentivize damaging products for returns): hero now reads "…all sales are final. If your order is damaged in transit, please report it within 48 hours of delivery — photos included — and our team will review your claim and resolve it promptly." — guardrails baked into the sentence ("in transit" = carrier damage only, 48h window, photo evidence, "review your claim" instead of an unconditional promise).
- **Em-dash sweep across the 4 restyled DEV-store pages (per Randell):** policy — hero "— photos included —" reworded to "report it with photos within 48 hours of delivery," and JSON-LD "exchanges — all sales are final" → semicolon; shipping — hero "Good news — your order…" → "Good news: your order…", stats pill "3–5 business days" (en-dash) → "3-5"; privacy + FAQ pages verified clean, no dashes found. Normal hyphens inside compound words (personal-care, 2-Business-Day, opt-in) intentionally kept.
- **[Custom] Info Banner image fit controls (both theme folders):** two new Media-block settings — "Image fit (desktop)": Fill/crop (cover, the old hardcoded behavior, still default), Fit (contain), Stretch (fill), Natural (height:auto — image sets the media column height, no crop/letterbox); "Image focal position": center/top/bottom/left/right via object-position. Values read from the FIRST Media block like the other layout settings; covers single image AND image-slider variants (shared `.custom-info-banner__image img` hook); mobile untouched (already renders natural). Existing instances unchanged (default = old behavior). Schema JSON validated, mirrored to shopify-live. Follow-up (Randell spotted duplicate identical images): the mobile-image fallback used to render the SAME desktop image twice (hidden one still downloads — double full-res fetch per banner). Now: only-desktop-uploaded → ONE img shown at all sizes; both uploaded → the original two-img desktop/mobile swap; only-mobile → that one everywhere. Applies to single-image + slider variants, both folders. BUG FIX round: the fit CSS was emitted UNSCOPED by each of the 4 instances → last banner's value won globally (Randell: "none of these settings do anything") — moved into the per-instance `#InfoBanner-{id}` scoped style block (desktop media query); base rule back to plain cover default. Natural mode reworked: media column switches to static/grid-flow so the image's own height can grow the banner (default absolute positioning pins media to content height by design). Expectation note: on the current images, Fill-vs-Fit differences are modest because the media boxes are close to the images' natural aspect (biggest on banner 2: box 1.46 vs image 1.30). Also: title/description briefly made fluid like the hero, then REVERTED per Randell (removed for mobile+tablet) — final state after 3 rounds: title 62px/-3px and description 20px/140% at ALL widths (all stacked-block size overrides removed, then the description's 350px max-width + auto-centering removed too — stacked descriptions now span the full content width, text still centered via the stacked block's text-align); round 4: PHONE (≤600) type sizes re-added to match custom-videos — title 34px, description theme-base/135% (tablet stays full 62px/20px); all 4 instances' first media blocks set to media_fit "contain" in index.homepage-style-2.json per Randell; SEO/WCAG audit (contrast excluded) with fixes: min-read BUG (chars÷200 ≈ 5× inflated read times, and 0 for short posts) → chars÷1100 with 1-min floor; blog cards' triple same-URL links reduced (image link aria-hidden + tabindex -1, alt moved off it since the h3 link is the accessible path); "Read more" links get sr-only article titles + aria-hidden arrow char; media images: new per-block "Image alt text" setting wired to all variants, loading=lazy added, img_url master → 1400x/1000x (blog 900x, bg 2000x — SEO/page-weight); empty bg_image no longer emits invalid `url()`; section-scoped sr-only utility class added. Passing already: h2/h3 structure, decorative star image + adjacent text, real anchors, Swiper's own pagination a11y.
- **[Custom] Info Banner fluid type (both folders, Randell chose fluid over fixed):** title `62px` → `clamp(34px, 4vw + 4px, 62px)` (Randell: match custom-steps-vertical__title exactly; first pass used a steeper 6.59vw slope reaching 62px at 1025px — replaced) and description `20px` → `clamp(var(--FONT-SIZE-BASE), 0.47vw + 15.2px, 20px)` — calibrated to hit the 34px/base floors at 600px and reach full 62px/20px exactly at the 1025px un-stack breakpoint, so desktop is unchanged and the old 62→34 snap at 600px is gone (600→34px, 768→45px, 1024→62px). The ≤600 phone block's font-size overrides removed as redundant (clamp floors supply them); its description line-height 135% kept. Probe-verified pre-change state at 375px was already 34/18 (phones matched all along — the mismatch was the stacked 601-1024 range). Post-change browser verification blocked: dev server wedged mid-session ("Failed to Upload Theme Files") — needs Randell restart.
- **[Custom] Customer Reviews SVG stars (both folders + both templates):** per-review "Stars Image" upload replaced with the hero banner's partial-fill SVG stars (identical star path/geometry, gray #DDD6CF base + #FB6D00 overlay clipped by width%); new per-block "Star rating" range setting (0-5, 0.1 steps, default 5, 0 hides the row) drives the fill; sizes match the old images (125px desktop / 105px phone); sr-only "Rated X out of 5 stars" added for screen readers (the old img had alt=""); `stars` image_picker removed from schema and the orphaned image values stripped from all 7 review blocks in index.homepage-style-2.json (both folders; blocks default to full 5 stars — matches how the old images looked). Note for live sync: existing blocks will render 5.0 filled stars until a rating is typed per block in the editor.
- **[Custom] FAQ interactivity (both folders):** reveal family — one trigger at 15%, title → subtitle → each FAQ row cascade (22px rise / 0.6s, 0.12s stagger CAPPED at 0.96s since the section allows 20 blocks); trigger hover: +/- icon scales 1.2 (hover-gated; rows are buttons so hover is honest). PLUS animated accordion open/close (was an instant hidden-attr snap): answers wrapped in a `__collapse` grid (0fr→1fr, 0.4s ease); `hidden` attr replaced by `visibility: hidden` on collapsed answers with a 0.4s-delayed visibility transition so they leave the tab order/AT tree after the close animation — collapse transitions gated behind `.anim-ready` too, so reduced-motion / animations-off / no-JS users keep the instant toggle; JS no longer toggles `panel.hidden`, only `is-open` + aria-expanded (accordion still works without anim-ready). No-JS state unchanged: first item open, rest closed.
- **[Custom] FAQ fluid type (both folders):** title `56px` → `clamp(43px, 4vw + 4px, 56px)` — floor corrected 34→43 per Randell: the FAQ's neighbors (reviews + coll. banners titles) ride Broadcast's heading-size-11 scale = 43px phone / 48 tablet / 56 desktop, unlike the 34px-floor family (videos/benefits/steps/info banner); desktop cap stays the FAQ's existing 56px (full size from ~1300px) and subtitle `20px` → `clamp(var(--FONT-SIZE-BASE), 0.47vw + 15.2px, 20px)` (same recipe as the info banner description); redundant ≤600 font-size overrides for both removed (the clamp floors supply them).
- **Image-weight fixes from the Lighthouse mobile audit (both folders; mobile perf 72, ~968KB flagged "properly size images"):** the whale was NOT the hero — the Customer Reviews product strips served `img_url: 'master'` for product images rendered 70px tall (3wellBallerinaopen.png alone = 657KB fully wasted) → product image '200x', buyer icon '220x', cart icon '70x' (≈820KB of the waste). Hero: image1 widths gained an 800 step (mobile DPR 1.75 was jumping 700→900), image2 mobile `sizes` corrected 290px→262px (matches its clamp floor) + 460 width step (~50KB). Info Banner: all 8 img variants got srcset (desktop-pair 700/1000/1400 @ sizes 50vw, mobile-pair 500/750/1000 @ 100vw, single-shown-everywhere 600/900/1400 @ 50vw/100vw — was flat 1400x, mkd1 = 41KB mobile waste). Steps Vertical: image `sizes` '470px' → '(max-width: 600px) 100vw, 470px'. Expected recovery ≈930KB of the 968. Lighthouse baseline (dev store, password via cookie, local Chromium): mobile 72/96/79/92, desktop 98/96/78/92; LCP mobile 6.1s (hero section) — re-audit pending dev-server restart (watcher wedged again mid-batch; earlier-day files confirmed synced, this batch not picked up).
- **[Custom] Info Banner button arrows (both folders):** the standard 22×13 arrow SVG added to the Button block's anchor — the last arrowless CTA on homepage-style-2 (the morning's "dormant" arrow-slide hover rule now activates; anchor already had gap 5px). Button-arrow audit while at it: hero/videos/steps-vertical/newsletter/product-slider have arrows, coll. banners' SHOP NOW has its own smaller arrow; still arrowless elsewhere: custom-steps-horizontal + custom-icon-features (both ACTIVE on product.coming-soon.json) and custom-feature-hero (dormant, placed nowhere) — left untouched pending Randell (different page, not reviewed today).
- **[Custom] Info Banner SVG note icon (both folders + both templates):** the Icon Note block's uploaded clock image (`Group.png`, 45×45, used once — quiz banner "Takes 1 minute") replaced with a hand-crafted inline SVG (ring r=20 stroke-3 + hands path, #2A2724 — potrace produced a filled disc on this line icon, so crafted geometry instead; side-by-side render-verified at 25px and 100px, crafted version crisper than the source). Image picker → "Show clock icon" checkbox (default on); quiz block converted in both templates; CSS max-width rules → explicit svg widths (25px desktop / 20px ≤1024) + flex-shrink 0. Note: the SVG is the clock glyph specifically — if a future note needs a different icon, the block needs a new option rather than an upload.
- **Section-title unification across homepage-style-2 (both folders, Randell: "make sure the other titles match"):** every custom section H2 now follows one pattern — full desktop size held down to 601px, 34px at ≤600px. Fixed the four outliers: benefits + coll. banners + reviews titles pinned to explicit 56px (they rode Broadcast's heading-size-11 stepped scale = 43px <750 / 48px 750-989 / 56px ≥990; coll. banners + reviews also lacked any phone size → now 34px, superseding the earlier "43px" FAQ-floor rationale); FAQ title clamp(43,4vw+4,56) → static 56px + 34px phone. Already conforming: videos, steps heading, benefits phone, newsletter, product slider (56/34) and info banner (62/34, its design size). Hero h1 untouched (deliberately fluid per its own dial-in). FAQ/banner description clamps (18→20px) kept — visually uniform with the static 20px descriptions.
- **[Custom] Info Banner title fluid REVERTED (both folders, Randell):** the morning's `clamp(34px, 4vw + 4px, 62px)` made the banner title shrink through tablet while every other section heading on the page holds full size until 600px — reverted to `62px` base + ≤600px `34px` override (the majority pattern: videos/steps-heading/benefits/newsletter). The description keeps its subtle clamp (18→20px). Net vs. yesterday: title behavior is back to the pre-fluid state.
- **[Custom] Steps Vertical tablet spacing (both folders):** new 601-1024px block drops `__box` margin-top 110px → 40px (tablet was inheriting the desktop 110px + the parent's 40px gap = 150px between boxes; now 80px; mobile stays 10px + 20px gap).
- **[Custom] Newsletter interactivity (both folders):** reveal family — one trigger at 15%, title → input → button → note cascade (22px rise / 0.6s, 0.12s stagger); EXTRA guard: reveal is skipped entirely when a success/error `__message` is present (post-submit reload autofocuses the message — animation would delay it); subscribe button got the site-convention hover (BRIGHTER bg 0.3s + arrow slide 5px, hover-gated, focus-visible); email input gained a visible focus state (`box-shadow: 0 0 0 1px #fff` — it had `outline: none` with no replacement, a keyboard-visibility gap); section tag gained `id="Newsletter-{{ section.id }}"`. Same guards (anim-ready gating, reduced-motion, animations-off, no-JS safe).
- **[Custom] Info Banner SVG rating stars (both folders + both templates):** the Rating block's "Star Image" picker replaced with the hero-style partial-fill SVG (same star path, #DDD6CF base + #FB6D00 fill clipped by width%) + new "Star rating" range setting (0-5, 0.1 steps, default 5, 0 hides stars — same UI as the reviews blocks); sizes preserved (95px desktop / 78px mobile from the old img rules); sr-only rating text added. The one configured instance (blog banner bPCMH4, Stars_Container.png, text "4.76/5") converted to star_rating 4.8 in both templates (nearest 0.1 step; fill delta <0.5px). Hero note: hero's own rating uses a free-text setting holding 4.76 — the two render identically, differing only in input UI.
- **[Custom] Customer Reviews uniform mobile cards (both folders):** ≤600px slides get the Swiper equal-height recipe (`height: auto; display: flex; flex-direction: column`) — every card stretches to the tallest slide so the height no longer jumps while swiping — and `.custom-customer-reviews__product` gets `margin-top: auto` pinning the product strip to the card bottom at a consistent position across all reviews. Note: "Show more" expansion still regrows the deck to the expanded height (wrapper recalculates) — inherent to equal heights.
- **[Custom] Customer Reviews interactivity (both folders):** same reveal family — one trigger at 15% visibility, title → subtitle → the WHOLE swiper carousel as one unit (22px rise / 0.6s, 0.12s stagger); individual slides deliberately NOT animated (Swiper loop clones + wrapper transforms make per-slide reveals unreliable). Honest hovers on the clickable parts: nav circle arrows scale 1.08 / 0.3s, product-link strip background eases #F8F3EF → #F1E8DF (hover-gated; existing focus-visible outlines already covered keyboard). Same guards (anim-ready gating, reduced-motion, animations-off, no-JS safe).
- **[Custom] Customer Reviews SVG quote icon (both folders + both templates):** the per-block "Quote Icon" image (`left-quote_1_2.png`, one shared 49×48 CDN image across all 7 blocks) replaced with an inline traced SVG (potrace pipeline: alpha-mask ≥200 → 4× bilinear upscale → trace, optTolerance 1.4/alphaMax 1.2; fill #2A2724 = the PNG's dominant color; side-by-side render-verified pixel-identical at the 25px display size — enlarged-view edge lumps exist from the tiny source but never render). Image picker → "Show quote icon" checkbox (default on); template blocks converted (quote_icon deleted, show_quote_icon:true ×7, both folders). CSS img rule → svg width 25px.
- **[Custom] Coll. Banners interactivity (both folders):** same reveal family — one trigger at 15% visibility, title → subtitle → each banner box cascade (22px rise / 0.6s, 0.12s JS-set stagger); section tag gained `id="CollBanners-{{ section.id }}"`. Because these boxes ARE links (unlike steps/benefits), hover feedback added: image zooms to 1.05 over 0.6s (`__media` got overflow:hidden to contain it) + SHOP NOW arrow slides 5px, hover-gated to `a.__link` only (linkless div boxes get nothing) with the arrow slide also on `:focus-visible`. Same guards (anim-ready gating, reduced-motion, animations-off).
- **[Custom] Coll. Banners fill-the-row items (both folders):** `.custom-collection-banners__item` `width: 32.3%` → `flex: 1 1 32.3%` — a row with only 2 boxes now grows them to split the full width (previously they stayed third-wide and space-between left a hole in the middle); full 3-box rows are visually unchanged (each grows ~equally to absorb the sub-1% remainder). A lone box in a row goes full-width (the existing `:last-child` content-position overrides anticipate a wide last banner). Mobile block's `width: 100%` → `flex-basis: 100%` to keep overriding the shorthand.
- **[Custom] Benefits interactivity (both folders):** same reveal family — section triggers once at 15% visibility; title → subtitle → each benefit item cascade in left-to-right (22px rise / 0.6s, 0.12s stagger per element set inline by JS); section tag gained `id="Benefits-{{ section.id }}"` for the script hook. Same guards (`.anim-ready`-gated hidden states = no-JS safe, prefers-reduced-motion / IntersectionObserver absent / theme animations off all skip). No hover effects added — the items aren't clickable and there's no button in this section.
- **[Custom] Benefits title mobile size (both folders):** `.custom-benefits__title` pinned to 34px in the section's ≤600px block, matching the phone title size used by custom-videos/info-banner/steps (it previously floated on the theme's `heading-size-11` scale with no explicit mobile size; the section rule wins by source order — section styles render after theme.css at equal specificity).
- **[Custom] Benefits mobile row spacing (both folders):** `.custom-benefits__list` ≤600px gap `0` → `25px 0` — 25px of vertical space between the wrapped 2-up item rows (column gap stays 0; horizontal spread still handled by justify-content: space-between). Also fixed stray indentation on padding-top in that rule.
- **[Custom] Videos mobile height removed (both folders):** deleted the ≤600px `.custom-videos__video { height: 220px }` override — mobile videos now size to their natural aspect ratio like desktop (the desktop-side height was already removed on 08-21; this was the last fixed-height remnant).
- **[Custom] Videos interactivity (both folders):** same reveal pattern — header, each video slide, and the button wrap rise 26px/0.65s individually as they enter view (per-element IntersectionObserver at 0.18, `.anim-ready`-gated, same reduced-motion/no-JS/animations-off guards); button got the site-convention hover (BRIGHTER bg 0.3s + svg arrow slide 5px — this button HAS the arrow, so the slide is live). Deliberately NO scale-settle on the videos: transforming a native `<video>` with visible controls looks glitchy and risks playback repaint jank. Section is on homepage-style-2 (above the makeup info banner) and available on product templates (Channelize coming-soon toggle — app block unaffected, reveal only touches the section's own elements).
- **[Custom] Info Banner interactivity (both folders):** same "alive" treatment as Steps Vertical, adapted to the banner's one-block layout — whole section triggers once at 15% visibility (media + each content block rise 22px/0.6s; content blocks cascade with a 0.12s-per-block stagger set inline by JS; image settles from 1.05 scale over 1.1s); hidden initial states gated behind JS-added `.anim-ready` (no-JS safe), skipped under prefers-reduced-motion / IntersectionObserver absent / theme animations disabled; button got the site-convention hover (BRIGHTER bg 0.3s + svg arrow slide 5px, hover-gated, focus-visible). Note: banner buttons currently have no svg arrows, so the arrow rule is dormant future-proofing; bg transition is the visible effect.
- **[Custom] Steps Vertical (both folders):** title font-size → hero clamp(34px, 4vw+4px, 62px) (was fixed 56px; old 26px phone override removed — phones now 34px floor, flagged); interactivity pass — scroll-in reveal per step (box rises 26px/0.65s as it enters view, content follows with 0.2s stagger, image settles from 1.05 scale; header + CTA reveal too) via section-local IntersectionObserver, hidden initial states gated behind JS-added `.anim-ready` (no-JS safe), skipped under prefers-reduced-motion and when theme animations are disabled; CTA button got the site-convention hover (BRIGHTER bg 0.3s + arrow slide 5px, hover-gated, focus-visible). Deliberately NO hover effects on the step boxes — they aren't clickable and false affordances would mislead. and the STACK BREAKPOINT raised 600→1024px — the section now stacks single-column on tablets too (two-column split, media width, and fit controls apply only >1024px; schema info texts updated to match).
