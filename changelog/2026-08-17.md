# 2026-08-17 (Monday)

- **Live Sync task KAN-194 created** (KAN-119, live-sync, ~45m): step-by-step replication of today's admin PAGE CONTENT work to live — Beauty Reinvented (body paste + template→Default + SEO fields + verify, THEN template file deletion stages for a push) and Press (body paste + fix dangling template suffix + SEO fields + verify). Explicitly notes the KAN-31 truck is separate (theme code + settings string).

- **Fierce Aging teardown complete (Randell):** discount expiry verified/archived, Roses re-enabled in Yotpo, sale visuals removed (hero, popup, mobile app banner + announcement bar). KAN-7 + all 10 subtasks closed with OUTCOME records. NOTE: the teardown was live theme-editor settings edits, so live settings no longer match the 8/12 sale-state mirror in shopify-live/shopify-dev — re-pull live settings before the next settings-dependent work (code-only pushes unaffected).
- **Beauty Reinvented dial-in round 1 (KAN-26, app-dev store page content — no theme files touched):** (1) hero title `<p>` → `<h1 class="br-hero__title">` — now the page's single h1 (Default page template has `show_title: false`, so no duplicate) and it picks up the theme heading font exactly like the live template's h1 (this was also the font-weight mismatch Randell saw); (2) page content left-aligned (`text-align: left` on `.br-page` + `.br-hero__text`) countering the Default template's `align_text: "text-center"` — matches the live template's rendering; CTA keeps its own centering; (3) **invisible-buttons root cause:** page content renders inside `.rte`, and theme.css:9645 `.rte a, p a { display: inline; padding: 0 0 5px; background: linear-gradient(currentColor…) 100% 1px }` beat `.br-btn` on specificity — flattened the button and replaced the pink fill with a 1px underline → white text on white page. Fixed with `.br-page a.br-btn` (higher specificity) countering display/padding/background/line-height, + hover rule; (4) same guard on the book-cover link (`.br-hero__cover a { display:block; padding:0; background:none }`). Textbook check-global-element-styles case — the live template's markup sat outside `.rte`, which is why none of this bit before the migration.
- **Beauty Reinvented dial-in round 2 (KAN-26):** FAQ reverted from the `<details>/<summary>` accordion to the live template's static always-open list (`.br-faq__item` > `.br-faq__q`/`.br-faq__a`, exact live markup + CSS) per Randell — no disclosure arrows, all answers visible. FAQPage JSON-LD unaffected.
- **Press page WCAG/SEO/practices pass (app-dev store page content; Randell scoped OUT contrast fixes + image re-hosting):** card titles h3→h2 (were skipping h1→h3; theme h2/h3 share the heading font so zero visual change, scoped `.press-grid .press-card__title` beats `.rte` rules); all 9 new-tab links + visually-hidden "(opens in new tab)" + aria-hidden arrows; countered `.rte a` leaks on card links (stray 1px underline + phantom padding); ItemList JSON-LD dangling `@id` refs (#organization/#nikol-johnson) replaced with inline Organization/Person objects; SEO listing set (title_tag "Press & Media Features | Nikol Beauty" + 146ch description_tag); all inline card styles consolidated into classes (identical values incl. the failing #fcc9c6 button + #999 labels — explicitly left per Randell); imgs + loading="lazy"; dangling templateSuffix "page" (no such template file — was falling back silently) cleared to Default. NOT done per Randell: READ MORE button/label contrast, re-hosting the 9 hotlinked external images (rot/perf risk documented).
- **Press cards redesign round 2 (Randell-directed):** READ MORE buttons now carry the THEME's button classes (`btn btn--primary btn--solid btn--small` — settings-driven vars, supersedes the old custom colors, fixes the failed-contrast button as a side effect); equal-height cards with the button pinned to the bottom (card+link+body flexed columns, `margin-top:auto`); grid gaps 20px→32px/24px; responsive ramp smoothed 1→3→4 to **1→2 (480px)→3 (768px)→4 (1024px)**. First mutation attempt died on a socket drop — verified NOT applied (updatedAt unchanged) before retrying; retry landed clean.
- **Press cards round 3 (Randell-directed):** READ MORE reverted from button to a plain arrow LINK (brand `var(--COLOR-PRIMARY)` fallback #b0006e, underlines on card hover), still bottom-pinned; descriptions clamped to max 4 lines (`-webkit-line-clamp:4` + ellipsis); card titles resized 1rem→1.125rem semibold, line-height 1.35. Round 4: `.press-card` max-width:280px removed (cards fill their grid column, justify-items center→stretch); READ MORE link color → #000. Round 5: card titles matched to product-card titles exactly — `font-size:calc(1rem * var(--FONT-ADJUST-BODY))`, weight 600 (same as `.product-item__title`). Round 6: the size formula matched but the FONT didn't — h2 inherits the heading stack; press titles now use `var(--FONT-STACK-BODY)` + normal letter-spacing/line-height 1.5, the full product-title recipe (20px desktop, auto-scales on mobile with the root font like product cards). Round 7 — the REAL product-title recipe is a main.css store customization (main.css:1740, from the Aug 4-7 card system work), not the theme default: press titles now `'Cormorant' 20px uppercase weight 600` at ALL breakpoints, matching `.product-item__title` exactly.
- **Beauty Reinvented WCAG/SEO pass (KAN-26, Randell approved all but contrast):** FAQ questions p→`<h3 class="br-faq__q">` (WCAG 1.3.1 + heading-navigable; theme h3 styles countered via `.br-page .br-faq__q` — font-family inherit, weight/size/margins restated, pixel-identical); FAQPage JSON-LD synced to ALL 8 visible Q&As with full answer text (was 5 shortened — Google guideline: markup must match visible content); Book schema + isbn 1726890198 (the ISBN-10 from the Amazon /dp/ URL), bookFormat Paperback, inLanguage en; cover link + `rel="sponsored"` + aria-label naming the Amazon destination; both buy buttons + visually-hidden "(opens in new tab)" (`.br-sr-only`) and aria-hidden on the → arrow; cover img + `fetchpriority="high"` (LCP); page SEO listing set via global.title_tag (56ch) + description_tag (~160ch) metafields — verified stored. DECLINED by Randell: byline #888 contrast fix (3.5:1, AA wants 4.5) — stays as designed. Verified passes recorded: single h1, pink #b0006e 6.9:1, theme is-focused outlines cover keyboard focus.
- **Free-shipping truck in the cart drawer SHIPPED to dev folder (KAN-31, Randell's design):** confirmed the drawer's free-shipping bar is THEME-NATIVE (Broadcast "Free shipping message" block, threshold setting $125 — no app). Additions: (1) `snippets/custom-icon-shipping-truck.liquid` — his truck SVG, embedded `<style>`/classes converted to direct fills (inline SVG styles leak document-wide); (2) `cart-drawer.liquid` — progress bar wrapped in `.free-shipping__track` + `.free-shipping__truck` riding the leading edge; (3) `main.css` — track headroom, truck 44px absolute at `--truck-pos` (clamped 5–95%), theme's scaleX/width bar transitions disabled in the drawer so JS owns the fill; (4) `main.js` — on every `theme:cart-drawer:open` the bar+truck animate 0→cart% together (1.2s ease-out cubic, rAF frame-locked), target captured from theme.js's own percent writes via `theme:cart:load`; `prefers-reduced-motion` jumps straight to the value; mid-animation sync guard. Minified copies regenerated. Cart-page + announcement-bar variants untouched (drawer only, per request). Cart drawer = active on all templates once pushed.
- **Free-shipping truck iteration rounds (KAN-31, all Randell-directed, dev folder):** truck 64px riding directly ON the bar (bottom -10px overlap); anchor moved from center to the FRONT WHEEL (translateX(-87%) — wheel center at ~87% of the SVG) so the front wheel marks the fill edge; track padding-top 35px; message 14px centered; fill = #ff51a0 pink gradient (#ff8ec2→#ff51a0 60%→#e6247f), track back to stock; amount `[data-left-to-spend]` bold #ff51a0; "free shipping" bold/uppercase/#000 via span INSIDE the block message setting (group-overlay.json — ⚠ SETTINGS: needs the same string pasted in the live editor at go-live) + selector lesson: the drawer message element carries BOTH classes, `.drawer__message.free-shipping` compound not descendant. **Success state:** "Congratulations!" bold gold #fab735 + "FREE SHIPPING" black/uppercase/bold, centered — via liquid `replace` AFTER `| t` in cart-drawer.liquid because **Shopify's t filter HTML-escapes locale strings unless the key ends `_html`** (first attempt put spans in en.default.json → rendered as literal text); locale string reverted to plain. **Stale-amount fix:** theme opens the drawer before its cart refresh lands → drive-in now fetches `/cart.js` itself and computes percent vs the $125 limit (currency-rate aware, fallback to old behavior on fetch error). **Trophy finish (his SVG):** both icons render in the truck slot; `paint()` toggles `.show-trophy` at value ≥99.9 so the truck visibly drives to the end THEN swaps to the trophy (centered anchor translateX(-50%) in that state); trophy snippet custom-icon-shipping-trophy.liquid with fills inlined (same SVG-style-leak treatment).
