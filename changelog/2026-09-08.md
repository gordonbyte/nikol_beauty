# 2026-09-08

- **Labor Day promo closed out in Jira:** KAN-8 + all remaining subtasks (KAN-29/39/40/41/48) marked Done with OUTCOME records on Randell's confirmation the sale ran Sep 2–7 and teardown completed; KAN-39/40 noted as closed retroactively (done at launch, never ticked off).
- **Episode-page dead clicks confirmed on the dev store domain (not just localhost)** — the residual Tolstoy carousel init race persists past the data-priority fix; escalated the architecture conversation instead of another patch.
- **tile-embed investigated at source level (no theme changes to episode pages):** read Tolstoy's shipped loader + tiles controller + tile widget renderer. Findings: `tolstoy-product-tile` (tag or class) is PER-ELEMENT — each element carries its own data-publish-id + data-product-id, fetches its own config from `/settings/product-tile`, renders a native inline `<video>` (own play/timebar/mute/captions controls — no player handshake to race), includes product card + variant picker + real add-to-cart CTA, supports `data-priority="high"`, and WRITES `data-status="success|failed"` + `data-tolstoy-rendered` on the element — the observable readiness signal the carousel lacks. Host scans by class OR tag, MutationObserver for late elements, lazy default = 200px-before-viewport, exposed as `window.tolstoyWidgetV2._realLoadWidget`. Also identified their bot detection (`navigator.webdriver` + spoof checks + impossible dimensions) — why headless never renders.
- **tile-embed live experiment set up (dev store only):** created dev-workspace widget "Product Tile TEST — episode layout (DEV)" `271nixpww3rj4` (tile-embed, sources system playlist product_tagged, live ON); new TEST-ONLY files `sections/custom-tolstoy-tile-test.liquid` + `templates/page.tolstoy-tile-test.json` (both folders; schema name initially over Shopify's 25-char limit, shortened to "[Custom] Tile TEST"): three `<tolstoy-product-tile>` elements (bb-cream, eye-primer, mascara — Liquid-resolved dev IDs 8112…) with data-priority=high, plus an on-page status board polling each tile's data-status/readiness timing every 300ms. Pushed to dev store theme #149231566950 (section first, template second per the push-order gotcha) and verified serving via authenticated fetch (3 tiles, real product ids). Test URL: /pages/video-gallery?view=tolstoy-tile-test. DELETE the widget, section, and template when the experiment concludes. Randell to eyeball: render, click-at-0s reliability, add-to-cart (note: theme cart-drawer bridge deliberately absent on the test page — header badge won't update; that's expected). ITERATION (Randell: no videos, "just words"): probing the endpoints revealed there are TWO tile components — `tolstoy-product-tile` (v2, per-element data-status, served by /settings/product-tile) needs a project type the connector can't create (endpoint says "No project found" for our widget), while the connector's "tile-embed" widget is the LEGACY tile (`class="tolstoy-tile"`, config via getWidgetConfig — verified answering with live tile settings incl. tileBorderRadius/playMode). Test page rewritten to the legacy element; status board now reports children/video/readyState (legacy writes no data-status). Pushed + verified 3 legacy tile elements serving. IMPORTANT CAVEAT if legacy tile works: the v2 findings (data-status readiness signal) do NOT apply to the legacy tile — re-evaluate the race-safety question against what actually renders. RESULT (Randell): legacy tiles RENDER and play inline, but are decorative-only — no product info/add-to-cart (confirmed by design: full settings + renderer read; only styling/autoplay/controls knobs), click = play/pause, no hover-play (`tileShouldAutoplay: false`, hover mode belongs to the collection-tile variant). v2 product-tile remains the component with the full shoppable feature set; not provisionable via API or (so far) dashboard — question drafted for the Tolstoy support ticket. Randell's AI-widget-builder attempt (dashboard) produced a product card, not a video widget; a Liquid-variant prompt (with an explicit can-you-access-my-Tolstoy-videos gate) was handed to Randell to retry. Theme-side custom tiles+player (all ingredients proven) is the standing fallback recommendation.
- **More widget-type tests on the dev workspace/store (per Randell):** (1) "Bubble TEST (DEV)" `g1of50z42n3lc` — bubble-feed, live, product_tagged source, self-injects bottom-right on every dev-store page via the app embed (no theme block; 50 videos in feed; config exposes a bubbleCustomCss field with documented tolstoy-bubble-* classes). (2) "Spotlight TEST (DEV)" `k9u4lko99y6th` — plain "embed" type isn't in the connector's enum (dashboard-only if it exists); spotlight created as the nearest creatable type ("one large featured video with the product beside it"), live, product_tagged, rendered via the same tolstoy-carousel element — added to the bottom of the tile test page (pushed + verified serving). Both test widgets + the tile widget/section/template to DELETE when the episode-page architecture decision lands.
