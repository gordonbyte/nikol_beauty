# 2026-08-11 (Tuesday)

- Board sync: overdue carryovers re-dated to today (KAN-150 checklist, KAN-147 badge, KAN-153 PageSpeed).
- **Discount-sync app test session (KAN-148 → DONE)** on nikolbeauty-dev with demo discounts A-F: collection targeting PASS (bundles boundary held, un-apply clean), amount-off both paths PASS ($/item tag + per-variant metafields, exact price math), mixed amount+percent on one product PASS incl. $8-beats-30% escalation (card re-aggregated to promo-amt-up-to-8), per-order guardrail PASS (app refused with clear reason), discounts/delete webhook auto-cleanup PASS. **Scheduled start/end flip DEFERRED to KAN-136** (Randell: test with the production cron post-deploy, not manual syncs). Shopify platform constraint documented: per-item amount discounts require specified items — never "all products" (appliesOnEachItem silently resets on all-items switch). App tunnel debugging en route: dead pinned ngrok URL → plain `shopify app dev` w/ fresh Cloudflare tunnel; router DNS wouldn't resolve new trycloudflare hostname (1.1.1.1 resolves fine — recommended switching machine DNS); root cause was his local site not running first. All 6 demo discounts deleted.
- **SALE WINDOW CORRECTED (Randell):** starts ON the Live Show **Thu Aug 13 8:00 PM ET** → through Sun Aug 16 ending **12:00 AM PST** (= Mon 8/17 3:00 AM ET). KAN-7 retitled/re-dated; KAN-45/KAN-36 → 8/13. **Caught: two scheduled discounts coexisted on live** — the original (still starting Wed midnight = ~2 days early, would've stacked 20%+20% from Thu) + Randell's corrected new one; he deleted the original; verified exactly one remains ("20% OFF National Fierce Aging Day", correct window).
- **PDP thumbnails question (live, read-only):** no bug — Essential Makeup Brush Set had ONE image until this morning (3 more uploaded ~11am; page now renders all 4 thumbs, verified headless). Gallery = GG Slider app rebuilding theme markup client-side. Systemic finding: **9 products have only 1 image** (gift card, foundation brush, 3 bundles, color corrector collection, eye roller, lip mask, hair clips) — photo/content task offered.
- **Apps inventory live vs dev (read-only fingerprinting; Admin API scopes denied):** live-only: GG Slider Gallery (**explains dev/live PDP gallery difference**), Junip, ReStock, CreditsYard, Channelize, Zipify, Rush, Clarity, Axon, eg-auto-add-to-cart, **BestPush (NEW, not in July inventory)**; both: SE Wishlist, Klaviyo, Triple Whale, Easy Variant Images, **ShopMy (NEW)**, Yotpo widgets, GTM, config-security.com (unidentified). Disabled embeds on live: Convert Experiences, Google&YouTube widget, Intelligems, Clarity brandAgents.
- **PageSpeed zoom-ins (read-only, no dev files touched):** Search/cart SEO 69 root-caused = Shopify platform noindex (`x-robots-tag: noindex, nofollow` header on /search; robots.txt disallow on /cart) — intentional, correct, natural ceiling; noted on KAN-180/KAN-182 (scope = performance only). Search desktop "88" re-measured at **96** (LCP 1.0s, TBT 70ms) — pure lab variance, no desktop search problem; mobile 61-67 is the real gap. Deep-run attributed the site-wide offenders with names+costs: render-blocking = theme.min.css 476ms + main.min.css 191ms + swatches.css 144ms + font-settings.css (→ KAN-175 comment); unused JS 166KiB = GTM 64 + swiper-bundle 35 (loads on no-slider pages!) + theme.js 35 + web-pixels 32, plus shopify-xr 25KiB on search (→ KAN-176 comment); unused CSS 56KiB = theme.min.css.
- **Videos page deep-run (KAN-178 comment):** desktop 74 confirmed (TBT 350ms score 50); mobile 61 with **LCP 8.9s scoring 1/100**. Hypothesis corrected: the heavy thumbnails are UPLOADED PNGs from /cdn/shop/files/ (lip_gloss_launch_pic, nikol_oscar, Eyeliener_Trick, SKIN_CARE…), not YouTube thumbs — ~862 KiB waste; fix = image_url + width/srcset for CDN WebP resizing. Render-blocking CSS costs 1,860ms mobile here (theme.min.css 2,679ms — KAN-175 at its worst). Also: slider-prev/next arrows unsized, payload 3.1MB.
- **Videos page perf items 1+2 SHIPPED (KAN-178):** `snippets/yt-video.liquid` — video covers now `image_url: width: 800` + srcset 400-1200w + sizes, `loading` param (default lazy); `sections/videos-featured.liquid` passes `loading: 'eager'` for the featured cover. Pushed scoped `--nodelete` to dev theme #186425934130 and verified rendered markup. **Featured cover 2,315 KiB PNG → 69 KiB WebP.** PSI re-run: desktop **74 → 94** (TBT 350→130ms, LCP 1.7→1.2s); mobile 58 (LCP 8.9→6.8s, weight 3,160→2,395 KiB — remaining mobile bottleneck = KAN-175 CSS chain). Snippet also serves yt-videos-list + video-product sections. NOTE: first push accidentally went to the app-dev store's theme (nikolbeauty-dev-mab1srre #149231566950 — two distinct dev stores; MCP points at app-dev, PSI/theme work targets nikolbeauty-dev.myshopify.com); harmless same-code drift, left in place.
- **PageSpeed epic built (KAN-174, purple):** scripted PSI API sweep of dev store (Randell's new API key after anonymous quota exhausted; script scratchpad/psi-scan.mjs, results psi-results.json). Baselines m/d: product 75/97 · collection 69/95 · home 67/92 · videos 60/89 · about 68/64 (desktop anomaly) · search 61/79 · list-collections 57/87 · cart 52/84 · **blog article 35/65 (TBT 5.4s, 1.2MB unused JS — embeds)**. Systemic: LCP 4.3-8.4s everywhere; ~165KiB unused JS + ~57KiB unused CSS/page. Structure: theme-wide KAN-175 (LCP, High, 8/17) / KAN-176 (unused JS, 8/18) / KAN-177 (unused CSS, 8/19); page tasks KAN-178-185 (blog High 8/17, cart High 8/18, videos 8/19, search+home 8/20, collection+about 8/21, product 8/24); subtasks KAN-186-190. Epic ~19h scheduled post-sale. KAN-153 + KAN-30 closed with pointers. FAQ page 404s on dev (handle differs from live); contact/rewards PSI runs were transient 500s — re-run pending.
