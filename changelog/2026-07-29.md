# Changelog — 2026-07-29

## Full-site JS-error sweep (live store, read-only)
- Crawled all 689 live URLs (sitemap + cart/search/404) in headless Chromium capturing console errors, uncaught exceptions, failed script loads.
- **One genuine theme-fixable error found:** `sections/about-header.liquid` (active on `/pages/about`) references `about-header-bg.png` + `about-header-bg-mobile.png` — neither exists in theme assets or live CDN (404 on every About visit, header bg never renders). Filed as KAN-32.
- Everything else was noise, verified with correlation checks: this machine's network intermittently resets CloudFront connections (curl reproduces it — affects Zipify/Awin/Junip probes); Cloudflare bot-challenged the crawler from page ~436 (caused fake theme.js JSON-parse + `reading 'options'` errors — 100% co-occurrence with challenge pages); crawler rate-limited SE Wishlist's config API (429 → its `is_enable_app` crash, 100% co-occurrence). Zipify checkout-extension threw one JSON-parse error outside the challenge window (app-side).

## Jira board stand-up (KAN project, nikolbeauty.atlassian.net)
- Connected Atlassian MCP; restructured the whole board: 5 stream epics (Promotions/Dev/Bugs/Improvements/Marketing, colored via `customfield_10017`), Questions epic dissolved (label `question` + Blocks links instead), verb-first titles, typo fixes, Goal/Details/Done-when/Estimate descriptions, Original estimate field set on all open issues, priority ladder applied.
- Fixed a real scheduling bug: Fierce Aging "Remove discount" was due 08-14, two days before the sale ends — corrected to 08-17.
- Promo runbook: every sale gets 8 standard subtasks (colors, discount, multiple-per-order setting, banner, popup, disable Roses, remove discount, re-enable Roses) — documented in epic KAN-1, CLAUDE.md, and auto-memory. Backfilled onto Lipstick Day/Fierce Aging/Labor Day.
- Fierce Aging discount DECIDED: 20% sitewide, excluding gift cards and bundles (recorded in KAN-7/KAN-12; KAN-17 closed).
- Filed KAN-42: "Show more" dead clicks on collection pages (from Microsoft Clarity; note the collection URL is `/collections/eyeliners` — no separate waterproof-eyeliners handle).
- Operating instructions (epic structure, daily-schedule procedure incl. reading Jira comments + dynamic re-prioritization, standing priorities: promos escalate by date; JS-error mission due ~08-15 with `js-errors` label) written into CLAUDE.md.

## Changelog sweep → flagged-backlog list
- Swept every daily note 06-24 → 07-24 for flagged-but-never-resolved items: 82 found, then verified against the live store (Admin GraphQL + storefront fetches) and `shopify-live/`.
- **Verified complete & removed:** #1 JSON-LD live render (live PDP now emits exactly ONE Product node with offers+aggregateRating), #2 FAQ rewards page (pasted to admin 07-27, live with full new content + FAQPage schema), #4 minify script/changelogs (committed 41c9b1c), #19 wishlist snippet mod (committed 50300de), #82 Intelligems routing (loader fully out of theme). #3 reduced (theme code IS pushed live; only the 4 style metafield definitions missing — verified absent). #35 reduced (1 of 5 images has alt now).
- **Confirmed still open** by spot-checks: #13 (upsell enabled in repo AND in the live published theme's group-overlay.json — the 07-17 editor deletion is gone), #23, #24, #50, #47, #48, #74 (theme.css only), etc.
- Remaining 74 items recorded in `changelog/flagged-backlog-2026-07-29.md` for Jira triage.

## Notes
- Live published theme is now "Copy of Nikolbeauty_7/27/26 National Lipstick Sale" (a duplicate made for the sale).
- Nothing was changed on the live store at any point — all checks read-only.
