# Changelog — 2026-07-21

## Discount Sync: per-discount badge color, price color & badge text (built)
- New feature so each tracked discount can carry its own **badge background color**, **sale-price text color**, and **badge text** (template with `{n}` placeholder; default `{n}% OFF`, "Up to" prefix still automatic for mixed-variant products). Plan: `~/.claude/plans/streamed-waddling-snail.md`.
- **App:** `prisma/schema.prisma` — added nullable `badgeBg`/`priceColor`/`badgeText` to `TrackedDiscount` (migration `20260721205652_discount_styling` applied). `discounts.server.js` `computeTargetState` now returns `variantDiscount` (winning discountId per variant). `sync.server.js` builds `styleByDiscount`, and `buildDesiredWrites`/`applyDiff` write **product-level** metafields `custom.promo_badge_bg` / `promo_price_color` / `promo_badge_text` for each product's **winning (highest-%) discount**; extended `AppliedState` with `styles` (diff-based set/delete, cleared on untrack/clear/uninstall like `promo_percent`). `api.sync.jsx` empty-state string updated for the new shape. UI (`app._index.jsx`): "Style" button per tracked discount → modal with two hex color pickers + text field + live preview; new `style` action intent (hex-validated server-side) that upserts then reconciles.
- **Theme (`shopify-dev/` only — NOT pushed):** `product-grid-item.liquid`, `product-price.liquid`, `cart-bar.liquid` now read the three metafields with fallback to `settings.promo_badge_bg_color` / default `{n}% OFF`. `settings_schema.json` promo header info updated. `THEME-CONTRACT.md` documents the new metafields. No CSS/JS asset changes (inline-liquid styling → no minify step).
- **Pending manual step:** the running `shopify app dev` server holds the Prisma engine DLL, so `prisma generate` couldn't finish (EPERM). Must stop the dev server → `npx prisma generate` → restart before the new fields work at runtime.

## Dev-store data seeding from live Matrixify export (nikolbeauty-dev-mab1srre)
- Source: `Downloads/Export_2026-07-21_105510.xlsx` — a full Matrixify backup of the live store (11 sheets). Parsed the xlsx (unzipped XML, inline-string cells; no sharedStrings) into clean JSON with a custom Node parser (scratchpad). Real entity counts after de-expanding Matrixify's row expansion: **70 products** (382 rows = products + variant/image sub-rows), ~172 custom collections, handful of smart collections, ~29 pages, ~501 blog articles, ~1205 redirects, ~462 metaobjects, menus, ~12k translations. Discounts (145,796 rows — 145,721 historical code discounts + 6,132 BUNDLE codes, only 75 automatic) intentionally **skipped** per user.
- Method: recreate via API using the **discount-sync app's offline token** (scope incl. `write_products`) read from its Prisma SQLite session, driving Admin GraphQL `productSet` (synchronous). Idempotent by handle (update-in-place on re-run). Fixed the interrupted-session leftover (`classic-beach-palette`, created earlier today) by updating it in place.
- **Products: 70/70 created**, full fidelity — options/variants (incl. bundles: 64-variant best-seller, 24-variant beginner, 12-variant complexion), price/compare-at (compare-at omitted when == price), SKU, barcode, taxable, inventory policy, per-variant inventory qty at Shop location, unit cost, weight+unit, HS code, country of origin, tags, product type, vendor, description HTML, and 282 images ingested from the live CDN (READY). 3 products kept DRAFT (figure-it-out-lipstick, new-beauty-lip-gloss, the-ultimate-brush-care-bundle).
- App token lacks `write_publications`; **published the 67 active products** to Online Store + Shop + POS via the MCP (store-owner) connection instead.
- **Collections: 131/131 created + published** (26 custom w/ product membership, 105 smart w/ rule sets incl. a 13-rule collection; Category rules mapped to `PRODUCT_CATEGORY_ID` taxonomy gids). Published via MCP.
- **Translations sheet dropped** — it's the source-language content inventory (Field/Original Value), no second-locale data to import.
- **Navigation menus done via MCP:** updated the store's default `main-menu` and `footer` in place (menuUpdate). Main menu = full "Makeup" mega-dropdown (nested to 3 levels) linking to the created collections/products (those work); plus top-level Videos/Live Shows/Quiz/Skin/Gift Cards/Wishlist/Rewards/Book. Footer = 10 links. Absolute `nikolbeauty.com` URLs rewritten to relative dev paths. Page-type links (Videos, policies, etc.) 404 until pages exist.
- **Remaining content SKIPPED per user (no new app):** pages (29), blogs + 501 articles, redirects (1205), metaobjects (~462 rows). Blockers: discount-sync offline token **expired ~16:58Z** and lacks content scopes; MCP safety policy **blocks `bulkOperationRunMutation`**; large article bodies can't be hand-fed through MCP tool calls. User declined creating a Custom App token. Recoverable later two ways: (a) a Custom App Admin token + the scratchpad scripts/JSONL, or (b) **Matrixify import** of the same xlsx limited to the remaining sheets (no dev app needed — the export's native tool). Parsers + grouped JSON already built in scratchpad.
- **Net result on nikolbeauty-dev-mab1srre:** 70 products + 131 collections (published) + main/footer nav = a browsable, discount-testable storefront catalog.

## Discount Sync app: Phases 1–2 built
- **Phase 1 complete.** Blocker resolved: original nikolbeauty-dev is a plain Plus store, not a Dev-Dashboard dev store — `shopify app dev` can't target it. User created a true dev store in the Fresh Beauty Studio, LLC org: **nikolbeauty-dev-mab1srre.myshopify.com** ("app-dev store"; original nikolbeauty-dev = "theme-dev store", gets the app later via install link). Node 22.14 required by CLI 4.x — installed via nvm alongside 20 (global switch reverts on reboot; commands pin PATH to `~/AppData/Local/nvm/v22.14.0` instead). Dev server runs via project-local CLI (`npx shopify app dev --store nikolbeauty-dev-mab1srre.myshopify.com`); scopes auto-granted; Prisma migrations applied (TrackedDiscount + AppliedState).
- **Phase 2 engine written — desired-state reconciliation.** Every sync: fetch all automatic discounts → resolve tracked+active ones to VARIANT level (explicit variant targets; product/collection/all targets expand to all variants; variant-only products don't leak coverage to sibling variants) → merge with max-%-wins (conflicts surfaced in UI) → build theme-contract writes (multi-variant: `custom.promo_percent` per variant + card tag; single-variant: tag only; `promo-<n>` uniform vs `promo-up-to-<max>` mixed) → diff vs per-shop AppliedState → apply only changes (metafieldsSet/metafieldsDelete, tagsRemove-before-tagsAdd so both tag forms never coexist) → save state. Untrack/deletion/edits all fall out of the same recompute; dryRun mode returns planned ops without writing. Amount-off discounts surfaced as unsupported (Phase 5). Admin UI: status banner, conflict warnings, Track/Untrack/Sync-now.
- Skeleton's untrack.server.js deleted (superseded by reconciler); skeleton bug fixed pre-merge (appliedProductIds vs appliedProducts field mismatch).
- Seeded dev store came with test data: 17 products (1- and 4-variant), an ACTIVE 30% basic discount + a BXGY (good unsupported-type filter test). Smoke test staged: discount pre-tracked via DB; cron endpoint verified reachable (`/api/sync` + SYNC_SECRET) — blocked only on first browser open of the app to store the offline session (0 sessions in DB).

## Phase 3 complete (code): cron + webhooks + cleanup + dry run
- Uninstall webhook now purges TrackedDiscount + AppliedState (sessions already handled). Design constraint documented in-code: API access is revoked at uninstall, so storefront cleanup CANNOT happen post-uninstall — the admin UI's new destructive **"Clear everything"** action (untrack all + reconcile → removes every app-written tag/metafield) is the pre-uninstall path.
- **"Preview changes (dry run)"** action added: runs the reconciler with dryRun, shows would-be op counts in a banner, writes nothing.
- Verified compiling + reachable on the restarted dev server: /app 410 (auth gate), /webhooks/discounts 400 on unsigned POST (HMAC verification working), /api/sync 401 without secret.
- Ops notes: first tunnel (trycloudflare quick tunnel) died with DNS NXDOMAIN — restart mints a new one (current: designated-xhtml-switching-unix.trycloudflare.com; Preview URL in admin is stable). Restart initially failed EPERM on prisma generate — orphaned node.exe children from the killed server held the query-engine DLL; killed by command-line match. Edit-tool tmp files in app/routes/ are picked up by flat-routes and briefly break the server manifest — transient, but avoid editing routes while the dev server is mid-request.
- ~~Phases 2+3 runtime verification gated on session~~ → session landed after tunnel restart; user opened app successfully.

## Phase 4 E2E — first pass PASSED (plus one bug found & fixed)
- **BUG (cron path): after the last tracked discount was removed, the shop dropped out of the cron loop before cleanup could run** — stale tags/metafields would persist until something else triggered a sync. Fixed in api.sync.jsx: shop list = tracked-discount shops ∪ shops with non-empty AppliedState. Verified: cleanup now runs (1 metafieldsDelete + 1 tagsRemove) and the product came back byte-clean (merchant tags preserved).
- **Verified against live Shopify (app-dev store, seeded data):**
  - Variant-target precision: 30% discount targeting ONLY the "Ice" variant of the 5-variant Complete Snowboard → `custom.promo_percent=30` (number_integer) on Ice alone, siblings untouched; product tag `promo-up-to-30` (partial-coverage form); merchant tags preserved.
  - BXGY discount correctly filtered out (unsupported type).
  - Untrack → surgical removal of exactly what was written.
  - Idempotency: repeat syncs = zero ops (twice).
  - Real UI path: user's Track click applied everything correctly on its own (metafields+tag present before any cron sync).
- **Remaining Phase 4 matrix** (needs discounts the app can't create — no write_discounts scope by design; user creates in app-dev store admin): scheduled start/end flip via cron, product-level uniform discount (promo-N tag form), collection-targeted discount, overlapping discounts (max-wins + conflict banner), webhook instant resync on discount edit, storefront rendering (requires Broadcast theme on a test store — Dawn doesn't have the promo contract).
