{% comment %}
  TODO (future improvement): convert these alternating image/text rows into a
  slider/carousel — that's the better design direction for this section.
{% endcomment %}
<style>
section.alt-rows-section {
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
    padding: 90px 0;
}

.alt-rows-title {
    text-align: center;
    font-size: 56px;
    line-height: 110%;
    letter-spacing: -1px;
    font-weight: 500;
    max-width: 900px;
    margin: 0 auto;
    color: #2A2724;
    font-family: 'Cormorant',sans-serif;
}

.alt-rows-description {
    text-align: center;
    font-size: 20px;
    line-height: 140%;
    font-weight: 400;
    color: #2A2724;
    padding-top: 20px;
    font-family: 'Inter',sans-serif;
}

.alt-rows-box-wrapper {
    margin-top: 50px;
}

.alt-rows-box {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: 25px;
    gap: 60px;
    background: #fff;
    margin-bottom: 40px;
}

.alt-rows-image {
    width: 50%;
}

.alt-rows-content {
    width: 50%;
    padding-right: 60px;
}

.alt-rows-image img {
    max-width: 610px;
    object-fit: contain;
}

.alt-rows-box-title {
    font-size: 44px;
    line-height: 110%;
    letter-spacing: -1px;
    font-weight: 500;
    color: #2A2724;
    font-family: 'Cormorant', sans-serif;
}

.alt-rows-box-description {
    font-size: 20px;
    line-height: 140%;
    font-weight: 400;
    color: #2A2724;
    padding-top: 20px;
    font-family: 'Inter', sans-serif;
}

.alt-rows-review-box {
    margin-top: 20px;
}

.alt-rows-review-stars img {
    max-width: 95px;
    object-fit: contain;
}

.alt-rows-review-text {
    font-size: var(--FONT-SIZE-BASE);
    line-height: 150%;
    font-weight: 400;
    color: #2A2724;
    padding-top: 5px;
    font-family: 'Inter', sans-serif;
}

.alt-rows-author-box {
    display: flex;
    align-items: center;
    gap: 5px;
    margin-top: 10px;
}

.alt-rows-verified {
    display: flex;
    align-items: center;
    gap: 4px;
}

.alt-rows-verified img {
    max-width: 14px;
    object-fit: contain;
}

.alt-rows-button-wrap a {
    display: flex;
    background: var(--BTN-PRIMARY-BG-COLOR);
    text-decoration: none;
    align-items: center;
    justify-content: center;
    width: 100%;
    max-width: 400px;
    height: 60px;
    margin: 0 auto;
    font-size: var(--BTN-FONT-SIZE);
    line-height: 26px;
    letter-spacing: 0;
    font-weight: 700;
    color: var(--BTN-PRIMARY-TEXT-COLOR);
    font-family: var(--BTN-FONT-STACK);
    cursor: pointer;
}

.alt-rows-box.alt-rows-reverse {
    flex-direction: row-reverse;
}

.alt-rows-box.alt-rows-reverse .alt-rows-content {
    padding-right: 0;
    padding-left: 60px;
}
.alt-rows-author-name {
    font-size: var(--FONT-SIZE-BASE);
    line-height: 150%;
    font-weight: 400;
    color: #2A2724;
    font-family: 'Inter',sans-serif;
}

.alt-rows-verified-text {
    font-size: var(--FONT-SIZE-BASE);
    line-height: 150%;
    font-weight: 400;
    color: #2A2724;
    font-family: 'Inter', sans-serif;
}
@media only screen and (max-width: 600px) {
section.alt-rows-section {
    padding: 50px 0;
}

.alt-rows-title {
    font-size: 33px;
}

.alt-rows-description {
    font-size: var(--FONT-SIZE-BASE);
    padding-top: 15px;
}

.alt-rows-box {padding: 15px;flex-wrap: wrap;gap: 20px;margin-bottom: 25px;}

.alt-rows-image {
    width: 100%;
}

.alt-rows-image img {
    max-width: 100%;
}

.alt-rows-box-title {
    font-size: 26px;
}

.alt-rows-content {
    width: 100%;
    padding: 0;
}

.alt-rows-box-description {
    padding-top: 10px;
    font-size: var(--FONT-SIZE-BASE);
}

.alt-rows-box.alt-rows-reverse .alt-rows-content {
    padding-left: 0;
}

.alt-rows-review-box {
    padding-left: 15px;
    margin-top: 15px;
}

.alt-rows-review-text {
    font-size: var(--FONT-SIZE-BASE);
}

.alt-rows-review-stars img {
    max-width: 85px;
}

.alt-rows-button-wrap a {
    font-size: var(--BTN-FONT-SIZE);
}
.alt-rows-author-name {
    font-size: var(--FONT-SIZE-BASE);
}

.alt-rows-verified-text {
    font-size: var(--FONT-SIZE-BASE);
}
.alt-rows-verified img {
    max-width: 12px;
}
}
</style>

<section
  class="alt-rows-section"
  style="background-image: url({{ section.settings.bg_image | img_url: 'master' }});"
>
  <div class="page-width">

    <div class="alt-rows-main-container">

      <!-- TOP TITLE -->
      <div class="alt-rows-title">
        {{ section.settings.title }}
      </div>

      <!-- TOP DESCRIPTION -->
      <div class="alt-rows-description">
        {{ section.settings.description }}
      </div>

      <!-- REPEATER BOXES -->
      <div class="alt-rows-box-wrapper">

        {% for block in section.blocks %}
          <div
            class="alt-rows-box {% if block.settings.image_position == 'right' %}alt-rows-reverse{% endif %}"
            {{ block.shopify_attributes }}
          >

            <!-- IMAGE SIDE -->
            <div class="alt-rows-image">
              {% if block.settings.image %}
                <img
                  src="{{ block.settings.image | img_url: 'master' }}"
                  alt=""
                  loading="lazy"
                >
              {% endif %}
            </div>

            <!-- CONTENT SIDE -->
            <div class="alt-rows-content">

              <div class="alt-rows-box-title">
                {{ block.settings.title }}
              </div>

              <div class="alt-rows-box-description">
                {{ block.settings.text }}
              </div>

              {% if block.settings.show_review %}
                <div class="alt-rows-review-box">

                  <div class="alt-rows-review-stars">
                    {% if block.settings.stars %}
                      <img
                        src="{{ block.settings.stars | img_url: 'master' }}"
                        alt=""
                        loading="lazy"
                      >
                    {% endif %}
                  </div>

                  <div class="alt-rows-review-text">
                    {{ block.settings.review_text }}
                  </div>

                  <div class="alt-rows-author-box">

                    <div class="alt-rows-author-name">
                      {{ block.settings.author }}
                    </div>

                    <div class="alt-rows-verified">
                      {% if block.settings.verified_icon %}
                        <img
                          src="{{ block.settings.verified_icon | img_url: 'master' }}"
                          alt=""
                          loading="lazy"
                        >
                      {% endif %}
                      <div class="alt-rows-verified-text">
                        {{ block.settings.verified_text }}
                      </div>
                    </div>

                  </div>
                </div>
              {% endif %}

            </div>
          </div>
        {% endfor %}

      </div>

      <!-- CTA BUTTON -->
      <div class="alt-rows-button-wrap">
        <a href="{{ section.settings.button_link }}">
          {{ section.settings.button_text }}
        </a>
      </div>

    </div>
  </div>
</section>

{% schema %}
{
  "name": "[Custom] Alternating Rows",
  "settings": [
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background Image"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Main Title"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Main Description"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "GET YOURS NOW"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    }
  ],
  "blocks": [
    {
      "type": "content_box",
      "name": "Content Box",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "select",
          "id": "image_position",
          "label": "Image Position",
          "default": "left",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "right", "label": "Right" }
          ]
        },
        {
          "type": "text",
          "id": "title",
          "label": "Box Title"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Box Description"
        },
        {
          "type": "checkbox",
          "id": "show_review",
          "label": "Show Review Box",
          "default": false
        },
        {
          "type": "image_picker",
          "id": "stars",
          "label": "Stars Image"
        },
        {
          "type": "textarea",
          "id": "review_text",
          "label": "Review Text"
        },
        {
          "type": "text",
          "id": "author",
          "label": "Author Name"
        },
        {
          "type": "image_picker",
          "id": "verified_icon",
          "label": "Verified Tick Icon"
        },
        {
          "type": "text",
          "id": "verified_text",
          "label": "Verified Text",
          "default": "Verified Buyer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "[Custom] Alternating Rows"
    }
  ]
}
{% endschema %}
